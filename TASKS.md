# משימות

סדר ביצוע. כל משימה מגדירה מתי היא "הסתיימה" — אל תעבור הלאה בלי לעמוד בזה.

---

## הושלם

### ✅ 0 · הנדסה לאחור של תבנית החקיקה
תוצר: `docs/template-spec.md`. הונדס משני קובצי docx אמיתיים שהוגשו לכנסת ה-25.

### ✅ 5 · רנדרר docx
תוצר: `packages/render/render_bill.py`. מקבל ייצוג מובנה, מפיק docx בתבנית.
מבחן: `tests/golden/test_kaytanot.py` — 8/8 שורות, התאמה מלאה בצורה ובטקסט.

### ✅ 1 · חילוץ כללי הניסוח
תוצר: `docs/drafting-rules.md`. כל עשרת הדפוסים ברצפה נמצאו עם מקור (בעיקר
"מדריך לניסוח חקיקה" של משרד המשפטים, PDF עמ' 22–62 — לא החוברת הסגולה
עצמה, ראו §0 במסמך למיפוי המקורות). תועדו גם דפוסים נוספים שאינם ברצפה.

**נמצאו שלוש סתירות/פערים בבדיקה, כולן הוכרעו** (ראו `docs/drafting-rules.md` §6):
1. "בלי הוראות אופרטיביות" בסעיף הגדרות — מקור יחיד: pptx. נשאר בתוקף, דרגת
   ודאות "שקופית"; `validator-checklist.md` #11 סומן כאזהרה רכה.
2. כותרת שוליים בחוק מתקן — המדריך גבר: `{סוג פעולה} סעיף {N}`, לא "מהות".
   תוקן במשימה 4 למטה וב-`validator-checklist.md` #4.
3. פורמט שם הצעת חוק מתקן — שני פורמטים תקפים, לא סתירה: פורמט המדריך
   (עם "תיקון מס' X") לשלב שאחרי מספור רשמי, לעומת פורמט קובץ הזהב
   (עם "תיקון – תיאור") להצעה פרטית טרומית — הנוכחי. הערה במשימה 12.

### ✅ 2 · סכמת `LegislativeNode`
תוצר: `packages/corpus/node.py` (dataclass לפי המפרט המקורי, ללא הרחבות)
ו-`packages/corpus/numbering.py` (`sort_section_numbers`). `number` שייך אך
ורק למרחב המספור של סעיפי החוק המתוקן (3א, 34כד) — לא למרחב המספור הרץ
של סעיפי הצעת החוק (1., 2., 3...), שכבר ממומש ב-`Line.number` ברנדרר ולא
נוגע כאן.

סיומות אות בסעיפים הן מיקום סידורי (א,ב,...,ט,י,יא,...), לא ערך גימטרי —
`sort_section_numbers` בנוי סביב רצף סיומות שנוצר פרוגרמטית ולא על חישוב
ערך. מבחנים: `tests/unit/test_numbering.py`, `tests/unit/test_node.py` —
כולם עוברים, כולל הרצף מההגדרה המקורית, המקרה `34כד`/`34לא`, ומספור מעורב.

לאחר מכן, בעקבות recon אמיתי (ראו §6 למטה): נוספו `is_normative`, `status`
(active/repealed/merged), `raw_amendment_note`, `numbering_space`,
`margin_title_raw`, `text_raw`, ו-`node_type` הורחב ל-part/chapter/siman.
נוספה גם `effective_source_ref()` — `source_ref` יושב רק בשורש ה-law
(שמסומן `is_normative=False`, כי הוא מטא-דאטה לא נוסח), וצמתים אחרים
יורשים אותו דרך טיפוס במעלה העץ, לא העתקה.

### ✅ 3 · Ingest מוויקיטקסט (שני חוקים)
תוצר: `packages/corpus/wikitext_parser.py` (`parse_wikitext` — פונקציה
טהורה, בלי רשת) ו-`packages/corpus/wikitext_client.py` (שליפה חיה,
לשימוש בבניית fixtures בלבד, לא מיובא על ידי הפרסר). `packages/corpus/
text_normalize.py` (`normalize_text`) — מנרמל גרשיים מסולסלים/גרשיים
עבריים/מקף עברי לגרסתם הישרה, בלי לגעת בשגיאות הקלדה בנוסח המקור.

Fixtures אמיתיים תחת `tests/fixtures/wikitext/`: קייטנות, מאבק בארגוני
פשיעה, **וגם עונשין** (נשמר מראש לטובת משימה 7, לא נבדק כאן). כל אחד
עם `.meta.json` (page id, revision id, revision timestamp) לבניית
`source_ref` בלי צורך ברשת בזמן טסט.

מבחנים: `tests/unit/test_wikitext_parser.py` — מריץ את הפרסר על שני
ה-fixtures ובודק: 8 סעיפי הקייטנות (כולל סעיף 7 שסומן `status=merged`
נכון), מספור רץ 1–38 בפרקי חוק מאבק בארגוני פשיעה (לא מתאפס בכל פרק),
קינון תלת-רמתי בסעיף 18 שם התווית ((א)/(1)) קובעת את `node_type` בלי
קשר לתבנית העוטפת (`{{ח:תת}}`/`{{ח:תתת}}`), ותוספת ראשונה עם
`numbering_space="schedule"` נפרד ממספור החוק.

**שני ממצאים שלא היו צפויים, ותוקנו תוך כדי מימוש:**
1. `{{ח:קטע4}}` מכיל את תוכנו (הערה) כארגומנט מוטבע באותה קריאה עצמה,
   לא כשורה נפרדת אחריו. ניסיון ראשוני לדלג על "השורה שאחרי קטע4" מחק
   בטעות תוכן אמיתי (פרט 1 בתוספת הראשונה של חוק מאבק בארגוני פשיעה).
   תוקן: קטע4 פשוט מדולג, אין מנגנון "דילוג על השורה הבאה".
2. סעיף עם כותרת ריקה שתוכנו כולו הערה אינו תמיד "שולב במקום אחר" —
   בתוספת הראשונה נמצא פרט עם התוכן "(נמחק)" בלבד, שהוא `repealed`,
   לא `merged`. הפרסר מבחין ביניהם לפי המילה "שולב" בהערה עצמה (קריאה
   של ניסוח שכבר קיים במקור, לא ניחוש של עובדה משפטית).

**לא נכלל בהיקף הזה (מתועד, לא נשכח):** `{{ח:מאגר}}`/`{{ח:תיבה}}` (מספר
מאגר, ס"ח) ו-`{{ח:חתימות}}` (תאריך חקיקה + חותמים) אינם נשלפים אוטומטית
מהוויקיטקסט; `source_ref` בטסטים נבנה ידנית מתוך ה-`.meta.json`.
`{{ח:כותרת}}` (שם החוק המלא) *כן* נשלף — נוסף במשימה 4, ראו שם.
`{{ח:חיצוני}}`/`{{ח:פנימי}}` מיושרים לטקסט תצוגה בלבד, בלי resolve אמיתי
(זה משימה 8).

---

### ✅ 4 · מנוע `amend`
תוצר: `packages/amend/transform.py` (`apply(before, transformations)` —
בונה עץ "אחרי" כטרנספורמציה מפורשת ומתועדת על ה"לפני" האמיתי, לא ככתיבה
עצמאית של עץ שלם, כדי לא להחזיר את בעיית המעגליות בדלת האחורית) ו-
`packages/amend/engine.py` (`amend(before, after) -> list[Line]` — מנוע
ה-diff עצמו, טהור). "לפני" נטען מ-fixture אמיתי (משימה 3) — לא מבונה
מהקובץ הזהב.

`packages/corpus/node.py` קיבל שדה `full_title` (שם החוק המלא מ-
`{{ח:כותרת}}`) ו-`wikitext_parser.py` נלמד לשלוף אותו — **לא** ב-
`margin_title`, ששמור לכותרת שוליים של סעיף ספציפי (בלבול בין שני
המושגים היה מבטל את הבהירות שהמשימה עצמה דרשה).

**היקף מכוון, לא מנוע כללי:** מזהה רק שני סוגי שינוי — הוספת ילד חדש
בתוך רשימת ילדיו של סעיף, והוספת מילים בתוך טקסט קיים. שינויים אחרים
(מחיקת/החלפת סעיף שלם, תיקון עקיף) לא נתמכים — יחכו למקרה זהב משלהם.

**כמה כללי ניסוח הוסקו ממקרה זהב יחיד** (מסומן בקוד "מוסק מדוגמה אחת"):
המספר הראשון-שנוגע-בו מקבל את כל שם החוק + הערת שוליים; סעיפים נוספים
מקבלים רק `בסעיף {N} לחוק העיקרי`; מוטציה בודדת מתלכדת לשורה אחת (סעיף
2) לעומת הוספה שמקבלת שורת פתיח נפרדת (סעיף 1); הוספה שמעוגנת בסוף
הרשימה (לא בעוגן טקסטואלי ספציפי) מקבלת מספר-פסקה מפורש לפי מיקומה
הסידורי. ייתכן שידרשו הכללה כשיגיע מקרה זהב שני.

**מבחנים:** `tests/unit/test_amend_kaytanot.py` — משווה את פלט `amend()`
מול רשימת `Line` קבועה ובלתי-תלויה (עותק של מה שאומת בעבר ידנית מול
`golden-kaytanot.docx`) — לא מול `BILL.lines`, כי אחרי הצעד הבא זה היה
טאוטולוגי. לאחר שהושוו זהות, הוחלפה הרשימה המקודדת ידנית ב-
`tests/golden/test_kaytanot.py` בקריאה אמיתית ל-`amend()` — מבחן הזהב
הקיים ממשיך לעבור מקצה לקצה: ingest אמיתי → `amend()` → רנדרר → docx
זהה למקור.

---

### ✅ 4א · דפוס החלפת מילים

**רקע:** נסיון להריץ טרנספורמציה שאין לה עוגן במקרה הזהב (קייטנות
סעיף 5: "מאסר ששה חדשים" → "מאסר שנה") חשף באג שקט ב-`_diff_text`:
בגלל חפיפת תו מקרית ("ש"), הפונקציה "חשבה" שרק "נה" הוכנס, והייתה
מפיקה שורת תיקון משפטית שגויה בלי שום שגיאה. לא הופק docx מהפלט הזה
(ראו קומיט קודם) — נוסף במקום זאת מגן (`NotImplementedError`) שמזהה
מוטציה שאינה הכנסה טהורה ומכשיל אותה בקול.

**החלטת עיצוב מרכזית (לא מובנת מאליה):** דיף מינימלי (prefix/suffix
משותפים, כמו ב-`_diff_text`) *לא* יכול לשמש להחלפה. "מאסר ששה חדשים"
→ "מאסר שנה" ו"ששה חדשים" → "שנה" מייצרים בדיוק אותו before/after —
גבול הביטוי המצוטט הוא החלטת ניסוח משפטית (מה ראוי לצטט לשם בהירות),
לא עובדה טקסטואלית שניתן לגזור. אלגוריתם שהיה "מחליט" לבד איפה הביטוי
מתחיל ונגמר הוא ניחוש גם אם הוא דטרמיניסטי — הפרה של חוק ברזל 2.

**הפתרון:** `ReplaceWords(target_id, old_phrase, new_phrase)` חדש
ב-`transform.py` — הביטויים הישן/חדש הם בחירה מפורשת (לא נגזרים),
בדיוק כמו `anchor_id` ב-`InsertAfter`. `old_phrase` חייב להופיע פעם
אחת בדיוק בטקסט המקורי — אפס הופעות או יותר מאחת הן שגיאה, לא ניחוש
(הופעה כפולה מכוונת היא דפוס נפרד, "החלפה בכל מקום", שורה 3 בטבלה
§1.1 — לא ממומש). אותו עיקרון-אי-ניחוש הוחל גם על `InsertWordsAfter`
הקיים: `anchor_substring` שלו חייב אף הוא להופיע פעם אחת בדיוק.

**עדכון (אותה משימה, המשך מיד אחרי המימוש הראשון):** הגרסה הראשונה
העבירה את הביטויים המפורשים ל-`engine.py` דרך סמן פנימי מוטבע בתוך
`.text` בעץ "אחרי" (`⟦replaced-from:old⟧new⟦/replaced⟧`, באותה משפחה
כמו `⟦footnote:key⟧` הקיים ממשימה 4). זה עבד, אבל נמצא שאין מחסום
מבני שמונע מקוד עתידי (בעיקר משימה 11: הכיוון ההפוך, הצעת חוק ← נוסח
משולב, שהמימוש הטבעי שלה הוא להציג את עץ ה"אחרי" כנוסח סופי) לקרוא
את `.text` ישירות ולהציג את הסמן הגולמי כאילו הוא נוסח חוק אמיתי —
פלט-נראה-תקין-אך-שגוי בדיוק מהסוג שהחוקים קיימים כדי למנוע, ותקלה
שהייתה מתגלה בייצור ולא בטסטים (כי בטסטים הזרימה תמיד מגיעה עד הסוף
ומסירה את הסמן). **תוקן:** `apply()` מחזירה עכשיו זוג — עץ "אחרי" נקי
לגמרי (`.text` מכיל נוסח חוק בלבד, בלי יוצא מן הכלל, בכל צומת) + רשימת
אנוטציות נפרדות (`ReplacementAnnotation`, `FootnoteAnnotation`) שמצביעות
על צמתים לפי `id`. `amend()` מקבלת את שתי הרשימות (`before`, `after`,
`annotations`) ומעבירה אותן ל-`_render_mutation`/`_render_insertion`
ישירות — בלי לפרסר שום דבר בחזרה מתוך מחרוזת. אותו תיקון הוחל גם על
מנגנון ה-`⟦footnote:key⟧` הקיים (עכשיו `FootnoteAnnotation`, עם
`offset` בטקסט הנקי, ולא סמן מוטבע). מחסום מבני קבוע:
`tests/unit/test_no_marker_leak.py` סורק כל צומת בעצי ה"אחרי" האמיתיים
(שני מקרי הזהב) ומוודא היעדר מוחלט של `⟦`/`⟧` ב-`.text`.

**מקרה זהב:** קייטנות סעיף 5 — `בסעיף 5 לחוק העיקרי, במקום "מאסר ששה
חדשים" יבוא "מאסר שנה".`, `side_heading="תיקון סעיף 5"`.
**מבחנים:** `tests/unit/test_replace_words.py` (`ReplaceWords`+`apply`:
מקרה תקין, אפס הופעות, יותר מהופעה אחת; אותו דבר ל-`InsertWordsAfter`),
`tests/golden/test_kaytanot_section5.py` (diff מלא מ-fixture אמיתי עד
`Line`, כולל docx), `tests/unit/test_no_marker_leak.py` (המחסום המבני).

---

### ✅ 5א · שרשרת provenance (חוק ברזל 3)

**רקע:** תכנון משימה 6 (ולידטור) חשף שבדיקה 15 ("אין בפלט טקסט חוק
שאין לו provenance") לא ניתנת למימוש: `Line` לא נשא שום שדה provenance,
ו-revision timestamp (המקור היחיד ל"תאריך הנוסח") נשלף בעבר רק בזמן
בניית ה-fixtures (נשמר ב-`.meta.json`) ומעולם לא זרם דרך `parse_wikitext`
או `amend()`. זו לא הייתה "בעיה שהוולידטור יחשוף" - זו הפרה קיימת של
חוק ברזל 3: המערכת הפיקה הצעות חוק בלי אפשרות לדעת מאיזה נוסח ומאיזה
תאריך הן נגזרו. תוקן **לפני** משימה 6, לא כ-n/a בתוכה.

**שרשרת provenance שנוספה:**
1. `wikitext_client.extract_revision_timestamp(export_xml)` - שולפת
   `revision/timestamp` מ-XML של `Special:Export` (קוד אמיתי, נבדק
   ב-`tests/unit/test_wikitext_client.py` מול XML סינתטי הבנוי מהערכים
   האמיתיים ב-`kaytanot.meta.json`) - במקום סקריפט scratchpad חד-פעמי
   שאבד.
2. `LegislativeNode.as_of` (שדה חדש, יושב בשורש בלבד, כמו `source_ref`)
   + `node.effective_as_of()` (אותו דפוס בדיוק כמו `effective_source_ref`).
3. `parse_wikitext(..., as_of=...)` - פרמטר אופציונלי חדש, מציב על
   השורש בלבד. לא מפענח/מנסח - "נוסח כפי שהופיע ביום X" הוא תפקיד
   שכבת התצוגה.
4. `Line.source_node_id` ו-`Line.as_of` (שדות חדשים, אופציונליים,
   ברירת מחדל `None`) ב-`render_bill.py`. **לא נקראים על ידי
   `render_line`/`write_docx`** - נבדק: מבחני הזהב (`test_kaytanot.py`,
   `test_kaytanot_section5.py`) מפיקים docx זהה בייטים למקור אחרי
   השינוי, בלי לגעת בלוגיקת הרינדור עצמה.
5. `amend()` ממלאת את שני השדות על **כל** `Line` שהיא מייצרת
   (`_stamp_provenance`) - הצומת ב"לפני" שממנו נגזרה השורה (מוטציה:
   הצומת שהוחלף/הוכנסו בו מילים; הכנסה: העוגן שאחריו מוכנס תוכן חדש -
   נלקח מ"לפני" בפועל, לא מ-`instruction.anchor` שמגיע מעץ ה"אחרי";
   כותרת סעיף: הסעיף/השורש עצמו) ו-`effective_as_of` שלו. אם אין
   `as_of` בעץ המקור - זורם `None`, לא ערך מומצא (חוק ברזל 3: "אם
   המערכת לא יודעת... היא אומרת זאת, לא מנחשת").

**מבחנים:** `tests/unit/test_wikitext_client.py` (שליפת timestamp,
כולל מקרה שבור: אין revision/timestamp ב-XML), `tests/unit/test_provenance.py`
(כל שורה מקבלת source_node_id+as_of; המקרה השבור המרכזי: בלי as_of
במקור, זורם `None` ולא מומצא ערך), ושני מקרי הזהב עודכנו להשוות גם
את שני השדות החדשים (לא רק n/a - השוואת `Line` מלאה, כולל provenance).

---

### ✅ 6 · ולידטור

תוצר: `packages/validate/validator.py` — `validate(bill, before, refs, *,
docx_path=None) -> list[Finding]`, טהור (בלי רשת/LLM). מריצה תמיד את כל 15
הבדיקות ומחזירה Finding לכל אחת (`status`: `עבר`/`נכשל`/`אזהרה`/`לא נבדק`) -
אף בדיקה לא נעלמת מהפלט, גם כשהיא "לא נבדק".

**היקף מכוון, לא ניתוח שפה חופשי:** כל בדיקה נגזרת ממה שכבר קיים כמידע
מובנה (`Bill.lines`, שדות `Line` כולל `source_node_id`/`as_of` ממשימה
5א, `before`, `refs`) - לא NLP חופשי על טקסט חופשי. בדיקה 2 (הערת שוליים
לאזכור חוץ) ובדיקה 11 (סדר א"ב בהגדרות) מוגבלות בפירוש - מסומן בקוד
בדיוק מה לא נבדק בכל אחת.

**שתי בדיקות `n/a` מפורש, לא מדולג בשקט:** בדיקה 10 (תיקונים עקיפים)
ובדיקה 13 (הצעות זהות/דומות) תלויות ביכולות שלא קיימות עדיין (הצעת חוק
מרובת-חוקים; חיפוש OData ממשימה 8 בהתאמה) - מוחזרות עם `status="לא
נבדק"` והודעה שמסבירה למה, לא מוסתרות ולא מסומנות "עבר".

**בדיקה 8 (שנת ההצעה בשם תואמת את שנת ההגשה):** `Bill.submitted_date`
הוא קלט חובה מהמשתמש (תאריך הגשה אמיתי), לא placeholder - אם הוא ריק
או לא תקין, הבדיקה **נכשלת** עם הודעה שאומרת מה חסר, לא מדלגת.

**בדיקה 14 (סגנונות docx):** מקבלת `docx_path` אופציונלי; בלעדיו -
"לא נבדק". בודקת שכל סגנון פסקה בטבלה שייך לרשימה התקנית המלאה
מ-`docs/template-spec.md` (`TableBlock`, `TableBlockOutdent`, `TableHead`,
`TableSideHeading`, `TableText`, ומקביליהן `2`) - **לא** משווה שדה-מול-שדה
מול תבנית (זה מה שמבחני הזהב כבר עושים).

**ממצא אמיתי מהרצת הוולידטור על שני מקרי הזהב (לא תיאורטי):** על הצעת
סעיף 5 (משימה 4א) בלבד — הצעה שמתקנת רק סעיף יחיד באמצעות מוטציה בודדת
(לא הכנסה) — בדיקה 3 **נכשלת בפועל**: `amend()` אף פעם לא בונה כותרת
"(להלן – החוק העיקרי)" עבור הענף של "מוטציה בודדת מתלכדת לשורה אחת"
(ראו משימה 4, ההערה "מוסק מדוגמה אחת" ב-`engine.py` על ההתלכדות הזו) -
גם כשזו הפעם הראשונה (והיחידה) שהחוק נזכר בהצעה. בקייטנות (משימה 4)
זה לא התגלה כי סעיף 1 (המטופל בענף ה"הכנסה", לא מוטציה) מגיע קודם
ומגדיר את זה. **זו לא תקלה בוולידטור - זו בדיוק העבודה שהוא אמור
לעשות: לתפוס פער אמיתי ב-`amend()` לפני שהוא מגיע למשתמש.** לא תוקן
כאן - מחוץ להיקף משימה 6 (ולידטור, לא מנוע). ראו דוגמת הפלט המלאה
שנשלחה למשתמש.

**מבחנים:** `tests/unit/test_validator.py` - מקרה שבור בנוי אחד לפחות
לכל אחת מ-15 הבדיקות (19 בדיקות סה"כ, כולל שני מקרים לבדיקות 8/15
ו-2 מקרי "תמיד לא נבדק" ל-10/13), פלוס בדיקת שפיות שה-15 ממצאים
תמיד מוחזרים לפי סדר.

---

### ✅ 6א · תיקון שני הפגמים שהוולידטור חשף

הוולידטור (משימה 6) לא רק בדק - הוא מצא. שני ממצאים אמיתיים, שנפתחו
כמשימה נפרדת כי הראשון פוגע במקרה השימוש המרכזי של המוצר (הצעת חוק
שמתקנת סעיף יחיד = המקרה הנפוץ ביותר בהצעות פרטיות טרומיות).

**1. בדיקה 3 - "(להלן – החוק העיקרי)" חסר במוטציה בודדת ראשונה.**
התנאי לבניית שם החוק המלא + "(להלן – החוק העיקרי)" הוא "האם זו הפעם
הראשונה שהחוק מוזכר בהצעה" (`touched_count == 1`) - לא "איזה ענף ב-
`amend()` מטפל בסעיף הזה", כפי שהיה בטעות. `_render_mutation` מקבלת
עכשיו `full_title`/`law_footnote_key` אופציונליים; כשהם ניתנים
(`touched_count==1`), השורה הבודדת בונה את הכותרת המלאה + ההגדרה
בתוך אותה שורה עצמה - וכשהקיצור מוגדר באותה שורה, הוא לא חוזר בה
("בסעיף N," לא "בסעיף N לחוק העיקרי,").

**2. בדיקה 6 - en-dash מול מקף בשנה עברית.** נבדקו הבייטים המדויקים
בארבע נקודות באותו קובץ זהב: `{{ח:כותרת}}` בוויקיטקסט (en-dash),
כותרת ההצעה (en-dash), דברי ההסבר - אזכור עצמאי של אותה שנה (en-dash)
- ושורת הפתיחה של הטבלה (מקף רגיל, יחיד מבין הארבעה). **הוכרע: כלל 4
(ההמרה ב-`engine.py`) טעה, לא הוולידטור.** מופע יחיד אינו עקביות; אם
הכלל היה אמיתי, דברי ההסבר (אותו מנסח, אותו מסמך, אותה שנה) לא היו
סותרים אותו. שגיאת הקלדה חד-פעמית במקור, לא מוסכמה - ראו
`docs/drafting-rules.md` §5.7. `.replace("–", "-")` הוסר לגמרי
מ-`engine.py`.

**תיקון המשך, גם הוא בבדיקה 6:** הריצה החוזרת על קייטנות חשפה עוד
מקרה - הגדרה חדשה שמוכנסת לחוק (`InsertAfter.new_child`, נוסח מצוטט
מקורי, `style="TableBlockOutdent"`) מזכירה שנה עברית (1985, "חוק
יסודות התקציב") שלוש פעמים **באותו משפט**, עם en-dash פעם אחת ומקף
רגיל פעמיים - חוסר עקביות אמיתי בתוכן המצוטט עצמו, לא בטקסט שהמנוע
בנה. חוק ברזל 1 אוסר על המנוע "לתקן" נוסח מצוטט - **בדיקה 6 הוגבלה**
לבדוק רק טקסט שהמנוע עצמו מנסח (`style != "TableBlockOutdent"`), לא
ציטוטים. מגבלת היקף ידועה ומתועדת: שורות מוטציה משלבות ניסוח מנהלתי
וציטוט באותו `style="TableBlock"` - לא מובחן ביניהם כרגע (לא רלוונטי
לשני מקרי הזהב הנוכחיים).

מקרה זהב (נקבע על ידי המשתמש, en-dash לא מקף): `tests/golden/test_kaytanot_section5.py`
עודכן - `בחוק הקייטנות (רישוי ופיקוח), התש"ן–1990 (להלן – החוק
העיקרי), בסעיף 5, במקום "מאסר ששה חדשים" יבוא "מאסר שנה".` עם
`footnotes=["kaytanot"]`.

**קייטנות (משימה 4) השתנה במכוון** - זו הפעם הראשונה שמבחן זהב סוטה
מ-`golden-kaytanot.docx` (לא זהה בייטים למקור): שורה 0 מתוקנת ל-en-dash
(`_known_source_typo_correction` ב-`test_kaytanot.py` מתעדת בדיוק את
התיקון החד-פעמי הזה, לא סטייה כללית). שורה 4 (ההגדרה עם "חוק יסודות
התקציב") **לא** שונתה - זה נוסח מצוטט מקורי, לא ניסוח מנוע.

**הסתיים:** הוולידטור עובר נקי על שני מקרי הזהב, חוץ מ-10/13 ("לא
נבדק") ומ-8 בקייטנות (placeholder מכוון ב-`submitted_date`) - אומת
בהרצה בפועל.

---

### ✅ 1.1 · סכמת Postgres (plan.md §1.1)

הורצה בפועל ב-Supabase (`legislator`, `aamxwjmmlsqkinrzirdz`) אחרי
סבב עיצוב מלא: הצגת תרשים+DDL → 4 תיקוני ברק (immutable
`law_versions` עם `ON DELETE RESTRICT`, `source_ref` עבר מ-`laws`
ל-`law_versions`, `position`→`raw_order` עם הערה מפורשת שהמיון
המשפטי בא מ-`parse_section_number` ולא ממנו, סדר insert מוגדר) →
הרצה. ראו `supabase/migrations/20260913173000_corpus_schema_v1.sql`
ו-`docs/strategy/decisions.md` לתהליך המלא.

5 טבלאות: `laws`, `law_versions` (insert-only, PK של `nodes` הוא
הזוג `(law_version_id, id)` בדיוק כדי שגרסאות לא ידרסו זו את זו),
`nodes`, `law_citations`, `section_amendment_tokens`, + view
`resolved_amendments` (לא טבלה - התאמה בין טוקן לציטוט היא שאילתה,
לא עובדה מאוחסנת).

**RLS הופעל** על כל 5 הטבלאות (בלי policies - יבואו בשלב 7), אומת
בפועל (לא רק pg_roles: שורת בדיקה זמנית הראתה anon/authenticated
חסומים, service_role עוקף - ראו decisions.md). pgvector לא בסכמה
הזו - 1.3 תעצב chunking נפרד (node אחד = chunk אחד לא מספיק - ראו
decisions.md).

**הסתיים כאשר:** הסכמה קיימת ב-DB ומאומתת (`list_tables`) - קוד
ה-ingest בפועל (שממלא את הטבלאות מ-wikitext) הוא משימה נפרדת,
עדיין לא התחילה.

---

### ✅ קוד ה-ingest (שלב א' - שני חוקי fixtures בלבד)

`packages/corpus/ingest_checks.py` (4 בדיקות שפיות טהורות: תבניות
לא מוכרות, ספירת סעיפים גולמי-מול-עץ, תוכן נורמטיבי, ייחודיות id) +
`packages/corpus/db_ingest.py` (fetch עם retry קטגורי - עד 3 ניסיונות
לכשל רשת, אפס לכשל פרסור/שפיות - בניית תוכנית SQL, לא ביצוע) +
`tools/ingest_law.py` (CLI). הורץ בפועל (לא רק טסטים) נגד ויקיטקסט
חי לשני חוקי ה-fixtures, לפי הוראה מפורשת - **לא** על הקורפוס המלא.

**קייטנות: הצליח.** 26 nodes, revision 2887014 (תואם את ה-fixture),
נכתב ל-DB ואומת בשאילתה חוזרת.

**מאבק בארגוני פשיעה: נכשל בבדיקת שפיות - זה בדיוק המנגנון שנועד
לפעול.** בדיקת `check_unique_ids` (נוספה תוך כדי, ראו למטה) תפסה
**באג אמיתי, לא-ידוע-קודם**: ב-`wikitext_parser.py`, המטפל ב-
`{{ח:סעיף}}` בונה `id=f"{law_id}/s{number}"` - מ-law_id, לא מ-
parent_node.id (בניגוד ל-subsection/paragraph, שכבר כן בונים id
יחסי-להורה). לכן סעיף בחוק עצמו וסעיף באותו מספר בתוספת
(numbering_space="schedule", שמתחילה למספר מחדש) מקבלים בדיוק אותו
id. בפועל: `maavak-2003/s1` עד `s8` (וילדיהם) מתנגשים בין החוק
לשתי התוספות שלו - 15 id-ים כפולים בסך הכול.

זה תוקף ישירות את מה שברק ביקש לבדוק: **הבאג היה קיים בעץ בזיכרון
מאז ומתמיד ולא נתפס** - שום צרכן קודם לא אכף ייחודיות id (לא טסט,
לא ה-API). ה-PK המורכב של `nodes` (`law_version_id, id`) הוא הצרכן
הראשון שהיה תופס את זה בפועל (ברעש, כישלון INSERT) - ו-
`check_unique_ids` תופסת את זה עוד לפני שמגיעים ל-DB בכלל, בדיוק
כפי שנועד. **מאבק לא הוכנס ל-DB - הריצה נעצרה לפני כל כתיבה.**

**תוקן (2026-09-13, אושר).** `wikitext_parser.py`: גם `chapter`
(`{{ח:קטע2}}`) בנה id מ-law_id באותו דפוס שגוי - לא התבטא בפועל
כי פרקים כיום תמיד ילדים ישירים של השורש, אבל יתבטא ברגע שתתווסף
קינון חלק/פרק/סימן (משימה 7). שני האתרים (chapter+section) תוקנו
יחד ל-id יחסי ל-parent_node. כל מבחני היחידה/זהב עברו (שינוי יחיד:
הטסט שתיעד את הבאג במפורש עודכן לתעד את התיקון). קייטנות נמחק
ונטען מחדש ב-DB (26 nodes, revision זהה). מאבק אומת ברמת הפייתון
(fetch חי, check_unique_ids ריק, `maavak-2003/תוספת 1/s1` שונה עכשיו
מ-`maavak-2003/פרק א/s1`) - טרם נכתב בפועל ל-Supabase.

`check_unique_ids` נוספה גם לטסטים של הפרסר עצמו (לא רק ingest):
`test_wikitext_parser.py`, `test_kaytanot.py`, `test_kaytanot_section5.py`.

---

## 7 · Ingest מלא + אינדוקס חיפוש

כל ~5,900 החוקים והתקנות. חיפוש היברידי (BM25 + embeddings).

**כולל את חוק העונשין**, שהוצא ממשימה 3 אחרי recon. חוק העונשין חושף
מבנה שלא קיים בשני החוקים הפשוטים של משימה 3: היררכיה של חלק/פרק/סימן
(`{{ח:קטע1/2/3}}`), חמש רמות קינון (`ת`/`תת`/`תתת`/`תתתת`/`תתתתת`),
אי-עקביות בין שם תבנית העומק לעומק הסמנטי (ראו `docs/data-sources.md`
"תקלות ידועות"), ותוספת "לוח השוואה" במרחב מספור נפרד (`numbering_space`).
הפרסר של משימה 3 צריך להתרחב, לא רק "לרוץ גם על עונשין".

**הסתיים כאשר:** שאילתה בשפה חופשית מחזירה את הסעיפים הרלוונטיים בעשירייה
הראשונה עבור 20 מקרי בדיקה שתגדיר.

**מנה 1, ניסיון א' (2026-09-13): 50 כותרות אקראיות מהקורפוס המלא (6,121 כותרות,
מנויות דרך `wikitext_client.list_category_titles` - קטגוריית
MediaWiki "בוט חוקים", אומת קרוב ל-"~5,900" המתועד). `tools/
batch_validate_ingest.py`, seed=42, בלי כתיבה ל-DB (רק fetch+parse+
בדיקות שפיות - ראו הערת היקף למטה).**

**⚠️ תוקן בניסיון ב' למטה: הקטגוריה מכילה גם תקנות/צווים/כללים -
המדגם הזה נבדק על היקף שגוי (ראו "תיקון היקף" למטה). נשאר לתיעוד
של איך התגלה הבאג, לא כבסיס להחלטות.**

**4/50 הצליחו (8%). לא נקי - לא עוברים למנה 2 לפי הכלל שהוגדר.**
כשלים לפי תבנית לא מוכרת (סעיף יכול להכיל כמה):
- `ח:מבוא`: 41/50 (82%!) - **הגורם הדומיננטי, לא ידוע קודם.** התבנית
  הזו לא הופיעה באף אחד משלושת חוקי ה-fixtures (קייטנות/מאבק/עונשין -
  כולם חוקים ראשיים). ההשערה: זו תבנית הפתיח הסטנדרטית של חקיקת משנה
  (תקנות/צווים/כללים - "בתוקף סמכותי לפי סעיף X לחוק Y, אני מתקין...")
  ולא של חוקים ראשיים - וחקיקת המשנה מהווה חלק ניכר מ-6,121 הכותרות.
  לא אומת מול המקור - השערה בלבד, לא ניחוש שנכתב כעובדה.
- `ח:קטע3` (סימן): 21/50 (42%) - הפער הידוע כבר (ראו למעלה, מ-recon
  על עונשין) - מתברר נפוץ בהרבה ממה שנראה ממקרה בוחן יחיד.
- `ח:הערה` ברמה העליונה: 6/50 - שונה מהטיפול הקיים ב-`{{ח:הערה}}`
  (מטופל רק מוטבע בתוך שורת תוכן, לא כשורה עצמאית ברמה העליונה).
- `ח:תתתתתת` (רמת קינון שישית): 2/50 - עמוק יותר מ-`_CONTENT_DEPTH`
  הקיים (חמש רמות: ת/תת/תתת/תתתת/תתתתת).
- `חוקי קורונה`/`חוקי אזרחות...`: 4/50 - תבניות תיוג/קטגוריה בסוף
  העמוד (כמו `חוקי עונשין` שכבר נראה בפתיח penal.wikitext) - כנראה
  עיטור בלבד, לא תוכן.
- **id כפול**: 6/50 - אותה משפחת באג שתוקנה (חלקם תוספות עם מספור
  חוזר, בדיוק כמו מאבק). **מקרה אחד שונה ולא מוסבר**: "כללי תאגידי
  מים וביוב" - התנגשות `פרק א/s4/ג` בלי אף תוספת מעורבת - מנגנון
  אחר, לא נחקר.

**הערת היקף:** המנה הזו בדקה fetch+parse+בדיקות שפיות בלבד, לא כתיבה
בפועל ל-Supabase (ראו דיווח לברק לנימוק - עלות context של הרצת SQL
גדול פר-חוק בסביבה הזו). הצלחה כאן = "הפרסר לא נכשל", לא "נכתב ל-DB".

**הערכת עלות embeddings - זהירה, לא סופית:** מ-4 ההצלחות בלבד (327
nodes, 26,640 תווי טקסט, ~6,660/חוק בממוצע) מוערך קורפוס מלא של
~40.7M תווים / ~10.2M טוקנים (קירוב ~4 תווים/טוקן). **הטיה ידועה:**
4 ההצלחות הן החוקים הפשוטים ביותר במדגם (42-206 nodes) - חוקים
גדולים/מורכבים יותר נוטים דווקא להיכשל בבדיקת שפיות, כך שהממוצע
הזה כנראה מוטה כלפי מטה. לא הכרעה על ספק - רק סדר גודל: במחירי
embedding נפוצים (כ-$0.02-$0.13 ל-מיליון טוקן, תלוי ספק/מודל, לא
נבחר) זה נופל בטווח חד-ספרתי עד נמוך-דו-ספרתי בדולרים - לא משנה
מסקנה, אבל המספר עצמו ידרוש מדידה חוזרת אחרי שהפרסר יכסה יותר
מהקורפוס.

**תיקון היקף (2026-09-13, הוכרע במפורש - ברק):** ה-`ח:מבוא` הדומיננטי
(82%) הוביל לשאלה ישירה - "יכול להיות שאתה בטעות גם אוסף תקנות ולא
רק חוקים?". בדיקה מול כל 6,121 הכותרות (לפי מילה ראשונה): **51%
תקנות, 18% חוק בלבד** (רק 1,094 כותרות), 16% צו, 6% כללי, 2% פקודת,
ושאר קטגוריות קטנות. תקנות/צווים/כללים מחוקקים ע"י הממשלה/שר מכוח
הסמכה בחוק - **לא** ברי-תיקון בהצעת חוק (מטרת הפרויקט). הכרעה
חד-משמעית: **"רק חוקים וחוקי יסוד. לא שום דבר אחר"** - כולל פקודות
(חקיקה ראשית מתקופת המנדט, מעמד דומה לחוק) שהוצאו במפורש. היקף זה
**זמני, לא קבוע** - ייתכן שיום אחד נרצה גם תקנות/צווים, אבל לא כרגע
(ברק, 2026-09-14). נוספה `wikitext_client.is_primary_legislation_title`
(`title.startswith("חוק ") or title.startswith("חוק-יסוד:") or
title == "חוק"`) - מאומתת: 1,112 כותרות תואמות מתוך 6,121, אפס
false positives (למשל "חוקת העבודה" לא תואם). `tools/
batch_validate_ingest.py` מסנן לפיה כברירת מחדל (`--include-secondary`
לביטול). ראו `docs/strategy/decisions.md` ו-`docs/data-sources.md`.

**מנה 1, ניסיון ב' (2026-09-13, מדגם מתוקן): אותו seed=42, n=50, אבל
מתוך 1,112 הכותרות המסוננות (חוקים/חוקי-יסוד בלבד) במקום 6,121.**

**29/50 הצליחו (58%) - עלייה חדה מ-8% לאחר תיקון ההיקף.** כשלים
לפי תבנית לא מוכרת (21 כשלים, תבנית יכולה להופיע בכמה):
- `ח:קטע3` (סימן): **13/21 - הגורם הדומיננטי כעת**, בדיוק הפער הידוע
  ממשימה 7 (`{{ח:קטע1/2/3}}` - חלק/פרק/סימן). לא הפתעה - הפרסר
  לא תומך בזה עדיין.
- `ח:מבוא`: 5/21 - עדיין מופיע, אבל נדיר בהרבה. **מתקן את ההשערה
  הקודמת** שהוא בלעדי לחקיקת משנה - חלק מהחוקים הראשיים עצמם
  משתמשים בו.
- `ח:הערה` ברמה העליונה: 3/21.
- תבניות תיוג/קטגוריה בסוף העמוד (`חוקי בחירות`, `חוקי מקרקעין`,
  `חוקי יסוד`, `מיזמים`, `חוקי אזרחות`, `תושבות וכניסה לישראל`):
  7 מופעים ב-5 חוקים - נראות עיטוריות בלבד, מועמדות טובות ל-allowlist
  (`_KNOWN_BENIGN_SKIP_TEMPLATES`), **לא נוספו** - לא התבקש.
- `ח:תתתתתת` (רמת קינון שישית): 2/21 - כנ"ל, עדיין לא נתמך.
- `צבע גופן`: 1/21, על כותרת חשודה "חוק התכנון והבניה/תזכיר חוק" -
  כנראה "תזכיר חוק" (טיוטה), תת-דף עם "/" - `is_primary_legislation_title`
  לא מסננת תתי-דפים כאלה. סומן, לא טופל.
- **id כפול**: 5/21 (~10%, יחס דומה לקודם) - אותה משפחת באג
  (תוספות עם מספור חוזר), פלוס פורמט תווית חדש שלא נראה קודם:
  תוויות מספריות בסוגריים `s(1)` (לא `s1`) בשתי תוספות (חוק להסדרת
  פעילות חברות דירוג האשראי; חוק הגז הפחמימני המעובה) - לא נחקר.

**הערכת עלות embeddings מתוקנת** (על המדגם הגדול והנכון): 1,870
nodes / 177,742 תווי טקסט מ-29 הצלחות (ממוצע 6,129 תווים/חוק) →
לקורפוס המתוקן (1,112 כותרות): **~6.8M תווים / ~1.7M טוקנים** -
ירידה חדה מ-~10.2M הטוקנים שהוערכו קודם על ההיקף השגוי (כצפוי,
הקורפוס האמיתי קטן פי ~5.5). אותה הסתייגות: כנראה עדיין מוטה כלפי
מטה (חוקים כושלים נוטים להיות גדולים/מורכבים יותר), אבל פחות
מקודם בזכות אחוז ההצלחה הגבוה בהרבה.

**הכרעה (2026-09-14, ברק):** "לנסות מנה 2 של 50 חוקים וחוקי יסוד
כדי לראות אם זה משתפר" - כלומר אפשרות (ב), לפני תיקון הפרסר.

**מדגם שני, 50 נוספים (2026-09-14, seed=43, אותה שיטה - מתוך 1,112
הכותרות המסוננות). זה לא "מנה 2 (500)" מהתוכנית המקורית - מדגם נוסף
של 50 כדי לבדוק יציבות התוצאה לפני הקפיצה ל-500.**

**23/50 הצליחו (46%) - לא השתפר; אם כבר, מעט פחות מ-58%, אבל באותו
סדר גודל.** ביחד עם המדגם הראשון: **52/100 (52%) על שני מדגמים
בלתי-תלויים.** כשלים (27, תבנית יכולה להופיע בכמה):
- `ח:קטע3` (סימן): **18/27 (67% מהכשלים!) - עוד יותר דומיננטי
  מבמדגם הראשון (61%).** שני המדגמים מסכימים: זה המכשול המרכזי,
  לא רעש סטטיסטי.
- `ח:מבוא`: 6/27.
- `ח:הערה` ברמה העליונה: 3/27.
- **שתי תבניות חדשות, לא נראו במדגם הראשון:** `ויקיפדיה` (2 - כנראה
  קישור-חוץ עיטורי, כמו התבניות שכבר סומנו כמועמדות ל-allowlist)
  ו-`ח:מאגר2` (1 - כנראה גרסה ממוספרת של `ח:מאגר` שכבר ב-allowlist,
  פשוט לא נתפסה). לא נוספו - לא התבקש.
- `ח:תתתתתת`: 2/27 - כנ"ל.
- **id כפול, מנגנון חדש שלא מוסבר:** חוק החוזים (חלק כללי) נכשל
  **רק** על `id כפול: 0039-law/פרק ג/s25/א` - בלי אף תוספת מעורבת
  ובלי תבנית לא-מוכרת נלווית. זה מקרה שני מאותה משפחה לא-מוסברת
  שסומנה כבר במדגם הקודם ("כללי תאגידי מים וביוב") - יש מנגנון
  התנגשות id נוסף, לא קשור לתוספות/מספור-מחדש, שעדיין לא נחקר.

**מסקנה:** שני מדגמים בלתי-תלויים (100 חוקים בסך הכול) מסכימים
ש-`ח:קטע3`/סימן הוא המכשול הבודד הגדול ביותר (61%-67% מהכשלים).
זו לא תנודה אקראית - זו ראיה חזקה שהרחבת הפרסר לחלק/פרק/סימן
(היקף משימה 7) היא המנוף בעל ההשפעה הגבוהה ביותר לפני שממשיכים
בקנה מידה גדול יותר.

**הכרעה (2026-09-14, ברק):** לתקן עכשיו, לפני מנה נוספת. בנוסף:
סדר עבודה הפוך למה שנראה - קודם ה-id הכפול הלא-מוסבר (באג שקט,
עלול להשתיק חלק מהעץ בלי הודעה - בדיוק כמו כמעט קרה במאבק), רק
אחר כך קטע3 (פער *מוגדר*, לא באג). שאלה נלווית: מתוך 48 הכשלים
(21+27) בשני המדגמים, כמה נכשלו **רק** על ח:קטע3 (בלי שום סיבה
נוספת)? נספר במפורש: **18/48 (37.5%)**. כלומר תיקון קטע3 לבדו,
בלי לגעת בשום דבר אחר, צפוי להעלות בערך מ-52/100 (52%) ל-70/100
(70%) - לא 90%+, כי 30 מהכשלים (62.5%) נובעים מסיבות אחרות
(מבוא/הערה/תבניות תיוג/id כפול).

**סעיף 2 - חלק/פרק/סימן, בוצע (2026-09-14):** בדיקה מקדימה אישרה
את ההשערה - `node.py` כבר תיעד `node_type: part|chapter|siman|...`
(ממשימה 2), והפער היה אך ורק במיפוי התבנית ב-`wikitext_parser.py`:
`{{ח:קטע2}}` (פרק) היה היחיד עם handler; `{{ח:קטע1}}` (חלק)
ו-`{{ח:קטע3}}` (סימן) לא היו ממופות כלל, אז דולגו בשקט. תוקן:
`_CONTAINER_TEMPLATES` מאחד את שלוש הרמות (חלק/פרק/סימן) לאותו
handler גנרי, עם שלוש רמות מבניות חדשות בין law ל-section (היו
שתיים - law/chapter/section - עכשיו ארבע: law/part/chapter/siman/
section). numbering_space עכשיו יורש מההורה כברירת מחדל (היה
מקובע ל-"law" בקוד הישן - לא היה משנה כל עוד chapter היה תמיד
ישיר מתחת לשורש, אבל עם part מעל chapter זה כבר לא נכון תמיד).

**מקרה זהב - עונשין אחרי התיקון:** `check_unique_ids` נקי לגמרי
על העץ המלא (673 סעיפים!). ספירת node_type: `part=4, chapter=20,
siman=75, section=673, subsection=666, paragraph=1107,
definition=122`. עץ לדוגמה (חלק א > פרק ד > סימן א > סעיף 18):
```
[part] id='penal-1977/חלק א' | חלק א׳: כללי
  [chapter] id='penal-1977/חלק א/פרק ד' | פרק ד׳: העבירה הפלילית והאחריות לה
    [siman] id='.../פרק ד/פרק ד סימן א' | סימן א׳: היסוד העובדתי שבעבירה
      [section] id='.../פרק ד סימן א/s18' | מבנה היסוד העובדתי
        [subsection] id='.../s18/א' | (א)
```
הערה קוסמטית (לא באג): עוגן הסימן בוויקיטקסט עצמו הוא המחרוזת
המלאה "פרק ד סימן א" (לא רק "סימן א") - ה-id חוזר על "פרק ד"
פעמיים בנתיב. לא גורם להתנגשות (מאומת), רק נראה מעט מיותר.
עונשין עדיין נכשל בבדיקת שפיות - אבל עכשיו **רק** על `ח:מבוא`/
תבניות תיוג (`חוקי עונשין`), לא על קטע1/קטע3 יותר - פער נפרד,
לא בהיקף הזה. `_KNOWN_STRUCTURAL_TEMPLATES` עודכן (`ח:קטע1`/
`ח:קטע3` נוספו). טסטים קיימים שתיעדו את הפער הישן עודכנו לאשר
את המצב המתוקן (`test_ingest_checks.py`, `test_db_ingest.py`) -
כל הסוויטה עוברת.

**סעיף 3 - allowlist, בוצע (2026-09-14):** נבדק תוכן אמיתי לפני
הוספה, כמבוקש. `{{ויקיפדיה|...}}` - קישור-חוץ עיטורי לערך ויקיפדיה
תואם, הארגומנט היחיד הוא שם החוק עצמו כטקסט (נבדק על שני חוקים
אמיתיים). `{{ח:מאגר2|...}}` - וריאנט ממוספר של `{{ח:מאגר}}` שכבר
ב-allowlist, מכיל רק מזהה מספרי גולמי (נבדק: `2204149`). שתיהן
נוספו ל-`_KNOWN_BENIGN_SKIP_TEMPLATES` עם טסט סינתטי מייעד.

**סעיף 1 (id כפול בחוק החוזים) - תוקן (2026-09-14).** הממצא: סעיף 25
מכיל **שני** `{{ח:תת|(א)}}` נפרדים בכוונה, כי לחוזה יש תיקון עתידי
(תוקף 7.1.2026) והוויקיטקסט מציג את שני הנוסחים (עד 6.1.2026 מול
מ-7.1.2026) תחת אותה תווית (א), מובחנים רק על ידי `{{ח:הערה}}`
מוטבעת, לא על ידי התווית עצמה. **גישת התיקון:** לא זיהוי סמנטי של
הדפוס הספציפי הזה (עוד ניחוש) - `_unique_child_id` כללי ב-
`wikitext_parser.py`: כל בניית id ילד עוברת דרכו, ואם ה-id
"הטבעי" כבר קיים בין האחים - מוסיפה סיומת סידורית יציבה (`-2`,
`-3`, ...). תופס כל מקור התנגשות עתידי, ידוע או לא, באופן מבני -
לא רק את הדפוס הזה. אין שינוי התנהגות כשאין התנגשות (מאומת:
כל הסוויטה עוברת, כולל מאבק/עונשין). מאומת על חוזים בפועל (fetch
חי): `s25/א` ו-`s25/א-2`, `check_unique_ids` נקי. נוסף טסט סינתטי
(`test_wikitext_parser.py`) ששוחזר על הדפוס המדויק, כדי לא להסתמך
על fetch רשת בטסט.

**מדגם שלישי, 50 חוקים (2026-09-14, seed=44 - אחרי שלושת התיקונים):**
**35/50 (70%) - תואם כמעט בדיוק את התחזית (18/48 = 37.5% כשלי
קטע3-בלבד → ~70%).** אישור חשוב: **אפס כשלי `id כפול` ואפס כשלי
`ח:קטע3`** בכל 50 החוקים - שני התיקונים מחזיקים על מדגם עצמאי
שלישי. 15 כשלים שנותרו:
- `ח:מבוא`: 9/15 - **עכשיו הגורם הדומיננטי היחיד**, בדיוק כמו
  שצפוי מ-recon על "חוק שירות נתוני אשראי" (שוב באותה 50, עדיין
  נכשל - תבנית "מבוא" עצמה עדיין לא מטופלת, רק תועדה).
- תבניות תיוג/קטגוריה נוספות שלא ב-allowlist (`חוקי יסוד`, `מיזמים`,
  `חוקי אזרחות`, `תושבות וכניסה לישראל`, `חוקי קורונה`, `חוקי עונשין`,
  `חוקי ביטחון סוציאלי`): 5 חוקים - אותה משפחה עיטורית כמו
  `ויקיפדיה`/`ח:מאגר2` שכבר אושרו, לא נבדקו/נוספו עדיין.
- `ח:תתתתתת`: 1/15 - כנ"ל, עדיין לא נתמך.

**מסקנה:** שלושת התיקונים (id כפול, קטע3, allowlist חלקי) הביאו
בדיוק את השיפור החזוי. הגורם היחיד שנותר משמעותי: `{{ח:מבוא}}`
(9/15, 60% מהכשלים) - טרם טופל (רק תועד ב-recon, ראו למטה).

**Recon על `{{ח:מבוא}}` (2026-09-14) - לא תוקן, נחקר:** ההשערה
המקורית (פתיח סטנדרטי של חקיקת משנה) **הופרכה** - זו קופסת-הערה
כללית (זוג פתיחה/סגירה כמו `{{ח:פתיח-התחלה}}`), לא ספציפית לחקיקת
משנה. בדיקת תוכן אמיתי על 4 חוקים חשפה שימוש כפול: (א) **הודעת
ביטול חוק** - "'''חוק שירות נתוני אשראי... בוטל ביום 12.4.2019'''"/
"'''חוק מטבע השקל... בוטל'''" - שני חוקים מבוטלים לגמרי עדיין
בקורפוס (ה"בוט חוקים" לא מסנן לפי תוקף בכלל, רק לפי סוג חקיקה).
(ב) הערות עריכה כלליות (שינויי שם משרד/תואם) - לא קשור לביטול.
**הכרעת ברק:** חוקים מבוטלים/שנושנו לא נטענים לדאטהבייס בכלל -
ראו סעיף החדש למטה ו-`docs/strategy/decisions.md`. תיקון הפרסר
עצמו ל-`{{ח:מבוא}}` (כקופסה מבנית, לא דילוג) עדיין לא הוכרע/בוצע -
תלוי בהחלטת הסינון.

**זיהוי חוקים מבוטלים/שנושנו מול KNS_IsraelLaw - מומש (2026-09-14).**
ראו `docs/strategy/decisions.md` לפירוט המחקר (מדידת התאמת שמות,
הגדרת "נושן" מהמדריך הרשמי, כלל פישור עמימות). **94.1% (לא 95.1%
- ברק דחה את נירמול השנה-בסוגריים כי הוא איחד בשקט חוקי הסדרים/
התייעלות שנתיים שונים: "היעד היה טעות, לא הנתונים").**

**מימוש:** `packages/corpus/knesset_odata.py` - קובץ שני שנוגע ברשת
ב-`packages/corpus` (הראשון: `wikitext_client.py`), מקור נתונים
נפרד (הכנסת, לא ויקיטקסט). `fetch_israel_laws()` (רשת, לא נבדקת),
`classify_validity(title, records)` טהורה - מחזירה `ValidityMatch`
(`law_validity_desc`, `match_method`: exact/normalized/ambiguous/
not_found, `matched_kns_name`), `should_ingest(match)` - עיקרון
"שום חוק לא נזרק בגלל שלא הצלחנו לבדוק אותו" (ברק): True גם
ל-not_found/ambiguous, רק False להתאמה **חד-משמעית** לבטל/פקע/
נושן. פישור עמימות: בוחר את התקף אם יש בדיוק אחד; אחרת ambiguous
(לא ניחוש). 19 טסטים סינתטיים (`tests/unit/test_knesset_odata.py`),
כולל המקרים האמיתיים שנמצאו (חפשי/חופשי, חוק הבחירות לכנסת עם
פישור, חוק שירות נתוני אשראי בטל).

**סכמה:** מיגרציה `20260914000000_add_law_validity_tracking.sql`
הורצה בפועל - `laws.law_validity_desc`, `laws.validity_match_method`
(שתיהן nullable, כמו `last_ingest_error` קודם). `db_ingest.
build_ingest_plan`/`_render_transaction_sql` מקבלות `validity:
ValidityMatch | None` אופציונלי - כשמועבר, נכתב ל-`insert into
laws`; לא יוצר תלות קשה ב-`knesset_odata.py` (הקורא אחראי לקרוא
ל-`should_ingest` *לפני* קריאה ל-`build_ingest_plan` - זה סינון,
לא רק תיעוד).

**`tools/batch_validate_ingest.py` מסונן כעת גם לפי תוקף כברירת
מחדל** (`--skip-validity-filter` לחריגה, `--kns-cache` למטמון של
2,024 הרשומות). מדפיס פירוט הוצאה לפי `LawValidityDesc` ופילוג
שיטת ההתאמה במדגם.

**66 הכותרות ללא התאמה (94.1%) - קובץ סקירה מלא:**
`docs/strategy/kns-unmatched-titles-2026-09-14.md`, מקובץ ל-3
קבוצות (12 סיומת-שנה-בסוגריים, 9 תתי-דף עם "/", 45 אחר), עם
מועמדי חיפוש-עזר (לא התאמה אוטומטית) לכל שורה - לביקורת אנושית,
כמבוקש. תצפית בונוס מהיצירה: כמה "מועמדים" הם למעשה **אותו חוק
בדיוק** עם הבדל תחילית קטן שלא כוסה ("חוק רשות פיתוח" מול "חוק
**ה**רשות הפיתוח"; "חוק עידוד החסכון" מול "חוק **ל**עידוד
החסכון") - הזדמנות לשיפור נירמול נוסף, לא נגעתי בו.

**רישום `id_collisions` (ברק, 2026-09-14: "תוודא שכל הפעלה שלו
נרשמת בלוג"):** `LegislativeNode.id_collisions` (רשימה, שורש בלבד)
- `_unique_child_id` מדווחת אליה בכל פעם שנדרשה סיומת סידורית,
בלי I/O בפרסר עצמו (עקבי עם "פונקציה טהורה בלבד"). `IngestPlan.
id_collisions` מעביר הלאה. `batch_validate_ingest.py` סופר
ומדפיס לפי חוק (stderr) + סיכום כולל. מדגם שלישי (50, seed=44):
עדיין אפס מקרים - ראו למטה למדגם החדש עם 500.

### מנה של 500 (seed=100, אחרי כל התיקונים + סינון תוקף)

סינון תוקף: 1,112 → 1,021 כותרות (הוצאו: 43 נושן, 28 בטל, 20 פקע).
**406/500 הצליחו = 81.2%.** אפס כשלי `ח:קטע3`, אפס כשלי `id כפול`
- שני התיקונים מחזיקים בקנה מידה גדול. `id_collisions`: 387 מקרים
ב-39/406 חוקים (9.6%) - **לא נדיר, אבל לא "אלפי מקומות" - דפוס אחד
מרוכז וחדש, ראו למטה.**

**ממצא id_collisions - תוספות שהן רשימות לא-ממוספרות.** רוב
המקרים (מ-3-4 חוקים בודדים: 96/69/38/34) מגיעים מתוספות שהן
רשימה פשוטה בלי מספור - למשל חוק מועצת הצמחים: `{{ח:קטע3|תוספת
פרק ג|ענף הירקות}}` ואחריו עשרות `{{ח:סעיף*}}` **בלי שום ארגומנט
מספר**, כל אחד עם `{{ח:ת}} <שם ירק>` יחיד. כולם מתנגשים על אותו
id `s` לפני הסיומת הסידורית. **תוקן ברמת החומרה** (ברק, 2026-09-14):
זו לא "בעיה עתידית" - "id שנקבע לפי מיקום ברשימה... טיוטה שמצביעה
על s-47 תצביע פתאום על ירק אחר. זה בדיוק מה שארכיטקטורת הגרסאות
נבנתה כדי למנוע - לא בעיה עתידית, שבירה של הבסיס."

**ניתוח לקראת תיקון (לא מומש - הצעה בלבד, כמבוקש):** נדגם על 10
מהחוקים המובילים - **289 סעיפים לא-ממוספרים, 4 קבוצות תוכן כפול
אמיתיות.** כל ה-4 הן `(נמחק)` - פריטי רשימה שנמחקו/בוטלו בנפרד
לאורך זמן ומשאירים placeholder זהה (חוק מועצת הצמחים, חוק הסדרת
מחקרים במחוללי מחלות ביולוגיים, חוק שירות הציבור, חוק מקורות
אנרגיה - ראו דוגמאות מלאות ב-decisions.md). כלומר גם גישת hash-
תוכן **חייבת** עדיין fallback ל-`_unique_child_id` הקיים (התוכן
עצמו לפעמים זהה באמת - זה לא כשל בגזירה, זו עובדה על המקור).

**מגבלה ארכיטקטונית שנמצאה:** תוכן הסעיף (למשל "אבטיח") נמצא
בילד `{{ח:ת}}` שנוצר *אחרי* הסעיף עצמו בפרסור חד-מעברי - אין hash
זמין ברגע בניית id הסעיף. הצעה מעשית: lookahead מקומי בשורה
הבאה בוויקיטקסט הגולמי (לא מעבר שני מלא על העץ) - כל הדוגמאות
שנבדקו מראות `{{ח:ת}}` יחיד מיד אחרי `{{ח:סעיף*}}` הריק, בלי
שורות ביניים. לא נבדק כדפוס אוניברסלי (רק על 10 חוקים) - סיכון:
אם יש מקרה עם כמה שורות ת/תוכן מפוצל, lookahead-שורה-אחת יחטוף
רק את הראשונה. הצעה בטוחה: אם lookahead לא מוצא תוכן ברור - נופל
חזרה למנגנון הקיים (position + סיומת), בלי לפגוע במה שיש. ממתין
להכרעה לפני מימוש.

**שני באגים חדשים שנמצאו וקרסו - תוקנו (2026-09-14):**

1. **טוקן תיקון פגום (חוק העמותות) - תוקן.** `תיקון: תשנ״ו,
   תשס״ה־2, תשס״ה־3 תשע״ד` - פסיק חסר במקור בין שני טוקנים (כנראה
   טעות הקלדה בוויקיטקסט עצמו). `amendment_history._parse_token`
   ניסתה `int("3 תשע״ד")` וקרסה. **תוקן לפי העיקרון "תוכנה שקורסת
   על קלט פגום היא באג בתוכנה":** `AmendmentToken` קיבל שני שדות
   חדשים - `raw` (טקסט הטוקן הגולמי, נשמר תמיד) ו-`unparsed` (True
   רק כשלא הצלחנו לפענח בכלל). טוקן כזה לא קורס, לא מנוחש (לא
   מפוצל לשני טוקנים בניחוש), `hebrew_year=None` כדי ש-
   `resolve_amendment_tokens` תתעלם ממנו בבטחה (0 מועמדים, בדיוק
   כמו "לא נמצא"). מיגרציה `20260914010000_add_amendment_token_raw_unparsed.sql`
   - `section_amendment_tokens.raw_token`/`is_unparsed`. 7 טסטים
   חדשים, כל הסוויטה עוברת. `חוק העמותות` נבדק בפועל - 660 nodes,
   לא קורס יותר.

2. **תבנית מוטבעת רב-שורתית (3 חוקים) - נחקר, לא מומש.** למשל
   `{{טורים שווים | משרד הבריאות | ...` בחוק "תקשורת דיגיטלית עם
   גופים ציבוריים" - תבנית טבלה שנפתחת בשורה אחת ונסגרת כמה שורות
   אחר כך. `_find_template` מניחה שהתבנית שלמה באותה שורה (`parse_wikitext`
   עובד שורה-שורה) - כשה-`}}` הסוגר לא נמצא במחרוזת, `text[i]`
   נופל מחוץ לגבולות. **ניתוח (לא מומש, כמבוקש):**
   - `_find_template` עצמה כבר "עיוורת לשורות" פנימית (סופרת
     depth תווים, `\n` הוא תו כמו כל אחר) - לא צריך שינוי שם, רק
     להזין לה מחרוזת שמכילה מספיק שורות קדימה.
   - הלולאה הראשית (`for raw_line in text.splitlines()`) היא
     שמניחה "כל קריאת תבנית שלמה בשורה אחת" - צריך לעבור למודל של
     היסט תו לתוך הטקסט המלא, לא איטרציה על שורות בלתי-תלויות,
     כדי שקריאה שלא נסגרת בשורה הנוכחית "תמשוך" שורות נוספות.
   - **מה זה שובר:** כל ה"remainder" הקיים (מה שנשאר אחרי תבנית
     עומק כמו `{{ח:ת}}`) מוגדר היום כ"שאר השורה הזו בדיוק" - זו
     בדיוק ההנחה שנשברת. מעבר לזיהוי-גבול מבוסס-תוכן ("סרוק קדימה
     עד שמגיעים ל-`\n` שאחריו תבנית-מבנה מוכרת בעומק 0") הוא שינוי
     אמיתי בטוקניזציה, לא תיקון נקודתי - כל נתיב קוד ב-`parse_wikitext`
     צריך ביקורת מחדש מול קלט רב-שורתי.
   - **ממצא נלווה, לא אותו באג:** בחיפוש אחר תבניות רב-שורתיות
     נמצאו גם בלוקים גולמיים של `<table>...</table>` (HTML, לא
     `{{...}}`) בחוק העונשין - 14 מקרים, כולם צמודים לציטוטי חוקים
     היסטוריים (טרום-1977, כנראה קשורים ל"לוח השוואה" שכבר מתועד
     ב-`data-sources.md`). אלה **נבלעים בשקט לגמרי היום** (לא
     קורסים - פשוט מדולגים, כי אף שורה בטבלה לא מתחילה ב-`{{`).
     בעיה נפרדת מהתבניות הרב-שורתיות (syntax HTML גולמי, לא
     MediaWiki) - כל פתרון שמטפל רק ב-`{{...}}` רב-שורתי **לא**
     יפתור את זה. לא נבדק אם יש שם נוסח מהותי.
   **הכרעה (ברק, 2026-09-14): לא עכשיו.** "תיארת עבודה שנוגעת בכל
   נתיב בפרסר בשביל 3 חוקים. נחזור אליה אחרי שהקורפוס טעון." רשום
   כמשימה פתוחה עם הניתוח המלא למעלה - לא לחזור על החקירה כשמגיעים
   אליה. הממצא הנלווה (בלוקי `<table>` גולמיים) **טופל בנפרד** -
   ראו `check_no_unconsumed_content` למטה, לא ממתין לתיקון הזה.

**`{{ח:מבוא}}` - תוקן (2026-09-14), בלי לגעת בפרסר בכלל.** אושר
ש-`ח:פתיח-התחלה` (ותבניות-קופסה דומות) מעולם לא טופלו במפורש
ב-`parse_wikitext` - הן פשוט נופלות מהענפים הידועים ומדולגות
מאליהן. אותו דבר תקף ל-`ח:מבוא` - נדרש שינוי אחד בלבד:
`_KNOWN_BENIGN_SKIP_TEMPLATES` ב-`ingest_checks.py`. מאומת: חוק
שירות נתוני אשראי (שכשל בעבר על `ח:מבוא`) מצליח עכשיו (296 nodes)
- הזיהוי שהוא בטל עבר במלואו ל-`knesset_odata.py` (מקור סמכותי),
לא לניחוש טקסט. עדכן 2 טסטים שהניחו את המצב הישן (עונשין נכשל
עכשיו רק על `חוקי עונשין` - תבנית תיוג, ראו allowlist למטה).

**allowlist מלא לאישור מרוכז (ברק, 2026-09-14: "תראה לי את הרשימה
המלאה... עם דוגמה לתוכן... ואאשר בבת אחת") - טרם נוסף, ממתין:**

| תבנית | דוגמה אמיתית (חוק) | תוכן מלא |
|---|---|---|
| `מיזמים` | חוק-יסוד: חופש העיסוק | `{{מיזמים\|ויקיפדיה=חוק יסוד: חופש העיסוק}}` |
| `חוקי יסוד` | חוק-יסוד: חופש העיסוק | `{{חוקי יסוד}}` |
| `חוקי מקרקעין` | חוק המכר (דירות) | `{{חוקי מקרקעין}}` |
| `חוקי בחירות` | חוק הבחירות לכנסת העשרים וארבע (הוראות מיוחדות ותיקוני חקיקה) | `{{חוקי בחירות}}` |
| `חוקי אזרחות, תושבות וכניסה לישראל` | חוק קביעת גיל | `{{חוקי אזרחות, תושבות וכניסה לישראל}}` - **שם תבנית אחד עם פסיק בתוכו, לא שתי תבניות** (אומת: `_top_level_templates` מחזירה מחרוזת אחת; הדיווח הקודם "חוקי אזרחות"+"תושבות וכניסה לישראל" כשתי שורות נפרדות היה פיצול שגוי שלי בסקריפט הניתוח, לא של הקוד עצמו) |
| `חוקי עונשין` | חוק העונשין | `{{חוקי עונשין\|בוויקיפדיה=חוק העונשין\|בכנסת=2000479}}` |
| `חוקי ביטחון סוציאלי` | חוק שעות עבודה ומנוחה | `{{חוקי ביטחון סוציאלי}}` |
| `חוקי קורונה` | חוק הארכת תקופות ודחיית מועדים... (נגיף הקורונה החדש) | `{{חוקי קורונה}}` |
| `חוקי מס` | חוק מס ערך מוסף | `{{חוקי מס\|[[w:חוק מס ערך מוסף\|על החוק]] • {{מאגר חקיקה\|2001068\|...}} • ...}}` |
| `חוק נתוני אשראי` | חוק נתוני אשראי | `{{חוק נתוני אשראי}}` - תבנית ניווט/הפניה, שם זהה לשם החוק עצמו, בלי ארגומנטים |

כל אחת עיטורית/ניווטית בעליל - אין נוסח בשום דוגמה. **אושר במרוכז
(ברק, 2026-09-14) - כל 11 נוספו ל-`_KNOWN_BENIGN_SKIP_TEMPLATES`.**
עונשין נקי לגמרי מבחינת תבניות אחרי זה (ראו בדיקה חדשה למטה
שכן עדיין תופסת אותו על סיבה שונה לגמרי).

**מסמכי מכל עומק חדשים - תועד בלבד, לא טופל (כמבוקש):**
- `ח:קטע5` (רמת מכל **חמישית**, מעבר לחלק/פרק/סימן) - 1/406 חוקים
  במדגם: חוק להגברת האכיפה של דיני העבודה.
- `ח:תתתתתת` (עומק תוכן שישי) - 8/406 חוקים: חוק הבנקאות (רישוי),
  חוק ההתייעלות הכלכלית (2017-2018), חוק ההתנתקות והפיצויים
  לנפגעיה, חוק התפזרות הכנסת העשרים וארבע ומימון מפלגות, חוק
  התקנים, חוק לחלוקת חיסכון פנסיוני בין בני זוג שנפרדו, חוק מיסוי
  רווחים ממשאבי טבע, חוק שירותי תשלום.
- `ח:תתתתתתת` (עומק תוכן שביעי) - 2/406 חוקים (תת-קבוצה של הרשימה
  למעלה): חוק לחלוקת חיסכון פנסיוני בין בני זוג שנפרדו, חוק מיסוי
  רווחים ממשאבי טבע.

### id לפי תוכן ברשימות לא-ממוספרות - מומש (2026-09-14)

ברק תיקן את ההערכה מהסבב הקודם: לא "בעיה עתידית ליציבות
provenance" - **שבירת הבסיס**. אושרה הגישה (hash-תוכן, לא מיקום),
עם שתי דרישות: (1) hash יציב בין ריצות/תהליכים - לא `hash()`
המובנה של פייתון (מלוח, `PYTHONHASHSEED`). (2) לוג לכל הפעלת
fallback, כדי לספור שכיחות על הקורפוס המלא.

**מימוש:** `wikitext_parser._short_hash` (`hashlib.sha1`, 8 תווים
ראשונים - יציב לחלוטין, לא תלוי-תהליך). `_lookahead_content`:
הצצה קדימה שורה-אחת מקומית (לא מעבר שני על העץ) - כשסעיף בלי
מספר טבעי, בודקת אם השורה הבאה (הלא-ריקה) היא בדיוק `{{ח:ת...}}`
ומחזירה את תוכנה השטוח; אחרת `None` (לא מנחשת - נופלת חזרה
למנגנון הישן: מיקום + סיומת סידורית מ-`_unique_child_id`). כשיש
hash - נרשם ל-`LegislativeNode.content_derived_ids` (שורש בלבד,
אותו דפוס בדיוק כמו `id_collisions`) - מועבר דרך `IngestPlan`
ומסוכם ב-`batch_validate_ingest.py`.

**מאומת על מועצת הצמחים בפועל:** 98 סעיפים קיבלו id לפי תוכן
(למשל `.../s-824f2100`). שמונה הפריטים הזהים ("(נמחק)") עדיין
מקבלים את *אותו* hash בסיסי כמצופה - וה-fallback הקיים (סיומת
`-2`/`-3`...) עדיין מפשר ביניהם. `check_unique_ids` נקי. טסט
סינתטי חדש (`test_wikitext_parser.py`) משחזר את הדפוס המדויק,
כולל בדיקת יציבות בין שתי ריצות פרסור נפרדות מאותו טקסט.

### HTML גולמי בקורפוס - נחקר, לא טופל

סרקתי את כל 1,021 הכותרות (חוק/חוק-יסוד + תוקף) לתגי HTML גולמיים.
**1,015/1,021 (99.4%) מכילים תג HTML כלשהו** - אבל רובם המכריע
`<br>` (996)/`<wbr>` (312), תגי שורה מוטבעים בתוך שורת `{{ח:ת}}`
שכבר נתפסת - לא מסוכנים באותה מידה (מלכלכים את הטקסט, לא מאבדים
אותו). **הממצא המשמעותי: `<table>`/`<tr>`/`<td>`/`<th>` ב-97
חוקים (9.5%), `<div>` ב-396 (38.8%).**

בדיקת מדגם (8 חוקים עם `<table>`): רוב המקרים `<table>` פותח
בשורה שכן מתחילה ב-`{{ח:ת}}` (למשל `{{ח:ת}} <table>`) - אבל זה
**לא באמת "מוטבע"**: רק תג הפתיחה נתפס (כטקסט גולמי, לא כטבלה),
והשורות שאחריו (`<tr>`/`<td>` בפועל) עדיין נופלות מחוץ לכל
`{{...}}` ונבלעות. חלק אמיתי גם "יתום" לגמרי (לא אפילו `{{ח:ת}}`
פותח). **חוק הביטוח הלאומי וחוק הבטחת הכנסה** - שני חוקים מרכזיים
מאוד, לא פינות אזוטריות - מלאים ב-`<table>`. דוגמה שנבדקה בפועל
(ביטוח לאומי): טבלת "הסעיף הקודם / הסעיף החדש" - לוח השוואה של
מספור פרקים, לא נוסח מהותי - אבל זו רק דוגמה אחת מתוך 97 חוקים,
לא נבדקו כולם. לא ידוע אם יש `<table>` עם נוסח מהותי (למשל טבלת
שיעורי גמלה) בין ה-97.

### בדיקת שפיות הפוכה: מה אבד, לא מה נוצר - מומש

ברק: "היום הבדיקה סופרת מה שנוצר; אני רוצה בדיקה שסופרת מה שאבד.
זו הבדיקה שהייתה תופסת את ה-`<table>`." `ingest_checks.
check_no_unconsumed_content`: כל שורה לא-ריקה שלא מתחילה ב-`{{`
= פער, חוץ משלושה סוגי תוכן ידועים-ולגיטימיים: (1) בין קופסאות
`ח:פתיח-התחלה`/`ח:חתימות`/`ח:מבוא` ל-`{{ח:סוגר}}` התואם (ציטוטים/
חתימות - מכוסים במקום אחר, `parse_citation_registry`); (2) בלוק
תוכן העניינים האוטומטי `<div class="law-toc">...</div>` (עודף
לגמרי מול העץ שכבר קיים); (3) שורות `[[קטגוריה:...]]`. נוסף
ל-`run_sanity_checks`.

**מאומת:** קייטנות/מאבק - נקיים לגמרי (אין רגרסיה, אין false
positive). **עונשין - נכשל שוב, אבל על 327 שורות `<table>`/`<tr>`/
`<td>` אמיתיות (לוח ההשוואה) - בדיוק התרחיש שהוגדר.** 4 טסטים
סינתטיים חדשים (table יתום, TOC לא נתפס, חתימות לא נתפסות,
קטגוריה לא נתפסת).

### `<table>` - הכרעה ומימוש (2026-09-14): לא מפרסרים, כן טוענים

**מחקר לפני מימוש (כמבוקש):**
- **מיקום 97 החוקים עם `<table>`:** 64 בתוספת, **46 בגוף החוק עצמו**
  (חוק יכול להיות בשניהם). **שתי דוגמאות מלאות מגוף החוק, כמבוקש:**
  - **חוק הביטוח הלאומי, §315ב(א):** טבלה של תקרות תשלום בעד טיפול
    בתביעה (אחוזים מחלק הגמלה, לפי סוג טיפול) - **תוכן מהותי אמיתי**,
    לא עיטור. `{{ח:תת}} <table style="...">` ואז `<tr><td>{{ח:תת|(1)}}
    ...` - השורות בתוך התא כן מכילות `{{ח:תת|...}}` בעצמן (מספור
    תצוגה בתוך תא), לא רמת מבנה אמיתית.
  - **חוק מיסוי מקרקעין (שבח ורכישה), §9(ג1ג)(3):** טבלת מדרגות מס
    רכישה צמודות למדד, לפי שנה (2013/2022/2023/2024/2025-2027) -
    **תוכן מהותי אמיתי**, סכומים בפועל.
  - מסקנה: ברק צדק - טבלה בגוף החוק היא לרוב תוכן מהותי (לא רק
    "לוח השוואה" עיטורי), ולא ניתן להניח שכל `<table>` דקורטיבי.
- **תוכן שנוצר-על-ידי-בוט מעבר ל-TOC - נבדק, סגור (ברק, 2026-09-14:
  "טוב, וזה סוגר את השאלה"):** נסרקו 250 חוקים אקראיים לכל
  `class="..."` (על div/span), magic words (`__X__`), ושורות
  interwiki (`[[xx:...`). **הממצא היחיד: משפחת `law-toc`/`law-toc-1/2/3`**
  - כל ה-`law-toc-N` הפנימיים תמיד מקוננים בתוך `<div class="law-toc">`
    החיצוני (כבר מטופל). אין magic words, אין interwiki. לא נמצא
    תוכן-בוט נוסף שדורש טיפול - השאלה נסגרה, לא רק נדחתה.

**מימוש (4 הסעיפים):**
1. **`node_type="raw_block"`** - `wikitext_parser._consume_html_table`
   אוספת את בלוק `<table>...</table>` הגולמי (כולל HTML מקונן בתוכו,
   בלי לפרק מבנה) לצומת יחיד. שני מסלולי זיהוי: (א) מוטבע מיד אחרי
   תבנית עומק (`{{ח:תת}} <table...>`) - הזיהוי נמצא לפני עיבוד
   ה-remainder הרגיל; (ב) "יתום" - שורה עצמאית שמתחילה ב-`<table`
   (לא רגיש לרישיות), מצורף כילד לצומת הפתוח הנוכחי ב-stack. שני
   המסלולים משתמשים באותה לוגיקת צריכה (חיפוש `</table>` בשורות
   הבאות, לא קורס אם לא נמצא - מחזיר מה שיש). `id` נגזר מ-hash
   התוכן (`_short_hash`, אותה תשתית כמו רשימות לא-ממוספרות) -
   `table-<hash>`. הלולאה הראשית עברה מ-`for` על `enumerate(lines)`
   ל-`while line_index < len(lines)` כדי לאפשר דילוג-קדימה אחרי
   צריכת בלוק רב-שורתי - **לא** אותו סוג שינוי כמו תבנית `{{...}}`
   רב-שורתית שנדחתה (שם הבעיה קינון `{{}}` שרירותי; כאן `<table>`/
   `</table>` הם סמנים פשוטים וחד-משמעיים, בלי קינון אמיתי שנצפה).
2. **נחשב כתוכן שנצרך** - `ingest_checks.check_no_unconsumed_content`
   עודכנה עם אותה לוגיקת זיהוי `<table>`/`</table>` (מוטבע/יתום) כמו
   הפרסר - לא מתריעה יותר על תוכן שבאמת נצרך. מאומת: עונשין (327
   שורות שהיו נתפסות) נקי לגמרי עכשיו, 14 צמתי `raw_block` נוצרו.
3. **לא ניתן לעריכה בממשק - תועד, לא מומש** (אין עדיין קוד ממשק/apps
   web בפרויקט). ראו `node.py` - הערה על `node_type` מתעדת שבתצוגה
   עתידית `raw_block` יוצג בלבד, לא ייערך.
4. **`NotImplementedError` מפורש על ניסיון תיקון** - `amend/engine.py:
   _diff_section` בודקת `node_type == "raw_block"` *לפני* שהיא מטפלת
   בהבדל טקסט כ-Mutation רגילה, וזורקת שגיאה מפורשת עם ה-id - לא
   מנחשת הוראת תיקון על טבלה. טסט ייעודי חדש
   (`test_raw_block_not_editable.py`).

**מאומת בפועל:** עונשין (14 raw_block), ביטוח לאומי (30), מיסוי
מקרקעין (2) - שלושתם נטענים נקי לגמרי עכשיו, `check_no_unconsumed_content`
ו-`run_sanity_checks` ריקים. כל הסוויטה עוברת.

**⚠️ חוב מוצר, לא פתרון (ברק, 2026-09-14):** `raw_block` מציל
327 שורות שהיו נעלמות בעונשין - שיפור עצום, ונשאר כפתרון לעכשיו.
**אבל: "אי אפשר לתקן מדרגות מס רכישה" הוא חור אמיתי במוצר**, לא
תכונה. חוק כמו מיסוי מקרקעין שהמדרגות שבו הן בדיוק מה שמשתמש
ירצה לתקן (עדכון סכום, שינוי אחוז) - היום בלתי אפשרי דרך המערכת.

**ייפתר כש:** נדע איך מייצגים טבלה בהוראת תיקון בהצעת חוק. **לפני
מימוש - חובה לבדוק ב-`ha-hoveret-ha-sgula.pdf` איך מנסחים תיקון
לטבלה בחוק** (יש תקדים בוודאי - טבלאות בחוקים לא תופעה חדשה
בכנסת). עד אז: raw_block הוא הפער התיעודי המרכזי של המערכת, לא רק
פרט טכני - כל חוק עם טבלה מהותית בגוף (46/97 בדגימה) חשוף לזה.

**נחקר (2026-09-14, חקירה בלבד - בלי קוד, כמבוקש):** `pdftotext`
על `ha-hoveret-ha-sgula.pdf` (106 עמ') + חיפוש "טבלה"/"תוספת"/"תרשים"/
"עמודה" לאורך כל המסמך. **תוצאה מעורבת - יש תקדים חלקי, לא מלא:**

- **§7.14 "הוספה, תיקון או ביטול של תוספת" - תקדים אמיתי, אבל
  לרשימות חד-ממדיות בלבד.** המדריך מתייחס לתוספת (השם הרשמי
  ל"טבלה"/רשימה המסופחת לחוק) כיחידה ממוספרת לכל דבר - הפריטים
  שבתוספת נקראים "פרטים" (לא "סעיפים", אבל **אותה דקדוק תיקון
  מדויק**: "בתוספת מס' X לחוק העיקרי, בפרט מס' Y, במקום 'טקסט קיים'
  יבוא 'טקסט חדש'"; הוספת פרט: "בתוספת מס' X ..., אחרי פרט מס' 5
  יבוא: '(6) הממונה לפי חוק הפיקדון...'"; ביטול תוספת שלמה: "התוספת
  מס' X לחוק העיקרי - בטלה"). כלומר: **תוספת חד-טורית (רשימת פריטים
  ממוספרים, כל אחד שורת-טקסט אחת) יש לה תקדים מלא ומדויק ב-§7.14 -
  בדיוק כמו סעיף רגיל, רק עם "פרט" במקום "סעיף".**
- **טבלה אמיתית, רב-טורית (כמו מדרגות מס רכישה - טור סכום + טור
  אחוז) - אין לה שום תקדים במדריך.** כל הדוגמאות בפועל (§7.14.2)
  הן שורה בודדת של טקסט לכל פריט, לא מספר עמודות עם ערכים נפרדים
  לכל אחת. "עמודה"/"עמודות" מוזכרות רק בהקשר הטכני-לגמרי של כלי
  ה-Word לתיקון פריסת הטבלה במסמך הניסוח עצמו (§4, "כלים מתקדמים") -
  לא כהנחיה איך *לנסח* תיקון לטבלה רב-טורית מהותית. **אין כאן כלום
  לגבי זה - כפי שברק ביקש לומר אם אין.**
- **"תרשים" (דיאגרמה/גרף)** - שני המופעים היחידים במסמך כולו הם
  לתרשים-הזרימה של הליך החקיקה **עצמו** (בתוכן העניינים של המדריך) -
  לא לתרשים כתוכן מהותי בתוך חוק. **אין תקדים כלל.**

**מסקנה מעשית לעתיד (לא מומש - חקירה בלבד):** `raw_block` שמייצג
תוספת חד-טורית (רשימת "פרטים" ממוספרת) יכול עקרונית לעבור ל-node_type
רגיל (כמו סעיף, עם "פרט" כתווית) ולהיות ניתן לעריכה סטנדרטית - יש
דקדוק תיקון מדויק לזה. `raw_block` שמייצג טבלה רב-טורית אמיתית
(מדרגות מס) **נשאר בלי פתרון ידוע** - המדריך לא מכתיב איך לנסח
הוראת תיקון לתא בודד בטבלה כזו; ייתכן שהפרקטיקה בפועל (מחוץ למדריך)
פשוט מחליפה את הטבלה כולה בטבלה חדשה, אבל זה ניחוש - לא אומת.

### 🆕 תוספת חד-טורית: raw_block -> node_type רגיל, ניתן לעריכה

משימה נפרדת (ברק, 2026-09-16) - **נפתחת כאן, לא מתחילה עדיין.**
לפי המסקנה לעיל: תוספת חד-טורית (רשימת "פרטים" ממוספרת בתוך
`<table>`) יש לה תקדים מלא ומדויק בחוברת הסגולה (§7.14 - "פרט" עם
אותו דקדוק תיקון בדיוק כמו "סעיף") - בניגוד לטבלה רב-טורית אמיתית
(מדרגות מס וכו') שנשארת חור מוצרי בלי פתרון ידוע ולא חלק מהמשימה
הזו. **היקף (כשמתחילים):** להבחין בפרסור בין תוספת חד-טורית (כל
שורה בתוך ה-`<table>` = פריט טקסט בודד, "פרט" אחד) לטבלה רב-טורית
אמיתית (יותר מעמודת-תוכן אחת לשורה) - ולתת ל-raw_block החד-טורי
node_type רגיל עם "פרט" כתווית, ניתן לעריכה כמו סעיף. טבלה רב-
טורית ממשיכה raw_block כרגיל. **לפני מימוש:** למפות מתוך 97 החוקים
עם `<table>` כמה בפועל חד-טוריים לעומת רב-טוריים - לא נמדד.

### מנה של 500 (seed=200) - תרומת raw_block לבדו, מבודדת

ברק ביקש מדידה מבודדת (לא מצטברת) של raw_block לבד, כי המנה
הקודמת (seed=100) איגדה שלושה תיקונים בלי מדידה ביניהם. **הבסיס
"לפני" נמדד מחדש בפועל** (לא שוחזר מהשיחה) - `git worktree` על
הקומיט שקדם ל-raw_block (`2941d7f`), אותו seed=200 בדיוק, אותם
מטמוני כותרות/KNS:

| | לפני raw_block (2941d7f) | אחרי raw_block (HEAD) |
|---|---|---|
| הצליחו | 425/500 (85.0%) | 459/500 (91.8%) |
| כשלי sanity | 72 | 38 |
| unexpected (קריסה) | 3 | 3 (אותם 3 חוקים - באג התבנית הרב-שורתית שנדחה) |
| id_collisions | 41/22 חוקים | 127/30 חוקים |
| content_derived_ids | 381/58 חוקים | 546/86 חוקים |

**תרומת raw_block לבדו: +34 חוקים (72→38 כשלי sanity, הפרש מדויק)**
- כל כשל שהיה על `<table>` נעלם. עליית id_collisions/content_derived_ids
צפויה - יותר חוקים מגיעים עכשיו עמוק מספיק בפרסור כדי להגיע לאזורים
האלה, לא רגרסיה.

**raw_block בפועל במנה:** 135 צמתי `<table>` ב-**34 חוקים** מתוך 459
שהצליחו. (נוסף `IngestPlan.raw_block_count` + מונה מקביל ל-id_collisions/
content_derived_ids ב-`batch_validate_ingest.py`, כמבוקש.)

### unconsumed_content שנשאר - 3 מקרים, ואז סריקת קורפוס-שלם

3 כשלי `check_no_unconsumed_content` שנשארו במנה אופיינו ידנית מול
הוויקיטקסט המקורי:
- **חוק רשות מקרקעי ישראל, שורה 286:** *לא* אובדן תוכן. השורה
  כפילות מדויקת של קטע טקסט שכבר מופיע במלואו בתוך השורה הבאה
  (287, אותו `{{ח:תת|(ט)|(1)}}`) - כנראה שארית עריכה כפולה במקור.
- **חוק מכוני כושר (רישוי ופיקוח), שורה 19:** אובדן תוכן אמיתי -
  הגדרה שלמה ("השר" – שר המדע התרבות והספורט) בלי תבנית
  `{{ח:ת|סוג=הגדרה}}` פותחת במקור.
- **חוק התחשבנות בין בתי חולים..., שורה 113:** אובדן תוכן אמיתי -
  משפט נורמטיבי שנשבר לשתי שורות פיזיות באמצע משפט.

ברק העיר על דפוס: אם טקסט יכול להישבר לשתי שורות באמצע משפט, זו
אותה הנחה שבורה כמו בתבנית הרב-שורתית שנדחתה (פרסור שורה-שורה).
**ביקש מדידה על הקורפוס השלם (לא מדגם) לפני כל הצעת פתרון**, כדי
להכריע אם התופעה עשרות חוקים (תיקון ממוקד) או מאות (מצדיק לחזור
על הדחייה של המעבר לפרסור לא-שורתי).

**נבנה `tools/scan_unconsumed_content.py`** - אותה לוגיקת דילוג
בדיוק כמו `ingest_checks.check_no_unconsumed_content` (שוכפלה
במכוון, לא יובאה - כלי ניתוח חד-פעמי, לא רוצה לצמד אותו לפורמט
ההודעה הפנימי), אבל על **כל 1,021 הכותרות המסוננות** (לא מדגם),
עם סיווג היוריסטי-ראשוני לשלוש קטגוריות (DUPLICATE/CONTINUATION/
STANDALONE - לפי סימן פיסוק מסיים בשורה הקודמת + בדיקת הכלה
בשורה הבאה).

**⚠️ הסיווג האוטומטי לקוי במידה ידועה ומתועדת:** רוב שורות התבנית
מסתיימות פיזית ב-`}}` (תחביר, לא פיסוק), לא בסימן פיסוק עברי - אז
הקטגוריה CONTINUATION "יורה" גם על שורות שהן בפועל STANDALONE
(כמו מכוני כושר, שסווג אוטומטית כ-CONTINUATION למרות שזו הגדרה
עצמאית). **לכן כל אחד מ-11 הממצאים אומת ידנית מול הוויקיטקסט
המקורי**, לא הוסתמך על הספירה האוטומטית.

**תוצאה סופית - 11/1,021 חוקים (1.1%) עם שורה לא-נצרכת כלשהי, לא
עשרות ולא מאות. מתוכם, בבדיקה ידנית:**
1. **7 חוקים - אובדן תוכן נורמטיבי אמיתי**, כולם מהמשפחה שברק
   זיהה (משפט/פסקה שממשיכים בלי תבנית פותחת, לא תמיד "שבירת
   שורה" טהורה - יש גם וריאציה של תבנית פגומה/מקוטעת במקור):
   חוק מכוני כושר, חוק התחשבנות בין בתי חולים, **חוק הגבלת שכר
   טרחה** (שורה 89, "- סכום נוסף כאמור בסעיף קטן (ג):" משלימה
   משפט שנקטע ב-88), **חוק הסכמים קיבוציים** (שורה 125, משפט
   נוסף באותה פסקה (1) בלי תבנית `{{ח:תתת}}` משלה), **חוק עונש
   מוות למחבלים** (שורה 58, "47א1 למפגש עם..." משלימה "לפי סעיף"
   שנקטע ב-57), **חוק איסור הונאה בכשרות** (שורה 490, משפט נוסף
   באותו סעיף קטן (א) בלי תבנית משלו), **חוק מוסדות חינוך
   תרבותיים ייחודיים** (שורה 72, `:ף (4) אין בהוראות...` - תבנית
   `{{ח:תתת|(4)}}` פגומה/מקוטעת במקור, לא רק חסרה).
2. **1 חוק - כפילות עריכה, לא אובדן** (חוק רשות מקרקעי ישראל,
   כבר אופיין למעלה).
3. **1 חוק - תשתית בוט שפירה, לא תוכן** (חוק הרשות לפיתוח
   ירושלים, שורה 170: `<includeonly>{{#ifeq:...[[קטגוריה:שלטון
   מקומי|...]]}}</includeonly>` - זריקת קטגוריה אוטומטית עטופה
   ב-`<includeonly>`, אותה משפחה בדיוק כמו `[[קטגוריה:...]]` הרגיל
   שכבר בטיפול, רק עטוף. לא נבדק כחלק מסריקת 250 החוקים הקודמת
   כי `<includeonly>` לא הופיע שם - ממצא חדש קטן, לא ה-`<table>`.
4. **1 חוק - תו בודד זניח** (חוק הנוער (שפיטה, ענישה ודרכי
   טיפול), שורה 673: `.` בשורה נפרדת, אחרי משפט שכבר הסתיים
   בנקודה משלו - שארית עריכה, לא תוכן).
5. **1 חוק חופף לבאג הידוע והנדחה** (חוק הגנה על בריאות הציבור
   (מזון) - 109 שורות, כבר ברשימת ה-unexpected/קריסת התבנית
   הרב-שורתית משתי המנות. הסריקה הזו עוקפת את הפרסר לגמרי ולכן
   חושפת את השורות הגולמיות שנבלעות בתוך אותה קריסה - לא תופעה
   חדשה, אותו באג שכבר תועד ונדחה).

**מסקנה למספר שביקשת:** 7/1,021 (0.68%) - לא עשרות, בהחלט לא
מאות. **זה לא מצדיק לחזור על הדחייה של פרסור לא-שורתי** - ההיקף
קטן משמעותית ממה שהצדיק את הדחייה במקור. שני מנגנוני-שורש שונים
בתוך ה-7: (א) משפט/פסקה שממשיכה בלי תבנית פותחת - 5 חוקים; (ב)
תבנית פגומה/מקוטעת בפועל במקור (`:ף` במקום `{{ח:תתת|`) - חוק
אחד. לא הוצע פתרון - ברק ביקש מפורשות את המספרים לפני הצעה.

### שלושה תיקונים קטנים (2026-09-14) - אחרי אישור המספרים

ברק אישר: "0.68% סוגר את השאלה - הדחייה של הפרסור הלא-שורתי
מוצדקת." ואישר שלושה תיקונים:

1. **`<includeonly>` עטוף-קטגוריה** - נוסף ל-`ingest_checks.
   check_no_unconsumed_content` (`_INCLUDEONLY_CATEGORY_RE`) **וגם
   ל-`wikitext_parser`** (לא רק לבדיקה - אחרת השורה הייתה נדבקת
   בטעות כהשלמת נוסח, ראו סעיף 3 למטה).
2. **תו פיסוק בודד בשורה נפרדת** (`.`/`,`/`;`/`:`) - `_STRAY_PUNCTUATION_RE`,
   באותם שני המקומות. מתעלם, לא תוכן.
3. **השלמת נוסח לצומת הקודם** - ההכרעה המלאה: "שורה שאינה
   מתחילה ב-{{ ואינה באזור מוכר מצורפת לצומת הקודם, בלי לנחש
   מבנה... אבל היא חייבת להיות מסומנת." מומש ב-`wikitext_parser.
   parse_wikitext` (הלולאה הראשית): שורה כזו מצורפת ל-`stack[-1]` -
   הצומת האחרון שנפתח - הן ל-`text` (אחרי `_flatten`) והן ל-`text_raw`,
   ומסומנת (`LegislativeNode.completed_by_continuation=True` + נרשמת
   ל-`root.continuation_completions`, אותו דפוס בדיוק כמו
   `id_collisions`/`content_derived_ids`). **חוק מוסדות חינוך תרבותיים
   ייחודיים (התבנית הפגומה `:ף (4)`) מקבל בדיוק אותו טיפול, בלי ניסיון
   שחזור** - כהוראה מפורשת.

   **⚠️ סיכון קריטי שנבדק ותוקן לפני המימוש:** אם משהו "מצורף לצומת
   הקודם" בלי מעקב אזורים, שורות חתימה/ציטוט (בתוך קופסת
   `ח:פתיח-התחלה`/`ח:חתימות`/`ח:מבוא`...`ח:סוגר`) ותוכן העניינים
   האוטומטי (`<div class="law-toc">`) היו נדבקים בטעות לצומת
   הנוסח הלא-נכון - **שני fixtures האמת (`kaytanot.wikitext`,
   `maavak-birgunei-plisha.wikitext`) מכילים בדיוק את התבניות
   האלה**, אז זו לא הייתה תיאוריה. לכן `wikitext_parser` קיבל מעקב
   אזורים מלא (`in_skip_zone`/`in_toc_zone`), משוכפל במכוון מ-
   `ingest_checks.py` (אותו דפוס כמו `_TABLE_OPEN_RE`/`_TABLE_CLOSE_RE`
   כבר קיים) - **לא רק לבדיקה, גם לפרסר עצמו**, שלא היה לו מעקב כזה
   בכלל עד עכשיו (הסתמך על כך שהשורות האלה נופלות סתם לדילוג הכללי).
   מאומת: שני ה-fixtures עדיין עוברים `run_sanity_checks` נקי, טסטים
   סינתטיים חדשים ל-3 האזורים (חתימות, `<includeonly>`, תו פיסוק).

   `ingest_checks.check_no_unconsumed_content` עודכנה בהתאם - אחרי
   התיקון, כל שורה שהייתה מדווחת כ"אבודה" נצרכת בפועל דרך ההשלמה,
   אז ה-`problems.append` הסופי הוסר (לא עוד false positive על תוכן
   שבאמת נשמר). הפונקציה נשארת קו-הגנה שקוף (לא הוסרה) למקרה של
   דרך פרסור עתידית שכן תדלג בשקט.

   **מאומת בפועל על 9/10 מהחוקים האמיתיים** (הרצה ישירה מול הרשת,
   לא רק פיקסצ'ר סינתטי): כל 7 מקרי אובדן-התוכן נטענים עכשיו נקי
   מבחינת unconsumed_content, עם `continuation_completions` נרשם
   כראוי. **חוק איסור הונאה בכשרות עדיין נכשל - אבל על פער אחר
   ונפרד לגמרי (`ח:הערה` ברמה העליונה, כבר בתור למשימה הבאה)**,
   לא רגרסיה מהתיקון הזה.

   כל הסוויטה (כולל שלושת ה-fixtures האמיתיים) עוברת אחרי התיקון.

### מנה של 1,021 (כל הקורפוס, אחרי התיקון) - פילוח כשלים מלא

ברק ביקש מדידה על הקורפוס השלם (לא מדגם), כדי להחליט אם להמשיך
לתקן או לעצור. **942/1,021 (92.26%)** הצליחו. `unconsumed_content`:
**0 כשלים נותרים** (7/7 תוקנו בפועל, לא רק במדגם). 79 כשלים,
פילוח:

| סיבה | חוקים |
|---|---|
| `ח:הערה` ברמה העליונה בלבד | 43 |
| עומק 6-7 (`ח:תתתתתת`/`ח:תתתתתתת`) בלבד | 22 |
| שניהם | 4 |
| **סה"כ ייפתר ע"י שני התיקונים** | **69 (87%)** |
| קריסה (`IndexError`, באג התבנית הרב-שורתית שכבר נדחה) | 6 |
| חד-פעמיים לא מסווגים | 4 |

**ברק: "תיקון אחד שסוגר 87% מהכשלים זה לא זנב ארוך, זה שני
פערים ידועים. תעשה את שניהם" - אישר מראש, בתנאי שכל אחד באמת
זול כמו שתואר, אחרת לעצור ולדווח.**

**ח:הערה ברמה העליונה - אומת ותוקן (זול כמצופה):** נבדקו כל
47 החוקים בפועל מול הוויקיטקסט - **47/47 מקרים (100%) נמצאים
בתוך קופסת `ח:פתיח-התחלה`/`ח:חתימות`/`ח:מבוא`...`ח:סוגר`
(אזור דילוג שכבר קיים וכבר מתעלם מהתוכן שבפנים, בלי קשר לשם
התבנית).** הפער האמיתי: `ingest_checks._top_level_templates`
לא מודעת לאזורי דילוג - סופרת כל תבנית ברמה העליונה בלי קשר
להקשר, ולכן דגלה `ח:הערה` כ"לא מוכרת" גם כשהיא בפנים לגיטימית.
**תיקון: הוספת `"ח:הערה"` ל-`_KNOWN_BENIGN_SKIP_TEMPLATES`** -
שורה אחת, בלי שינוי ב-`wikitext_parser.py` (הפרסר עצמו כבר נכון).

**עומק 6-7 - אומת ותוקן (זול כמצופה):** נבדק מול הוויקיטקסט -
אותה משפחה בדיוק כמו עומק 1-5 (`{{ח:תתתתתת|(תווית)}}`/
`{{ח:תתתתתתת|(תווית)}}`, אותו תחביר ארגומנטים, כולל `סוג=הגדרה`).
עומק 6 (`ח:תתתתתת`) נמצא בפועל בחוק התכנון והבניה ועוד; עומק 7
(`ח:תתתתתתת`) נמצא ב-4 חוקים (חוק מיסוי רווחים ממשאבי טבע ועוד).
**תיקון: שתי שורות ב-`_CONTENT_DEPTH`** (`wikitext_parser.py`,
עומק 5/6) **+ שתי שורות ב-`_KNOWN_STRUCTURAL_TEMPLATES`**
(`ingest_checks.py`) - בדיוק כמו שתואר, בלי לוגיקה חדשה.

**מאומת בפועל:** חוק הארכיונים (ח:הערה בלבד), חוק התכנון
והבניה (עומק 6, 4,794 nodes), חוק מיסוי רווחים ממשאבי טבע
(עומק 7), חוק הביטוח הלאומי (שניהם) - כולם נטענים נקי. כל
הסוויטה עוברת.

**מנה על כל 1,021 (אחרי שני התיקונים): 1,010/1,021 = 98.92%.**
לא בדיוק 99% (חסר חוק אחד) - דווח מדויק, לא עוגל. 11 כשלים
נותרים, כולם בשתי הקטגוריות שברק כבר הגדיר כמקובלות מראש
(קריסות + חד-פעמיים).

### `צבע גופן` - תיקון שלישי זול (אותו מנגנון כמו ח:הערה)

חוק התכנון והבניה/תזכיר חוק נכשל אחרי תיקון עומק 6-7 על תבנית
נפרדת: `תבנית לא מוכרת ברמה העליונה: צבע גופן`. ברק ביקש בדיקה
לפני תיקון ("אם זו תבנית עיצוב - ל-allowlist ונגמר").

**נבדק בפועל:** `{{צבע גופן|...}}` היא עיטור טקסט (צבע גופן
אדום) - שני מופעים בקובץ מקוננים בתוך ארגומנט של `{{ח:קטע2/3}}`
(לא שורה עליונה בפני עצמה, לא רלוונטי), ומופע שלישי שכן שורה
עליונה בפני עצמה - **בתוך קופסת `{{ח:מבוא}}`...`{{ח:סוגר}}`**,
אזהרת עריכה שמציינת שזה נוסח המשלב תזכיר חוק. **אותה סיבה
בדיוק כמו ח:הערה** - זו התבנית היחידה בכל הכשלים שנשארו עם השם
הזה (מופע יחיד בכל הקורפוס). **תיקון: הוספת `"צבע גופן"`
ל-`_KNOWN_BENIGN_SKIP_TEMPLATES`** - שורה אחת. מאומת: החוק נטען
נקי (4,359 nodes). כל הסוויטה עוברת.

### חוק התקשורת (שידורים) - מנגנון שלישי, לא באג

ברק שם לב שהכשל שונה במהותו: "העץ לא מכיל אף צומת נורמטיבי"
(לא "תבנית לא מוכרת" כמו כל השאר) - ביקש בדיקה נפרדת.

**נבדק בפועל:** הוויקיטקסט של החוק מכיל **13 שורות בלבד** - כותרת,
קופסת מאגר/תיבה (מפנה גם לפסק-דין: "בג״ץ 49765-07-26"), ו-`ח:מבוא`
יחיד: **"בבג״ץ 29765-07-26 ניתן צו ארעי המשהה את כניסתו לתוקף של
חוק התקשורת (שידורים), התשפ״ו–2026."** אין אף `{{ח:סעיף}}` בטקסט
כלל - **לא באג פרסור, שיקוף נכון של המציאות**: החוק התקבל, אבל
בג״ץ עיכב את כניסתו לתוקף בצו ארעי, ונכון לרגע השליפה דף הוויקיטקסט
שלו הוא בעצם הודעת-עיכוב בלבד, בלי גוף נורמטיבי.

**זה אכן מנגנון שלישי, כפי שברק שיער** - לא "תבנית לא מוכרת" (הפרסור
הצליח במלואו על מה שיש), ולא "תוכן שאבד" (אין תוכן נורמטיבי לאבד) -
`check_has_normative_content` תופסת בדיוק את המקרה שהיא נועדה לתפוס:
עץ ריק-מנוסח. **שאלה למוצר, לא לקוד:** האם חוק שקפא בצו ארעי שייך
לקורפוס בכלל (הוא "בתוקף" מבחינת KNS_IsraelLaw כנראה, אבל אין לו
עדיין תוכן לטעון)? לא הוצע פתרון, לא נבדק אם יש עוד חוקים דומים -
מחוץ לתחום שהתבקש כרגע.

**נבדק בפועל: כמה עוד? נסרק כל 1,021 הכותרות (לא רק הכשלים) לבדיקת
`{{ח:סעיף}}` אחד לפחות. תוצאה: חוק התקשורת (שידורים) הוא היחיד -
מקרה בודד, לא קטגוריה.**

### מנה סופית על כל 1,021 (אחרי צבע גופן): 1,011/1,021 = 99.02%

**10 חוקים לא נטענים - בשמם, כמבוקש (ברק: "אני רוצה לדעת מה חסר
במערכת"):**

**קריסה - IndexError, באג התבנית הרב-שורתית שכבר נדחה עד אחרי
טעינת הקורפוס (6):**
1. חוק תקשורת דיגיטלית עם גופים ציבוריים
2. חוק הגנה על בריאות הציבור (מזון)
3. חוק התמודדות עם תקיפות סייבר חמורות במגזר השירותים הדיגיטליים
   ושירותי האחסון (הוראת שעה)
4. חוק העברת סמכויות (ממונים על מחוזות וקציני מחוז)
5. חוק העבירות המינהליות
6. חוק זכויות נפגעי עבירה

**תבנית לא מוכרת - לא נחקר, פער תיעודי פתוח (3):**
7. **חוק חופש המידע** - תבנית ברמה העליונה בשם זהה לשם החוק עצמו.
8. **חוק הפטנטים** - `ח:חיצוני` ברמה העליונה.
9. **חוק להגברת האכיפה של דיני העבודה** - `ח:קטע5`.

**מנגנון שלישי, נחקר ואופיין - לא באג (1):**
10. **חוק התקשורת (שידורים)** - קפא בצו ארעי של בג״ץ, אין לו עדיין
    תוכן נורמטיבי לטעון (ראו לעיל).

---

### ✅ law_id יציב לכל חוק בקורפוס - מומש (2026-09-14)

עיצוב מלא (תחיליות מקור, סדר קדימות IsraelLaw→Bill→ידני, הגילוי
ש-`ח:מאגר2`=`KNS_Bill.Id`, המיפוי הידני ל-4 החוקים בלי מספר כנסתי)
מתועד ב-`docs/data-sources.md` §2 (כדי לא לשכפל - נוגע ישירות למראי
מקום, משימה 8). מימוש: `packages/corpus/law_id.py` (`build_law_id`/
`resolve_law_id` - נקודת אכיפה יחידה, ראו docstring שם), `packages/
corpus/manual_law_ids.py`. `tests/unit/test_law_id.py`.

**באג שנת-התאמה - נמצא ותוקן (2026-09-14).** ריצת `tools/load_corpus.py`
המלאה הניבה 998 רשומות manifest אבל רק 997 קובצי SQL - שני חוקים
אמיתיים שונים ("חוק התוכנית לסיוע כלכלי (הוראת שעה - חרבות ברזל)",
2025, ו-"...(תשפ"ד)", 2023) התנגשו על `law-2211393`. שורש הבעיה:
`classify_validity`/`find_israel_law_id` מתאימים לפי שם-בסיס אחרי
הסרת שנה (עמידות מול וריאנטים בכתיב) בלי לבדוק שהשנה בפועל תואמת -
חוק 2025 (בלי רשומת `KNS_IsraelLaw` עצמאית) הותאם בשקט לרשומת 2023.
5 החוקים המותאמים-לפי-שם האחרים בקורפוס נבדקו ונמצאו תקינים (שנה
נכונה בכל אחד).

**תוקן:** שתי הפונקציות (`packages/corpus/knesset_odata.py`) מקבלות
`wikitext_full_title` אופציונלי (הכותרת המלאה מ-`{{ח:כותרת}}`, כולל
שנה) - כשניתן, כל התאמה (exact/normalized) מאומתת גם מול שנה;
חוסר-התאמה **לא** נחשב "עמימות" (`ambiguous`) אלא גורם למעבר לשכבת
ההתאמה הבאה, כי ידוע בוודאות שהמועמד שגוי - לא רק שיש כמה מועמדים
סבירים. עמימות אמיתית (כמה מועמדים תקפים ל-`_pick_valid_candidate`)
עדיין מחזירה מייד בלי מעבר לשכבה הבאה, כבמקור. `law_id.resolve_law_id`
ו-`tools/load_corpus.py` (מחלץ `full_title` מ-`{{ח:כותרת|...}}`)
מעבירים אותו תמיד כעת. תוספת ל-`tests/unit/test_law_id.py`: מקרה
סינתטי שמשחזר בדיוק את הבאג (מועמד יחיד, שנה שונה) ומוודא נפילה
ל-`bill-` נכונה במקום `law-` שגוי, ומקרה תאימות-לאחור (בלי הפרמטר -
אותה התנהגות כמו לפני התיקון).

**הסתייגות מכוונת שברק זיהה (2026-09-14): אותו מנגנון משמש גם
לסינון התוקף המקורי.** `should_ingest`/`classify_validity` הכתובה
נגד כל ~1,112 הכותרות **לפני** סינון תוקף (קובעת אילו ~1,021 נכנסות
לקורפוס בכלל) משתמשת באותה לוגיקת התאמה-לפי-שם - ונבדקה כרגע רק
בכיוון law_id, לא בכיוון הזה. חוק שהותאם בטעות לרשומת שנה אחרת יכול
היה לקבל את תוקפה: אם הרשומה הלא-נכונה מסומנת "בטל"/"פקע"/"נושן"
והנכונה "תקף", החוק נזרק מהקורפוס בשקט. ביקורת מלאה על נתיב הסינון
המקורי בתהליך - ראו סעיף הבא לתוצאות (יתעדכן).

**✅ הביקורת בוצעה (2026-09-14) - תוצאה: אף חוק לא נזרק בטעות.**
סקריפט חד-פעמי (`validity_year_audit.py`, לא נשמר בריפו - חד-פעמי)
הריץ את שתי הגרסאות של `classify_validity` (בלי/עם `wikitext_full_title`)
על **כל 1,112 הכותרות** (לא רק ~1,021 שעברו את הסינון - גם אלה
שנפסלו, כדי לתפוס גם היפוך בכיוון ההפוך). **0 כשלי רשת, 7 אי-התאמות
מתוך 1,112 (0.6%), 0 מהן הפכה את `should_ingest`.**

כל 7: ההתאמה הישנה (בלי בדיקת שנה) קבעה "תקף" בביטחון מלא מול
רשומת KNS יחידה (`exact`) - עם התיקון, השנה לא תואמת ולכן ההתאמה
נדחית ותוצאת התוקף הופכת ל-`not_found` (לא ידוע) - וכיוון ש-
`should_ingest` כולל כברירת מחדל כל דבר לא-ידוע (לא רק "תקף"
במפורש), התוצאה זהה בשני המקרים: **`True -> True` בכל 7, בלי יוצא
מן הכלל.**

| חוק | שנה בכותרת מלאה (ויקיטקסט) | שנה ברשומת KNS שהותאמה (הישנה) |
|---|---|---|
| חוק ההגנה על חוסים | 1966 | 1965 |
| חוק הלוואות לשכר לימוד | 2002 | 2001 |
| חוק הפעלת רכב (מנועים ודלק) | 1960 | 1961 |
| חוק התאגידים הביטחוניים (הגנה על אינטרסים ביטחוניים) | 2005 | 2006 |
| חוק התוכנית לסיוע כלכלי (הוראת שעה – חרבות ברזל) | 2025 | 2023 |
| חוק להגנת רכוש מופקד | 1964 | 1965 |
| חוק מס הכנסה (תוספת פחת על נכסים עסקיים) | 1964 | 1958 |

**דפוס ברור: 6 מתוך 7 הם פער של שנה בודדת (±1), לא חוק אחר
לגמרי.** תואם היטב תופעה ידועה בציטוט חקיקה עברית - שנה עברית
(שחוצה את גבול השנה הלועזית בספטמבר-אוקטובר) יכולה להיות מתורגמת
לשנה הלועזית לפי תחילתה או סיומה, ותאריך החקיקה בפועל מול תאריך
הפרסום ברשומות יכולים ליפול בשני צידי אותו גבול - **לא בהכרח שגיאה,
אלא אי-בהירות מוסכמה ידועה.** **רק המקרה השביעי - "חרבות ברזל" -
הוא פער אמיתי של שנתיים בין שני חוקים אמיתיים ושונים** (זה בדיוק
המקרה שכבר תועד למעלה כבאג `law-2211393`, וגם כאן - באותו אופן -
לא גרם לנזק כי אין רשומת KNS אחרת מתחרה ל-2025 שהייתה יכולה "לתפוס"
את הסטטוס השגוי בכיוון ההפוך).

**המחיר המדויק של התיקון (לא בחינם):** 7 החוקים האלה עוברים מסיווג
תוקף מפורש ("תקף", מקור: KNS) לסיווג "לא ידוע" (`not_found`) -
המערכת עדיין תטען אותם (`should_ingest`=True כברירת מחדל), אבל
בלי לדעת את מצב התוקף הרשמי שלהם מ-KNS. זו פשרה מכוונת - עדיפות
לאי-ודאות גלויה על פני ודאות כוזבת. **קובצי הביקורת המלאים
(`validity_year_audit_results.json`/`_mismatches.json`, 1,112/7
רשומות) לא נשמרו בריפו - חד-פעמיים, בסקראצ'פד בלבד.**

---

### 3 התבניות הלא-מוכרות (7,8,9 לעיל) - נחקרו, לא תוקנו (2026-09-14)

חקירה בלבד לפי בקשת ברק ("בלי נגיעה בפרסר עצמו הלילה - כל נגיעה שם
מגלה משהו שדורש הכרעה"). שלושתן נחקרו ע״י שליפת הוויקיטקסט הגולמי
+ הדף/התבנית עצמה. שתיים מתוך השלוש אכן חשפו החלטה תלויה-הכרעה -
בדיוק כפי שברק חזה.

**7. חוק חופש המידע - `{{חוק חופש המידע}}`: דקורטיבי, לא תוכן נורמטיבי.**
התבנית (`תבנית:חוק חופש המידע`) היא `{{ניווט קבוצות|...}}` - קופסת
ניווט ויקי סטנדרטית (קישורים לתקנות/צווים קשורים + ויקיפדיה + אתר
הכנסת), **לא** תוכן משפטי. ממוקמת אחרי `{{ח:סוף}}` (מחוץ לגבול הטקסט
המשפטי) ולפני תגית הקטגוריה. אין סיבה עקרונית שלא לדלג עליה - היא
לא תוכן החוק. **נותר פתוח:** האם היא ייחודית לחוק הזה, או שדפוס דומה
(קופסת ניווט בשם-החוק-עצמו, אחרי `{{ח:סוף}}`) חוזר בחוקים נוספים
שכרגע *כן* נטענים בהצלחה (ואולי הקופסה שם פשוט מדולגת בשקט ע״י
מנגנון אחר, או שלא קיימת) - לא נבדק כרגע, מחוץ להיקף.

**8. חוק הפטנטים - `ח:חיצוני` "ברמה העליונה": לא תבנית לא-מוכרת -
המשכיות משפט חוצה-שורה, אותה משפחת באג כמו התיקון הקודם ל-raw_block.**
`ח:חיצוני` עצמו **כן** מוכר ומטופל כרגיל ע״י הפרסר בכל שאר הקורפוס -
מופיע 151 פעמים ב-`penal.wikitext` בלבד (מבין מבחני הזהב הקיימים)
בלי בעיה. התבנית עצמה (`תבנית:ח:חיצוני`) היא תבנית קישור (חיצוני אם
הארגומנט הראשון מתחיל ב-http/mailto, פנימי אחרת) - **תוכן נורמטיבי**
לכל דבר (הטקסט המקושר הוא חלק מהטקסט המשפטי עצמו), בדיוק כמו
`{{ח:פנימי}}` הקיים - לא דקורטיבי. הכשל בפועל: שורה 62 בוויקיטקסט
מתחילה ב-`{{ח:חיצוני|...}}` אבל היא **לא** קריאת-תבנית ברמת-שורה
אמיתית - היא המשך המשפט מהשורה הקודמת (`{{ח:ת}} ((רשימת המדינות
החברות` נגמר ב-`((` פתוח, נסגר רק ב-`))` בסוף שורה 62). הלולאה
הראשית מנסה לשלוח לפי שם-התבנית שפותחת את השורה ולא מזהה `ח:חיצוני`
כפתיח-שורה חוקי. **זו בדיוק אותה משפחת בעיה שהתיקון הקודם (continuation-
attach, ראו למעלה) טיפל בה - אבל שם השורה החורגת הייתה טקסט גולמי
בלי שום תבנית; כאן השורה החורגת *מתחילה* בתבנית inline לא-פותחת-שורה.
דורש הכרעה: האם להרחיב את מנגנון ה-continuation-attach לזהות גם
'שורה שנפתחת בתבנית inline מוכרת אבל לא-מבנית' כהמשך, לא רק טקסט
גולמי - החלטה תכנונית, לא תיקון-שורה.**

**9. חוק להגברת האכיפה של דיני העבודה - `ח:קטע5`: לא סתם עומק
חסר - חושף שההחלטה הקיימת על `ח:קטע4` עשויה להיות שגויה.** `ח:קטע5`
לא ממופה בכלל (לא ב-`_CONTAINER_TEMPLATES`, לא ב-`_CONTENT_DEPTH`,
אין לו טיפול מיוחד כמו ל-`ח:קטע4`). תבנית `ח:קטע5` בוויקי (`תבנית:
ח:קטע5`) **זהה מילה-במילה** לתבנית `ח:קטע4` (כולל קטגוריית ה-noinclude
שעדיין כתובה "קטע4" בטעות - כנראה הועתקה-הודבקה). **אבל** - וזה
הממצא המשמעותי - השימוש בפועל בוויקיטקסט של החוק הזה סותר את ההחלטה
התיעודית הקיימת בקוד ("קטע4 עוטף רק הערה, לא רמת מבנה אמיתית - מדולג
לגמרי"): כאן שתיהן קריאות מרובות-ארגומנטים עם כותרת אמיתית -
`{{ח:קטע4|תוספת 2 חלק ב סימן ב|סימן ב׳: בטיחות בעבודה|תיקון: ק״ת
תשע״ז}}` ואז `{{ח:קטע5|תוספת 2 חלק ב סימן ב משנה א|סימן משנה א׳:
בטיחות משטחי עבודה|...}}` - שתי רמות מבנה אמיתיות ומכותרות (סימן
ותת-סימן בתוך תוספת), לא הערות. כלומר: ייתכן שה"מדינה 4" הנוכחי
(דילוג מלא, איבוד תוכן) כבר גורם לאובדן כותרות-מבנה אמיתיות בחוקים
קיימים שכן נטענים בהצלחה - לא רק בחוק הזה שנכשל. **דורש הכרעה: לחזור
ולבדוק את כל מופעי `ח:קטע4` בקורפוס (לא רק בחוק הזה) - האם יש בהם
גם מקרים אמיתיים של כותרת-מבנה, לא רק הערה בודדת כפי שהוחלט בעבר.**

---

### ✅ 7א · פענוח תחביר היסטוריית התיקונים בוויקיטקסט

recon על חוק העונשין (משימה 3) חשף שהתבנית `{{ח:סעיף|...}}` נושאת שם
ארגומנט נוסף — "תיקון: ..." — שרשם פר-סעיף מתי הוא תוקן, וארגומנט
`אחר=...` שאינו מובן. זה בדיוק מה שחוק ברזל 3 דורש, ברזולוציה טובה
בהרבה מ-timestamp כללי של העמוד (הסעיף המסוים תוקן לאחרונה ב-X, לא
"הדף נערך ב-Y"). התחביר לא פוענח ולא ינוחש — הוא נשמר גולמית כרגע
ב-`LegislativeNode.raw_amendment_note` (ראו `packages/corpus/node.py`).

**מקרה בוחן:** סעיף 34כד בחוק העונשין —
```
{{ח:סעיף|34כד|הגדרות|תיקון: [1939], [תשי״ז], [תשל״ה], תשמ״ח־3, תשנ״ד־3,
תשנ״ה־4, תשנ״ו־6, תשנ״ח־2|אחר=[א/5]}}
```
שאלות פתוחות: מה ההבדל בין ערך בסוגריים מרובעים (`[1939]`) לערך בלי
(`תשמ״ח־3`)? מה מייצג הסיומת אחרי מקף (`־3`, `־4`, `־6`, `־2`)? מה זה
`אחר=[א/5]`? האם אפשר לאמת מול היסטוריית העריכות של הדף (`action=history`)
או מול רשימת הציטוטים בפתיח הדף?

**הסתיים כאשר:** יש פונקציה טהורה שמפרסרת מחרוזת "תיקון: ..." לרשימת
תיקונים מזוהים (שנה/מספר תיקון), עם טסטים על סעיף 34כד ולפחות שני
סעיפים נוספים מחוק העונשין, ואימות צולב מול מקור עצמאי (למשל היסטוריית
העריכות בוויקי, או רשימת הציטוטים בפתיח הדף) — לא ניחוש שנראה סביר.

**מצב (2026-09-13, סגור):** `packages/corpus/amendment_history.py` +
`tests/unit/test_amendment_history.py` (16/16 עובר). פרסור הטוקנים
מוגמר. האימות הצולב הנדרש בוצע בפועל מול מקור עצמאי אמיתי - רשימת
הציטוטים הכרונולוגית שבפתיח `penal.wikitext` עצמו (`'''נוסח
מאוחד:'''`/`'''נוסחים קודמים:'''`) - לא ניחוש: תשנ״ד־3 בסעיף 34כד
וגם בסעיף 1 נפתר, שניהם, לאותו "תיקון מס' 39 (חלק מקדמי וחלק כללי)" -
הרפורמה הגדולה הידועה בחלק המקדמי, שתואמת את טווח הסעיפים הרחב שנושא
את אותו תיקון. תוך כדי הבנייה התגלה ותוקן גם באג אמיתי בפרסור עצמו:
`{{ח:תיבה|...}}` תומך גם בצורה דו-ארגומנטית (בלי קישור, כמו
`{{ח:תיבה|XVI|ת״ט}}`), ורג'קס תלת-ארגומנטי קשיח "בלע" את הציטוט הבא
אחריו בשקט - תוקן ל-`.*?\}\}` לא-חמדן.

**שלוש ההכרעות שסגרו את המשימה (ברק, 2026-09-13 - עצירה לפי CLAUDE.md
"מתי להחליט לבד ומתי לעצור", לא החלטה עצמאית):**

1. טוקן בלי סוגריים ובלי סיומת "־N" (כמו "תשס״ז" לבדו) - **אין ברירת
   מחדל.** נבדק בפועל: 131 מופעים בקורפוס, 117 מהם (89%) לשנה שלהם
   יש יותר מציטוט אחד - "להניח ־1" היה באג שקט, לא ברירת מחדל סבירה.
   `ordinal_in_year=None`; `resolve_amendment_tokens` מחזירה את כל
   מועמדי אותה שנה, כמו legacy (סעיף 2).
2. טוקני legacy (בסוגריים מרובעים, טרום-1977) - אין להם "־N" מקביל
   במקור עצמו (162 מופעים: 84 דו-משמעיים, 4 בלי אף מועמד, רק 74
   חד-משמעיים). `ordinal_in_year=None` גם כאן - אותו טיפול כמו סעיף
   1, אותו מבנה נתונים.
3. הארגומנט "אחר=..." (כמו "[א/5]", "[יא/32א]" - 255 מופעים, פורמט
   עקבי [אות/מספר]) - **לא פוענח.** נשאר גולמי ב-`other_raw`. נפתחה
   משימה נפרדת למטה (7ב) - לא נעבד עליה עכשיו.

**עקרון-על שהוכרע יחד עם השלוש (נוסף ל-CLAUDE.md "ה-ingest שומר
עובדות, לא פרשנויות"):** הקורפוס שומר את `raw_amendment_note` כפי
שהוא; פענוח/פתרון שלו (`resolve_amendment_tokens`) הוא פונקציית
שאילתה שרצה מעל טקסט שמור, לא עמודה מחושבת שנכתבת ב-ingest - כדי
ששיפור עתידי באלגוריתם לא יחייב ingest חוזר לכל הקורפוס. כשהפתרון
לא ודאי (0 או יותר ממועמד אחד) - לא בוחרים ברירת מחדל.

---

## 7ב · פענוח הארגומנט "אחר=" ב-{{ח:סעיף}}

נפתחה מתוך 7א (ראו למעלה) - לא נעבדת עכשיו, ממתינה בתור.

255 מופעים בחוק העונשין בלבד, בפורמט עקבי `[אות/מספר]` (למשל `[א/5]`,
`[יא/1]`, `[יא/32א]` - ראו `ParsedAmendmentNote.other_raw`). לא נבדקה
עדיין שום השערה למשמעות - כולל אם "אות" מציין חלק/פרק ו"מספר" שורה
בלוח השוואה, שזו רק תחושה לא-מאומתת, לא ממצא.

**הסתיים כאשר:** יש השערה למשמעות `אחר=` שאומתה מול מקור עצמאי (לא
רק "נראה שמתאים") - עם טסטים על כמה מופעים אמיתיים מהקורפוס, באותה
רמת קפדנות שהוחלה על 7א.

---

## 8 · OData: הצעות זהות + מראי מקום

שני תוצרים משירות אחד:
1. **בדיקת הצעות זהות/דומות** — חובה לפי החוברת הסגולה. חיפוש דמיון על
   `KNS_BillName`, וייצור השורה בפורמט המחייב.
2. **טבלת מראי מקום** — מיפוי `שם חוק → ס"ח התש..., עמ' ...` להערות השוליים.

**הערה מ-recon על ויקיטקסט (משימה 3):** תבנית `{{ח:חיצוני|...}}` בוויקיטקסט
מפנה לשם דף יעד ולטקסט תצוגה בלבד — היא **לא** מכילה מראה מקום (ס"ח) של החוק
המוזכר. כדי לבנות הערת שוליים לפי `drafting-rules.md` §4 צריך את ה-ס"ח של
כל חוק שמוזכר, וזו בדיוק התלות שהשירות הזה (OData) פותר — הוא מקור טוב
יותר לטבלת מראי המקום מאשר שליפה חוזרת של דפי ויקיטקסט נוספים.

`https://knesset.gov.il/Odata/ParliamentInfo.svc/` — OData **v3**.

**הסתיים כאשר:** עבור הצעת חוק שידוע שהונחה בעבר, המערכת מייצרת אוטומטית
את שורת "הצעת חוק זהה/דומה בעיקרה הונחה על שולחן הכנסת ה-... (פ/.../..)".

---

## 9 · דברי הסבר (LLM)

קלט: הוראות התיקון, נוסח לפני/אחרי, ומטרת התיקון במילות המשתמש.
פלט: 2–5 פסקאות בסגנון `Hesber`.

כללים: תמונה כוללת של מטרת התיקון; אין חובה להתייחס לכל סעיף; לשון
"מוצע לקבוע"; **אין להשתמש בלשון פוגענית** (הנחיה מפורשת של היועצת המשפטית
לכנסת).

---

### ✅ 10 · ממשק מינימלי

תוצר: `apps/api/` — FastAPI, מקומי בלבד (בלי Supabase/Vercel/אימות), עובד
רק על שני חוקי fixture (קייטנות, מאבק בארגוני פשיעה — לא עונשין, שלא עבר
בדיקות משימה 3), בלי ingest מרשת, ורק שני הדפוסים הקיימים (הוספה/החלפת
מילים). הוחלף הרעיון המקורי (ProseMirror/TipTap) בפתרון פשוט יותר שמגיע
לאותה תוצאה בלי עורך עשיר - ראו "איך מגדירים שינוי" למטה.

**סטאק (הוכרע במפורש בשיחה, לא Next.js כמתוכנן בארכיטקטורה המקורית):**
דף Jinja2 יחיד + JS ונילי, לא Next.js/React. הנימוק: השאלה שהמסך הזה
אמור לענות עליה ("האם הצעות החוק נכתבות כמו שצריך") היא שאלה על הפלט, לא
על הממשק - דף אחד עונה עליה מהר יותר, בלי npm/build/CORS בין שני שרתים.
**`apps/api` הוא שכבה דקה בלבד** (כל endpoint: פרסר בקשה → קריאה לפונקציה
טהורה קיימת → סריאליזציה) בכוונה - זה מה שנשאר בלי שינוי כשיוחלף ה-frontend
ב-Next.js בעתיד (מתוכנן, לא נשלל - ראו `CLAUDE.md`). **אין state בשרת**:
רשימת השינויים הממתינים חיה ב-JS בדפדפן, לא בסשן - כל בקשה עצמאית לגמרי.

**איך מגדירים שינוי בפועל (זה החלק הלא-טריוויאלי):** המנוע מצפה לביטוי
מדויק (`ReplaceWords`/`InsertWordsAfter`), לא לעריכה חופשית. הפתרון:
המשתמש **מסמן טקסט בעכבר** מתוך נוסח מוצג כמחרוזת שטוחה (בלי `<span>`
פנימי, כדי שבחירה תמיד תהיה תת-מחרוזת נקייה). **הערבון לדיוק לא בא מה-UI
אלא מהרצה חוזרת של הבדיקה הקיימת ב-`transform.apply()`** (`count()==1`) -
השרת לא סומך על המחרוזת שנשלחה, קורא ל-`apply()` הקיים, ומחזיר את
`ValueError` שלו כמו שהוא אם הביטוי לא נמצא/לא ייחודי. לכן: אף כשל
בבחירת טקסט לא יכול לייצר docx שגוי, לכל היותר הודעת שגיאה וניסיון חוזר.
תיבת אישור לפני שליחה מציגה את הביטוי שנבחר עם **רווחים מסומנים חזותית**
(רקע צבעוני, לא רווח רגיל - רווח נגרר נראה זהה לכלום) כדי לתפוס טעויות
לפני השליחה, לא במקומה.

**מבנה מסך:** בחירת חוק (2 בלבד) → פרטי הצעה (כולל מראה מקום, **שדה חובה**
כשאין ערך ידוע - הצעה בלי הערת שוליים למקור נכשלת בבדיקה 2 בוולידטור,
עדיף לחסום בטופס מאשר לייצר מסמך פסול) → מסך מפוצל (לפני / **נוסח משולב
אחרי** - עץ ה"אחרי" קיים כבר אחרי `apply()`, זו רק תצוגה, עם הדגשת הסעיפים
שהשתנו) → רשימת הוראות תיקון חיה → **פאנל ולידציה עם כל 15 הבדיקות תמיד**
(כולל "עבר" ו"לא נבדק", לא רק כשלים) → הורדת docx.

**גילוי מבני בזמן הבנייה:** חוק מאבק בארגוני פשיעה בנוי בפרקים (כל
הסעיפים מקוננים תחת `chapter`, לא ילדים ישירים של השורש). `amend()` מחפש
סעיפים רק ב-`before.children` הישירים - לא היה זורק שגיאה, רק מחזיר
`lines=[]` בשקט. נבדק במפורש (`amendable` מחושב, לא מוצהר) - ה-UI מציג
את החוק לקריאה בלבד עם הודעה ברורה, בלי לגעת ב-`amend()` (שינוי היקף
שדורש מקרה זהב משלו, לא חלק ממשימה 10).

**מבחנים:** `tests/unit/test_api.py` - מקצה לקצה דרך ה-API (`preview`+
`docx`) על מקרה זהב אמיתי (זהה בייטים לפלט `amend()` הישיר), מקרה שבור
(ביטוי לא קיים → שגיאת `apply()` חוזרת כמו שהיא, לא 500), וחוק לא-amendable
לא קורס. מבחני הזהב הקיימים (`packages/`) לא נגעו - `apps/api` קורא להם
בלבד.

---

### ✅ 10ב · הרחבות מנוע להוספת סעיפים/סעיפים קטנים - השלמת §8.5

תוצר מלא של §8 (`docs/drafting-rules.md`): §8.1-8.4 (מספור בהוספה,
תיקון עיגון עוגן ממוספר, איסור הוספה לפני הילד הראשון, סעיף קטן ראשון)
כבר תועדו שם מסשנים קודמים ולא חזרו כאן. **§8.5 (הוספת סעיף ראשי חדש,
§7.12) הוכרעה ומומשה בסשן הזה** - זו הייתה השאלה הפתוחה האחרונה
שחסמה מימוש.

**גישת רשת (רלוונטי להמשך):** בסשן קודם `knesset.gov.il` היה חסום
לגמרי (`EGRESS_BLOCKED`). בסשן הזה **ה-API עצמו נגיש** (`knesset.gov.il/
Odata/ParliamentInfo.svc/...` עובד), אבל **`fs.knesset.gov.il` (שרת
הקבצים שמארח את ה-docx/pdf של הצעות החוק בפועל) עדיין חסום** - כלומר
אפשר לשאול את ה-API (סטטוסים, מספרים, מטא-דאטה) אבל לא להוריד קבצי
הצעות חוק חדשים מהרשת. נבדק גם דרך WebFetch - אותה תוצאה. **המשמעות:
הכרטיס הפתוח בטרלו (בורד "מה נעשה היום", "מנסח החקיקה") לבקש מאביטל
10-15 הצעות חוק מאושרות בפורמט וורד נשאר רלוונטי ואף חשוב יותר** - זה
המקור המעשי היחיד להצעות חוק נוספות עד שהחסימה על `fs.knesset.gov.il`
תוסר.

**איך §8.5 הוכרעה בפועל (לא ניחוש):** במקום להוריד קובץ חדש (חסום),
נמצא ש-`reference/skeleton-pshia.docx` שכבר יושב בריפו **הוא עצמו**
הצעת חוק פרטית טרומית אמיתית - פ/6158/25, ח"כ צביקה פוגל, הכנסת 25.
אומת מול ה-OData (`KNS_Bill?$filter=... and PrivateNumber eq 6158`):
`BillID=2233987` תואם ל"מספר פנימי" שבראש הקובץ, `SubTypeDesc="פרטית"`.
הקובץ מדגים בדיוק את דפוס "סעיף פנימי" (הוספת פרק עם שני סעיפים
חדשים). בדיקת ה-XML הגולמי הראתה: מרכאה פותחת כתו הראשון של כותרת
השוליים החדשה, מרכאה סוגרת כתו האחרון של תוכן הסעיף האחרון, בלי
מרכאות נפרדות בתאים שבאמצע, ובלי terminator אחרי המרכאה הסוגרת.
פירוט מלא + ציטוט ה-XML: `docs/drafting-rules.md` §8.5.

**מומש:** `transform.InsertSectionAfter` (`packages/amend/transform.py`) -
מספור אוטומטי בלבד (`next_inserted_label`/`next_appended_label`, §8.1),
אסור לקבוע `number` מראש. `engine._render_new_section` +
`engine._wrap_closing_quote_only` (`packages/amend/engine.py`) - וריאציה
של `_wrap_new_content` הקיים, בלי מרכאה פותחת (כבר על `inner_heading`)
ועם `terminator=""` (לא `"."` - אומת מול הראיה). **הפער שתועד ב-§8.5
תוקן בפועל:** `amend()` עבר עד כה רק על `before_sections.keys()` -
סעיף חדש שלא קיים ב"לפני" בכלל היה בלתי-נראה (אפס פלט, לא שגיאה).
הלולאה הראשית עוברת עכשיו על איחוד מספרי הסעיפים משני העצים; סעיף
שלא ב-`before_sections` מטופל כהוספה, עם השכן הקודם בממוין כעוגן.

**היקף מכוון, לא הכלה כללית:** הוספה בודדת בלבד - הוספה רצופה של יותר
מסעיף ראשי חדש אחד (או פרק שלם, בדיוק כמו הדוגמה ב-`skeleton-pshia.docx`
עצמה) אינה נתמכת; `amend()` זורק `NotImplementedError` מפורש, לא מנחש
עוגן. יחכה למקרה זהב משלו.

**מבחנים:** `tests/unit/test_insert_section_after.py` - עץ סינתטי (אין
עדיין מקרה זהב מלא דרך `amend()` על הוספה בודדת - הראיה האמיתית
שנמצאה מדגימה פרק עם שני סעיפים, מחוץ להיקף "הוספה בודדת"), כולל
`number` שנקבע מראש (שגיאה) והוספה רצופה של שני סעיפים (`NotImplementedError`).
כל מבחני הזהב הקיימים (`test_kaytanot.py`, `test_kaytanot_section5.py`,
`test_amend_kaytanot.py`) הורצו מחדש ועברו נקי - אין רגרסיה.

---

## 11 · הכיוון ההפוך: הצעת חוק ← נוסח משולב

Parser להוראות תיקון. מקבל טקסט הצעת חוק, מחיל אותו על עץ החוק.
זו בדיוק העבודה שמתנדבי ספר החוקים הפתוח עושים ידנית היום.

---

## 12 · סוגי מסמכים נוספים

תזכיר חוק, הצעת חוק ממשלתית, חוק המתקן חוק יסוד, חוק חדש.

שים לב: `styles.xml` מכיל כבר סגנונות לכריכת רשומות ותוכן עניינים
(`Cover1-Reshumot`, `TOC`, `HeadMitparsemetBaze`) — כלומר אותה תבנית משרתת
גם הצעות ממשלתיות. `reference/reshumot-1924-government-bills.pdf` הוא קורפוס
הזהב לפורמט הזה, אבל המבנה שם שונה: חלוקה לפרקים, דברי הסבר צמודים לכל פרק,
ותוכן עניינים.

**שם הצעת חוק מתקן בשלב זה:** ראו `docs/drafting-rules.md` §5.6 — פורמט המדריך
עם מספר תיקון רץ ("...(תיקון מס' X)(מהות)...") רלוונטי לשלב שאחרי מספור רשמי
(פרסום ברשומות/הצעה ממשלתית), בניגוד לפורמט "(תיקון – מהות)" ללא מספר
שמשמש כרגע להצעה פרטית טרומית. יש להבחין בין השניים כשמגיעים למשימה הזו.

**אל תתחיל את זה לפני שסוג אחד עובד מושלם מקצה לקצה.**
