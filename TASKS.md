# משימות

סדר ביצוע. כל משימה מגדירה מתי היא "הסתיימה" — אל תעבור הלאה בלי לעמוד בזה.

---

## הושלם

### ✅ 0 · הנדסה לאחור של תבנית החקיקה
תוצר: `docs/template-spec.md`. הונדס משני קובצי docx אמיתיים שהוגשו לכנסת ה-25.

### ✅ 5 · רנדרר docx
תוצר: `packages/render/render_bill.py`. מקבל ייצוג מובנה, מפיק docx בתבנית.
מבחן: `tests/golden/test_kaytanot.py` — 8/8 שורות, התאמה מלאה בצורה ובטקסט.

### ✅ 1 · חילוץ כללי הניסוח
תוצר: `docs/drafting-rules.md`. כל עשרת הדפוסים ברצפה נמצאו עם מקור (בעיקר
"מדריך לניסוח חקיקה" של משרד המשפטים, PDF עמ' 22–62 — לא החוברת הסגולה
עצמה, ראו §0 במסמך למיפוי המקורות). תועדו גם דפוסים נוספים שאינם ברצפה.

**נמצאו שלוש סתירות/פערים בבדיקה, מתועדים ב-§6 של המסמך — טרם הוכרעו:**
1. אין מקור כתוב לכלל "בלי הוראות אופרטיביות" בסעיף הגדרות (רק שקופיית pptx).
2. TASKS.md משימה 4 טוענת שכותרת שוליים בחוק מתקן "מתארת את מהות התיקון" —
   המדריך (§3.4.2) אומר ההפך: היא מזהה סעיף+סוג פעולה בלבד, לא מהות.
3. פורמט שם הצעת חוק מתקן: המדריך (2008) משתמש בשני סוגריים נפרדים
   ("(תיקון מס' X)(מהות)"), אבל קובץ הזהב האמיתי (2023-2025) ו-
   `validator-checklist.md` #7 משתמשים בסוגריים אחד עם מקף
   ("(תיקון – מהות)"). ייתכן שינוי נוהג עם השנים — לא הוכרע.

---

## 2 · סכמת `LegislativeNode` ← **המשימה הבאה**

```python
LegislativeNode:
    id            # מזהה יציב: "penal-1977/s4/a/1"
    node_type     # chapter|section|subsection|paragraph|subparagraph|definition
    number        # "4", "3א", "34כד", "(א)", "(1)"
    margin_title  # כותרת שוליים — רק לסעיף ראשי
    text
    children[]
    source_ref    # מקור + תאריך נוסח
```

**הסתיים כאשר:** `sort_section_numbers()` עוברת טסטים על הרצף
`3, 3א, 3ב, ..., 3י, 3יא, 4` ועל `34כד`, ועל מספור מעורב בתוך אותו חוק.

---

## 3 · Ingest מוויקיטקסט

מקור: ספר החוקים הפתוח על ויקיטקסט. MediaWiki API + dumps. רישיון חופשי.

התחל ב-20 חוקים לדוגמה, כולל: חוק הקייטנות (רישוי ופיקוח) התש"ן–1990,
חוק המאבק בארגוני פשיעה התשס"ג–2023, חוק העונשין התשל"ז–1977.

**הסתיים כאשר:** ההיררכיה של שלושת החוקים האלה מפורסרת נכון, כולל סעיפים
עם אות (`3א`), סעיפי הגדרות, ופרקים.

---

## 4 · מנוע `amend`

הלב של המערכת. מקבל: עץ חוק לפני, עץ חוק אחרי. מחזיר: רשימת הוראות תיקון.

הדפוסים הקנוניים מפורטים ב-`docs/drafting-rules.md`. הכללים הנלווים:
- הסעיף הראשון מאזכר את החוק בשמו המלא ומגדיר `(להלן – החוק העיקרי)`
- בהוספת סעיף חדש חובה כותרת שוליים ומספר בתוך המרכאות
- כותרת השוליים מתארת את מהות התיקון, לא את תוכן הסעיף
- סדר כרונולוגי לפי סדר הסעיפים בחוק; תיקונים עקיפים בסוף

**הסתיים כאשר:** בהינתן עץ חוק הקייטנות לפני ואחרי, המנוע מייצר בדיוק את
שמונה השורות של `reference/golden-kaytanot.docx`, והפלט שלו מוזן לרנדרר
ומבחן הזהב ממשיך לעבור מקצה לקצה.

---

## 6 · ולידטור

15 בדיקות. הרשימה המלאה ב-`docs/validator-checklist.md`.
כל כשל = אזהרה מוצגת למשתמש, לא כשל שקט.

**הסתיים כאשר:** כל 15 ממומשות, ולכל אחת יש טסט שמוכיח שהיא תופסת מקרה שבור.

---

## 7 · Ingest מלא + אינדוקס חיפוש

כל ~5,900 החוקים והתקנות. חיפוש היברידי (BM25 + embeddings).

**הסתיים כאשר:** שאילתה בשפה חופשית מחזירה את הסעיפים הרלוונטיים בעשירייה
הראשונה עבור 20 מקרי בדיקה שתגדיר.

---

## 8 · OData: הצעות זהות + מראי מקום

שני תוצרים משירות אחד:
1. **בדיקת הצעות זהות/דומות** — חובה לפי החוברת הסגולה. חיפוש דמיון על
   `KNS_BillName`, וייצור השורה בפורמט המחייב.
2. **טבלת מראי מקום** — מיפוי `שם חוק → ס"ח התש..., עמ' ...` להערות השוליים.

`https://knesset.gov.il/Odata/ParliamentInfo.svc/` — OData **v3**.

**הסתיים כאשר:** עבור הצעת חוק שידוע שהונחה בעבר, המערכת מייצרת אוטומטית
את שורת "הצעת חוק זהה/דומה בעיקרה הונחה על שולחן הכנסת ה-... (פ/.../..)".

---

## 9 · דברי הסבר (LLM)

קלט: הוראות התיקון, נוסח לפני/אחרי, ומטרת התיקון במילות המשתמש.
פלט: 2–5 פסקאות בסגנון `Hesber`.

כללים: תמונה כוללת של מטרת התיקון; אין חובה להתייחס לכל סעיף; לשון
"מוצע לקבוע"; **אין להשתמש בלשון פוגענית** (הנחיה מפורשת של היועצת המשפטית
לכנסת).

---

## 10 · UI

מסך מפוצל RTL. ימין: נוסח החוק הקיים, ניתן לעריכה. שמאל: ההצעה המתהווה.
למטה: פאנל ולידציה. כפתור הורדת docx.

שקול ProseMirror/TipTap עם סכמה מותאמת לצמתים המשפטיים במקום contenteditable
חופשי — זה פותר גם את ה-provenance, כי כל צומת בעורך ממופה לצומת בחוק.

---

## 11 · הכיוון ההפוך: הצעת חוק ← נוסח משולב

Parser להוראות תיקון. מקבל טקסט הצעת חוק, מחיל אותו על עץ החוק.
זו בדיוק העבודה שמתנדבי ספר החוקים הפתוח עושים ידנית היום.

---

## 12 · סוגי מסמכים נוספים

תזכיר חוק, הצעת חוק ממשלתית, חוק המתקן חוק יסוד, חוק חדש.

שים לב: `styles.xml` מכיל כבר סגנונות לכריכת רשומות ותוכן עניינים
(`Cover1-Reshumot`, `TOC`, `HeadMitparsemetBaze`) — כלומר אותה תבנית משרתת
גם הצעות ממשלתיות. `reference/reshumot-1924-government-bills.pdf` הוא קורפוס
הזהב לפורמט הזה, אבל המבנה שם שונה: חלוקה לפרקים, דברי הסבר צמודים לכל פרק,
ותוכן עניינים.

**אל תתחיל את זה לפני שסוג אחד עובד מושלם מקצה לקצה.**
