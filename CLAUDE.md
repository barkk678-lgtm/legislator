# מנסח החקיקה

> **המפתחות ב-`/root/.claude/legislator.env`. טען משם בתחילת כל סשן.
> אל תבקש מברק להדביק מפתח — הוא שמור.**
>
> הקובץ מחוץ לריפו, הרשאות 600, ולעולם לא נכנס אליו. **אף קוד אינו
> קורא `os.environ` ישירות** — הכול עובר דרך `packages/config/env_file.py`
> (`require`, `require_supabase`, `get`), שם הסביבה גוברת ומה שחסר
> נטען מהקובץ. בטרמינל: `set -a; . /root/.claude/legislator.env; set +a`.
> **ערך של מפתח לא מודפס לעולם** — לא בלוג, לא בשגיאה, לא בדיווח לברק.
> הקובץ שורד דחיסת שיחה (הוא על הדיסק), אך **לא** החלפת קונטיינר.

מערכת לניסוח הצעות חוק ישראליות. מפיקה קובץ `.docx` תקין בתבנית החקיקה
הרשמית של הכנסת ומשרד המשפטים.

קרא את הקובץ הזה במלואו בתחילת כל סשן. **`docs/README.md` הוא האינדקס**
שאומר מי מקור האמת לכל נושא, ו-`PROJECT_CONTEXT.md` מסביר למה המוצר בנוי
ככה ולמי. `TASKS.md` מחזיק את המשימות ואת סדר הביצוע; `docs/` את המפרטים.

**שפת תקשורת: עברית, תמיד.** כל תשובה לברק בצ'אט נכתבת בעברית - בלי יוצא
מן הכלל, בלי קשר לשפה של תוכן שהוצג באותו turn (פלט כלי, ציטוט מוויקיטקסט/
קוד/הודעת שגיאה באנגלית וכו'). זה חל על כל הטקסט הפונה למשתמש - סיכומים,
שאלות, דיווחי סטטוס. קוד/הערות-קוד נשארים לפי "עברית בקוד" למטה (משתני
Python באנגלית) - זה לא סותר; זו הבחנה בין קוד לתקשורת.

---

## ניהול משימות

**כל בקשה של ברק שאינה חלק מתוכנית לילה - נפתחת כ-task מיד כשהיא
מגיעה, גם אם לא מתחילים אותה באותו רגע.** (ברק, 22.9.2026.)

הלקח נלמד מבקשה שנפלה: "בדיקות אוטומטיות בכל push ובדיקות כמו
לקוח מול האתר החי" נמסרה בתוך הודעה ארוכה, לא נפתח לה task, ולא
בוצעה - ולא הופיעה גם בדוח, כי הדוח נבנה מרשימת ה-tasks. בקשה
בלי task אינה קיימת: היא לא מנוהלת, לא מדווחת, ולא נזכרת.

**דוחפים בסוף כל משימה שעוברת את הבדיקות.** (ברק, 22.9.2026:
"עכשיו שיש CI ובדיקה חיה, דחיפה בטוחה יותר מאשר קוד שיושב מקומית
ומתרחק מהאתר.") משהו מסוכן במיוחד - שואלים קודם.

הלקח נלמד בדרך הקשה: 15 קומיטים של לילה שלם ישבו מקומית, והאתר
החי הריץ את הקוד של שלשום. כל בדיקות היחידה עברו. מה שתפס את זה
היה `tests/live_smoke.py` בהרצה הראשונה שלו - 24/24 מול master,
21/24 מול האתר.

## חוקי ברזל

אלה גוברים על כל שיקול אחר. אם משימה נראית כאילו היא דורשת לשבור אחד מהם —
המשימה שגויה, לא החוק.

1. **מודל שפה לעולם לא מייצר נוסח חוק.**
   נוסח חוק מגיע משני מקורות בלבד: טקסט שנשלף ממאגר מאומת, או טקסט שהמשתמש
   הקליד. מודל שמנסח "בסעיף 4(א) לחוק העונשין נאמר..." מתוך זיכרון — זה באג
   חמור. אין "בערך נכון" בנוסח חוק.

2. **הוראות התיקון נוצרות דטרמיניסטית.**
   `diff` מובנה ← תבנית ניסוח קבועה. `packages/amend` אסור שיכיל קריאה ל-LLM
   או לרשת. אותו קלט חייב לתת אותו פלט, תמיד.

3. **לכל פסקה בפלט יש provenance.**
   מזהה הצומת שממנו נגזרה ותאריך הנוסח. אם המערכת לא יודעת עד מתי הנוסח
   מעודכן — היא אומרת זאת למשתמש, לא מנחשת.

4. **ל-LLM שלושה תפקידים בלבד:**
   ניסוח דברי הסבר, שכתוב ניסוח שהמשתמש כתב, ואיתור סעיפים רלוונטיים מתיאור
   חופשי. לא יותר.

   **החלטת מוצר (2026-09):** שם הצעת החוק (`Bill.title`) ודברי ההסבר
   (`Bill.explanatory`) מנוסחים בחופשיות כברירת מחדל, והמשתמש עורך את
   הטיוטה — טיוטה ערוכה עדיפה על שדה ריק, וזה טקסט שהמשתמש רואה מיד.
   זה תקין תחת חוק ברזל 4 כי שניהם טקסט מלווה, לא נוסח חוק. **הגבול
   המפורש:** החופש הזה חל אך ורק על שם ההצעה ועל דברי ההסבר. כל מה
   שנכנס לטבלת החקיקה עצמה — מספרי סעיפים, ביטויים מוחלפים/מוכנסים,
   עוגני טקסט, נוסח מצוטט — נשאר דטרמיניסטי לגמרי ומגיע רק מהמשתמש או
   מהקורפוס (`packages/amend`), לעולם לא מניסוח חופשי של מודל.

5. **כל פלט עובר ולידטור לפני שהוא מוצג.** ראה `docs/validator-checklist.md`.

6. **אין לגרד את נבו או דינים.** יש להם תנאי שימוש. המקורות המותרים מפורטים
   ב-`docs/data-sources.md`.

7. **הסתייגות שיש בה צל של ספק — נחסמת, לא מסומנת.**
   סעיף 86(ד)(2) לתקנון הכנסת, **בנוסח המלא מהקורפוס**: "הסתייגות
   לא תשלול את קיומה של מדינת ישראל כמדינתו של העם היהודי, לא תהיה
   גזענית במהותה והיא לא תכיל כינוי או ביטוי פוגע או גזעני, או
   פגיעה בכבוד הכנסת; התעורר ספק, תכריע בעניין נשיאות הכנסת."

   **המערכת שמרנית הרבה יותר מהתקנון.** הסיפה על נשיאות הכנסת
   מתארת מה קורה **אחרי** הגשה — ואנחנו לא מגיעים לשם. כל הסתייגות
   שיש בה ולו צל של ספק לגבי גזענות, שלילת קיום המדינה כמדינת העם
   היהודי, כינוי או ביטוי פוגע, או פגיעה בכבוד הכנסת — **נחסמת
   ואינה מוצגת כלל.** לא אזהרה, לא סימון, לא "שקול בעצמך", לא
   הצגה עם הסתייגות. היא פשוט לא קיימת בפלט.

   שים לב ל**"כינוי"**: הסייג חל גם על שם של אדם בהקשר מבזה, לא
   רק על תוכן.

   **המאזן שמכתיב את הסף:** פסילה בטעות של הסתייגות תקינה עולה
   אחת מתוך אלפיים. הסתייגות פוגענית שיצאה מהמערכת עולה את החברה.

8. **מצב הכמות מייצר אך ורק וריאציות על ערכים שקיימים בהצעה.**
   מספר, תאריך, יחידת זמן, סכום, מילה שנמצאת בסעיף — ותו לא. הוא
   **אינו כותב טקסט חדש בשום דפוס**, וזו ההגנה המבנית שמאחורי חוק
   ברזל 7: מנוע שלא מחבר מילים לא מסוגל לחבר מילה פוגענית.

   **כל הוספה עתידית של דפוס שמייצר טקסט חדש שוברת את ההגנה
   ודורשת אישור מפורש של ברק.** הדפוס `בסופו יבוא "<טקסט חופשי>"`,
   שנצפה ב-13% מהסתייגויות אמיתיות, הוצא ממצב הכמות במכוון בדיוק
   מהסיבה הזו (2026-09-18) — עדיף לוותר על 13% תשואה מאשר להחזיק
   טענה מבנית שהיא כמעט-נכונה.

9. **כלל משפטי מיושם מהקורפוס, לא ממסמך ביניים.**
   מסמך חזון, סיכום או הודעה בצ'אט מסבירים **למה**; חוק מחייב
   **מילה במילה**. כשמיישמים סייג משפטי - לשלוף את הנוסח מהקורפוס
   ולצטט אותו בקוד.

   **המקרה שהוליד את הכלל:** ציטוט של 86(ד)(2) שהגיע ממסמך ביניים
   השמיט את המילה **"גזעני"** — הסייג הקריטי ביותר בפיצ'ר
   ההסתייגויות — ואת הסיפה על נשיאות הכנסת. שתי ההשמטות התגלו רק
   בשליפה מה-DB.

**הערה על היקף (2026-09, ראו `docs/strategy/plan.md` §2.2):** כלים
עתידיים שאינם נוגעים בנוסח חוק (שאילתא, הצעה לסדר, מומחה תקנון,
פרוטוקולים, מחקר) אינם כפופים לחוקי ברזל 1-2 (אין שם נוסח חוק
לייצר דטרמיניסטית) - אבל **חובת המקור חלה עליהם במלואה**: כלי
שעונה על שאלת תקנון בלי להצביע על הסעיף הוא כלי פגום, לא כלי גמיש.

---

## גבולות ארכיטקטוניים

```
packages/corpus/     ingest ויקיטקסט + OData  →  LegislativeNode
packages/amend/      diff מובנה  →  הוראות תיקון      [טהור: בלי רשת, בלי LLM]
packages/render/     הוראות + מטא-דאטה  →  docx        [טהור]
packages/validate/   בדיקות על הפלט                    [טהור]
packages/llm/        דברי הסבר, איתור סעיפים           [היחיד שקורא למודל]
apps/api/            FastAPI
apps/web/            דף Jinja2 יחיד + JS ונילי, RTL (משימה 10)
```

`amend`, `render` ו-`validate` הם ספריות טהורות. אם אתה מוצא את עצמך מוסיף
להן `import requests` או קריאה למודל — עצור, משהו בתכנון שגוי.

**`apps/api` הוא שכבה דקה בלבד** מעל `amend`/`render`/`validate`/`corpus` -
כל endpoint הוא "פרסר בקשה → קריאה לפונקציה טהורה קיימת → סריאליזציה". אין
בו לוגיקה משפטית ואין לוגיקת תצוגה. `apps/web` (משימה 10) הוא דף Jinja2
יחיד + JS ונילי, לא Next.js — החלטת היקף למינימום עובד (`docs`-ה"אחד עובד
עדיף על מסגרת גנרית ריקה"), **לא** ביטול הכיוון המקורי: מעבר ל-Next.js
מתוכנן לעתיד, לא נשלל. בזכות ש-`apps/api` נקי מהחלטות, המעבר ל-frontend
אחר לא אמור לגעת בו.

## גבולות רשת (ממצא שחזור, 2026-09-16/17)

**לסביבת ה-agent (איפה שסשן Claude Code הזה רץ) יש allowlist רשת
סגור וצר.** נבדק בפועל (לא הונח): `api.anthropic.com` עובד, אבל
`api.openai.com` **וגם** `google.com`, `api.voyageai.com`,
`api.cohere.ai/com`, `api.mistral.ai` **כולם** נדחים באותה מדיניות
בדיוק (403 "gateway answered... policy denial", מאומת via
`curl "$HTTPS_PROXY/__agentproxy/status"`). זו לא רשימת-חסימה
ממוקדת-OpenAI - זו רשימת-היתר סגורה שרק כמה שירותים ספציפיים (בעיקר
Anthropic, npm/pypi/רשמי-Anthropic) נמצאים בה. חריג יחיד שנמצא:
`generativelanguage.googleapis.com` (Gemini) כן נגיש - כנראה לא
מכוון, לא מדיניות מוצהרת.

**ל-Vercel (production) יש רשת פתוחה.** נבדק בפועל: `https://
legislator-tau.vercel.app/api/admin/openai-check` (endpoint אבחון,
`apps/api/main.py`) מצליח לקרוא ל-`api.openai.com` בהצלחה, בזמן
שאותה קריאה בדיוק נכשלת מסביבת ה-agent.

**המשמעות המעשית: כל אינטגרציה חיצונית עתידית (ספק LLM/embeddings/
API חדש, לא Anthropic) חייבת לרוץ/להיבדק מ-Vercel, לא מסביבת
ה-agent.** אם קריאת רשת לשירות חיצוני חדש נכשלת כאן עם "connection
refused"/"CONNECT tunnel failed" - זו קודם כל שאלת מדיניות-רשת של
הסביבה, לא בהכרח בעיה במפתח/בקוד. תבנית עבודה שכבר קיימת בקוד
לבדיקה/הרצה-מ-Vercel: `apps/api/admin_ingest.py` (endpoint מוגן-טוקן
שמריץ עבודה שדורשת גישה חיצונית, ראו TASKS.md/night-report.md
2026-09-17 לפרטים).

## כל API חיצוני מעומד עד שהוכח אחרת - בלי יוצא מן הכלל

**הדפוס הזה הכה שלוש פעמים בפרויקט, כל פעם בשקט מוחלט:** התשובה
תקינה, אין שגיאה, אין אזהרה - פשוט חסרות שורות.

1. **PostgREST** (`db-max-rows=1000`): חוק העונשין נטען עם 1,000
   מתוך 2,682 צמתים - עץ תקין-למראה, חסר 60% מהתוכן.
2. **PostgREST שוב:** 94 מתוך 1,094 חוקים לא נרשמו לתור האינדוקס
   ולכן לא היו קיימים מבחינת ה-ingest (ביניהם פקודת העיריות, חוק
   שנדרש במפורש). ההרצה דיווחה "הושלם".
3. **OData של הכנסת** (100 שורות לבקשה): `$top=5000` החזיר 100 בלי
   להתריע. ואז, באותו קובץ שנכתב כדי למנוע את זה, תקרת בטיחות
   משלנו החזירה 5,000 מתוך 15,326 שורות - בשקט.

**שלושת הכללים:**

1. **להניח שכל תשובה מעומדת**, גם כשהתיעוד לא אומר זאת וגם כשנראה
   שהכול חזר. אין API שמבטיח "הכול בבת אחת".
2. **לאמת את המספר שחזר מול מה שנתבקש.** אם ביקשנו 5,000 וקיבלנו
   בדיוק 100 או בדיוק 1,000 - זו תקרה, לא סוף הנתונים. מספר "עגול
   וחשוד" הוא הסימן היחיד שיש, כי שגיאה לא תגיע.
3. **לא לסמוך על פרמטר גודל** (`limit`, `$top`, `per_page`).
   השרת חותך אותו בשקט. משתמשים במנגנון העימוד של השרת עצמו -
   `Range` ב-PostgREST, `@odata.nextLink` ב-OData, `Link: rel=next`
   ב-REST רגיל - וממשיכים עד שאין המשך.

**ושלושתם חלים גם על הקוד שלנו:** תקרת בטיחות מפני לולאה אינסופית
היא לגיטימית, אבל **חריגה ממנה חייבת לזרוק שגיאה ולא להחזיר תוצאה
חלקית.** זו בדיוק הטעות מספר 3 למעלה.

**המימושים:** `apps/api/supabase_rest.py` (`fetch_all`, `count_rows`)
ו-`packages/knesset/odata.py` (`fetch`, `count`). אין לכתוב לולאת
עימוד רביעית - שלושת המימושים המקומיים שהיו קודם הם בדיוק הסיבה
ששכחו פעמיים. `tests/unit/test_supabase_rest.py` סורק את הקוד ונכשל
על קריאה ישירה שאינה שליפת שורה בודדת.

**לספירה אסור להשתמש ב-`len()` על גוף התשובה** - הגוף חתוך. משתמשים
במנגנון הספירה של השרת (`Prefer: count=exact` + `content-range`,
או `$count=true`), שמחזיר את הסכום האמיתי גם מעל התקרה.

### תשובה ריקה חשודה בדיוק כמו תשובה עגולה

אותו דפוס הכה פעם רביעית, בצורה הפוכה: שליפת ההצעות שתיקנו חוק
נבנתה כשרשרת `Id eq .. or Id eq ..`, והשרת מחזיר **400 כבר ב-25
תנאים** (מגבלת מורכבות על עץ הביטוי). התוצאה: **ככל שחוק מתוקן
יותר, כך סביר יותר שהוא יחזיר אפס ציטוטים** - כלומר בדיוק החוקים
החשובים ביותר (העונשין, ביטוח לאומי, תכנון ובנייה) נראו כאילו אין
להם היסטוריית תיקונים.

**הכלל: תשובה ריקה מ-API חיצוני היא ממצא שדורש אימות, לא עובדה.**
כשהיא מגיעה דווקא מהמקרה שאמור להיות העשיר ביותר - זו שגיאה עד
שהוכח אחרת. לבדוק על המקרה הקיצוני הידוע, לא על מקרה ממוצע: כאן
חוק העונשין (169 קישורים) הוא מבחן ההחלטה, ולא חוק עם שלושה
תיקונים שעובר בכל מימוש. `Id in (...)` עובר עם 200 מזהים ונשבר
סביב 400 - נמדד, לא הונח.

## ביצועים מתדרדרים עם גדילת ה-DB, והתסמין מופיע במקום אחר (2026-09-17)

מופע Supabase Free tier קטן, ורוב הביצועים שלו נשענים על כך שהטבלאות
החמות יושבות במטמון. **כשה-DB גדל, נתונים חדשים דוחקים טבלאות ותיקות
מהמטמון, ושאילתות שעבדו מצוין מתחילות לרדת לדיסק - בלי שאף שורת קוד
השתנתה.**

קרה בפועל: אינדוקס סמנטי הגדיל את ה-DB מ-215MB ל-448MB, נתוני הווקטורים
(search_chunks + אינדקס HNSW) דחקו את `nodes` מהמטמון, וה-view
`amendable_law_ids` - שסורק 246K שורות - קפץ מ-43ms ל-**7.7 שניות**.
התוצאה: `/api/laws` ו-`/api/laws/search` החזירו **500**, כי הם חרגו
מתקרת הזמן של פונקציית Vercel. שני מסלולים שאיש לא נגע בהם.

**המשמעות המעשית:** 500 פתאומי במסלול שלא נגעתם בו, אחרי עבודה
שהגדילה את ה-DB, הוא קודם כל חשד לשאילתה שאיבדה את המטמון - לא באג
בקוד החדש. לבדוק עם `EXPLAIN (ANALYZE, BUFFERS)`: `read=` גבוה מול
`shared hit=` מעיד על ירידה לדיסק, והפתרון הוא בדרך כלל אינדקס שתואם
בדיוק את תנאי השאילתה (כאן: אינדקס חלקי על `nodes`, שהחזיר 7,679ms
ל-43ms). **אל תניחו שמה שהיה מהיר יישאר מהיר** - סריקה מלאה שעבדה על
DB קטן היא פצצת זמן.

## כיסוי החיפוש הסמנטי חלקי - במכוון (2026-09-17)

**רק 67 מתוך 1,094 החוקים מאונדקסים לחיפוש סמנטי**, לא כולם. הסיבה:
Supabase Free tier מוגבל ל-500MB, וכל 1,094 החוקים היו תופסים ~700MB
(כיול מדוד, לא הערכה: 16.4KB/chunk כולל אינדקסים). 67 החוקים נבחרו
לפי דירוג מתועד (`docs/indexing-priority-250.md` - ממוצע אחוזוני גודל
ותיקונים, עם 25 חוקים מחויבים שברק דרש), והרשימה המלאה של 250 שמורה
ב-`data/indexing_priority.json` עם חלוקה לשלבים - שלב 2 מוכן להרצה
(`?phase=2`) ברגע שיהיה שדרוג ל-Pro, בלי לחשב מחדש.

**המשמעות: חיפוש סמנטי שלא מחזיר תוצאה הוא לא בהכרח באג.** לפני
שמתחילים לתקן retrieval - לבדוק אם החוק הרלוונטי בכלל מאונדקס
(`indexed_laws` view, או שדה `coverage` שכל תשובת `/api/semantic-search`
מחזירה). הממשק כבר מבדיל בין שתי המצבים במפורש; אל תסירו את ההבחנה
הזו "כי היא מיותרת" - היא נדרשה במפורש כדי שהדגמה מול קהל לא תיתקע
על מסך ריק בלי הסבר.

**ה-ingest שומר עובדות, לא פרשנויות.** קורפוס שנשמר בדאטהבייס (שלב 1)
מכיל את מה שנשלף מהמקור כפי שהוא - טקסט גולמי, מבנה שנגזר ממנו בוודאות
(כמו עץ LegislativeNode עצמו, או רשימת ציטוטים שמפוענחת בוודאות ממקור
מובנה). כל פענוח שעשוי להשתפר עם הזמן - ניחוש, הכרעה בין אפשרויות,
צימוד לוגי בין שני מקורות שאינו ודאי - יושב בשכבה נפרדת שרצה מעל הקורפוס
השמור, לא נכתב לתוכו ב-ingest. **המבחן:** אם שיפור באלגוריתם הפענוח היה
מחייב ingest חוזר לקורפוס כדי להתעדכן - האלגוריתם יושב במקום הלא נכון.
דוגמה קונקרטית (TASKS.md 7א): `raw_amendment_note` (הטוקן הגולמי מ-
{{ח:סעיף}}) נשמר כפי שהוא; פענוח/פתרון שלו (`amendment_history.py`) הוא
פונקציה טהורה שרצה בזמן שאילתה על טקסט שכבר שמור - לא עמודה שמחושבת
ונכתבת פעם אחת ב-ingest. כשהפענוח לא ודאי (יותר ממועמד אחד, או שאין
סיומת מפורשת במקור) - לא בוחרים אחד "כברירת מחדל". שמירת הטוקן הגולמי
בלי הכרעה עדיפה על הכרעה שעלולה להיות שגויה.

**הקורפוס מייצג מציאות; validate/drafting-rules חלים על מה שמנוסח,
לא על מה שנטען.** `packages/corpus` מייצג את מה שקיים בחוק בפועל, על
כל חריגותיו - חוק ישן עם פסקאות (1)(2) ישירות מתחת לסעיף ראשי, בלי
סעיפי משנה (א)(ב) ביניהם, הוא מבנה לגיטימי לחלוטין ואסור לפרסר
"לתקן" אותו לצורה קנונית. `packages/validate` ו-`drafting-rules.md`
חלים אך ורק על הוראות התיקון שהמערכת מייצרת (הצעת החוק החדשה) - הם
**לעולם לא** רצים על נוסח שנטען מוויקיטקסט. אם סעיף בקורפוס "נראה
לא תקין" למי שרגיל לניסוח מודרני - זו לא בעיה בפרסר, זו עובדה על
החוק. השלכה מעשית: תפריט הוספת רמות (subsection/paragraph) מציע
תמיד לפי המבנה שקיים בפועל באותו סעיף ספציפי - אם יש בו פסקאות
ישירות בלי סעיפי משנה, זה מה שמוצע, בלי לכפות רמת ביניים קנונית
שלא קיימת שם.

---

## מצב נוכחי

> עודכן 2026-09-18. הטבלה הקודמת כאן הכריזה ש-`amend`, `corpus`
> ו-`validate` ריקים, בזמן ששלושתם בנויים ועובדים - סתירה מול הקוד
> שהייתה מטעה כל סשן חדש. מי שמעדכן: הקוד גובר, לא הטבלה.

| רכיב | מצב |
|---|---|
| `packages/render` | **עובד.** מפיק docx תקין בתבנית |
| `packages/amend` | **עובד.** diff → הוראות תיקון, רקורסיבי בכל עומק |
| `packages/corpus` | **עובד.** פרסור ויקיטקסט, מספור, ingest ל-Supabase |
| `packages/validate` | **עובד.** 15 בדיקות + `validate_draft` למסמך חיצוני |
| `packages/documents` | **עובד.** חילוץ docx, תקציר, ביקורת ניסוח |
| `packages/llm` | **עובד.** הממשק היחיד שקורא למודל |
| `packages/knesset` | **עובד.** OData - ציטוטים, הצעות דומות, שאילתות |
| `apps/api` | **פרוס** ב-`legislator-tau.vercel.app`, מוגן בסיסמה |
| נוסח משולב (לשונית `merge`) | **"בקרוב"** - אין backend |

מצב מלא ומדורג של מה שלא עובד: `docs/open-gaps.md`.

הרצה אחרי כל שינוי: `for f in tests/unit/*.py tests/golden/*.py; do python3 "$f"; done` —
כולם חייבים להחזיר "עבר". שני מקרי הזהב (קייטנות, קייטנות סעיף 5)
רגישים במיוחד לשינויים ב-`render` וב-`amend`.

---

## עקרונות עבודה

- **התחל מהטסט.** לכל יחידה ב-`amend` יש מקרה זהב ב-`reference/`. כתוב את
  הטסט מהמקור האמיתי, ואז את הקוד.
- **אל תיגע ב-`render_bill.py` בלי להריץ את מבחן הזהב.** הוא הונדס לאחור
  ממדידות, לא מתיעוד. כל "שיפור" אינטואיטיבי עלול לשבור התאמה למקור.
- **עברית בקוד:** שמות משתנים באנגלית, מחרוזות ותיעוד בעברית. הערות בקוד
  בעברית — זה קוד שמתחזקים אנשים שעובדים בעברית.
- **אל תוסיף תלויות** בלי סיבה טובה. המצב בפועל (2026-09-18):
  `fastapi`, `jinja2`, `httpx`, `pyluach`, `python-multipart`,
  `python-docx` ב-`requirements.txt`. `render` עצמו עדיין ספריית
  תקן בלבד, וזה שווה לשמר.
  **שים לב:** `lxml` מיובא ב-`packages/validate/validator.py`
  (בדיקה 14) וב-`tests/golden/test_kaytanot.py`, אבל **אינו
  ב-requirements** - בפרודקשן זה לא מתפוצץ רק כי בדיקה 14 מקבלת
  `docx_path=None` ולא מגיעה ל-import. שבריריות ידועה, לא באג חי.
- **קומיטים קטנים.** משימה אחת מ-`TASKS.md` = קומיט אחד.

---

## מתי להחליט לבד ומתי לעצור

**החלט לבד והמשך:**
- בחירות מימוש שלא משנות התנהגות כלפי המשתמש
- תיקון באג שנתפס בטסט
- כללי ניסוח שיש להם מקור מפורש ב-`drafting-rules.md`

**עצור ושאל:**
- סתירה בין שני מסמכים בפרויקט
- כלל ניסוח שאין לו מקור
- מקרה שבו הקוד הקיים והוולידטור לא מסכימים
- שינוי שיגרום למבחן זהב להיכשל
- כל דבר שדורש סטייה ממקור אמיתי
- החלטת מוצר: מה המשתמש רואה, איך זה מתנהג
- **החלטה שקשה/יקרה לבטל** - למשל עיצוב סכמת דאטהבייס שקורפוס שלם
  יישב עליה (שינוי אחריה הוא מיגרציה על כל הנתונים, לא עריכת קוד).
  זו לא רק "החלטת מוצר" - זו הסיבה שהיא עוצרת: המחיר של טעות גבוה
  ולא הפיך. הצג תרשים/DDL/תכנון - **בלי להריץ** - וחכה לאישור.

**הלולאה:** טסט ירוק → מימוש → טסט → עדכון `TASKS.md` → קומיט → דיווח.
מותר להמשיך לשלב הבא **באותו שלב ראשי** בלי לחכות לאישור, אבל לא לעבור
לשלב ראשי הבא (ראו `docs/strategy/` לחלוקת השלבים).

**הלולאה מסתיימת בדיווח ובעצירה - לא בתזמון המשך.** אל תתזמן לעצמך
(wakeup/check-in עצמי) המשך אוטונומי, גם בתוך אותו שלב ראשי שמותר
להמשיך בו. ברק קובע מתי ממשיכים, לא טיימר.

---

## מלכודות ידועות

- **חשבון Supabase משותף.** בחשבון/ארגון ה-Supabase שבו יושב פרויקט
  `legislator` יש גם שני פרויקטים נוספים שאינם קשורים לפרויקט הזה
  בשום צורה ("121", "ניהול מפעלים"). אין לגעת בהם - לא בסכמה, לא
  בנתונים, לא בהגדרות - בשום פעולת MCP/CLI, גם לא בטעות תוך כדי בדיקה
  כללית של החשבון. כל פעולת Supabase בפרויקט הזה חייבת להיות מכוונת
  במפורש ל-`legislator` (`aamxwjmmlsqkinrzirdz`).
- **RTL** — אין `<w:bidi/>` ברמת הפסקה. הכיווניות מגיעה מ-`sectPr`,
  מ-`bidiVisual` בטבלה, ומ-`<w:rtl/>` בכל run. פרטים במפרט.
- **en-dash בשנה עברית** (`התשל"ז–1977`) יושב ב-run נפרד ללא `hint="cs"`.
- **מספרי סעיפים אינם מספרים.** `3א`, `34כד`, `1א` חוקיים. הסדר: 3, 3א, 3ב...
  3י, 3יא. `sorted()` רגיל ייכשל. צריך פונקציית מיון ייעודית עם טסטים.
- **OData של הכנסת: `.../Odata/...` הוא v2 (לא v3 כפי שתועד כאן בעבר),
  ויש גם v4 חדש ונגיש ב-`.../OdataV4/...` (מומלץ, `KNS_Law` מוזגה
  ל-`KNS_Bill`). `pyodata` תומך ב-v2 בלבד. ללכת על HTTP גולמי + XML/JSON.
  **כשמנסים לשאוב מידע מהכנסת ולא מצליחים (שדה חסר, שאלה מה קיים):
  `reference/knesset-odata-manual.docx` הוא המדריך הרשמי המלא - לבדוק
  שם לפני שמנחשים.** `fs.knesset.gov.il` ו-`main.knesset.gov.il` חסומים
  ברשת הסביבה הנוכחית (בניגוד ל-`knesset.gov.il` עצמו) - ראו
  `docs/data-sources.md` §2.
- **`pdftotext` על עברית** מחזיר טקסט לוגי עם סימני RLM. לנרמל לפני עיבוד.
- **`xml:space="preserve"`** חובה בכל `<w:t>` שמתחיל או מסתיים ברווח. זו
  התקלה הכי שכיחה.
- **תוספת ל-`_KNOWN_BENIGN_SKIP_TEMPLATES` (`ingest_checks.py`) דורשת
  סריקת קורפוס מלא, לא דוגמה בודדת.** תבנית *לא מוכרת* גורמת לכשל
  ingest בקול - מתגלה מעצמה. תבנית *ב-allowlist* לעולם לא נכשלת, גם
  אם במקרה בודד היא כן עוטפת תוכן מהותי - כשל שקט. בדיוק זו הייתה
  הצורה של באג `ח:קטע4` (ברק, 2026-09-16): אושר "עוטף רק הערה" לפי
  התנהגות ברוב המופעים, בלי לבדוק את כולם - 9.7% מהמופעים (9 חוקים
  שכבר טעונים) התבררו כרמת-מבנה אמיתית שאבדה בשקט. קבוצת "11
  התבניות שאושרו במרוכז" (`ויקיפדיה`/`מיזמים`/`חוקי-*`) אושרה לפי
  דוגמה אחת לכל תבנית - אותה שיטה בדיוק, סיכון דומה, טרם נסרקה
  במלואה. **הכלל מעכשיו: כל הוספה עתידית ל-allowlist דורשת סריקה על
  כל הכותרות המועמדות (לא רק החוקים שנטענו בהצלחה - חוק שנכשל
  מסיבה אחרת עדיין יכול לחשוף שימוש מבני בתבנית), לא רק דוגמה
  מייצגת.**

---

## כל regex לזיהוי ערך חייב טסט שלילי, לא רק חיובי (2026-09-18)

**הכלל:** regex שמזהה ערך בטקסט עברי — שנה, מספר, סכום, מזהה סעיף,
שם — נבדק על **מה שהוא לא אמור לתפוס** לפחות באותה קפדנות שבה הוא
נבדק על מה שכן. טסט שרק מאשר שהתבנית תופסת את המקרים הנכונים אינו
בדיקה, הוא אישוש.

**למה זה כלל ולא הערה — שני מקרים באותו שבוע:**

1. **בדיקה 6 בוולידטור.** `[א-ת]-\d{4}` נועדה לתפוס מקף רגיל בשנה
   עברית. היא תפסה את `ו-2010` בצירוף "לשנים 2009 ו-2010" — ו'
   החיבור. ההצעה נכשלה על לא-עוול-בכפה, **והמודל שהסביר את הממצא
   ניסח הסבר משכנע על ביטוי אחר לגמרי** שהיה תקין.

2. **זיהוי שנה עברית בחקירת ההסתייגויות**, יומיים אחר כך.
   `ה?ת[קרש][א-ת"״׳\']{0,4}` תפסה את המילה **"תקופה"**. אותו כשל
   בדיוק, בקוד חדש, אחרי שהראשון כבר תועד.

**הסיבה המבנית:** אותיות עבריות בודדות הן גם תחיליות דקדוקיות
(ו', ב', ל', ה', מ', ש', כ'), ומחרוזת שנראית כמו ראשי תיבות של שנה
היא לעתים תחילת מילה רגילה. תבנית שמתחילה ב"אות עברית" תופסת חלקי
מילים כמעט תמיד.

**בפועל:** לכל regex כזה, רשימת מחרוזות שליליות בטסט — מילים עבריות
רגילות מאותו שדה סמנטי, וצירופים עם ו'/ב'/ל' מובילות. עוגן לתבנית
(מקף, סוגריים, גבול מילה) עדיף על הרחבת האותיות המותרות.

## כלל שנגזר ממבנה עדיף על כלל שנלמד מדוגמה (2026-09-18)

**הכלל:** כשצריך לקבוע כלל ניסוח, עדיף לגזור אותו מ**מבנה** המסמך
ואז **לאמת אותו מול התנהגות אנושית מתועדת**, מאשר ללמוד אותו
מדוגמה. הראשון ניתן לבדיקה; השני הוא הכללה ממקרה אחד.

**למה זה עדיף, ולא רק "יותר יפה":** כלל שנלמד מדוגמה מסביר את
הדוגמה בהגדרה — אין לו כוח ניבוי, ואי אפשר לדעת אם הוא נכון או
שהותאם. כלל שנגזר ממבנה מנוסח **לפני** שרואים את התשובה, ולכן
ההתאמה שלו להתנהגות אנושית היא ראיה ולא מעגליות.

**הדוגמה שהולידה את הכלל (שומר ציטוט שם החוק):**
`anchors.law_citation_spans` מזהה ציטוט שם חוק לפי מבנה — שם
חוק/פקודה, סוגריים אופציונליים, שנה עברית + en-dash + שנה לועזית.
הוא נכתב מהמבנה בלבד, **בלי לראות** אילו ערכים בני אדם בחרו.
ואז הושווה: מתוך תשעה ערכים בנוסח, הוא סימן חמישה — **בדיוק אותם
חמישה שאיש מ-484 המסתייגים לא נגע בהם.**

ההתאמה הזו היא ראיה. אותו כלל, אילו נלמד מהרשימה האנושית, היה
חסר ערך כהוכחה.

**בפועל:** כשנראה שצריך רשימה של מקרים — לחפש קודם את המבנה
שמייצר אותם, ואז לבדוק את המבנה מול הנתונים. `drafting-rules.md`
§9.1 הוא התבנית.

## הגנה קריטית - טיפוס שאינו מסוגל לייצג את המצב האסור (2026-09-18)

**הכלל:** כשהגנה היא קריטית, עדיף לבנות **טיפוס שלא יכול לבטא את
המצב האסור** מאשר לסנן אותו אחרי שנוצר. סינון תלוי בכך שמישהו זכר
לקרוא לו; טיפוס נאכף על ידי השפה, ושבירה שלו שוברת טסט.

**ההבדל המעשי:** "המנוע לא מייצר טקסט פוגעני כי הוא לא מחבר מילים"
היא הצהרה בתיעוד - עד שמישהו מוסיף שדה. "למחלקה אין שדה טקסט
חופשי, והפונקציה המסננת מקבלת רק את המחלקה השנייה" היא עובדה על
הקוד, שאפשר לכתוב עליה טסט.

**הדוגמה שהולידה את הכלל** (`packages/reservations/`):
`model.Reservation` (מצב הכמות) מחזיקה עוגן, ערך וציר - **ואין בה
שדה טקסט**; הטקסט נגזר ב-`@property` מתבנית קבועה.
`guards.screen_content` מקבלת `DraftedReservation` בלבד וזורקת
`TypeError` על כל דבר אחר. שתי שורות טסט מוודאות את שניהם.

**הצד השני של אותו דפוס - fail-closed:** כשכן נדרש סינון, ברירת
המחדל בכל מסלול כישלון היא **חסימה**: תשובה לא חד-משמעית, קריסה,
שירות לא זמין, פלט ריק. ומודל בתוך שומר רשאי **רק להוסיף חסימות**,
לעולם לא לאשר - כך הוא אינו קובע עובדה (חוק ברזל 9) אלא רק מרחיב
את מה שכבר נחסם.

## מבחין יחיד הוא הנחה; שני מבחינים שמסכימים על כל הקורפוס הם ראיה (2026-09-19)

**הכלל:** כשצריך לסווג תופעה בקורפוס לשתי משפחות, לא די בסימן אחד.
מוצאים **שני סימנים עצמאיים**, ובודקים שהם מסכימים על **כל** המופעים.
סתירה אחת מספיקה כדי לפסול את הסיווג — ואז עוצרים ברעש, לא מנחשים
לפי הסימן ששרד.

**למה זה עובד:** מבחין יחיד מסביר את הדוגמאות שממנה נגזר, ואין דרך
לדעת אם הוא נכון או שהותאם. שני מבחינים שנגזרו מקורות שונים
(ארגומנט מפורש מול מבנה) ומסכימים אלפי פעמים — ההסכמה עצמה היא
הראיה.

**ומה קורה כשהם חולקים — זה החלק החשוב.** ההתנגשות אינה תקלה
שמעקפים; היא האינפורמציה. בסיווג `{{ח:סעיף*}}` (2026-09-19) הצעתי
כמבחין שני את התבנית שבשורה הבאה, והקורפוס הפריך אותה על **10
מופעים**: `{{ח:סעיף*|4|...|עוגן=תוספת פרט 4}}` שאחריו `{{ח:תת|(א)}}`.
**שני הסימנים היו נכונים** — זה אכן פרט בתוספת, ולפרט הזה יש
סעיפים קטנים. המבחין שלי פשוט לא ענה על השאלה שחשבתי: הוא הבחין
בין "יש מבנה פנימי" ל"אין", ולא בין שתי המשפחות.

המבחין המתוקן (`עוגן=` מול נוכחות מספר מיקומי) נתן **אפס סתירות
על 1,890 מופעים**. ההפרכה היא ששלחה אותי לשאול מה באמת מבדיל.

**בפועל:**
- `raise` על סתירה, לא ברירת מחדל שקטה. ראו
  `wikitext_parser.StarredSectionAmbiguity`.
- מבחין שלישי מותר **רק** כשהשניים הראשונים שותקים, ומתועד ככזה.
- כשמבחין נופל — לבדוק על מה הוא באמת ענה, לפני שמחליפים אותו.

## מכה שלישית של אותה מלכודת = כלל, לא עוד שם ברשימה (2026-09-19)

**הממצא:** `number = call.args[0]` לא הבחין בין ארגומנט מיקומי
לארגומנט בעל שם. התוצאה: מספר הסעיף שנשמר היה המחרוזת
`"עוגן=סעיף 30.א"`. התיקון הראשון טיפל ב-`עוגן=`. אחר כך התגלה
`אחר=` — **2,274 מופעים ב-16 חוקים** — ואותו באג בדיוק חזר.

הפיתוי בפעם השלישית הוא להוסיף `אחר=` לרשימת השמות. **זו
תשובה שנשברת בפעם הרביעית**, על שם שעוד לא ראינו.

**הכלל:** כשאותה מלכודת מכה פעם שלישית, היא אינה שלושה באגים
אלא **משפחה אחת שלא הוגדרה**. מגדירים את המשפחה ומטפלים בה
כולה: *"ארגומנט שיש בו `=` לפני התו הראשון אינו מיקומי"* —
`^[^=|{}]+=`, בלי רשימת שמות כלל.

**וזה מה שמבדיל תיקון שורש מתיקון נקודתי, במספר:** אחרי הכלל
נשארו **0 מתוך 368** הצמתים שהמספר שלהם הכיל `=`. רשימת שמות
הייתה מחסלת 368 ומשאירה את הבאים.

**סימן זיהוי:** אם אתה כותב `or name == "..."` ליד תנאי שכבר
תוקן פעם, עצור. שאל מה המשפחה, לא מה השם הבא.

**והכלל עצמו צריך טסט שלילי** (ראו למטה): `{{ח:הערה|7}}ג1`
מכיל `=`? לא — אבל הוא מכיל `|` ו-`{}`, ולכן ההגדרה חייבת
לשלול אותם במפורש.

## התאמת תחילית על תשובת מודל בעברית היא באג (2026-09-19)

**הממצא:** השומר הקריטי ביותר במערכת - 86(ד)(2), זה שמחליט אם
הסתייגות שמודל ניסח מותרת - בדק את תשובת המודל כך:

```python
if answer.startswith("לא"):   # ← חור fail-open
    return ALLOWED
```

**"לאחר בדיקה הטקסט תקין" מתחיל ב"לא" ועובר.** גם "לאמיתו של
דבר", גם "לאור האמור". מודל שמסרב לענות במילה אחת ומסביר -
והוא יעשה את זה - עוקף את השומר לגמרי.

**הכלל:** מילת מפתח עברית היא כמעט תמיד **תחילית של מילים
אחרות**: לא/לאחר, כן/כנראה, תקין/תקינות, יש/ישנם, אין/איני.
תשובת מודל נבדקת בהתאמה **מדויקת** אחרי ניקוי רווחים ופיסוק,
לעולם לא ב-`startswith` וב-`in`.

**זו אותה משפחה בדיוק** כמו "כל regex לזיהוי ערך חייב טסט
שלילי" (למטה) - שם האות העברית הבודדת היא תחילית דקדוקית, כאן
המילה השלמה היא תחילית לקסיקלית. שתיהן נראות נכון על הדוגמה
שבגללה נכתבו.

**וכשהכיוון מסוכן - הפוך את ברירת המחדל.** בשומר fail-closed
המחרוזת שמותר לה **לעבור** היא זו שנבדקת בהתאמה מדויקת; כל דבר
אחר חוסם. אילו נבדקה שם מחרוזת החסימה, אותו באג היה fail-open.

## בדיקה חדשה מורצת על הקוד השבור לפני שסומכים על "עבר" (2026-09-19)

**הממצא:** ברק ביקש לחפש עוד מקומות שבהם שינוי ב-API לא עודכן
בצד הלקוח. כתבתי בודק סטטי שמשווה שדות שהלקוח קורא מול שדות
שה-endpoint מחזיר. הוא החזיר **"אפס בעיות"**.

לפני שדיווחתי, הרצתי אותו על הקוד **שלפני** התיקון - שם `d.total`
היה שבור ואני יודע זאת בוודאות. **הוא לא תפס גם שם.** הסיבה:
המחרוזת `"total":` כן מופיעה בגוף ה-endpoint - זו בדיוק השורה
השבורה `"total": result["total"]` - ולכן התאמת מפתחות סטטית
עיוורת לה. הבאג הוא `KeyError` בזמן ריצה, לא שדה חסר.

**הכלל:** בדיקה חדשה חייבת להיות מורצת על הקוד השבור לפני
שסומכים על "עבר". אם היא אינה תופסת את הבאג שבגללו נכתבה, היא
חסרת ערך - **וגרועה מכלום**, כי היא נותנת ביטחון שווא. "אפס
ממצאים" מבודק שלא הוכח הוא אמירה על הבודק, לא על הקוד.

**מה שנעשה במקום:** `tests/unit/test_endpoint_smoke.py` קורא
לכל נקודת קצה בפועל ובודק שאינה מחזירה 500. הבודק הסטטי נזרק.
והבדיקה האמיתית מצאה **שלושה** 500 שלא ידעתי עליהם.

**נגזרת:** אותו דבר בדיוק נכון לשומר. שומר שלא ראית נכשל על
קלט שהוא אמור לחסום הוא שומר לא בדוק.

## בדיקה שתוצאתה תלויה במכונה אינה בדיקה (2026-09-19)

**הממצא:** ברגע שהמפתחות התחילו להיטען מקובץ, שישה טסטים נפלו -
`test_api`, `test_semantic_search`, `test_llm_draft`,
`test_admin_ingest`, `test_embeddings_errors`,
`test_reservations_quality_api`. אף אחד מהם לא נשבר. הם פשוט
**עברו עד אז מסיבה שאינה הסיבה שבגללה נכתבו**: "בלי
`SUPABASE_URL` -> 503" עבר כי במקרה לא היה `SUPABASE_URL`
במכונה, לא כי הקוד מטפל נכון בהיעדרו. `test_api` "החזיר 2
חוקים" כי במקרה לא הייתה גישה ל-DB.

**הכלל:** בדיקה שהתוצאה שלה משתנה לפי מה שמוגדר בסביבה, לפי מה
שיש במטמון, או לפי מה שקיים על הדיסק של המכונה - אינה בדיקה.
היא מדידה של המכונה. אם היא ירוקה, זה לא אומר שהקוד נכון; אם
היא אדומה, זה לא אומר שהקוד שגוי.

**והתיקון הוא בידוד, לא התאמה.** הפיתוי כשטסט כזה נופל הוא
לשנות את הציפייה שלו כדי שיעבור שוב - וזה בדיוק מה שהורס אותו:
הוא הופך לטסט שמתעד את המכונה הנוכחית. הטסט צריך **לנתק** את
עצמו ממה שהוא לא בודק: `tests/support/isolate_env.py` מסירה את
המפתחות מהסביבה **וגם** מצביעה את טוען הקובץ לנתיב שאינו קיים,
כך שהבדיקה נותנת אותה תוצאה בכל מכונה - עם מפתחות או בלעדיהם.

**סימן מזהה:** טסט שיש בו `assert os.environ.get(X) is None,
"הבדיקה מניחה ש..."` מודה בעצמו שהוא תלוי-מכונה. ההנחה הזו היא
באג, לא תיעוד.

**המשפחה הרחבה:** זה אותו כלל בדיוק כמו "מדידה על מה שיש במטמון
אינה מדידה על הקורפוס" (ראו למטה, המבחין הכפול). בשני המקרים
התוצאה נכונה לגבי מה שבמקרה היה זמין, ומוצגת כאילו היא נכונה
לגבי העולם.

## "לא נמצא" ו"לא הצלחתי לבדוק" הם שני מצבים שונים (2026-09-22)

**הכלל:** קוד שתופס חריגה ומחזיר ריק, `None`, `[]` או ברירת מחדל -
**מוחק את ההבחנה בין "אין נתונים" ל"הבדיקה נכשלה"**. אלה שני מצבים
שונים לחלוטין מבחינת המשתמש, והוא חייב לדעת באיזה מהם הוא. `except`
שמחזיר ריק בלי לסמן שמשהו נכשל הוא באג, גם כשהוא "לא מפיל כלום".

**זה הדפוס שתפס אותנו חמש פעמים**, ובכל פעם הוא נראה אחרת:

| # | איפה | מה שהמשתמש ראה | מה שקרה באמת |
|---|---|---|---|
| 1 | חיפוש סמנטי | "לא נמצאו תוצאות" | החוק פשוט **לא מאונדקס** |
| 2 | כלי המחקר | תשובה מספרית תקינה | **פרמטר נזרק** בשקט (כנסת/טווח זמן) |
| 3 | `_diff_section` | עריכה "הצליחה" | **לא הופקה הוראת תיקון** כלל (עומק 2) |
| 4 | `insert_preview` | תווית `(ד)` | **מספור מהאחים הלא-נכונים** |
| 5 | `_attach_initiators` | "אין יוזמים" | **400 מהפיד** (`$select` על שדה שלא קיים) |

**מה מאחד את כולם:** בכל המקרים **הקוד ידע** שמשהו לא תקין - וזרק את
הידיעה. התיקון אינו "לטפל בשגיאה" אלא **להעביר את ההבחנה הלאה**.

### הצורה הנכונה

1. **שני שדות, לא אחד.** `initiators: []` עם
   `initiators_unavailable: false` = "נבדק, אין". עם `true` =
   "לא הצלחתי לבדוק". רשימה ריקה לבדה אינה מבדילה.
2. **הודעה שאומרת מה לא קרה**, לא הודעה כללית:
   *"לא הצלחתי לבדוק אם קיימות הצעות דומות... **זו אינה תשובה
   ש'אין' הצעות דומות** - הבדיקה לא רצה."*
3. **ערך מוחזר שנבדק.** `writeDrafts` החזירה `false` על כישלון
   מאז ומתמיד, ו**איש לא בדק**. החיווי "נשמר אוטומטית" הציג הצלחה
   גם כשהכתיבה נכשלה - חיווי שמשקר בדיוק במצב שבו הוא הכי נחוץ.
4. **`except Exception` רחב הוא ריח.** תפיסה צרה (`OdataError`,
   `MissingSecret`) מוכיחה שחשבו על מצב הכשל; תפיסה רחבה בולעת גם
   באגים שלא נצפו - וכך 400 על שם-שדה שגוי נראה כמו "אין נתונים".

### מתי ריק **כן** תקין

כשה"ריק" הוא **תשובה אמיתית** ומצב הכשל נראה במקום אחר: נפילה לגיבוי
דטרמיניסטי כש-LLM אינו זמין (`llm_draft`), fixtures בלבד כש-Supabase
לא מוגדר (`law_registry`), או עובדה ידועה על המקור (`node.find_sections`
על "לוח השוואה"). בכל אלה ההתנהגות **מוצהרת בדוקסטרינג**, וזה ההבדל.

**הביקורת המלאה:** `docs/silent-failures-audit-2026-09-22.md` - כל 110
ה-`except` וכל 20 ה-`catch` במאגר, מסווגים לפי מה שהמשתמש רואה.

## כל השוואה בין שני נוסחים בעברית נעשית ברמת מילים, לא תווים (2026-09-22)

**הכלל, לפני ההסבר:** מי שכותב קוד שמשווה שני נוסחים - דיף, זיהוי
מה השתנה, מציאת prefix/suffix משותפים, פיצול לאזורי שינוי - **חייב
לעבוד על אסימוני מילה**. השוואה ברמת תווים אסורה בשכבה הזו, גם
כשהיא "עובדת" על הדוגמה שלפניך.

**למה זה מבני ולא עניין של זהירות:** בעברית, לשתי מילים שונות לגמרי
יש לעתים קרובות אותיות משותפות בקצוות. דיף תווים רואה בהן חפיפה
אמיתית ומפצל *בתוך* מילה. התוצאה אינה שגיאה שנזרקת - היא **הוראת
תיקון תקינה תחבירית ושגויה משפטית**, שעוברת את כל הבדיקות ומגיעה
למשתמש.

**שלוש הפעמים שזה קרה, וזו הסיבה שזה כלל ולא אזהרה:**

1. `engine._diff_text`: "מאסר ששה חדשים" -> "מאסר שנה". האות **ש**
   המשותפת גרמה לפונקציה "לחשוב" שרק "נה" הוכנס אחרי "...מאסר ש".
2. `diff_translate`: אותה תקלה בדיוק, ולכן נוסף שם
   `_snap_prefix_suffix_to_word_boundaries` - **תיקון נקודתי**,
   שמתקנן את הגבולות *אחרי* שחושבו ברמת תווים.
3. **פיצול לאזורי שינוי (22.9):** כתבתי `SequenceMatcher` על תווים,
   ו-"רשיון" -> "היתר" התפצל לשני אזורים בגלל האות **ר**: הוספת
   "הית" והחלפת "רשיון"->"ר". שתי הוראות תיקון, שתיהן שגויות.

**מה שמלמד המופע השלישי:** התיקון הנקודתי מ-(2) לא מנע את (3), כי
הוא ישב בפונקציה אחת ולא בכלל. קוד חדש שנכתב באותה שכבה התחיל שוב
מתווים. לכן הכלל הוא על **השכבה**, לא על הפונקציה.

**הצורה הנכונה:** לפרק את שני הנוסחים לרצפי-מילה עם המיקום של כל
אחד (`diff_translate._tokenize`), להשוות את רשימות המילים, ורק אז
לתרגם את האינדקסים חזרה למיקומים בטקסט. גבול מילה = רווח או פיסוק
מפריד (`. , ; :`), לא whitespace בלבד - אחרת נקודה בסוף משפט נדבקת
לביטוי.

**המשפחה:** זו אותה משפחה של "מכה שלישית של אותה מלכודת = כלל"
(למטה). ההבדל כאן: שתי המכות הראשונות כבר תוקנו נקודתית, והשלישית
הוכיחה שתיקון נקודתי אינו מונע הישנות בשכבה.

## נתוני בדיקה נגזרים ממבנה שקיים בקורפוס, לא ממבנה שנראה סביר (2026-09-22)

**הממצא:** כדי למפות את גבול העומק של שכבת העריכה בניתי עץ
סינתטי עם שרשרת קינון בעומק 1-4, והרצתי עליו את ארבע השכבות.
התוצאה נראתה חד-משמעית: מעומק 2 ומעלה, הניסוח נשבר - העוגן צוטט
כטקסט (`אחרי "ראשון." יבוא:`) במקום לפי תווית (`אחרי פסקה (1)`).

**זה היה שגוי לגמרי.** העץ שבניתי ייצר `node_type` בשמות
`subparagraph` ו-`item` - טיפוסים שנראו סבירים ו**אינם קיימים
בקורפוס בכלל**. `_render_insertion` נפל אצלם לענף ברירת המחדל,
ולכן ציטט טקסט. מדידה על שרשראות הטיפוסים שהפרסר באמת מייצר
(`subsection`, `paragraph`, `definition`, `raw_block` - וזה הכול)
הראתה את ההפך: הניסוח עובד בכל עומק אמיתי, והגבולות האמיתיים
היו במקומות אחרים לגמרי - באג מספור שהציע תווית שגויה בשקט,
ופסקה בתוך הגדרה.

**הכלל:** נתוני בדיקה - עץ, קלט, מקרה קצה - נגזרים מ**מה שקיים
בקורפוס** ונמדד, לא ממה שנראה סביר מהיכרות עם התחום. לפני שבונים
פיקסצ'ר סינתטי: לספור מה באמת מופיע (אילו `node_type`, אילו
שרשראות, באיזה עומק), ולבנות לפי הספירה.

**למה זה מסוכן במיוחד:** בדיקה על מבנה מומצא לא נכשלת - היא
**עונה על שאלה אחרת**. היא נתנה לי "כשל" משכנע בשכבה שעבדה
תקין, וכמעט הסתירה שני פערים אמיתיים בשכבות אחרות. תוצאה שגויה
שנראית חד-משמעית גרועה מאין-תוצאה.

**מתי בכל זאת מותר עץ סינתטי:** כשהוא **מכיל רק טיפוסים
ושרשראות שנספרו בקורפוס**, ונבנה כדי לבודד תנאי שקשה לשחזר
בחוק אמיתי - כמו `tests/unit/test_engine_anchor_label.py`,
שמתעד במפורש למה הוא סינתטי ומה בקורפוס הוא משקף.

**המשפחה:** אותו כלל בדיוק כמו "מדידה על מדגם אינה מדידה על
הקורפוס" ו"בדיקה שתוצאתה תלויה במכונה אינה בדיקה". בשלושתם
התוצאה נכונה לגבי משהו - המדגם, המכונה, העץ שהמצאתי - ומוצגת
כאילו היא נכונה לגבי העולם.

## מה המערכת לא

היא מפיקה **טיוטה**, לא חוות דעת משפטית. זרימת העבודה מניחה שהלשכה המשפטית
של הכנסת בודקת אחרי. אין להציג שום פלט כ"מוכן להנחה".

## פלט מודל אינו עובדה — חובת גיבוי דטרמיניסטי

**כל פלט של מודל שמתיימר לדווח עובדה על המסמך — ליקוי, מספר, שם,
מיקום — חייב גיבוי דטרמיניסטי. המודל מסביר ומנסח; הוא לא קובע
עובדות.** (ברק, 2026-09-18.)

בפועל, שלוש חובות:

1. **העובדה נקבעת בקוד, לפני המודל.** אם אין בדיקה דטרמיניסטית
   שקבעה שיש ליקוי — אין ליקוי. המודל לא "מוצא"; הוא מקבל רשימה
   סגורה ומנסח אותה.
2. **ערך שהמשתמש בחר מוזרק בקוד, לא נכתב על ידי המודל.** שם שר,
   שם חבר כנסת, מספר סעיף, תאריך — המודל כותב את הגוף, לא את
   הכתובת. ערך שהמודל כתב יכול לסטות פעם אחת מתוך ארבע בלי שאיש
   ישים לב.
3. **מה שאי אפשר לאכוף בקוד — אומרים עליו את זה.** שומר שמאמת
   *מיקום* אינו מאמת *טענה*, ואסור להציג אותו כאילו כן.

**המקרה שהוליד את הכלל:** פיצ'ר ביקורת הטיוטה הציג ממצא משכנע
ומנומק על "מקף רגיל בשנה עברית התשס״ט–2009" — בעוד שבנוסח עצמו
יושב שם en-dash תקין. הבדיקה הדטרמיניסטית עצמה נכשלה על `ו-2010`
(ו' החיבור), והמודל, שלא הצליח לאתר את הליקוי, ניסח הסבר סביר
במקום לומר "לא מצאתי". **ביקורת שממציאה בעיה גרועה מביקורת שלא
מוצאת אותה** — הקורא מבזבז זמן על תיקון של כלום ומאבד אמון בכלי.

ראו `packages/documents/critique.py` (`_explanation_is_anchored`)
ו-`packages/validate/validator.py` (`_HYPHEN_YEAR_RE`).
