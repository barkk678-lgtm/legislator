# requirements.txt בשורש הריפו - לצורך פריסת Vercel בלבד (הבילדר מחפש
# אותו לצד ה-entrypoint או בשורש; ראו pyproject.toml). להרצה מקומית
# ממשיכים להשתמש ב-apps/api/requirements.txt (כולל uvicorn, שלא נדרש
# כאן - Vercel מריץ את אובייקט ה-ASGI ישירות, לא uvicorn).
fastapi
jinja2
pyluach
httpx  # 2026-09-16: law_registry.py משתמש בזה בפועל לחיבור ל-Supabase REST - לא רק לטסטים
python-multipart
python-docx
