# דוח עבודה עצמאית - ליל 2026-09-16/17

נכתב לפי המבנה שברק ביקש: לא שרשרת, כל משימה שנתקעת מתועדת כאן
והעבודה ממשיכה למשימה הבאה. מתעדכן תוך כדי הלילה, לא רק בסוף.

## תקציר מהיר (לקריאה ראשונה בבוקר)

**הושלמו במלואם, נבדקו חי מול Anthropic API אמיתי (כולל בדפדפן
אמיתי בחלק) - ד', ה', ו', ז', ח'.** `packages/llm` (שכבת LLM
משותפת), דברי הסבר+שם הצעה אוטומטיים, כלי שאילתא+ייצוא לוורד, כלי
הצעה לסדר, ומעטפת הצ'אט בממשק (7 לשוניות, 2 פעילות). 34 קבצי בדיקה
עוברים, `git status` נקי, הכל pushed ל-`master`.

**ב' - תוכנן והוצג בלבד (כמבוקש), לא ממומש.** תיקון מוצע ל-
`numbering.py` לתמיכה במספור ספרה-אות-ספרה, כולל באג אמיתי (לא
תיאורטי) שנמצא ב-`next_appended_label`.

**א' וג' - תשתית מלאה נבנתה ונבדקה עד הגבול האפשרי, אבל שתי חסימות
תשתית אמיתיות מנעו סיום בפועל:**
1. **`SUPABASE_SERVICE_ROLE_KEY` לא זמין בסשן** (חוסם כתיבת REST -
   טעינת חוקים חדשים למשימה ג', טעינת embeddings למשימה א').
2. **`api.openai.com` חסום ברמת מדיניות ה-proxy** של סביבת ה-agent
   (403, לא בעיית מפתח) - חוסם *לגמרי* כל שימוש ב-embeddings
   (משימה א' בלבד) - גם בדיקה, גם ingest, גם מדידת עלות.

שתי החסימות מתועדות בהרחבה בהמשך הדוח, כולל בדיוק מה כן נבנה/נבדק
סביבן ומה נדרש כדי לסיים (בעיקר: להריץ את הסקריפטים המוכנים מסביבה
עם הרשאות/גישת-רשת מלאות - `tools/build_search_chunks.py`,
טעינת תקנון הכנסת). שום דבר לא עקף את החסימות האלה בדרכים לא-ראויות
(RLS policies זמניים, execute_sql לכתיבה) - נבדק ונדחה במפורש.

---


`packages/llm/` - `client.py` (קריאה גולמית ל-Anthropic Messages API,
httpx בלבד, בלי תלות SDK חדשה) + `service.py` (הממשק הציבורי היחיד -
`draft()` לניסוח חופשי של טקסט מלווה, `answer_with_sources()` לתשובה
מעוגנת-מקורות עם אכיפת ציטוט וסירוב-בלי-מקור). ראו הדוקסטרינגים
לפירוט המלא של שני המצבים ולמה הם נפרדים (חוק ברזל 4).

**שתי בעיות אמיתיות נמצאו ותוקנו תוך כדי בדיקה חיה מול ה-API
האמיתי** (לא רק מוקים): (1) בלי `"thinking":{"type":"disabled"}`
במפורש, `claude-sonnet-5` מכניס content block מסוג thinking שיכול
לצרוך את כל `max_tokens` ולהחזיר תשובה ריקה. (2) `temperature`
נדחה לגמרי (HTTP 400 "deprecated for this model") - לא רק ערך
מסוים, השדה כולו. שני התיקונים ב-`client.py`, מתועדים בדוקסטרינג.

**נבדק בפועל, לא רק ב-mock:** `tests/unit/test_llm_service.py`
(7 בדיקות, כולן עוברות, `complete_fn` מוזרק - אותו דפוס `fetch=`
כמו ב-`test_db_ingest.py`, אין רשת בטסטים) **וגם** קריאה חיה אמיתית
מול ה-API (`ANTHROPIC_API_KEY` שברק נתן): `draft()` הפיק דברי הסבר
תקינים בעברית; `answer_with_sources()` ענה נכון עם ציטוט אמיתי על
שאלה עם מקור רלוונטי, **וסירב נכון** על שאלה לא-קשורה ("מה השכר
הממוצע בישראל" מול מקור על תקנון הכנסת) עם הסבר סביר. עלות/טוקנים
לדוגמה: קריאת `answer_with_sources` בודדת - 376 input, 134 output.

## משימה ה - דברי הסבר ושם הצעה אוטומטיים (הושלם)

**לא מהתחלה - הרחבה של מנגנון קיים.** התגלה תוך כדי שכבר יש
`explanatory_draft.py`/`bill_title.py` דטרמיניסטיים (בלי LLM, כמכוון
מראש - `bill_title.default_bill_title` השאיר `PLACEHOLDER` בדיוק
בחלק ה"מהות" של השם, במפורש בשביל שהמשתמש ישלים ידנית). `apps/api/
llm_draft.py` חדש **מוסיף** שכבת LLM דרך `packages/llm.draft()` -
לא מחליף: אם `ANTHROPIC_API_KEY` חסר/הקריאה נכשלת, נופל בחזרה
לדטרמיניסטי הקיים (הבדיקה `test_llm_draft.py` מוודאת את זה במפורש,
כולל שבסביבת הבדיקות עצמה - בלי מפתח - `/draft` מחזיר 200 ולא קורס).

**endpoint נפרד ומכוון** (`POST /api/laws/{id}/draft`), **לא** מוזרם
לתוך `/render`/`_bill_from_meta` - כי `/render` נקרא בכל preview חי
בזמן שהמשתמש מקליד; לוגיקת LLM שם הייתה מוסיפה latency/עלות-רשת
לכל הקלדה. המשתמש (בעתיד, בממשק) מפעיל את זה בכפתור נפרד, ועדיין
עורך את התוצאה - בדיוק ההחלטה המתועדת ב-CLAUDE.md §4.

**נבדק חי, לא רק ב-mock:** שרת מקומי + עריכה אמיתית בסעיף 4 לחוק
הקייטנות (fixture), קריאה אמיתית ל-`/draft`. פלט (מקוצר): שם
"הצעת חוק הקייטנות (רישוי ופיקוח) (תיקון – הרחבת תנאי הכשירות
למנהל קייטנה), התשפ"ז–2026" (בלי PLACEHOLDER), 3 פסקאות דברי הסבר
ב"מוצע לתקן"/"מוצע להעניק", בלי לשון פוגענית, בלי נגיעה בנוסח החוק
עצמו (רק תיאור התיקון). 5 בדיקות אופליין (`test_llm_draft.py`,
`complete_fn`/monkeypatch של `llm_draft.draft`) + כל 30 קובצי הבדיקה
הקיימים עדיין עוברים (הרצה מלאה, לא רק הקובץ החדש).

## ⚠️ חסימה מרכזית שמשפיעה על משימות א' ו-ג' (נמצאה בתחילת הלילה)

**`SUPABASE_SERVICE_ROLE_KEY` לא זמין בסשן הזה.** ברק נתן הלילה
`ANTHROPIC_API_KEY`/`OPENAI_API_KEY` (שני אלה זמינים ותקינים - נבדק),
אבל לא את מפתח ה-service_role - זה נדרש מהיסטוריה קודמת של הפרויקט
(`apps/api/law_registry.py` קורא אותו כ-`SUPABASE_URL`/
`SUPABASE_SERVICE_ROLE_KEY`), ולא הודבק מחדש הלילה. MCP Supabase
חושף רק מפתחות `anon`/`publishable` (`get_publishable_keys`) - לא
`service_role` - אין דרך תוכנתית לשחזר אותו.

**למה זה חוסם בדיוק את משימות א'/ג', ולמה לא עקפתי עם execute_sql:**
שתי המשימות דורשות **כתיבה** בהיקף לא-זניח ל-Supabase (טעינת
embeddings/chunks במשימה א', טעינת 3 חוקים חדשים במשימה ג'). ברק
קבע במפורש הערב: "`execute_sql` רק לשאילתות חקירה... REST לא [מעיר
אותי]" - כלומר כתיבה חייבת REST+service_role, לא execute_sql, גם
בהיקף קטן (3 חוקים) - זו בדיוק הסיבה שהוא ציין ("זה מה שאפשר את
הטעינה הלילית של 1,092 חוקים"). **לא עקפתי את זה** - השתמשתי
ב-`apply_migration` (DDL, לא כתיבת דאטה) איפה שנדרש סכימה חדשה,
וב-`execute_sql` רק לשאילתות קריאה חד-פעמיות שדרשו JOIN (כמו בסבב
הקודם הערב) - לא לכתיבה.

**מה בכל זאת בוצע במלואו בלי לחכות למפתח** (ראו הפירוט למטה בכל
משימה): כל קוד הליבה, הסכמה (`search_chunks` + `pgvector`, DDL
בלבד), בדיקת embeddings אמיתית מול OpenAI (`OPENAI_API_KEY` שכן
זמין), ותוכנית ה-ingest ל-3 חוקי משימה ג' (SQL בנוי ומאומת, לא
מבוצע). **מה שממתין ל-`SUPABASE_SERVICE_ROLE_KEY`:** הרצת שני
סקריפטים מוכנים (`tools/load_search_chunks_to_supabase_rest.py`,
טעינת 3 חוקי משימה ג' דרך `load_one_law` הקיים) - פקודה אחת כל
אחד, מוכן לרוץ ברגע שהמפתח יינתן.

**נבדק ונדחה במפורש כאפשרות עוקפת:** פתיחת policy זמני ב-RLS כדי
לאפשר כתיבה עם מפתח ה-anon/publishable (כן זמין דרך MCP) - **לא
בוצע**. זה משנה יציבה אבטחתית (RLS) בלי סקירה שלך, בדיוק ההפך
מהכוונה מאחורי ההפרדה service_role/anon שכבר תוקנה הערב. נשאר חסום,
לא עקפתי.

## משימה ג - הכנת מקורות מומחה התקנון (חלקית - חסומה בכתיבה)

**איתור (הושלם):** שלושת המקורות אותרו בוויקיסורס. **שניים כבר
בקורפוס** - `חוק-יסוד: הכנסת` (`law-2000037`) ו-`חוק הכנסת,
התשנ"ד–1994` (`law-2000325`) - נבדק ישירות מול ה-DB, שום פעולה
נדרשת. **השלישי, "תקנון הכנסת", לא בקורפוס** (לא חקיקה ראשית,
לא תחת "קטגוריה:בוט חוקים") - קיים בוויקיסורס בכתובת "תקנון הכנסת".

**התאמת מבנה (נבדק בפועל, לא הונח):** אותה מערכת תבניות בדיוק
כמו כל חוק אחר (`{{ח:התחלה}}`, `{{ח:קטע1}}`/`{{ח:קטע2}}`,
`{{ח:סעיף}}`, `{{ח:חיצוני}}` וכו'). הרצתי בפועל דרך `tools/
ingest_law.py law-tkanon-haknesset "תקנון הכנסת" --law-is-new`
(dry-run, לא כותב ל-DB): **פרסר בהצלחה, 0 בעיות sanity** - 1,101
nodes, 148 סעיפים, 279 טוקני תיקון, revision 2950622 (as_of
2025-07-29). ה-SQL המלא בנוי ומוכן ב-scratchpad, **לא בוצע** -
חסום על `SUPABASE_SERVICE_ROLE_KEY` (ראו למעלה). source_ref
שהוזן ("י\"פ תשכ\"ח, 590") מהתיבה הראשונה בוויקיטקסט עצמו (יומן
פרסומים, לא ס"ח - תקנון הכנסת אינו חקיקה ראשית ולכן לא מתפרסם
ב-ס"ח) - כדאי לך לוודא שזה הפורמט הרצוי לפני טעינה בפועל.

**נשאר לביצוע (אחרי שהמפתח יינתן):** הרצת אותו `tools/ingest_law.py`
שוב עם `--out` לקובץ, ואז `load_one_law()` (מ-`tools/
load_corpus_to_supabase_rest.py`, כבר קיימת - לא קוד חדש) על
הקובץ הזה. לא בניתי ממשק/צ'אט - כמבוקש במפורש, רק המקור.

## משימה ו - כלי שאילתא (הושלם)

**חיפוש פורמט אמיתי לפני קביעה (כמבוקש) - נמצא חלקית, מדווח בשקיפות:**
כללי התוכן המחייבים נמצאו במקור הכי טוב האפשרי - `תקנון הכנסת` עצמו
(שכבר נטען כ-wikitext אמיתי במשימה ג של אותו לילה): §46(א) (חובה -
מנוסחת כשאלה, עניין עובדתי בתחום תפקיד השר), §47(א)(1-3) (איסורים -
לשון פוגענית/גזענית/פוגעת בכבוד הכנסת; בקשת חוות דעת כללית/עניין
היפותטי/מחוץ לתחום השר; פרטים אישיים פרטיים), §48/49/50 (מגבלת
מילים: רגילה 50, דחופה 40, ישירה - בלי הגבלה). **נבדקו גם**: OData
של הכנסת (`KNS_Query`) - יש מטא-דאטה אמיתית (נושא/סוג/תאריכים) אבל
לא גוף הטקסט המלא; ניסיונות לגשת ל-fs.knesset.gov.il/shakuf.co.il/
shatil.org.il לדוגמה מוחשית נחסמו ע"י ה-proxy של הסביבה. **בלי
לגרד נבו/דין** (חוק ברזל 6) - לא ניסיתי בכלל. מבנה הכתובת/שורת
הנושא במסמך הסופי הוא מוסכמה סבירה, לא מקור מאומת כמו מגבלות
התוכן - מסומן ככה במפורש בדוקסטרינג `query_tool.py`, לא מוצג כברור
מאליו.

**המימוש:** `apps/api/query_tool.py` (`draft_query()` - קורא רק
ל-`packages/llm.draft()`, לא `answer_with_sources()`, כי זו הכוונה
של תיאור-שהמשתמש-נתן, לא תשובה עובדתית ממקור - חוק ברזל 4, תפקיד 2)
+ `packages/render/simple_doc.py` חדש - כלי docx כללי (כותרת+פסקאות,
לא טבלת חקיקה), כי `render_bill.write_docx` ספציפי לגמרי לטבלת
הסעיפים. **אותה טכניקה בדיוק** כמו `render_bill` (swap רק
`word/document.xml`, שאר ה-skeleton נשאר בייטים זהים) - לא תלות
`python-docx` חדשה בפרודקשן, וישמש גם משימה ז. שני endpoints:
`POST /api/query/draft`, `POST /api/query/export` (בלי state בשרת -
הלקוח שולח את הטיוטה המלאה אחרי עריכה, לא רק מזהה).

**אימות docx - ממצא כן/לא חשוב:** `soffice --headless --convert-to
pdf` נכשל גם על **הקובץ הידוע-כתקין** `reference/golden-kaytanot.docx`
("source file could not be loaded") - כלומר זו מגבלת סביבת ה-sandbox
(LibreOffice headless לא מאותחל כאן במלואו), **לא** פגם בקובץ
שנוצר. אימות בפועל: `pip install python-docx` (זמנית, לבדיקה בלבד -
**לא** נוסף ל-requirements.txt) קרא את הקובץ שנוצר בהצלחה, כל
הפסקאות נכונות. כלי בדיקה, לא תלות אפליקציה.

**נבדק חי מול Anthropic API אמיתי, כולל מקרה קצה מכוון:** תיאור
משתמש כועס/לא-עובדתי במפורש ("זו שערוריה", "אף אחד לא עושה כלום")
על רכבת קלה בתל אביב - הפלט: נושא "עיכובים בתפעול הרכבת הקלה בתל
אביב", גוף מנוסח כשאלה עובדתית ("מהם הגורמים לעיכובים... אילו
צעדים ננקטים..."), 26 מילים (בתוך מגבלת 50 של שאילתה רגילה), בלי
זכר ללשון הכועסת המקורית - **בדיוק ההתנהגות שכללי §47 דורשים**.

6 בדיקות אופליין (`test_query_tool.py`, כולל בדיקת write_query_docx
עם python-docx) + כל 30+1 קבצי הבדיקה הקיימים עדיין עוברים.

## משימה ז - כלי הצעה לסדר היום (הושלם)

זהה מבנית לכלי השאילתא, **בלי ייצוא** (ברק, במפורש) - `apps/api/
agenda_tool.py`, `draft_agenda()` דרך `packages/llm.draft()`. פורמט
`נושא:`/`נימוק:`/`בקשה:` נבדק מול תקנון הכנסת עצמו: §52(א) מחייב
לצרף דברי הסבר/נימוק (לא רק נושא) - זה מקור ישיר. **בשונה משאילתה -
אין מגבלת מילים מפורשת** בסעיפים שנבדקו. איסור לשון פוגענית מסומן
במפורש בדוקסטרינג כ"קו-מנחה כללי-סביר, לא סעיף ספציפי שנמצא
במפורש" - שקיפות על מה שונה מ-§47 המצוטט (שאילתה) לעומת מה שלא אומת
באותה רמה (הצעה לסדר). endpoint יחיד: `POST /api/agenda/draft`.

נבדק חי מול Anthropic API אמיתי (תיאור "לא נסבל, צריך לעשות משהו
כבר" על זיהום אוויר ליד בתי ספר → נושא/נימוק/בקשה מנוסחים כראוי,
עובדתי ומכובד). 3 בדיקות אופליין חדשות + כל 30+2 קבצי הבדיקה
הקיימים עדיין עוברים.

## משימה ח - מעטפת צ'אט בממשק (הושלם)

תפריט צד עם כל 7 הלשוניות (בדיוק כמו `docs/design/legislator-mockup.
html`: הצעות חוק/נוסח משולב/שאילתות/הצעות לסדר/מומחה התקנון/סיכומי
פרוטוקולים/מחקר - כולל `nav-sep` באותו מקום). **קומפוננטת צ'אט אחת**
(פונקציות `appendMsg`/`wordCountHtml` משותפות ב-`app.js`) משרתת את
שני הכלים - שאילתות ו-הצעות לסדר, כל אחד עם טופס קצר מעליה (שר/מאת/
סוג לשאילתה, מאת בלבד להצעה לסדר) ו-`.composer` לשליחת הודעות. ארבע
הלשוניות הנותרות מציגות "בקרוב" (`.coming-soon`), כמבוקש במפורש.

**מנגנון "המשך שיחה" לא היה מפורש במשימה - הוחלט תוך כדי מימוש:**
כל הודעת משתמש נוספת ל-`topicHistory` בצד הלקוח ונשלחת כמכלול לשרת
(השרת עצמו בלי state, כמו כל שאר ה-API, 10ב) - כך "קצר את זה"/"שנה
ניסוח" בהודעה שנייה עובדים כהמשך טבעי, לא רק פנייה ראשונית. אומת
בפועל (ראו למטה) - הודעת המשך אכן הניבה טיוטה מעודכנת, וה-docx
שיוצא בפועל שיקף את הגרסה **המעודכנת**, לא הראשונית.

**אומת מקצה לקצה בדפדפן אמיתי (Playwright, לא רק ידנית):** מעבר בין
לשוניות (topbar/tab מתחלפים נכון, כולל בדיקה ששאר הלשוניות מוסתרות
בפועל); שאילתא אמיתית מול Anthropic API על "זמני המתנה במוקד 100"
דרך הממשק המלא (מילוי טופס → הודעה → טיוטה עם ספירת מילים); הודעת
המשך ("קצר את זה") הניבה טיוטה שנייה אמיתית; **הורדת docx אמיתית
דרך כפתור הדפדפן** (לא רק קריאת API ישירה) - נפתח בהצלחה עם
python-docx ומכיל את הטקסט המעודכן (מהגרסה המקוצרת, לא הראשונית -
מוכיח שהלקוח שולח את המצב הנוכחי, לא הראשוני, ל-`/export`); הצעה
לסדר עם כפתור העתקה מופעל אחרי טיוטה; ארבע לשוניות "בקרוב" מוצגות
נכון. שתי שגיאות קונסול לא-קשורות/מוכרות מהסבבים הקודמים (Google
Fonts preconnect מאחורי ה-proxy, `/favicon.ico` 404) - לא מה-JS
החדש עצמו.

## ⚠️ חסימה שנייה, קריטית למשימה א' - api.openai.com חסום ברמת המדיניות

בניגוד ל-`api.anthropic.com` (עובד מצוין כל הלילה, ראו משימות ד-ח),
**`api.openai.com` חסום לגמרי ברמת ה-proxy הארגוני של הסביבה** -
לא בעיית מפתח: `curl "$HTTPS_PROXY/__agentproxy/status"` מראה
`"gateway answered 403 to CONNECT (policy denial)"` על כל ניסיון
חיבור ל-`api.openai.com:443`, ו-`api.anthropic.com` מופיע במפורש
ב-`noProxy` בעוד `api.openai.com` לא. **זו מדיניות רשת של הסביבה
הזו, לא ניתנת לעקיפה מבפנים** (לא ניסיתי לעקוף - proxy/DNS/וכו').

**המשמעות למשימה א':** בקשת ברק המפורשת - `text-embedding-3-large`
של OpenAI - **לא ניתנת להרצה או לבדיקה בכלל בסביבה הזו**, גם עם
`OPENAI_API_KEY` תקין (שכן סופק). לא ניתן למדוד עלות בפועל (כמבוקש
במפורש - "מדוד את העלות בפועל") ולא ניתן לאמת התנהגות אמיתית
(כמו שנעשה עם raw_block מול Anthropic הערב) - כי אין בכלל תקשורת
עם ה-API.

**מה כן נבנה ונבדק בלי OpenAI (ראו סעיפי משנה למטה):** `packages/
corpus/chunking.py` (לוגיקה טהורה - נבדקת אופליין במלואה, בלי צורך
ב-embeddings בכלל), סכמת `search_chunks`+`pgvector`+פונקציית חיפוש
היברידי ב-DB (DDL בלבד, לא תלויה ב-OpenAI). **מה שנשאר חסום**:
`packages/llm/embeddings.py` נכתב (אותו דפוס מדויק כמו `client.py`,
כולל טיפול ב-`EmbeddingTooLongError` לפי המגבלה המתועדת של המודל
- 8,192 טוקן) אבל **לא נבדק בפועל בכלל**, לא רק לא-מבוצע-בהיקף-מלא
כמו החסימה הראשונה. סקריפט ה-ingest נכתב מוכן-להרצה, לא נבדק.
**כדי להשלים בפועל: להריץ מסביבה עם גישת רשת ל-`api.openai.com`**
(למשל production/Vercel, שם ה-egress כנראה לא חסום - לא אומת).

## משימה א - חיפוש סמנטי היברידי: מה הושלם בפועל למרות שתי החסימות

**סכמת DB - מוחלת בפועל, לא רק כתובה.** `supabase/migrations/
20260917000000_search_chunks_hybrid_search.sql`: טבלת `search_chunks`
(law_id, section_number, chunk_index, node_ids, context_prefix,
body, contains_raw_block, embedding vector) + פונקציית RPC
`hybrid_search_chunks` (ts_rank_cd + דמיון קוסינוס, משוקללים).
**תיקון אמיתי שנתפס תוך כדי הרצה מול ה-DB (לא הונח מראש):** ניסיון
ראשון עם `vector(3072)` (ברירת המחדל של text-embedding-3-large)
נכשל - `"column cannot have more than 2000 dimensions for hnsw
index"` (מגבלת pgvector עצמו). תוקן ל-`vector(1024)` דרך פרמטר
`dimensions` הרשמי של OpenAI (`embeddings.py`) - **הוחל בהצלחה על
ה-DB האמיתי, אומת** (`search_chunks` קיימת, 0 שורות, מוכנה ל-ingest).

**קוד שנכתב ומוכן, אבל לא נבדק מקצה לקצה (חסום כפול):**
- `packages/corpus/chunking.py` - היחיד שכן **נבדק במלואו אופליין**
  (7 בדיקות, כולל fixture אמיתי ואי-חיתוך raw_block - ראו למעלה).
- `packages/llm/embeddings.py` - נכתב, מגבלת האורך (8,192 טוקן)
  מתועדת מתיעוד OpenAI, לא אומתה מול ה-API עצמו (חסום).
- `tools/build_search_chunks.py` - סקריפט ה-ingest המלא (REST,
  לא execute_sql - אותו דפוס בדיוק כמו `load_corpus_to_supabase_
  rest.py`: progress log, idempotent, ממשיך אחרי כשל בחוק בודד).
  מוכן לרוץ עם שלושת משתני הסביבה (`SUPABASE_URL`,
  `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY`).
- `apps/api/semantic_search.py` + `GET /api/semantic-search` -
  endpoint החיפוש עצמו (embedding לשאילתה + RPC יחיד).

**באג אמיתי שהתפס ותוקן על ידי הבדיקה האופליין שכן אפשר היה לכתוב
(לא הצלחה חלקית - זו בדיוק הנקודה של לבדוק גם כשלא הכל עובד):**
`api_semantic_search` תפס במפורש רק `SemanticSearchConfigError`,
אבל `search()` קרא ל-`embed_one()` (OpenAI) *לפני* בדיקת תצורת
Supabase - כך ש-`EmbeddingConfigError` (סוג שגיאה שונה, מ-`packages/
llm`) לא נתפס בכלל וגרם ל-500 גולמי עם traceback, לא 503 ברור.
**תוקן:** בדיקת תצורת Supabase עוברת *לפני* קריאת embedding (חוסך
גם קריאת API בתשלום כש-DB לא מוגדר ממילא), ושתי מקורות השגיאה
(Supabase, OpenAI) מתכנסים לסוג אחד (`SemanticSearchConfigError`)
כלפי `main.py`. 3 בדיקות אופליין (`test_semantic_search.py`) + כל
36 קבצי הבדיקה הקיימים עדיין עוברים.

**סיכום מצב משימה א':** תשתית מלאה (סכמה+קוד) קיימת ומוכנה; שום
נתון לא נטען (0 embeddings) ושום חיפוש אמיתי לא בוצע - שתי החסימות
(service_role, api.openai.com) חוסמות את זה לגמרי בסביבה הזו, לא
חלקית. "מדוד עלות בפועל" ו"מדוד גידול ב-DB" **לא בוצעו בפועל** -
אי אפשר בלי embeddings אמיתיים. הערכה גסה בלבד (לא מדידה): לפי
תמחור OpenAI המתועד ל-text-embedding-3-large ומספר הסעיפים בקורפוס
(~45,000, ראו TASKS.md) - סביר שבטווח של עשרות סנטים בודדות לכל
הקורפוס, אבל **זו הערכה מהזיכרון, לא מדידה - אל תסתמך עליה כמספר
סופי**, בדיוק הסוג של "בערך נכון" שהפרויקט הזה נמנע ממנו במפורש
במקומות אחרים (חוק ברזל 1).

---

## המשך בוקר 2026-09-17 - תגובה ל-4 הנחיות ברק

### 1. service_role סופק - תקנון הכנסת נטען בפועל

SQL שכבר נבנה אתמול (`tools/ingest_law.py`, 0 בעיות sanity) נטען
עכשיו דרך REST (`load_one_law`, אותו נתיב מוכח כמו 1,093 חוקי
הקורפוס) - **`law-tkanon-haknesset` קיים כעת ב-DB, אומת ישירות**:
1,101 nodes, `version_id=1210`. משימה ג' סגורה.

### 2. בדיקת חלופות ל-OpenAI - שלוש תשובות, נבדקו בפועל לא הוערכו

נבדקה נגישות רשת אמיתית (לא רק openai) ל-6 ספקים חלופיים +
Vercel, דרך `curl "$HTTPS_PROXY/__agentproxy/status"` (מראה סיבת
דחייה מדויקת לכל ניסיון):

- **נתיב חלופי ל-api.openai.com מכאן: לא נמצא.** זו **לא** חסימה
  ספציפית ל-OpenAI - `google.com` עצמו נדחה (403 policy denial),
  וכך גם `api.voyageai.com`, `api.cohere.ai/com`, `api.mistral.ai`.
  המדיניות היא allowlist (רשימת היתר סגורה - `api.anthropic.com`,
  npm/pypi וכו'), לא רשימת-חסימה של OpenAI בלבד - "לעקוף" את זה
  מבפנים אינו אפשרי במבנה הזה.
- **ספק embeddings אחר שכן נגיש: רק `generativelanguage.
  googleapis.com` (Gemini, כולל מודלי embedding משלו)** - נבדק
  ישירות (תשובת HTTP אמיתית מהשרת, לא חסימת proxy) ולא מופיע ברשימת
  ה-noProxy הרשמית - כנראה חריג לא-מכוון, לא מדיניות מוצהרת. Voyage
  (השותף הרשמי שאנתרופיק ממליצה עליו ל-embeddings, כי לענתרופיק
  עצמה אין API ילידי ל-embeddings) ו-Cohere **חסומים באותה מידה
  בדיוק כמו OpenAI** - זו לא חלופה זמינה.
- **Vercel: כן, ואומת בפועל, לא רק היגיון אדריכלי.** `https://
  legislator-tau.vercel.app/` עצמו **כן** נגיש מכאן (200, בלי דחיית
  proxy בלוג) - כלומר אפשר לבקש מקוד שרץ שם לבצע את קריאת ה-API,
  ואני יכול לגעת ב-URL הזה מכאן כדי להפעיל/לבדוק אותו. **בניתי ובדקתי
  את זה בפועל, לא רק תיאוריה:** endpoint אבחון חדש,
  `GET /api/admin/openai-check` - embedding יחיד למחרוזת קבועה
  קצרה (עלות זניחה), מדווח `{"ok": true/false, ...}`. עובד נכון
  מקומית (עם/בלי `OPENAI_API_KEY`, שני המצבים נבדקו). **התשובה
  הסופית האם זה עובד בפועל מ-Vercel תלויה בהעלאה שם והרצה -
  הקישור למטה כולל את זה, אתה יכול לבדוק ישירות ולהגיד לי.**

**המלצה:** אופציה 3 (Vercel) היא הברורה מביניהן - לא דורשת ספק
חדש/מפתח חדש, ומשתמשת באותו `OPENAI_API_KEY` שכבר מוגדר שם (כפי
שאמרת אתמול). ברגע שתאשר שה-`openai-check` מחזיר `ok:true` בפרודקשן,
הצעד הבא הטבעי הוא endpoint מוגן (לא ציבורי-לגמרי, ראו שיקול
עלות/ניצול-לרעה) שמפעיל את `tools/build_search_chunks.py` בפרקים
(Vercel functions מוגבלות בזמן ריצה - ingest מלא ל-1,093 חוקים
ידרוש קריאות מרובות, לא בקשה אחת) - לא בניתי את זה עדיין, ממתין
להחלטה שלך על הכיוון.

### 3. next_appended_label - כבר חלק מאותה תוכנית

מאומת: `docs/strategy/section-numbering-fix-plan-2026-09-16.md`
כבר כולל את הבאג הזה כסעיף 2 בתוך "מה עלול להישבר" (לא כפריט
תכנון נפרד) - שום שינוי נדרש, כבר במקום הנכון.

### 4. כלי מומחה התקנון - נבנה, "חיבור לא בנייה מאפס" כפי שאמרת

`apps/api/rules_expert.py` - `ask(question)` קורא ל-`packages/
llm.answer_with_sources()` הקיים (בלי שינוי בו) על שלושת המקורות
שנטענו (תקנון הכנסת, חוק הכנסת, חוק-יסוד: הכנסת). **retrieval
כרגע מילות-מפתח (Jaccard-ish), לא embeddings** - מסומן במפורש
בדוקסטרינג כפתרון-ביניים עד שמשימה א' תיפתר, לא כתחליף. מעטפת
הצ'אט (משימה ח) הורחבה ללשונית השלישית (`#rules`) - אותה קומפוננטה
בדיוק (`appendMsg`), בלי טופס נוסף (לא צריך שר/יוזם כמו שאילתא).

**נבדק חי, כולל בדפדפן אמיתי:** "כמה חתימות נדרשות להצעת אי-אמון
בממשלה?" → תשובה מדויקת ומצוטטת נכון (`law-tkanon-haknesset/44` -
61 חתימות למסלול המזורז, אין מינימום לרגיל) - עובדה אמיתית מהתקנון
שנטען הבוקר, לא מהזיכרון הכללי של המודל. שאלה מחוץ לתחום ("מה
העונש על גניבת רכב") **נדחתה נכון** עם הסבר. 3 בדיקות אופליין
(`test_rules_expert.py`, על `_rank_sources` בלבד - `ask()`/
`_load_sources()` דורשים DB/LLM אמיתיים, נבדקו חי כמתואר) + כל 35
קבצי הבדיקה עדיין עוברים.

### 5. קישור לגרסה החיה

`https://legislator-tau.vercel.app/` - Vercel מחובר ל-`master`
ומעדכן אוטומטית בכל push (מאומת בעבר). דחפתי את כל השינויים מהבוקר;
לשונית "מומחה התקנון" צריכה לעבוד שם ברגע שהפריסה מסתיימת (בדרך
כלל דקה-שתיים) - **לא אימתתי את זה ישירות מול production** (רק
מקומית + Playwright), כדאי שתבדוק בעצמך את השאלה האמיתית שרצית לשאול.

**עדכון (מיד אחרי):** נבדק בפועל מול production, לא רק מקומית -
`GET /api/admin/openai-check` על `https://legislator-tau.vercel.app`
הצליח (`{"ok":true,"dimensions":1024,"latency_ms":680-2854}`) -
**OpenAI נגיש מ-Vercel בפועל, לא רק בתיאוריה.** `POST /api/rules/ask`
על production ענה נכון על שאלת אי-אמון עם ציטוט תקין. שני הכלים
עובדים על production ברגע זה.

## endpoint ה-ingest המוגן (ברק, 2026-09-17 - "תתחיל, שלוש דרישות אבטחה קודם")

`apps/api/admin_ingest.py` + `POST /api/admin/ingest-chunks`:

1. **טוקן** - `INGEST_SECRET` (משתנה סביבה, בלי ברירת מחדל - אם לא
   מוגדר, ה-endpoint חסום לגמרי, "נכשל סגור" לא "נכשל פתוח"; נבדק
   אופליין). טוקן שנוצר: `[הוסר-סוד]`
   (32 בייט אקראיים, `secrets.token_urlsafe`) - **צריך שתגדיר אותו
   ב-Vercel (Settings → Environment Variables) בשם `INGEST_SECRET`,
   ואז יתפוס בפריסה הבאה.**
2. **מגבלת קצב** - `MAX_CALLS_PER_HOUR=5`, `MAX_CHUNKS_PER_CALL=300`
   (טבלת `ingest_log` משמשת גם לספירת הקריאות בשעה האחרונה וגם
   ללוג עצמו). **חישוב הנזק המרבי (לא הערכה - מחיר `text-embedding-
   3-large` אומת מול תיעוד OpenAI, $0.13/1M טוקן קלט):** chunk מרבי
   (2,000 תווים) ≈ עד ~1,500 טוקן בעברית (אומדן שמרני) ≈ $0.0002.
   5×300×$0.0002 ≈ **$0.30/שעה, ~$7/יום** אם הטוקן דלף והורץ ברצף
   בלי הפסקה - בסדר גודל "כמה דולרים", לא כל היתרה. בפועל, רוב
   הסעיפים קצרים בהרבה מ-2,000 תווים - העלות האמיתית תהיה נמוכה
   משמעותית (ראו מדידה בפועל למטה).
3. **לוג** - `ingest_log`: זמן, laws/chunks/טוקנים, עלות מחושבת
   מ-`usage.total_tokens` **האמיתי** שמוחזר מ-OpenAI (לא הערכת
   תווים) - `embeddings.embed_batch_with_usage()` חדש בשביל זה.

**הרצה-חוזרת (נבדק בעיצוב, לא רק מוצהר):** `search_chunks` קיבלה
`unique(law_id, section_number, chunk_index)` + upsert
(`on_conflict`+`merge-duplicates`) - כתיבה כפולה לאותו chunk לא
יוצרת שורה כפולה. `ingest_progress` (טבלה חדשה) מסמנת חוק כ-'done'
רק אחרי שכל ה-chunks שלו נכתבו; אם התקציב נגמר באמצע חוק, הוא נשאר
'pending' וה-chunks שכבר נכתבו לא מחושבים שוב (נבדקים לפי מפתח לפני
שליחה ל-OpenAI - `_existing_chunk_keys`). קריאה הבאה פשוט ממשיכה.

**CLAUDE.md עודכן** עם ממצא-שחזור "גבולות רשת" (כמבוקש) - allowlist
צר בסביבת ה-agent (חסם גם `google.com`, לא רק OpenAI) מול רשת פתוחה
ב-Vercel, מאומת ב-`openai-check`. כל אינטגרציה חיצונית עתידית תרוץ
משם.

4 בדיקות אופליין חדשות (`test_admin_ingest.py`, על שכבת האבטחה
בלבד - "נכשל סגור" בלי `INGEST_SECRET`) + כל 39 קבצי הבדיקה עדיין
עוברים. **טרם הורץ בפועל** (ממתין שתגדיר `INGEST_SECRET` ב-Vercel) -
ברגע שזה יקרה, ארוץ על 50 חוקים ואדווח עלות/גידול DB בפועל, כמבוקש,
לפני שממשיכים לקורפוס המלא.



## תוצאות הרצת 50 חוקים (ברק, 2026-09-17) - נתונים מדויקים

5 קריאות ל-`POST /api/admin/ingest-chunks` על production (הגעתי
למגבלת `MAX_CALLS_PER_HOUR=5` בדיוק - קריאה שישית קיבלה 429 נקי).
תקציב ה-`MAX_CHUNKS_PER_CALL=300` היה האילוץ המחייב ברוב הקריאות,
לא מספר החוקים - מתוך 50 המבוקשים הושלמו **27 חוקים** (עוד חוק אחד,
`law-2000002`, הושלם באמצע - הוכחה חיה שהרצה-חוזרת/אידמפוטנטיות
עובדת: 231 מתוך 501 ה-chunks שלו נכתבו בקריאה 1, וקריאה 2 המשיכה
בדיוק מהנקודה הזו בלי לשלם שוב על מה שכבר נכתב).

**מדויק, לפי SQL ישיר מול ה-DB (לא רק תשובות ה-API):**
- 1,500 chunks נוצרו, embedding=1024 מימדים לכל אחד
- 641,992 טוקן אמיתי (`usage.total_tokens` מ-OpenAI, לא הערכה)
- **$0.0835** עלות בפועל (641,992 × $0.13 / 1,000,000)
- גודל ה-DB: 191MB → 215MB (**+24MB** לכל ה-1,500 chunks, כלומר
  ~16.4KB/chunk כולל אינדקס HNSW+GIN - זה מספר הכיול המדויק
  ששימש לכל חישובי האחסון למטה)

**בדיקת חיפוש סמנטי (כמבוקש - שאילתה במילים אחרות לגמרי מהשם):**
לקחתי חוק שנכנס ל-27 (`law-2000026`, חוק תובענות ייצוגיות) וניסחתי
שאילתה על "מקרה שבו קבוצת אנשים תובעת יחד חברה בגלל נזק דומה שנגרם
לכולם" - בלי המילה "תובענה" או "ייצוגית" בכלל. `hybrid_search_chunks`
מצא **אך ורק** תוצאות מ-`law-2000026`, וכולן עם `full_text_rank: 0`
(כלומר ההתאמה הייתה וקטורית-סמנטית טהורה, לא מילות-מפתח) - הוכחה
שהחיפוש הסמנטי עובד נכון על נתונים אמיתיים, לא רק על ה-happy-path
שנבדק קודם.

**האקסטרפולציה לקורפוס המלא חרגה מ-500MB (~950-970MB לפי ההערכה
המקורית) - ולכן לא המשכתי להריץ, כמבוקש במפורש, וחיכיתי להחלטה.**

## רשימת 250 החוקים לאינדוקס (ברק: "לא Pro עכשיו, 250 בלבד, תציע קריטריון")

### הקריטריון שאני מציע - מה זמין בקורפוס ומה לא

בדקתי בפועל (SQL ישיר, לא הנחה) שלושה מקורות סיגנל אפשריים:

1. **גודל** (`total_text_len` - סכום אורך הטקסט של כל ה-nodes בגרסה
   הנוכחית של החוק) - **זמין ואמין**. חציון בקורפוס: 8,776 תווים.
   מקסימום: 603,285 (חוק התכנון והבניה).

2. **כמות תיקונים** (`section_amendment_tokens`, כפי שציינת -
   `raw_amendment_note` מקורי, מפורק לטוקנים בודדים לכל סעיף) -
   **זמין ואמין**. 45,280 טוקני-תיקון בקורפוס כולו. מקסימום לחוק
   בודד: 1,346 (חוק התכנון והבניה - גם הכי גדול וגם הכי מתוקן,
   לא הפתעה).

3. **כמה חוקים אחרים מפנים לחוק הזה ("תדירות אזכור")** - **בדקתי
   ומצאתי שזה לא זמין כגרף-הפניות אמיתי בסכימה הקיימת.** יש שתי
   טבלאות שנראות בהתחלה כמו מועמדות (`law_citations`, 324 שורות,
   ו-`section_amendment_tokens` עצמה) - אבל בדקתי בפועל (לא הנחתי)
   ומצאתי ששתיהן מתעדות את **ההיסטוריה של החוק על עצמו** (אילו
   פקודות/חוקים תיקנו *אותו*), לא מי *מצטט* אותו מתוך הטקסט המהותי
   של חוק אחר. גרף-הפניות אמיתי (חוק א' מזכיר את חוק ב' בתוך סעיף
   מהותי) היה דורש חיפוש טקסט חופשי של כותרת כל חוק בתוך הטקסט של
   כל שאר החוקים - מבחינה חישובית זה O(מספר חוקים²) על 246,630
   nodes, לא ריאלי כ-query בודד בלי אינדקס טקסט ייעודי (לא קיים
   היום). **מסקנה: הסיגנל הזה לא נכלל בציון - לא כי הוא לא חשוב,
   אלא כי הוא לא זמין בלי עבודת הכנה נוספת שלא ביצעתי.**

**הציון שהצעתי:** ממוצע פשוט של שני האחוזונים (percent_rank, לא
z-score - ההתפלגות מוטה מאוד, אחוזון עמיד יותר בפני חריגים) של גודל
ותיקונים, משקל 50/50. הרעיון: חוק גדול אבל סטטי (למשל פקודה ישנה
שלא זזה עשרות שנים) מקבל ציון בינוני, לא גבוה אוטומטית רק בגלל
גודל - התיקונים מבטאים "חוק חי" שעורכי-דין ומחוקקים עדיין נוגעים
בו.

### 11 הקטגוריות המחויבות - אותרו, עם שתי החלטות שקילות שאתה צריך לבדוק

מצאתי את כל 11 הקטגוריות שביקשת (`law-2000479` העונשין,
`law-2000613` תכנון ובניה, `law-2000198` ביטוח לאומי [נוסח משולב],
`law-2000289` חוק החברות, `law-2001104` ניירות ערך, `law-2000907`
פקודת העיריות [נוסח חדש], `law-2001189` רישוי עסקים, `law-2000292`+
`law-2000293` חוק החוזים (שני החלקים - חלק כללי ותרופות - שניהם
נחשבים "חוק החוזים" בפועל), `law-2000416` המקרקעין, `law-2000322`
הירושה) + **14 חוקי-יסוד** (המספר האמיתי כולל לימוד תורה 2023
וישראל-מדינת-הלאום 2018).

**שתי החלטות שקבעתי לבד, תבדוק שאתה מסכים:**
- מצאתי **שני** law_id לחוק-יסוד: חופש העיסוק (`law-2000043` ו-
  `law-2000044`) - זה תואם היסטוריה אמיתית (חוקק 1992, בוטל וחוקק
  מחדש 1994 בנוסח מחוזק). `law-2000043` מסומן `validity_match_method:
  not_found` (לא נמצא תואם-תוקף) לעומת `law-2000044` שמסומן `תקף`/
  `exact` - **החלטתי לכלול רק את `law-2000044` (הגרסה התקפה)**
  ולהשמיט את הכפילות ההיסטורית.
- מצאתי `bill-173858` - "חוק-יסוד: משק המדינה (הצעות חוק והסתייגויות
  שבביצוען כרוכה עלות תקציבית) (הוראת שעה), התשס"ב–2002" - זו הוראת
  שעה נקודתית שכותרתה *מתחילה* ב"חוק-יסוד:" אבל היא לא אחד מחוקי-
  היסוד המוכרים עצמם, אלא תיקון/תוספת צרה לחוק-יסוד: משק המדינה.
  **החלטתי להשמיט אותה** מרשימת ה-14 "חוקי היסוד" עצמם.

### הממצא הקריטי: 250 חוקים לא נכנסים ב-285MB עם מרווח 50MB - גם לא קרוב

חישבתי את גידול האחסון לפי הכיול האמיתי שנמדד ב-50 חוקי הבדיקה
למעלה (16.4KB/chunk, כולל אינדקסים; יחס תווים→chunks: 1,500 chunks
מתוך 847,855 תווי-טקסט של 28 החוקים שכבר אונדקסו = ~565 תווים/chunk
בממוצע בפועל, לא הנחה תיאורטית).

- **25 החוקים המחויבים בלבד** (11 הקטגוריות + 14 חוקי-היסוד) כבר
  שוקלים **~70MB** - כמעט 30% מהתקציב, לפני שהוספתי אף חוק "מדורג".
- **250 החוקים המדורגים ביותר לפי הציון** (כולל ה-25 המחויבים) היו
  שוקלים **~477MB** - כמעט כפול מ-285MB הפנויים כולם, לא רק חורג
  מהיעד של 235MB עם המרווח.
- כשאני "**חותך מהתחתית**" כמו שביקשת - ממשיך ברשימה המדורגת ומוסיף
  חוק אחרי חוק עד שהאחסון מגיע ל-235MB (285MB פחות מרווח 50MB) -
  התוצאה היא **67 חוקים בלבד** (25 מחויבים + 42 מדורגים), לא 250.

**הסיבה:** החוקים "החשובים" באמת בקורפוס הזה - בין אם לפי גודל,
תיקונים, או שילוב של השניים - הם בעצמם גדולים (חציון הטקסט בין
400 המועמדים המובילים הוא כ-30,101 תווים, פי 3.4 מחציון הקורפוס
כולו). זה לא ארטיפקט של הקריטריון - זה משקף את המבנה האמיתי: מעט
מאוד חוקים "חשובים אבל קצרים" קיימים בקורפוס. אי אפשר להגיע ל-250
חוקים איכותיים (כלומר, שבאמת עומדים בקריטריון) בתוך 235MB.

**הרשימה המלאה (67 חוקים, בתוך התקציב, ממוינת - מחויבים קודם ואז
מדורגים):**

| # | law_id | שם | חובה/דורג | גודל(תווים) | תיקונים | ציון |
|---|---|---|---|---|---|---|
| 1 | law-2000613 | חוק התכנון והבניה, תשכ"ה–1965 | מחויב | 603,285 | 1346 | 1.0000 |
| 2 | law-2000198 | חוק הביטוח הלאומי [נוסח משולב], התשנ"ה–1995 | מחויב | 445,126 | 1127 | 0.9991 |
| 3 | law-2001104 | חוק ניירות ערך, תשכ"ח–1968 | מחויב | 295,700 | 815 | 0.9982 |
| 4 | law-2000479 | חוק העונשין, תשל"ז–1977 | מחויב | 253,466 | 772 | 0.9954 |
| 5 | law-2000289 | חוק החברות, התשנ"ט–1999 | מחויב | 278,085 | 536 | 0.9927 |
| 6 | law-2000907 | פקודת העיריות [נוסח חדש] | מחויב | 225,869 | 663 | 0.9927 |
| 7 | law-2001189 | חוק רישוי עסקים, תשכ"ח–1968 | מחויב | 131,668 | 269 | 0.9703 |
| 8 | law-2000416 | חוק המקרקעין, תשכ"ט–1969 | מחויב | 74,850 | 115 | 0.9167 |
| 9 | law-2000322 | חוק הירושה, תשכ"ה–1965 | מחויב | 58,167 | 101 | 0.8994 |
| 10 | law-2000040 | חוק-יסוד: הממשלה | מחויב | 33,205 | 62 | 0.8335 |
| 11 | law-2000037 | חוק-יסוד: הכנסת | מחויב | 19,904 | 80 | 0.7946 |
| 12 | law-2000049 | חוק-יסוד: משק המדינה | מחויב | 11,669 | 25 | 0.6514 |
| 13 | law-2000050 | חוק-יסוד: נשיא המדינה | מחויב | 8,078 | 16 | 0.5522 |
| 14 | law-2000042 | חוק-יסוד: השפיטה | מחויב | 9,577 | 7 | 0.5037 |
| 15 | law-2000292 | חוק החוזים (חלק כללי), תשל"ג–1973 | מחויב | 13,277 | 3 | 0.4822 |
| 16 | law-2000293 | חוק החוזים (תרופות בשל הפרת חוזה), תשל"א–1970 | מחויב | 5,888 | 1 | 0.3166 |
| 17 | law-2000045 | חוק-יסוד: ירושלים בירת ישראל | מחויב | 1,398 | 6 | 0.2887 |
| 18 | law-2000047 | חוק-יסוד: מבקר המדינה | מחויב | 2,179 | 3 | 0.2740 |
| 19 | law-2000046 | חוק-יסוד: כבוד האדם וחירותו | מחויב | 1,591 | 3 | 0.2475 |
| 20 | law-2000044 | חוק-יסוד: חופש העיסוק | מחויב | 1,349 | 3 | 0.2328 |
| 21 | law-2000041 | חוק-יסוד: הצבא | מחויב | 436 | 1 | 0.1263 |
| 22 | law-2073986 | חוק-יסוד: ישראל – מדינת הלאום של העם היהודי | מחויב | 2,034 | 0 | 0.0933 |
| 23 | law-2000051 | חוק-יסוד: משאל עם | מחויב | 920 | 0 | 0.0366 |
| 24 | law-2000048 | חוק-יסוד: מקרקעי ישראל | מחויב | 357 | 0 | 0.0069 |
| 25 | law-2245265 | חוק-יסוד: לימוד תורה | מחויב | 57 | 0 | 0.0000 |
| 26 | law-2000002 | חוק התקשורת (בזק ושידורים), התשמ"ב–1982 | דורג | 243,621 | 797 | 0.9959 |
| 27 | law-2000595 | חוק השיפוט הצבאי, תשט"ו–1955 | דורג | 242,531 | 712 | 0.9941 |
| 28 | law-2000577 | חוק הרשות השניה לטלויזיה ורדיו, התש"ן–1990 | דורג | 191,342 | 561 | 0.9890 |
| 29 | law-2000249 | חוק ההוצאה לפועל, תשכ"ז–1967 | דורג | 188,347 | 591 | 0.9886 |
| 30 | law-2000845 | פקודת בתי הסוהר [נוסח חדש], תשל"ב–1971 | דורג | 159,749 | 602 | 0.9854 |
| 31 | law-2001072 | חוק מיסוי מקרקעין (שבח ורכישה), תשכ"ג–1963 | דורג | 170,809 | 523 | 0.9854 |
| 32 | law-2000111 | חוק ביטוח בריאות ממלכתי, התשנ"ד–1994 | דורג | 190,507 | 416 | 0.9849 |
| 33 | law-2000503 | חוק הפיקוח על שירותים פיננסיים (ביטוח), התשמ"א–1981 | דורג | 185,791 | 432 | 0.9849 |
| 34 | law-2000601 | חוק השקעות משותפות בנאמנות, התשנ"ד–1994 | דורג | 167,937 | 473 | 0.9826 |
| 35 | law-2001392 | חוק שירות המדינה (גמלאות) [נוסח משולב], תש"ל–1970 | דורג | 216,031 | 312 | 0.9822 |
| 36 | law-2001122 | חוק סדר הדין הפלילי [נוסח משולב], התשמ"ב–1982 | דורג | 136,583 | 507 | 0.9808 |
| 37 | law-2000780 | חוק לעידוד השקעות הון, תשי"ט–1959 | דורג | 121,306 | 662 | 0.9803 |
| 38 | law-2000626 | חוק התקנים, תשי"ג–1953 | דורג | 226,495 | 260 | 0.9794 |
| 39 | law-2001218 | חוק שוויון זכויות לאנשים עם מוגבלות, התשנ"ח–1998 | דורג | 282,250 | 219 | 0.9771 |
| 40 | law-2000237 | חוק הגנת הצרכן, התשמ"א–1981 | דורג | 145,085 | 316 | 0.9753 |
| 41 | law-2000191 | חוק הבחירות לכנסת [נוסח משולב], תשכ"ט–1969 | דורג | 99,547 | 793 | 0.9753 |
| 42 | law-2000901 | פקודת הסמים המסוכנים [נוסח חדש], תשל"ג–1973 | דורג | 123,524 | 340 | 0.9739 |
| 43 | law-2001068 | חוק מס ערך מוסף, תשל"ו–1975 | דורג | 117,991 | 374 | 0.9735 |
| 44 | law-2000381 | חוק המים, תשי"ט–1959 | דורג | 109,545 | 380 | 0.9735 |
| 45 | law-tkanon-haknesset | תקנון הכנסת | דורג | 148,409 | 279 | 0.9730 |
| 46 | law-2000021 | חוק תאגידי מים וביוב, התשס"א–2001 | דורג | 134,823 | 294 | 0.9726 |
| 47 | law-2000140 | חוק בתי המשפט [נוסח משולב], התשמ"ד–1984 | דורג | 100,270 | 333 | 0.9657 |
| 48 | law-2000529 | חוק הרשויות המקומיות (בחירות), תשכ"ה–1965 | דורג | 89,436 | 485 | 0.9657 |
| 49 | law-2001070 | חוק מס רכוש וקרן פיצויים, תשכ"א–1961 | דורג | 94,030 | 431 | 0.9657 |
| 50 | law-2000798 | חוק לשכת עורכי הדין, תשכ"א–1961 | דורג | 100,561 | 310 | 0.9648 |
| 51 | law-2000884 | פקודת המכרות | דורג | 121,823 | 234 | 0.9648 |
| 52 | law-2001203 | חוק הדואר, התשמ"ו–1986 | דורג | 94,404 | 369 | 0.9643 |
| 53 | law-2007081 | חוק הפיקוח על שירותים פיננסיים (שירותים פיננסיים מוסדרים), התשע"ו–2016 | דורג | 122,641 | 206 | 0.9620 |
| 54 | law-2000889 | פקודת המשטרה [נוסח חדש], תשל"א–1971 | דורג | 86,120 | 345 | 0.9602 |
| 55 | law-2000439 | חוק הנכים (תגמולים ושיקום), תשי"ט–1959 [נוסח משולב] | דורג | 96,533 | 240 | 0.9575 |
| 56 | law-2000950 | פקודת מסילות הברזל [נוסח חדש], תשל"ב–1972 | דורג | 104,277 | 195 | 0.9565 |
| 57 | law-2000451 | חוק הסדרת העיסוק בייעוץ השקעות, בשיווק השקעות ובניהול תיקי השקעות, התשנ"ה–1995 | דורג | 96,012 | 236 | 0.9565 |
| 58 | law-2001098 | חוק משפחות חיילים שנספו במערכה (תגמולים ושיקום), תש"י–1950 | דורג | 80,101 | 324 | 0.9552 |
| 59 | law-2000697 | חוק יסודות התקציב, התשמ"ה–1985 | דורג | 91,384 | 241 | 0.9552 |
| 60 | law-2000234 | חוק הגנת הפרטיות, התשמ"א–1981 | דורג | 119,307 | 158 | 0.9529 |
| 61 | law-2000017 | חוק שמירת הנקיון, התשמ"ד–1984 | דורג | 102,184 | 178 | 0.9524 |
| 62 | law-2000297 | חוק החמרים המסוכנים, התשנ"ג–1993 | דורג | 152,886 | 125 | 0.9474 |
| 63 | law-2000963 | פקודת פשיטת הרגל [נוסח חדש], התש"ם–1980 | דורג | 98,649 | 156 | 0.9451 |
| 64 | law-2006522 | חוק רישוי שירותים ומקצועות בענף הרכב, התשע"ו–2016 | דורג | 167,949 | 117 | 0.9442 |
| 65 | law-2000324 | חוק הכניסה לישראל, תשי"ב–1952 | דורג | 73,364 | 231 | 0.9433 |
| 66 | law-2001400 | חוק שירות הקבע בצבא-הגנה לישראל (גימלאות) [נוסח משולב], התשמ"ה–1985 | דורג | 106,537 | 132 | 0.9419 |
| 67 | law-2001386 | חוק שירות בטחון [נוסח משולב], התשמ"ו–1986 | דורג | 64,222 | 265 | 0.9419 |

### מה שצריך החלטה שלך לפני שממשיכים - שלוש אפשרויות (לא בחרתי לבד)

1. **לרדת ל-67 חוקים בפועל** (כל המחויבים + כל מה שנכנס מהדירוג) -
   שומר על "רק חוקים שבאמת חשובים", לא מרפד את המספר. השדרוג ל-Pro
   (שדחית ל"עלייה לאוויר") ייתן את שאר ה-183 כשזה יקרה.
2. **לרפד עד 250 עם חוקים קטנים/פחות חשובים** (מתחת לציון שהראיתי
   למעלה) רק כדי להגיע למספר - זה עומד ביעד המספרי אבל פוגע בטוהר
   הקריטריון: יכנסו חוקים שהציון שלהם קרוב לאפס.
3. **לצמצם את "משקל" כל חוק באחסון** (למשל: להשמיט raw_block/נספחים
   מה-embedding, chunking גס יותר, embedding בפחות מימדים) כדי לדחוס
   יותר חוקים חשובים לאותו תקציב - לא נבדק, ידרוש שינוי טכני נוסף
   ומדידה חדשה לפני שסומכים עליו.

**לא בחרתי אף אחת מהן לבד - זו בדיוק הסיבה שהרשימה מגיעה אליך לפני
הרצה, כמו שביקשת.**


## הרצת שלב 1 - 67 חוקים (ברק: "זה POC להדגמה, 67 מספיקים")

**התוצאה: כל 67 החוקים של שלב 1 מאונדקסים, אפס שגיאות.**

| מדד | בפועל |
|---|---|
| חוקי שלב 1 שאונדקסו | 67 מתוך 67 |
| סה"כ חוקים מאונדקסים ב-DB | 92 (67 + 25 שאריות מריצת הבדיקה המוקדמת) |
| chunks | 17,748 |
| טוקנים | 7,540,772 |
| עלות מצטברת (כולל ריצת הבדיקה) | **$0.98** |
| גודל ה-DB | 215MB → **448MB** |
| פנוי מתקרת ה-Free tier | **52MB** (הדרישה: לפחות 50MB) |
| chunks שדולגו (חורגים מ-8,192 טוקן) | 1 |
| קריאות במסלול היזום | 61 |

כל 11 הקטגוריות שברק דרש מאונדקסות, וכל 14 חוקי היסוד.

### שלוש תקלות אמיתיות שההרצה חשפה

**1. שגיאת אורך מ-OpenAI הפילה חוק שלם.** חוק התכנון והבניה (מקום 1
ברשימה) נכשל כולו כי הסיווג ב-embeddings.py זיהה רק "maximum context
length"/"too long", בעוד OpenAI החזירה "maximum input length is 8192
tokens". התוצאה: `EmbeddingRequestError` במקום `EmbeddingTooLongError`,
ומנגנון הדילוג על chunk בודד לא נכנס לפעולה. אחרי התיקון החוק עלה
בשלמותו (1,174 chunks שכבר נכתבו לא חויבו שוב - האידמפוטנטיות עבדה),
עם chunk אחד מדולג - raw_block ענק שלא ניתן לפצל.

**2. גדילת ה-DB הפילה מסלולים שלא נגעתי בהם.** כש-search_chunks
ואינדקס ה-HNSW גדלו, הם דחקו את טבלת nodes ממטמון מופע ה-Free tier.
מאותו רגע ה-view `amendable_law_ids` (סריקה מלאה של 246K שורות) ירד
לדיסק ולקח **7.7 שניות** - מעבר לתקרת הזמן של פונקציית Vercel, כלומר
**500 על /api/laws ועל חיפוש שמות החוקים**. אינדקס חלקי תואם-תנאי
החזיר את זה ל-43ms. לקח לקראת שדרוג ל-Pro: ככל שה-DB גדל, שאילתות
ותיקות שסמכו על מטמון עלולות ליפול בלי שינוי קוד.

**3. PostgREST חתך את תור האינדוקס ב-1,000 שורות - בשקט.**
`_ensure_progress_seeded` שלף את רשימת החוקים בקריאה אחת בלי עימוד.
PostgREST מגביל תשובה ל-1,000 שורות (db-max-rows) ולא מתריע, ולכן
**94 מתוך 1,094 החוקים מעולם לא נרשמו ל-ingest_progress** - הם לא
היו קיימים מבחינת ה-ingest. אחד מהם: **פקודת העיריות**, שברק דרש
במפורש. הבאג שקט לחלוטין - ההרצה דיווחה "remaining: 0" והכול נראה
תקין. אחרי התיקון (עימוד דרך Range) נחשפו 8 חוקים נוספים של שלב 1
והורצו, פקודת העיריות ביניהם (565 chunks).

### שתי הדרישות שנבנו יחד עם ההרצה

**מסלול הרצה יזומה** - `?bypass_rate_limit=true`, מוגן באותו
`INGEST_SECRET`, עוקף **רק** את מגבלת הקצב: הטוקן עדיין נדרש, תקציב
ה-chunks לקריאה עדיין נאכף (הוא ששומר על הקריאה בתוך תקרת הזמן של
Vercel), ושומר המרווח עדיין עוצר ב-507. כל קריאה כזו מסומנת
ב-`ingest_log.rate_limit_bypassed`. המגבלה (5/שעה) נשארה ברירת המחדל.

**הבחנת כיסוי בממשק** - `/api/semantic-search` מחזיר `coverage` בכל
תשובה, ולשונית "מחקר" מרנדרת את ההבדל: חיפוש "לשון הרע" מחזיר היום
"חוק איסור לשון הרע קיים במאגר אך אינו מאונדקס לחיפוש סמנטי" ולא מסך
ריק. נבדק חי בפרודקשן.

### חמש שאלות מוכנות להדגמה (נבדקו חי, כולן התאמה סמנטית טהורה)

"התאמה סמנטית טהורה" = `full_text_rank = 0`, כלומר המערכת מצאה את
הסעיף **בלי אף מילה משותפת** עם השאילתה - זו הנקודה שכדאי להדגיש מול קהל.

| שאלה | מה נמצא | דמיון |
|---|---|---|
| עובד שנפצע בעבודה ולא יכול לחזור לעבוד - מה מגיע לו | ביטוח לאומי, ס' 92-93 (נפגעי עבודה) | 0.633 |
| כמה זמן אפשר להחזיק אדם במעצר לפני שמביאים אותו לשופט | סדר הדין הפלילי, ס' 103 (הבאה לבית המשפט) | 0.616 |
| מה הסמכות של ראש עיר לגבות תשלום מתושבים עבור שירותים | פקודת העיריות, ס' 330ב (כללים לגביית חובות) | 0.584 |
| האם חייבים להנגיש בניין ציבורי לאנשים בכיסא גלגלים | שוויון זכויות לאנשים עם מוגבלות ס' 19ח **+** רישוי עסקים ס' 8ב | 0.558 |
| חתמתי על הסכם אחרי שהצד השני הטעה אותי בכוונה - אפשר לבטל | חוק החוזים (חלק כללי), ס' 15 (הטעיה) ו-ס' 17 (כפיה) | 0.474 |

שאלת בונוס להדגמת מגבלת הכיסוי: **"לשון הרע"** - מחזירה את הודעת
"קיים אך אינו מאונדקס", שימושי כדי להראות שהמערכת יודעת מה היא לא יודעת.

### מה לא לשאול בהדגמה (נבדק ונכשל)

שאלות שנשענות על חוקים שאינם ב-67: הגנה על עובדים (חשיפת שחיתות),
שכירות/פיקדון, תאונות דרכים, בית משותף. גם "אדם נפטר בלי צוואה - איך
מחלקים בין בן הזוג לילדים" החזירה תוצאה חלשה (סעיף הגדרות של חוק
גמלאות צף למעלה) - **"ילד שנולד מחוץ לנישואין - מה זכויותיו בעיזבון"
עובדת הרבה יותר טוב** ומחזירה שלושה סעיפים נכונים מחוק הירושה.
