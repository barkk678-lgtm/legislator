# חוב פתוח — כל פער ידוע שלא טופל

> **עודכן 2026-09-18 בערב.** כל מספר כאן נמדד מחדש מול ה-DB והקוד
> בזמן הכתיבה, לא הועתק מהגרסה הקודמת. הדירוג הוא **לפי השפעה על
> מי שמשתמש במוצר** — לא לפי מאמץ ולא לפי עניין טכני.

## מקרא

| | |
|---|---|
| 🔴 | המשתמש מקבל תוצאה שגויה או חסרה, בלי לדעת |
| 🟠 | המשתמש נחסם — לא עובד, אבל זה גלוי |
| 🟡 | פיצ'ר חסר; המוצר עובד בלעדיו |
| ⚪ | חוב פנימי; המשתמש לא מרגיש |

---

# מה נסגר מאז הבוקר

| # | פער | מה סגר אותו |
|---|---|---|
| 6 | **שמונה פקודות מרכזיות לא נטענות** | הכרעת `raw_block` + ingest. **16 חוקים נטענו**, ובהם מס הכנסה, החברות, המכס, התעבורה |
| 8 | `ingest_checks` מדווח על תבניות שכבר עוכלו | `_top_level_templates` משתמש באותו מבט מאוחד כמו הפרסר |
| — | בדיקה 6 בוולידטור נכשלה על `ו-2010` | הרגקס דורש צורת שנה עברית ממשית. 40/40 |
| — | `lxml` מיובא ואינו ב-requirements | נוסף לשני הקבצים |
| — | שני שמות למפתח ה-service_role | אוחד ל-`SUPABASE_SERVICE_ROLE_KEY` |

# מה נוסף מאז הבוקר

| חדש | למה |
|---|---|
| 🔴 **ה-ingest הכפיל את בעיות הנתונים** | ראו §2 ו-§3 — הפקודות הגדולות נגועות הרבה יותר מהממוצע |
| 🟠 **`.doc` בינארי אינו נתמך בהעלאה** | המשתמש יעלה את מה שקיבל, וזה לעתים `.doc` |
| 🟠 **טבלה שטוחה = אפס סעיפים** | הצעה אמיתית מ-40 הפיקסצ'רים נכשלת בכל הכלים |
| 🟡 **מצב האיכות אינו מחובר לממשק** | בנוי ונבדק, לא חשוף |
| 🟡 **20 מתוך 41 סעיפים מניבים אפס הסתייגויות** | המנוע לא מכסה חצי מהצעה טיפוסית |
| ⚪ **תקרת 50 ערכים היא לתחום ולא לעוגן** | סעיף אחד הניב 1,900 |
| 🟡 **טבלה רב-טורית — חור מוצרי** | היה ב-`DECISION_HISTORY.md` ולא ברשימה הזו |

---

# 🔴 אובדן שקט

### 1. `ח:קטע4` — ייתכן שכותרות מבנה נזרקות מחוקים שכן נטענים

ההחלטה בקוד: התבנית "עוטפת רק הערה, לא רמת מבנה אמיתית — מדולג
לגמרי". חוק להגברת האכיפה מראה שימוש **סותר**: `{{ח:קטע4|תוספת 2
חלק ב סימן ב|סימן ב׳: בטיחות בעבודה|...}}` — כותרת מבנה אמיתית.
`DECISION_HISTORY.md` מעריך **90 כותרות ב-11 חוקים**.

**למה ראשון:** אם ההנחה שגויה, חוקים ש**נטענו בהצלחה** מציגים עץ
חסר סימנים, והמשתמש אינו יודע. **חקירה בלבד** — סריקת כל המופעים
לפני כל נגיעה בפרסר.

### 2. 429 סעיפים עם מספר ריק ב-42 חוקים — **גדל מ-212**

`find_sections` מדלגת עליהם, כלומר **אי אפשר לערוך אותם**.

| חוק | סעיפים ריקים |
|---|---|
| פקודת מס הכנסה | **111** |
| פקודת השטרות | **92** |
| פקודת המסים (גביה) | 44 |
| חוק הביטוח הלאומי | 38 |
| פקודת העיריות | 37 |

**הממצא החשוב:** 16 החוקים שנטענו היום הוסיפו **217** סעיפים כאלה —
יותר ממה שהיה בכל 1,093 החוקים הקודמים יחד. הפקודות הישנות נגועות
בסדר גודל אחר, ו-**111 סעיפים לא-ערוכים בפקודת מס הכנסה** הוא פער
מוצרי ולא סטטיסטיקה.

### 3. 80 צמתים עם שארית wikitext בשדה המספר — **גדל מ-19**

`אחר=[<wbr>(א)]`, `עוגן=סעיף פרק כא תוספת`, `{{ח:הערה|7}}ג1`, `'4ח`.
ארגומנט תבנית שדלף ל-`number`. אותה תוצאה: הסעיף אינו נראה לעריכה.

### 4. 87 מספרי סעיף כפולים באותו חוק

`find_sections` בונה `dict[number, node]` — **אחד מכל זוג נעלם
בשקט**. נמדד לראשונה היום (קודם היה ידוע מקרה בודד).

### 5. 397 בלוקי `raw_block` ב-119 חוקים

נשמרים ואינם אבודים, אבל **אינם ניתנים לעריכה**. גדל מ-366 בעקבות
הכרעת מילון המונחים — זו תוצאה מכוונת, אבל המשתמש שינסה לערוך
טבלה כזו ייתקל בקיר.

---

# 🟠 חסימה גלויה

### 6. `.doc` בינארי אינו נתמך בהעלאה

`python-docx` קורא `.docx` בלבד. כל הכלים (תקציר, ביקורת,
הסתייגויות) דוחים `.doc`. **המשתמש מעלה את מה שקיבל מהוועדה**, וזה
לעתים הפורמט הישן — 2 מתוך 4 נוסחי הקריאה 2-3 היחידים שקיימים.
יש לי חולץ עובד (טבלת pieces של Word 97) שנכתב היום לצורך חקירה
ואינו מחובר לשום מסלול.

## המשתמש מקבל `.doc` מהוועדה — והתשובה אינה "שימיר בעצמו"

**המשימה:** 2 מתוך 4 נוסחי הקריאה 2-3 היחידים שקיימים ב-OData הם
`.doc` בינארי. המשתמש מעלה את מה שקיבל, לא את מה שנוח לנו.

### נבדק בניסוי, לא בהערכה (2026-09-19)

התקנתי `libreoffice-writer` בסביבה (`apt` זמין כאן), המרתי את שני
הקבצים האמיתיים, והרצתי עליהם את `extract_bill` הרגיל:

| | `extract_doc.py` שלי | דרך LibreOffice |
|---|---|---|
| טקסט עברי | ✅ | ✅ |
| מספרי סעיפים | ❌ | ✅ `1.` `2.` `3.` … |
| **עומק הזחה** | ❌ **לא ניתן לשחזור** | ✅ `[0,1,2,4,5]` |
| כותרות שוליים | חלקי (תאים) | ✅ |
| שם ההצעה והמגיש | ❌ | ❌ **אבל מזהיר** |

**ההמרה משחזרת בדיוק את מה שחסם את המחלץ שלי.** `gridSpan` נבנה
מחדש ב-docx, ולכן `extract_bill` הקיים עובד עליו בלי שינוי.

מה שאובד: הסגנונות בעלי השם (`Head HatzaotHok`, `Hesber`), ולכן
שם ההצעה והמגיש אינם מזוהים. **זה נכשל ברעש** —
`warnings=["לא זוהה שם הצעת חוק…", "לא זוהה מגיש."]` — ולא בשקט.

### למה זה עדיין לא פתרון מוצר

`libreoffice` הוא בינארי. **ה-Python runtime של Vercel אינו
מריץ בינאריים**, וההתקנה שעשיתי כאן היא בקונטיינר ארעי שנעלם.
שלוש דרכים אמיתיות, וההכרעה של ברק:

1. **המשתמש ממיר ב-Word.** עובד היום, ובעצם **איכותי יותר** —
   Word שומר את הסגנונות בעלי השם, ולכן גם שם ההצעה והמגיש
   יזוהו. החיסרון: שלב ידני שהמשתמש צריך לדעת עליו.
2. **שירות המרה נפרד** (קונטיינר עם LibreOffice) שה-API קורא לו.
   אוטומטי, אבל תשתית נוספת לתחזק, ושם ההצעה עדיין יאבד.
3. **לפרסר את מאפייני הטבלה ב-MS-DOC** ולשחזר עומק מה-SPRM-ים.
   אפשרי אך עבודה משמעותית, בשביל 2 קבצים ידועים.

**ההמלצה: 1.** ההפרש בין "המשתמש לוחץ שמירה בשם" לבין תשתית
המרה שלמה אינו מוצדק בשביל 2 מתוך 2,680, **והמסלול הידני מפיק
תוצאה טובה יותר**. מה שכן חסר היום: הודעת ה-415 מסבירה את
הסיבה אבל אינה אומרת "פתח ב-Word ושמור כ-docx" כהוראה מעשית.

---

### 7. מספור אוטומטי של Word → אפס סעיפים

`13948386` מבין 40 הפיקסצ'רים מפיק 299 שורות ו-**0 ממוספרות**.
**נחקר 2026-09-19 — והאבחנה הקודמת כאן הייתה שגויה בשני דברים:**
הטבלה אינה שטוחה (עומקים 0-2 נקראים נכון, כותרות השוליים נקראות
נכון), והחילוץ **אינו** מזהיר — `warnings` יוצא ריק.

מה שבאמת קורה: 55 מספרי הסעיפים אינם טקסט כלל. הם `w:numPr`
(`numId=26`, `ilvl=0`, `numFmt=decimal`, `lvlText=%1.`) על פסקה
**ריקה** בעמודה 1. Word מצייר "1." "2." בזמן התצוגה; `python-docx`
מחזיר `""`. שני סימנים בלתי תלויים מסכימים על הזיהוי: 55 פסקאות עם
מספור אוטומטי מול 55 שורות עם כותרת שוליים — התאמה מלאה.

**וזה לא רק החוק הזה, ולא רק 422.** ב-`13948363` וב-`13948394`
המספור מעורב: רוב המספרים מוקלדים ואחד אוטומטי. שם החילוץ מצליח
ו**מפיל סעיף אחד בשקט** — גרוע מ-422.

### 7ב. המספור המעורב — **הסעיף שנופל הוא תמיד סעיף 1**

נחקר לעומק 2026-09-19, לבקשת ברק. בשני הקבצים המעורבים
(`13948363`, `13948394`) הדפוס **זהה, ולא אקראי**:

| | סעיף 1 | סעיפים 2 ואילך |
|---|---|---|
| מקור המספר | `w:numPr` (אוטומטי) | טקסט מוקלד |
| מה `python-docx` מחזיר | `""` | `"2."`, `"3."` … |

**והסעיף שנופל הוא זה שמגדיר את "החוק העיקרי".**

```
13948363  שורה 0:  תיקון סעיף 6ג  | (ריק) | בחוק שיווי זכויות האישה,
                                              התשי"א–1951 (להלן – החוק העיקרי)…
13948394  שורה 0:  תיקון סעיף 2   | (ריק) | בחוק הפיקוח על מעונות יום
                                              לפעוטות, התשע"ט–2018 (להלן – החוק העיקרי)…
```

כל הסעיפים שאחריו אומרים "בסעיף 6 לחוק העיקרי" ומפנים להגדרה
שנמחקה.

**הנזק במורד הזרם, נמדד ולא משוער.** `_reservation_sections`
פותחת סעיף חדש רק כשיש `line.number`, וטקסט שלפני הסעיף
הממוספר הראשון **נזרק לגמרי**:

- `13948394` — 5 סעיפים בהצעה, 4 נתפסים.
- **`13948363` — 2 סעיפים בהצעה, 1 נתפס. חצי מההצעה נעלם.**

ובשניהם `warnings == []`. ההצעה נראית תקינה לחלוטין.

**לא תוקן** — ברק ביקש חקירה. הכיוון המתבקש: לקרוא את `w:numPr`
ולגזור ממנו את התווית לפי `numFmt`/`start`, או לפחות **לסמן
אזהרה** כשיש פסקה ממוספרת-אוטומטית בעמודת המספר. אזהרה לבדה
הייתה הופכת את שלושת המקרים האלה מאובדן שקט לאובדן גלוי.

### 7א. סעיף נופל בין הכיסאות ב-`Table InnerSideHeading`

נמצא תוך כדי החקירה של §7. ב-`13695200` סעיף **12.** ("החלטת
הממונה ודרישת תשלום") קיים בטבלה, ו-`extract_bill` מפיל אותו
לגמרי — לא `number` ולא `inner_number`. השורה: עמודה 0 בסגנון
`Table InnerSideHeading`, עמודה 1 `12.`, עמודה 2 `(א)`, הגוף
בעמודה 3. 23 סעיפים מתוך 24 מחולצים.

### 8. חוק הפטנטים — סוגריים כפולים `((...))` רב-שורתיים

אותה משפחת באג שתוקנה, בתחביר אחר. `((רשימת המדינות` נפתח בשורה
אחת ונסגר באחרת.

### 9. 92 מתוך 1,109 חוקים מאונדקסים לחיפוש סמנטי

17,748 chunks. הממשק מבדיל בין "לא נמצא" ל"לא מאונדקס", אבל
**16 החוקים שנטענו היום אינם באינדקס** — כולל מס הכנסה והתעבורה.
רשימת 250 המדורגת מוכנה.

### 10. המנוע אינו מנסח סעיפי סיום בכלל

תחילה, תחולה, הוראת מעבר, תקנות ראשונות — הוולידטור **בודק**,
`amend()` לא **מייצר**. כמעט כל הצעה אמיתית כוללת סעיף תחילה.

### 11. הוספת פרק שלם

`skeleton-pshia.docx` מדגים (`הוספת פרק ב'1`). דפוס נפרד מהוספת
סעיפים רצופים שמומשה.

### 12. תיקון צו, לא חוק

**דורש הכרעה משפטית לפני קוד.** שתי צורות נצפו באותה הצעה.

---

# 🟡 פיצ'רים חסרים

### 13. שתי בדיקות בוולידטור שגויות על הצעות אמיתיות

| בדיקה | נכשלת | הסיבה |
|---|---|---|
| 4 (כותרת שוליים) | 25/40 | לא מכירה כותרת לכמה סעיפים ולא כותרת שנוקבת בשם חוק |
| 7 (שם ההצעה) | 22/40 | לא מכירה `(תיקוני חקיקה)` ולא הצעת חוק חדש |

**מכפיל את הכיסוי של פיצ'ר הביקורת** — היום 5 מתוך 15.
(בדיקה 12 ירדה מ-4 כשלים ל-2 ואינה עוד ברשימה.)

### 14. מצב האיכות בהסתייגויות אינו מחובר לממשק

בנוי, נבדק, חוסם כראוי — לא חשוף. דורש קריאת LLM לכל סעיף.

### 15. 20 מתוך 41 סעיפים מניבים אפס הסתייגויות

סעיפים הצהרתיים בלי ערך מספרי. המנוע לא מכסה **חצי** מהצעה
טיפוסית, ומצב האיכות הוא התשובה לזה.

### 16. שלוש בדיקות שמעולם לא רצו

10 (תיקונים עקיפים), 13 (הצעות דומות — דורש OData), 14 (סגנונות
docx — רץ רק על מה שאנחנו מפיקים).

### 17. משימה 11 — הכיוון ההפוך: הצעת חוק ← נוסח משולב

הפיצ'ר בעל הערך הגבוה ביותר שטרם נבנה. הלשונית מציגה "בקרוב".

### 18. משימה 8 — שורת "הצעה זהה או דומה"

חובה לפי החוברת הסגולה. תשתית OData קיימת, החיבור לא.

### 19. טבלה רב-טורית — אין תקדים לניסוח תיקון

**חור מוצרי, לא פינה:** 46 מתוך 97 הטבלאות יושבות בגוף החוק ולא
בתוספות. אי אפשר לתקן את מדרגות מס הרכישה. `amend()` זורק
`NotImplementedError` מפורש במקום לנחש.

### 20. הצבעות מליאה בכלי המחקר · 21. התרעת ק״ת · 22. PDF פחות מדויק · 23. סוגי מסמכים נוספים

נחקרו ותועדו, לא מומשו. PDF מסומן מפורשות בממשק.

---

# ⚪ חוב פנימי

### 24. תקרת 50 הערכים היא לתחום ולא לעוגן

סעיף אחד הניב 1,900 הסתייגויות מ-4 עוגנים — ~475 לעוגן. תקין לפי
הכללים, אבל ייתכן שהתקרה צריכה להיות לעוגן.

### 25. משימה 7ב — הארגומנט `אחר=`

255 מופעים בחוק העונשין. אין עדיין השערה מאומתת.

### 26. `next_appended_label`: `4א` → `4ב` או `5`?

הכרעה פתוחה. קודם זה קרס, אז אין התנהגות לשמר. שלוש שורות.

### 27. אזהרות אבטחה ב-Supabase

view `resolved_amendments` עם `SECURITY DEFINER`, ושלוש פונקציות
עם `search_path` לא מקובע.

### 28. `{{דוכיווני}}` בפקודת רופאי השיניים

כשל שפיות בודד. ייתכן שנסגר עם `raw_block` — **לא אומת**.

---

## סיכום

| רמה | בוקר | ערב |
|---|---|---|
| 🔴 אובדן שקט | 5 | **5** (אותם מספרים גדלו) |
| 🟠 חסימה גלויה | 7 | **7** (2 נסגרו, 2 נוספו) |
| 🟡 פיצ'ר חסר | 8 | **11** |
| ⚪ חוב פנימי | 4 | **5** |
| **סה״כ** | 24 | **28** |

**שלושה שהייתי לוקח ראשונים:**

1. **§2+§3+§4 יחד — איכות הנתונים בפקודות הגדולות.** 429 + 80 + 87
   הם אותה משפחה, וכולם גדלו היום. **111 סעיפים לא-ערוכים בפקודת
   מס הכנסה** הוא הפער שהמשתמש ייתקל בו ראשון.
2. **§1 `ח:קטע4`** — חקירה בלבד, וזולה, ומה שיימצא בה משנה אמון.
3. **§13 בדיקות 4 ו-7** — מכפילות את כיסוי פיצ'ר הביקורת.

---

## מה **אי אפשר** לבדוק מהסביבה של הסוכן (2026-09-19)

מיפוי בפועל של מה שנגיש ומה שלא, אחרי שהמפתחות סודרו. זו רשימת
**השינויים שנבדקים רק בפרודקשן** — כל דבר כאן עולה לאוויר בלי
שהורץ אצלי ולו פעם אחת.

| יעד | מצב | |
|-----|-----|--|
| `api.anthropic.com` | נגיש | נבדק, החזיר תשובה |
| `*.supabase.co` | נגיש | נבדק, 200 |
| `he.wikisource.org` | נגיש | 256 חוקים נמשכו |
| `legislator-tau.vercel.app` | נגיש | 200 |
| `knesset.gov.il` (OData) | נגיש | |
| **`api.openai.com`** | **חסום** | `connect_rejected` — מדיניות ארגונית של ה-proxy, לא המפתח |
| **`fs.knesset.gov.il`** | **חסום** | מסמכי הוועדות. נגיש מ-Vercel |

### מה נופל בגלל חסימת OpenAI

`api.openai.com` הוא ספק ה-embeddings היחיד. **כל הנתיבים האלה
אינם ניתנים להרצה מקומית ונבדקים אך ורק בפרודקשן:**

1. **`tools/build_search_chunks.py`** — בניית chunks ואינדוקס.
   כל שינוי ב-chunking, בגודל המנה או בטיפול בחריגות אורך.
2. **`/api/admin/ingest-embeddings`** — כולל
   `_embed_batch_skipping_too_long`, לוגיקת ה-fallback
   לחתיכה-אחת-בכל-פעם, וספירת העלות.
3. **`/api/semantic-search`** — `embed_one` לשאילתה, ואז ה-RPC.
   הדירוג ההיברידי עצמו נבדק על וקטורים מדומים; **ההתאמה בין
   וקטור אמיתי לתוצאה אמיתית — לא**.
4. **`/api/diag/embeddings`** — נקודת האבחון עצמה.
5. **עלות ה-embeddings בפועל** — `usage.total_tokens` האמיתי.

**מה כן נבדק, ולמה זה לא מספיק:** `tests/unit/test_embeddings_errors.py`
מכסה את טיפול השגיאות (400 באורך מול 400 אחר) מול כפיל, ולכן
**ההיגיון** של הטיפול מכוסה. מה שאינו מכוסה הוא שהחוזה מול OpenAI
עצמו לא השתנה — שם שדה, צורת הודעת שגיאה, ממד הווקטור. בדיוק
המשפחה שכבר הפילה אותנו: `temperature` ש-claude-sonnet-5 החל
לדחות לגמרי, ולא היה נתפס בשום טסט עם כפיל.

**הסימן שזה נשבר בפרודקשן:** `/api/diag/embeddings` מחזיר שגיאה,
או ש-`ingest_progress` מצטבר ב-`status='failed'` בלי שהקוד השתנה.

### ומה נופל בגלל `fs.knesset.gov.il`

משיכת נוסחי הוועדות (`.docx`/`.doc`/PDF). עוקף בפועל:
`/api/admin/knesset-bill-docs` רץ מ-Vercel ומחזיר את הקובץ, וזה
מה שמילא את `tests/fixtures/real-bills/` (40 הצעות). כל בדיקה על
מסמך חדש מחייבת או את הנתיב הזה או קובץ שברק מעלה ידנית.

---

## 2,157 תבניות עיצוב של ויקיפדיה בתוך נוסח החוק (2026-09-19)

`{{מוקטן}}` (1,268 מופעים ב-63 חוקים), `{{דוכיווני}}` (889 ב-11),
`{{ממורכז}}` (241), `{{טורים שווים}}` (228), `{{ש}}` (117),
`{{מוגדל}}` (71). אלה **אינן** תבניות `ח:` של מרחב החקיקה, ולכן
`_flatten` אינה יודעת לפרק אותן והן נשארות בטקסט כפי שהן.

### כן, המשתמש רואה אותן

נבדק בפועל, לא הונח: `node_view` מחזירה את `text` כמות שהוא,
וה-JS עושה `escapeHtml`. כלומר בלשונית החוק מוצג למשתמש הטקסט

```
{{טורים שווים ממורכז | {{מוקטן|טור א׳}} {{ש}} שם החוק או הפקודה |
 {{מוקטן|טור ב׳}} {{ש}} התקנות שבהן נקבעה עבירה ... }}
```

במקום טבלה בת שתי עמודות. זהו **פגם תצוגה אמיתי**, לא לכלוך
בנתונים בלבד.

### אבל כמעט ולא דולפות להוראת תיקון

| מרחב | צמתים | חוקים | |
|------|-------|-------|--|
| `schedule` | 3,606 | 133 | `amend()` אינה רואה אותם כלל |
| **`law`** | **124** | **39** | **אלה כן יכולים להגיע לנוסח תיקון** |

`amend()` עובדת על `find_sections(..., numbering_space="law")`
בלבד, ולכן 96.7% מהמקרים אינם יכולים להגיע למסמך Word. 124
הצמתים במרחב `law` הם החשיפה האמיתית, והיא לא נבדקה אחד-אחד.

**לא תוקן.** דורש החלטה: לפרק את תבניות העיצוב לטקסט (ולאבד את
העיצוב), לרנדר אותן, או לסמן אותן כ-`raw_block` שאינו ניתן
לעריכה.

---

## `אחר=` — ארגומנט בעל שם שלא היה מוכר (2026-09-19)

נמצא תוך כדי סריקת 1,498 הצמתים הלא-מזוהים. **2,274 מופעים
ב-16 חוקים**, כמעט כולם (2,250) על `{{ח:סעיף}}` הרגיל ולא על
המכוכב. הערך הוא תווית חלופית בסוגריים מרובעים: `אחר=[יד]`,
`אחר=[166(4)]`, `אחר=[(א)]` — כנראה מספור הסעיף במהדורה אחרת.

**והוא נופל לאותה מלכודת שכבר תועדה:** הפרסר לוקח
`number = call.args[0]`, בלי להבחין בין ארגומנט מיקומי לארגומנט
בעל שם. ב-`{{ח:סעיף*|אחר=[יד]}}` המספר שנשמר הוא המחרוזת
`"אחר=[יד]"`. זה חלק מ-368 הצמתים שהמספר שלהם מכיל `=`.

**לא תוקן** - נגיעה בפרסר טעונה אישור, והכלל הזה חוצה את
`{{ח:סעיף}}` הרגיל ולא רק את המכוכב.

---

## דריפה: ה-DB מפגר אחרי ויקיטקסט (2026-09-19)

**איך נמצא:** בדקתי ש-28 החוקים שהוחלפו נמצאים במצב הסופי ולא
בביניים (ברק ביקש, אחרי שפקודת השטרות אכן נתקעה בביניים פעם
אחת). 27 מתוך 28 תואמים לפרסר הנוכחי. **חוק ניירות ערך אינו** —
ולא בגלל הפרסר:

| | |
|---|---|
| `wikitext_revision_id` ב-DB | 3,078,252 (נטען 16.9) |
| revision בוויקיטקסט היום | **3,083,549** |
| ההפרש בעץ | סעיף 35ג נוסף, ועוד 5 פסקאות |

**החוק תוקן בוויקיטקסט אחרי שטענו אותו.** זה לא מצב ביניים של
פרסר אלא **תוכן ישן**, וזו בעיה אחרת לגמרי: המשתמש מקבל נוסח
שאינו הנוסח העדכני, בלי שום סימן.

**לא כומת על הקורפוס המלא.** ניסיתי ונחסמתי פעמיים: דפדוף
PostgREST נחתך ב-1,000 (שוב), ואגרגציות אינן מופעלות בשרת. הדרך
הנכונה כשנגיע לזה: שאילתת SQL אחת שמחזירה `law_id` ו-
`wikitext_revision_id`, מול משיכה של ה-revision העדכני לכל חוק.
`latest_known_revision_id` ו-`latest_checked_at` כבר קיימים
בטבלת `laws` בדיוק בשביל זה — ולא נבדק מתי עודכנו לאחרונה.

**מה זה אומר על "לטעון הכול מחדש":** הנימוק נגד היה שבירת
provenance ל-1,109 חוקים במקום ל-28. אם יתברר שעשרות חוקים
מפגרים בתוכן, הטעינה המחודשת נדרשת ממילא — ואז היא לא שוברת
provenance בחינם אלא מעדכנת נוסח. **זה משנה את ההכרעה**, ולכן
המדידה הזו קודמת להחלטה הבאה על טעינה מלאה.

## הערות "צריך להיות" (צ״ל) - נמדד 21.9.2026, סגור

`[צ״ל: X]` היא הערה שמנסחי הוויקיטקסט הוסיפו לנוסח הרשמי כדי לסמן
טעות דפוס במקור. לדוגמה, בחוק השימוש בהיפנוזה כותרות השוליים של
סעיפים 27 ו-28 הוחלפו ברשומות, וההערה מתקנת: *"תקנות [צ״ל: עונשין]"*.
זה תוכן משפטי ממשי, לא רעש.

**הספירה המלאה בקורפוס (1,109 חוקים):**

| מיקום בוויקיטקסט | מופעים | מגיע למסך |
|---|---|---|
| `{{ח:הערה}}` - הערת שוליים בגוף החוק | 207 | כן |
| `{{ח:תיבה}}` - תיבת היסטוריית הפרסום בכותרת | 36 | **לא** |
| `{{מוקטן}}` בתוך `{{ח:קטע4\|\|...}}` (עוגן ריק) | 2 | **לא** |
| `{{מוקטן}}` במקום אחר | 2 | כן |
| כלל לא הערה (אצ״ל, זצ״ל, דצ״ל, נוסח פקודת 1921) | 10 | כן, כטקסט רגיל |
| **סה״כ** | **257** | **219** |

**ה-DB מחזיק 219 ב-102 חוקים - זהות מדויקת מול הפרסר**, לא אי-שוויון.

### מה לא מגיע למסך, ולמה

**36 בתיבת הפרסום** - `{{ח:תיבה}}` היא רשימת התיקונים וקישורי
הרשומות בראש הדף, לא נוסח החוק. ההערות שם מתקנות את *מספר התיקון
בציטוט ההיסטורי* (`תיקון מס׳ 59 [צ״ל 60]`), לא את נוסח החוק. **לא
פער** - התיבה כולה אינה נטענת, במכוון.

**2 בתוך `{{ח:קטע4||...}}`** - קטע4 עם ארגומנט ראשון ריק עוטף רק
הערה ומדולג לגמרי (החלטה מ-16.9.2026, אחרי סריקת קורפוס-שלם: 90.3%
מהמופעים). שתי ההערות האלה (`law-2000329`, `law-2000331`, שתיהן
"[צ״ל: סעיף 2]" בכותרת תוספת) הן תופעת לוואי של ההחלטה ההיא שלא
תומחרה בזמנו. **פער אמיתי, קטן.** לא תוקן - שינוי בקטע4 נוגע
ב-927 מופעים בשביל 2.

### החיווי בממשק

כל צומת שיש בו הערת צ״ל מקבל תג כתום בכותרת, עם ההערה ב-tooltip.
208 הערות ב-97 חוקים. **התג יושב בכותרת ולא בתוך הנוסח**: הנוסח
הוא `contentEditable` ונקרא על ידי צינור ה-diff, ועטיפת חלק ממנו
ב-`<span>` הייתה נכנסת להוראת התיקון.

`SCRIVENER_NOTE_RE` ב-`app.js`, בדיקה ב-`tests/unit/test_scrivener_notes.py`
(קוראת את הביטוי מהקובץ עצמו ומריצה ב-node - לא העתק שיכול להתיישן).

**ידוע ולא נתפס:** מופע אחד, `law-2001104`, כתוב
`{{ח:הערה|[ככל הנראה צ״ל 8/10,000]}}` - "ככל הנראה" מפריד בין הסוגר
ל-צ״ל. לא הורחב בשבילו: הסוגר הצמוד הוא מה שמונע תפיסה של אצ״ל,
זצ״ל ודצ״ל, ומופע יחיד אינו מצדיק לוותר עליו.

## היסטוריית הצעות - אחסון בדפדפן, ומה שחסר בו (21.9.2026)

הטיוטות נשמרות ב-`localStorage` של הדפדפן ולא בדאטהבייס, מפני
שאין עדיין הרשמה וטבלה משותפת בלי מזהה משתמש הייתה מערבבת בין
אנשים. כל טיוטה מחזיקה את `law_versions.id` שעליו היא נבנתה,
וכשפותחים אותה והמזהה השתנה - המשתמש מקבל אזהרה והעריכות שלו
נטענות כפי שהן, בלי דריסה.

**שלושה פערים ידועים, שכולם נסגרים עם המעבר לדאטהבייס:**

1. **הגנת הטיוטות של הריצה היומית אינה רואה אותן.**
   `tools/check_for_update.py` מדלג על חוק שיש עליו טיוטה פתוחה -
   והוא מחפש טבלת `drafts` בדאטהבייס. טיוטה בדפדפן אינה שם, ולכן
   החוק *כן* יתעדכן מתחתיה. **זה מטופל בצד השני:** הטיוטה זוכרת
   את מזהה הגרסה ומזהירה בפתיחה. זו הגנה בדיעבד ולא מניעה.

2. **הטיוטות קשורות לדפדפן, לא לאדם.** מעבר למחשב אחר, או ניקוי
   נתוני האתר, מאבד אותן. נאמר למשתמש במפורש בפאנל: "נשמרות
   בדפדפן הזה בלבד".

3. **אין שיתוף.** שני אנשים שעובדים על אותה הצעה אינם רואים זה
   את זה.

המבנה שנשמר (`{id, law_id, version_id, title, edits, insertions}`)
עובר לטבלה כמו שהוא כשתגיע ההרשמה - אין כאן מודל נתונים זמני
שיצטרך להיכתב מחדש.

## תבניות ויקי שעדיין מגיעות למסך - 647 צמתים (נמדד 21.9.2026)

"68 טבלאות ה-HTML" התברר כקצה של משהו גדול יותר. מדידה על כל
1,109 קובצי הוויקיטקסט, אחרי שני סבבי הרנדר:

| מקור | צמתים | חוקים | מה המשתמש רואה |
|---|---|---|---|
| `raw_block` - בלוק `<table>` גולמי | 320 | 102 | טבלה שלמה עם `{{ח:פנימי}}`, `{{ש}}`, `{{מוקטן}}` בתוכה |
| תבנית מבנה `ח:` בתוך טקסט | 317 | 8 | `{{ח:תת\|(א)}}` באמצע נוסח סעיף |
| תבנית עיצוב שנותרה | 10 | 3 | בעיקר `{{שבר}}`/`{{sfrac}}` |

המספרים מאומתים מול ה-DB הפעיל (320+317+10=647), כלומר זה בדיוק
מה שמוצג היום.

### א. raw_block - 320 צמתים, 102 חוקים

`<table>` נשמר verbatim כ-`raw_block` ואינו עובר ב-`_flatten`
בכלל, ולכן כל תבנית בתוכו שורדת. **זו הקבוצה הגדולה והנפוצה
ביותר**, ומופיעה בתוספות ובלוחות תעריפים - בדיוק המקומות שבהם
יושבים סכומים ומועדים.

**ההצעה:** להריץ `_flatten` על תוכן התאים בלבד, בלי לגעת בשלד
ה-HTML. התגיות (`<table>`, `<tr>`, `<td>`) נשארות כפי שהן -
הן מה שמחזיק את המבנה - ורק הטקסט שבין התגיות עובר רנדר.
**דורש הכרעה: זו נגיעה בפרסר.**

### ב. תבנית מבנה בתוך טקסט - 317 צמתים, 8 חוקים בלבד

`{{ח:תת}}` בתוך `.text` של צומת פירושו שהפרסר בלע שורה מבנית
כטקסט במקום לפתוח ילד. **מרוכז מאוד** - שמונה חוקים, ורובו
המכריע בפקודת הרוקחים ובחוק מס חברות מזערי, שניהם בעלי מבנה
טבלאי דו-לשוני חריג.

**זה שינוי מבני, לא רנדר**: תיקון כאן משנה את **צורת העץ**, ולכן
גם את מספרי הצמתים ואת העוגנים של שכבת העריכה. **דורש הכרעה
נפרדת, ואני ממליץ לא לגעת בו לפני שיש גולדן על אחד משמונת
החוקים.**

### ג. שבר מתמטי - 10 צמתים, 3 חוקים

`{{שבר|מונה|מכנה}}` ו-`{{sfrac}}`, בעיקר בחוק מס חברות מזערי
בקבוצה רב-לאומית. **לא נוספו לרנדר בכוונה:** הסרת המעטפת תהפוך
"a/b" ל-"a b" - שינוי משמעות, לא עיצוב. צריך החלטה איך מציגים
שבר בטקסט רץ ("a חלקי b"? "a/b"?).

## כלי המחקר: מה נדרש לסינון לפי כנסת (נמדד 22.9.2026)

שתיים משלוש התבניות אינן מחילות אף מגבלה. המגבלה הנפוצה ביותר
בשאלות אמיתיות היא **כנסת מסוימת**. מה שלמטה נמדד מול הפיד החי,
בקשה אחת לכל שאלה — לא הונח.

### הממצא שמשנה את התמונה: הפיד לא חוסם סינון

בקוד כתוב שהפיד חוסם `filter` ולכן אין חיתוך. **זה נכון רק
בחלקו, וההכללה הזו היא שהקפיאה את שלוש התבניות.** ה-WAF חוסם
`filter(...)` **בתוך `$apply`** (סטטוס 473). `$filter` רגיל
ברמה העליונה עובד — כולל על תכונה מורחבת:

| שאילתה שנבדקה | תוצאה |
|---|---|
| `KNS_BillInitiator?$filter=KNS_Bill/KnessetNum eq 25` | **200**, 16,832 שורות |
| `KNS_Bill?$filter=contains(Name,'חינוך') and KnessetNum eq 25` | **200**, 170 שורות |
| `KNS_Bill?$filter=contains(Name,'חינוך') and PublicationDate gt 2024-01-01Z` | **200** |
| `KNS_Bill?$filter=contains(Name,'חינוך') and SubTypeDesc eq 'פרטית'` | **200**, 1,200 שורות |
| `groupby((KnessetNum,StatusID,SubTypeDesc),aggregate(...))` | **200**, 508 שורות ב-4 שניות |

### מה זה אומר לכל תבנית

**`bills_on_topic` — כמעט חינם.** כנסת, טווח תאריכים וסוג הצעה
הם כולם שדות של `KNS_Bill`, והוספתם היא סעיף נוסף באותו
`$filter` הקיים. **אותה עלות רשת בדיוק** (בקשה אחת). זו ההרחבה
שכדאי לעשות ראשונה.

**`pass_rates` — זול, ומחזיר תשובה אחרת לגמרי.** הצבירה הקיימת
היא `groupby((KnessetNum,StatusID))`. הוספת `SubTypeDesc` כמימד
שלישי רצה ב-4 שניות והחזירה 508 שורות (5 עמודים מול 4 היום).
**המספר שזה מגלה:** בכנסת ה-25 הוגשו 6,839 הצעות פרטיות ומתוכן
עברו 199 — **2.9%**, לא 7.6% ששיעור-כל-ההצעות מראה. כלומר
השאלה "כמה הצעות חוק פרטיות באמת עוברות" קיבלה מספר גבוה פי
2.6 מהאמת.

**`top_initiators` — אפשרי, אבל יקר.** אין ב-`KNS_BillInitiator`
שדה כנסת כלל (השדות: `BillID, Id, IsInitiator, LastUpdatedDate,
Ordinal, PersonID`), ולכן נדרש `$filter=KNS_Bill/KnessetNum eq N`.
לכנסת 25 זה 16,832 שורות = **169 בקשות** (הפיד מחזיר 100 לעמוד).
מתחת לתקרת `_MAX_PAGES=400`, אבל יקר: התבנית הזו כבר היום היקרה
ביותר (11 בקשות).

**מה שלא עובד:** `groupby((PersonID,BillID))` על 169,310 רשומות
היוזמים = 1,694 עמודים, מעל התקרה. נוסה ולא הסתיים ב-300 שניות.

### ההמלצה, לכשיוחלט לבנות

1. **`bills_on_topic` קודם** — שלוש מגבלות, סעיף filter נוסף,
   אפס עלות רשת.
2. **`pass_rates` אחריה** — מימד `SubTypeDesc`, עמוד אחד נוסף.
3. **`top_initiators` אחרונה**, ורק עם מטמון **לכל כנסת בנפרד**
   (המטמון הקיים הוא שבוע — מתאים). בלי מטמון, שאלה אחת = 169
   בקשות מ-IP אחד של Vercel, וזה בדיוק מה שמזמין את ה-WAF.

**לא נבנה — תיעוד בלבד** (ברק, 22.9.2026).
