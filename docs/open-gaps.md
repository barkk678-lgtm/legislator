# חוב פתוח — כל פער ידוע שלא טופל

> **ביקורת שנייה, 23.9.2026 — מול `git log`, לא רק מול ה-DB.**
> הביקורת הראשונה באותו יום מדדה כל פריט מול הדאטהבייס, ולכן
> ראתה רק פערי **נתונים**. פער שנסגר ב**קוד** נשאר ברשימה כאילו
> הוא פתוח: ברק זיהה חמישה כאלה בקריאה אחת. עכשיו לכל פריט
> מחפשים **קומיט סוגר**, ופריט שיש לו — יוצא מהרשימה עם ההפניה.
>
> **למה זה קריטי:** ברק משתמש ברשימה הזו כדי להחליט מה הבא.
> רשימה שחציה סגור אינה רק לא מסודרת — היא מייצרת החלטות על
> מידע שגוי.
>
> **הכלל שנוסף ל-CLAUDE.md בעקבות זה:** סגירת task כוללת עדכון
> `open-gaps.md` **באותו קומיט**.

## מה נסגר — עם הקומיט הסוגר

| # | הפער כפי שהיה רשום | נסגר ב- | האימות עכשיו |
|---|---|---|---|
| — | 42.3% מהחוקים לא-amendable | `1156f13` | **0 מתוך 1,109** |
| 1 | `ח:קטע4` — כותרות מבנה נזרקות בשקט | `ce2b46a` (15.9) | `sub_siman` קיים בפרסר; **100 צמתים ב-11 חוקים** ב-DB |
| 13 | בדיקות 4 ו-7 שגויות על הצעות אמיתיות | `360d189` (23.9) | 25→**0** ו-22→**0** מתוך 23 מתקנות |
| 14 | מצב האיכות בהסתייגויות לא מחובר לממשק | task 32 | חשוף בממשק |
| 16 | בדיקות שמעולם לא רצו | `c7e38c0` (23.9) | 1, 4, 7, 12 רצות; ראו §16 למה שנשאר |
| 17 | הנוסח המשולב — "הפיצ'ר בעל הערך הגבוה שטרם נבנה" | `07149b6`, `f9cbc7b`, `c8aaecf`, `eedf721` (22.9) | הלשונית כלי עובד; ברק בדק בעצמו על 13948412 ו-13948380 |
| 24 | תקרת 50 הערכים בהסתייגויות | `30b4bf2` (18.9) | התקרה **הוסרה** לפי הכרעת ברק; `measure()` כבר לא מחזיר "כמה אפשר" |
| 25 | `אחר=` — ארגומנט בעל שם שדולף למספר הסעיף | `18383e8` (19.9) | **0 צמתים** עם `=` במספר (היו 368) |
| 27 | `resolved_amendments` עם `SECURITY DEFINER` + 3 פונקציות בלי `search_path` | — | שתי האזהרות **אינן מופיעות יותר** ב-advisors. ראו §27 למה שכן מופיע |
| 28 | `{{דוכיווני}}` בפקודת רופאי השיניים | `434736e` + `49acb3e` | `_BILINGUAL` ב-`render_templates`, מחובר, מכוסה בבדיקה |
| 4 | **96 סעיפים נעלמים בגלל מספר כפול** | 23.9.2026 | `find_sections` מחזירה את הנוסח שבתוקף; `amend` חוסמת רק דו-משמעות אמיתית. ראו §4 |
| 4א | לוחות שתויגו כמרחב `law` (89 סעיפים, 13 חוקים) | 23.9.2026 | שורה אחת ב-`wikitext_parser`; אומת בפרסור חי |
| ח8 | **כל הצעת חוק שהורדה נפתחה בוורד עם "תוכן שאינו ניתן לקריאה"** (לא היה רשום - ברק מצא) | 25.9.2026 | שלושה פגמים ב-`render_bill`: הערת continuationNotice (id=1) נמחקה והגדרות וורד הפנו אליה; טבלה בלי שורות; שורת סעיף פנימי עם 8 עמודות מול רשת של 7. `docx_check` תופס את שלושתם, ו-0 ממצאים על 54 קובצי וורד אמיתיים. בבדיקה החיה **26.9: ברק פתח קבצים ב-Word - האזהרה נעלמה. סגור סופית.** |
| ת2 | **מומחה התקנון הציג `[מקור:law-tkanon-haknesset/52]` בתוך התשובה** - רגרסיה מול task 94 | 25.9.2026 | task 94 תרגם רק את רשימת המקורות שמתחת לתשובה; התגים בגוף הטקסט לא תורגמו באף מסלול. עכשיו "(תקנון הכנסת, סעיף 52)" בזרם ובטקסט הסופי, כולל תג שנחתך בין קטעים. אומת מקצה לקצה: 0 תגים, 14 אזכורים בעברית. בבדיקה החיה (`--with-llm`) |
| ח3 | **"אי אפשר למחוק סעיף" - "מעדכן..." נתקע לנצח** (לא היה רשום) | 25.9.2026 | שני פגמים. (1) `refreshPreview` בלי טיפול בשגיאה: 500, 504 של Vercel, ניתוק - כל אחד השאיר את החיווי לנצח בלי הודעה (שוחזר בשלושתם); עכשיו תקרת זמן והודעה בעברית. (2) מחיקת כל הטקסט של סעיף נעלמה מההצעה בשקט, וכותרת שוליים ריקה הפיקה `יבוא ""`; עכשיו "סעיף N – בטל" (§7.13). ביטול סעיף **קטן**/פסקה - עדיין לא, ראו ח4 |
| ח2 | **הוספת סעיף "לא נשמרת"** בחוק בתי המשפט (עבירות שענשן מוות), אחרי שני שינויים (לא היה רשום) | 25.9.2026 | רענון ההצעה שחזר כשתפריט ההוספה פתוח בנה את העץ מחדש **ומחק את התפריט** עם מה שהוקלד. השערת הסעיפים שנמחקו נשללה: 16/16 הוספות הצליחו בחוק הזה כשאין מרוץ. עכשיו התפריט עובר לעץ החדש כמות שהוא. ובנוסף: "סעיף ראשי" בלי כותרת שוליים - הודעה, לא פוקוס שקט |
| ח6 | **"חוק יסוד: הכנסת" - אפס תוצאות** בחיפוש החוקים (לא היה רשום) | 25.9.2026 | הסיבה: הסרת אמות הקריאה מחקה את הי' של "יסוד" שאחרי מקף ("חק-סד" מול "חק יסד"). מקף בין אותיות = רווח, לפני ההסרה; נקודתיים/פסיק = מפריד; שכבה אחרונה בלי רווחים ובלי אותיות סופיות ("בינלאומית" -> "בין-לאומית"). 59 שמות מקופים בקורפוס: **36 -> 59** נמצאים כשמקלידים ברווח. בבדיקה החיה |
| ת4+ת5 | **תשובה חמה למחמאה הוזרמה ואז נמחקה**; קללה קיבלה "לא מצאתי תשובה" (לא היה רשום) | 25.9.2026 | `packages/llm/chat_layer.py` - סיווג **לפני** התשובה, משותף לתקנון, שאילתות והצעות לסדר. חולין: תשובה חמה בלי שומר; עלבון: "כאן זו לא מליאת הכנסת – נא להתבטא בכבוד 😉" + 3 וריאציות; כל ספק -> עניינית. לפני: 5/5 הודעות חולין/עלבון הוזרמו ונמחקו. אחרי: 0. על 12 שאלות ענייניות: 0 הוחלפו |
| צ1-צ3 | גלילה שרודפת אחרי הזרם; אין אייקון העתקה; Enter בלבד (לא היו רשומים) | 25.9.2026 | שכבה משותפת בלקוח (appendMsg / followStream / bindComposer) לכל חלונות הצ'אט. תשובה ארוכה נעצרת על ראשה, ממשיכים לעקוב רק אם המשתמש גלל למטה; העתקה sticky בפינה הימנית, וורד בשמאלית; Shift+Enter יורד שורה. tests/browser/test_chat_kit.py 7/7 |
| ש2 | **"4 מתוך 11 מקורות נבדקו"** - קופסה צהובה בשאילתות קודמות (לא היה רשום) | 25.9.2026 | הסיבה **אינה מגבלת קצב**: ה-WAF של הכנסת חוסם כל סינון עם 4 contains() ומעלה, תמיד. יחידה של 4+ מילים נשלחת עכשיו עם 3, והרביעית נבדקת מקומית. + מטמון לפי צירוף (שרת + דפדפן, יום), ניסיונות חוזרים עם פיזור, תור אחד של 4. **87/95 -> 93/93** על עשרה חיפושים. בלי קופסה צהובה; מה שנשאר - "חלק מהתוצאות לא נטענו — נסה שוב". docs/measurements/past-queries-2026-09-25 **תיקון (ש4, אותו לילה): המדידה רצה בלי סינון הכנסת, ולכן החמיצה את עיקר הבעיה.** הכלל האמיתי הוא 4 סוגריים ומעלה, והסינון של ברירת המחדל הוסיף שניים - כל צירוף של 2-3 מילים נחסם באתר. תוקן ב-ש4. |
| ש1 | **קובץ הוורד של השאילתה לא נראה כמו שאילתה של הכנסת** | 25.9.2026 | נבנה על שלד של שאילתה אמיתית (reference/query-template.docx, tools/build_query_template.py): סמל המדינה, "הכנסת", heading 1-3, מספור וורד, David. מול הדוגמה ברמת ה-XML: הקובץ הקודם שונה ב-7 מתוך 7 השורות, החדש זהה ב-7. שורת "חבר/חברת הכנסת X שאל/ה את Y" חזרה (בכל שש הדוגמאות); המגדר מ-KNS_Person, ובלעדיו הצורה הכפולה. לא נכנסים: מספר, תאריך, מועד לתשובה. test_query_docx_knesset **26.9 (ברק): נכון להשמיט מספר, תאריך ומועד תשובה.** |
| ש3 | **אייקון הוורד בשאילתות - לפעמים חסר, ולא בפינה השמאלית העליונה** | 25.9.2026 | חסר: שיחה שנפתחה מ"שיחות קודמות" הציגה את הטיוטה כטקסט גולמי ("נושא: ... גוף: ...") בלי אייקון. עכשיו הטיוטה נשמרת עם התור ונפתחת באותה פונקציה; שיחה שנשמרה קודם - מזוהה לפי "נושא:/גוף:". מיקום: האייקון נוסף בסוף הבועה - משמאל, אבל 231px מתחת לראשה. tests/browser/test_query_word_icon.py: 7/7; על הקוד הקודם 5 נכשלות |
| ש4 | **שאילתות קודמות: תוצאות מתחום אחר** (משטרה -> בריאות), ואפס ל"מחסור בשוטרים" | 25.9.2026 | שלוש סיבות: (1) ה-WAF חוסם 4 סוגריים ומעלה - סינון הכנסת העטוף חסם כל יחידה של 2-3 מילים; (2) 474 = חסימת IP, מפסק ו"לא זמין כרגע" במקום "לא נמצא"; (3) אין תחום - עכשיו עד 3 היבטים (תופעה, גוף, מקום), התאמה בתחילת מילה. **33% -> 93%** בנושא, 8 נושאים באתר החי. docs/measurements/past-queries-2026-09-25 |
| 132 | **הצעות דומות: "לא הצלחתי לבדוק את היוזמים" בכל התוצאות** (לא היה רשום - נמצא בבדיקה שברק ביקש, 26.9) | 26.9.2026 | ברק ביקש לבדוק שמלכודת חומת האש (4 סוגריים ומעלה, ש2) לא קיימת בהצעות הדומות ובפרסומי החוק. **הסוגריים - לא:** כל סינון בשניהם עם סוגר אחד לכל היותר; נבדק גם באתר החי (כותרת עם סוגריים מקוננים, חוק עם 62 קישורים). **אבל מלכודת אחות - כן:** הפיד דוחה שרשרת `or` ארוכה (25 נמדדו בפרסומי החוק; כאן נדחו גם עד 24 שמות), ושמות היוזמים נשלפו במנות של 40 `Id eq X or ...`. עשר הצעות פרטיות עם הרבה חותמים עוברות את זה - וכל עשר התוצאות הוצגו בלי יוזמים. שוחזר באתר החי ("הגנת הצרכן (תיקון – עסקה ברוכלות)": 10/10, שלוש פעמים). עכשיו `in (...)` (כמו בפרסומי החוק, 200 מזהים עוברים), מנות של 150. **ואותה מלכודת בשאילתות קודמות:** שמות המגישים נשלפו במנות של 30 `or` - באתר, 18 מגישים קיבלו את כל השמות ו-30 קיבלו אפס. תוקן באותה דרך. tests/unit/test_knesset_filter_limits.py - פיד מזויף שאוכף את שתי המגבלות; נכשל על הקוד הקודם |
| ש5 | **השיחות הקודמות בשאילתות - כרטיס שלישי מתחת לחלוניות, תופס הרבה מקום** | 25.9.2026 | עכשיו כמו במוקאפ: כפתור "היסטוריה" עם מספר השיחות ליד "צור שאילתה חדשה", רשימה צפה עם שדה חיפוש (בשם ובתוכן, כל מילה בנפרד), קיבוץ לפי זמן (היום / השבוע / החודש / קודם), שם ותאריך קצר (24.9), חמש האחרונות ו"כל N השיחות ←"; שם השיחה הפתוחה ליד "ניסוח השאילתה". כשל שמירה או קריאה - הכפתור בכתום והודעה בתוך הרשימה (לא "אין שיחות"). נסגרת ב-Escape ובלחיצה בחוץ. tests/browser/test_qconv_history.py: 25/25, נכשל על הקוד הקודם |
| ש6 | **"נושא", "גוף", "רצוני לשאול" לא מודגשים בצ'אט השאילתות** | 25.9.2026 | תיבת הטיוטה הציגה רק את הגוף - הנושא לא הופיע בכלל. עכשיו "**נושא:** ...", "**גוף:**" ו"**רצוני לשאול:**" בבולד; קובץ הוורד נבנה מהנתונים ולא מה-HTML, ולא השתנה. test_query_word_icon (נכשל על הקוד הקודם) |
| ש7 | **סינון לפי כנסת לא עדכן את התוצאות** | 25.9.2026 | לחיצה על כנסת שינתה רק את הכפתור. עכשיו: הסרה - השורות שלה יורדות מיד, בלי בקשה; הוספה - החיפוש רץ רק על הכנסת החדשה, עם הפירוק השמור (בלי קריאה למודל), ומה שנמצא נוסף למטה בלי כפילות; "הכול" - מוסיף את מה שחסר. tests/browser/test_pq_knesset_toggle.py: 10/10, על הקוד הקודם 5 נכשלות |
| ת1 | **`**` ו-`#` בתשובות מומחה התקנון** | 25.9.2026 | שני מקורות: המודל כתב Markdown (נמדד: "# ההבדל בין...", "## הצעה לסדר היום", 7 זוגות `**`), והממשק הציג textContent. הנחיה: בלי כותרות, הדגשה במשורה (אחרי: 0 כותרות, 0-2 הדגשות). ממשק: rulesAnswerHtml - מבריח HTML, `**x**` -> בולד, `#` נמחק, תבליטים. tests/browser/test_rules_markup.py: 6/6, על הקוד הקודם 4 נכשלות |
| ת3 | **מומחה התקנון - הצ'אט לבד, בלי המקורות לצדו** | 25.9.2026 | מסך מפוצל: הצ'אט מימין, המקורות משמאל (תקנון הכנסת, חוק הכנסת, חוק-יסוד: הכנסת - לשונית לכל אחד), עם כותרות החלקים והפרקים, כותרות השוליים ותוויות היחידות. מעל המקור - תגית לכל סעיף שאוזכר בתשובות, בסגנון תגיות הכנסות בשאילתות. לחיצה על תגית או על אזכור בתוך התשובה ("תקנון הכנסת, סעיף 52") פותחת את המקור הנכון, גוללת בתוך החלונית לסעיף ומסמנת אותו. מזהה כל סעיף בחלונית = מזהה המקור ששומר הציטוט בדק (find_sections), ולכן האזכור מגיע בדיוק לסעיף שנבדק; אזכור של סעיף שאינו קיים נשאר טקסט. השרת: GET /api/rules/reading (78KB בדחיסה, טעינה ראשונה ~4 שניות, אחר כך מהזיכרון). **26.9 (הכרעת ברק): השורה "מקורות: ..." מתחת לתשובה הורדה** - חזרה על התגיות והקישורים. tests/browser/test_rules_split.py: 13/13; tests/unit/test_rules_expert.py - test_reading_items_ids_match_cited_source_ids |
| ח10 | **"שים לב: 1 הוספות לא בוצעו (ראו כרטיסי השגיאה בעץ)" - לא מובן** | 25.9.2026 | עכשיו שורה לכל כשל, בגוף ראשון: "לא הצלחתי לקלוט את התיקון שביקשת לעשות בסעיף 5", "...בכותרת השוליים של סעיף 2", "לא הצלחתי לקלוט את הוספת סעיף 6א כפי שביקשת", "...הוספת סעיף קטן (ג) לסעיף 4". גם עריכות שלא נקלטו - קודם לא נאמרו בשורה הזו בכלל. tests/browser/test_failure_lines.py |
| ח5 | **"מעדכן..." בחלונית הצעת החוק לא בולט** | 25.9.2026 | היה 12px באפור בהיר, בלי רקע. עכשיו פס מודגש (14px, בולד, רקע וגבול כחולים) עם ספינר, דביק לראש החלונית, והתוכן מעומעם עד שהעדכון חוזר. tests/browser/test_preview_pending_visible.py: 7/7, על הקוד הקודם 5 נכשלות; test_preview_failure עדיין 3/3 |
| ח9 | **דברי ההסבר חוזרים על הטבלה** ("מוצע לתקן את סעיף 5.") | 25.9.2026 | הקובץ מעולם לא קרא למודל - הדטרמיניסטי מילא תמיד. עכשיו /docx מנסח עם המודל: שם החוק, כותרת השוליים, נוסח מלא לפני/אחרי, הוראת התיקון; מהות השינוי בדין; שומר דטרמיניסטי נגד נימוק מומצא. שלוש דוגמאות: docs/measurements/explanatory-2026-09-25. ההורדה איטית ב-~3 שניות **26.9: ברק אישר - עובד.** |
| ח17+צ5 | **הורדת Word איטית - הצעת חוק "כדקה", גם שאילתה** (26.9) | 26.9.2026 | נמדד באתר החי, חמש העריכות של ברק בחוק-יסוד: הכנסת: **11.2-11.5 שניות**, מתוכן render 0.5 וכתיבת הקובץ 0.01 - השאר הוא המודל של דברי ההסבר (11-14 שניות, וכפול כשהשומר מבקש ניסוח מחדש). בשאילתה: mk_gender פנה למאגר הכנסת בכל הורדה, עד 20 שניות לעמוד, וכישלון לא נשמר. **עכשיו שום דבר חיצוני לא רץ בלחיצה:** דברי ההסבר נכתבים ברקע 2.5 שניות אחרי שהעריכה נרגעת (/explanatory), נשמרים לפי המצב המדויק (גם בטיוטה) ונשלחים עם ההורדה; אם הניסוח למצב הזה עוד רץ - ממתינים רק ליתרה ("מכין את דברי ההסבר…"). המגדר נשלף כשממלאים את שם חבר/ת הכנסת, וכישלון נשמר 10 דקות. tests/unit/test_download_no_external.py, tests/browser/test_download_fast.py (9/9; על הקוד הקודם 7 נכשלות); בבדיקה החיה תקרה - 6 שניות להצעת חוק, 3 לשאילתה |
| ח13 | **תיקון בסעיף קטן מפנה רק לסעיף הראשי** (קובץ שהמערכת ייצאה לברק, law-2000037) | 26.9.2026 | שוחזר מהקובץ: שתי עריכות בסעיף 6 (בסעיף קטן (ג) ובסעיף קטן (ד)) יצאו "בסעיף 6 לחוק העיקרי, במקום..." פעמיים - בלי היחידה, בלי מספור. **עריכה יחידה בסעיף קטן כבר הייתה נכונה** ("בסעיף 6(ג), במקום..."); הבאג היה בענף של כמה הוראות באותו סעיף, ובכל סוגי העריכה בו: החלפה, מחיקה, ביטול יחידה, כותרת שוליים והוספה (אחרי / בסוף). עכשיו לפי §7.10.8: "בסעיף 6 לחוק העיקרי –" ואחריו "(1) בסעיף קטן (ג), במקום...;" "(2) בסעיף קטן (ד), אחרי...". פסקה בתוך סעיף קטן - "בסעיף קטן (א)(8), ..."; נוסח לפני הסעיפים הקטנים - "ברישה, ...". **ובדרך:** פריטי "אחרי ... יבוא:" יצאו בלי מספר בכלל בקובץ (בזהב זה מספור אוטומטי של Word) - עכשיו ממוספרים. tests/unit/test_multi_item_locator.py (7/7, נכשל על הקוד הקודם); בבדיקה החיה |
| ח16 | **"מספר פנימי: ????????" בראש כל קובץ Word של הצעת חוק** | 26.9.2026 | **לא שארית תבנית ויקיטקסט** (החשד בתוכנית): שורת הכותרת של render_bill, שמעתיקה את שלד ההצעה האמיתית ("מספר פנימי: 2233987"). את המספר הכנסת נותנת; עכשיו השורה מודפסת רק כשיש מספר אמיתי. tests/unit/test_no_placeholders_in_docx.py (נכשל על הקוד הקודם); בבדיקה החיה. **הסריקה שביקשת - שאריות אחרות כן קיימות, בפרסר (ממתין להכרעה - wikitext_parser):** `{{ח:ת}}` / `{{ח:תת|(א)}}` - 316 צמתים ב-7 חוקים (פקודת הרוקחים 218, חוק הסייבר 45, חוק המזון 44, ועוד 4); `[[Image:...]]` וסוגריים שבורים - 21 צמתים ב-19 חוקים; תגי HTML (`<wbr>`, `<s>`, `<span>`) - 728 צמתים ב-99 חוקים (פקודת הסמים 126, חוק המזון 62, חוק שירות ביטחון 46...). הם מגיעים לקובץ כשעורכים צומת כזה. "פנימי" כשארית - אפס (673 המופעים הם מילים אמיתיות) **26.9, ח16-ב (ברק: לתקן את הפרסר) - תוקן ונטען.** נמדד: 392 חוקים (1,912 צמתים) - מעל הסף, ולכן טעינה מלאה. **(1) תבניות עומק באמצע טקסט** (316 צמתים, 7 חוקים): "{{ח:תת|(1)}}" -> "(1)", "{{ח:ת}}" -> כלום. **(2) תמונות** (16 צמתים, 14 חוקים): "[[Image:Uniform Organic Logo.jpg|150px|link=]]" -> "[תמונה: Uniform Organic Logo]". סוגריים שבורים (5 צמתים) - שגיאה במקור עצמו ("[[סעיפים 10 עד 12],"), מדיה-ויקי מציגה אותה כך - לא נגענו. **(3) תגי HTML** (1,590 צמתים, 380 חוקים) - רובם `<wbr>` ב-908 כותרות שוליים ב-339 חוקים ("אי-<wbr>תלות"); `<sup>`/`<sub>` -> תווי Unicode ("1¹⁄₄", "NH₃"); `<span>`/`<div>`/`<i>`/`<u>` - התג יורד, התוכן נשאר; **`<s>` הוא נוסח שאינו בתוקף** (מילה כפולה מחוקה, "(פורק)", סעיף שבוטל בבג"ץ, סוגריים מחוקים) - התוכן יורד. רק תגים מוכרים: "55 מ"מ < LVEDD" נשאר. נשארו במכוון: `<math>` וטבלה בתוך טקסט - 62 צמתים ב-3 חוקים. **אחרי הטעינה:** 1,109 חוקים, 266,391 צמתים ו-1,109 חוקים ניתנים לעריכה - זהה לפני; 0 תבניות, 0 תמונות, 0 תגים מהסוג שטופל; 409 שורות ביומן ההחלפות, אף אחת בלי גרסה או עם מספר צמתים שונה. tests/unit/test_inline_markup.py (על הקוד הקודם נכשל, כולל יציבות מזהים); tools/reload_corpus.py; בבדיקה החיה |
| ח18+ח19 | **הודעת כשל לא יורדת אחרי שהשינוי נקלט; הודעה בלי מיקום מדויק ובלי סיבה** | 26.9.2026 | ח18 - לא שוחזר כהודעה "תקועה" בארבעה מסלולים; שני דברים כן: (1) **רענון בזמן שהפוקוס בשדה דילג על הסימון של כל השדות** - שדה שנכשל נשאר אדום גם אחרי שהעריכה בו נקלטה, כל עוד המשתמש עבד בשדה אחר; עכשיו השדות האחרים מסומנים בכל רענון, והשדה שבפוקוס מאבד את האדום כשהוא נקלט. (2) המיקום "בסעיף 6" לכשל בסעיף קטן גרם לכשל חדש להיראות כהודעה ישנה. ח19 - המיקום עכשיו מלא: "בסעיף 2(א)(1)", "בסעיף 1, בהגדרה \"X\"", והסיבה במילים של משתמש כשהיא ידועה (ביטוי שמופיע יותר מפעם אחת; שינוי שחיבר/פיצל חלק ממילה; כותרת שוליים לסעיף משנה; יחידה שכבר לא קיימת); סיבה לא ידועה - רק המיקום, בלי מונח פנימי. **ובדרך:** החלפת סדר של שתי מילים ("אדם קייטנה" -> "קייטנה אדם") **הפילה את השרת** (500, "השרת לא הצליח לעדכן") - עכשיו עריכה שלא נקלטה, ושאר העריכות ממשיכות. tests/browser/test_error_messages.py (8/8; על הקוד הקודם 6 נכשלות), tests/unit/test_word_swap_status.py (נכשל על הקוד הקודם); בבדיקה החיה |
| ח12+ח15+ח11 | **פלוס ופח פעמיים; פח על רישה מוחק רק חלק; התפריט מציע רמות לא תקינות** | 26.9.2026 | ח12 - שוחזר: יחידה בלי טקסט משלה (סעיף 2 בחוק הקייטנות - כל תוכנו בפסקה בלי מספר; סעיף 6 בחוק-יסוד: הכנסת - כולו בסעיפים קטנים) קיבלה שורה ריקה עם זוג, ואחריה הרישה עם זוג משלה. עכשיו זוג אחד לכל יחידה: הרישה נושאת את הזוג של היחידה, ויחידה בלי רישה - בכותרת שלה. ח15 - הפח על הרישה פועל על היחידה כולה: בסעיף 7 בחוק-יסוד: הכנסת ("אלה לא יהיו מועמדים לכנסת:", (1)-(10) וסיפא) כל השדות נמחקים ונראים מחוקים, וההוראה "סעיף 7 – בטל" (עד כאן נמחקו הרישה והפסקאות שבתוכה, והסיפא נשארה). ח11 - אותה רמה תמיד; רמה נמוכה רק כשאין ליחידה עדיין ילדים ברמה הזו ("(א) הופך ל-(א)(1)"); רמה גבוהה רק מהתוכן האחרון של היחידה - בין 6(א) ל-6(ג) אין "סעיף ראשי" (השרת היה ממקם אותו אחרי סעיף 6 כולו). אפשרות שהשרת לא תומך בה - לא מוצגת (עד כאן "- לא נתמך"). ובדרך: בקשת בדיקה שנכשלה ברשת השאירה "בודק..." לנצח - עכשיו הודעה. **26.9, ח11-ב (ברק: לבנות; הנוסח והתקדים אושרו - סגור):** "(א) הופך ל-(א)(1)" נתמך עכשיו גם מסעיף קטן בלי פסקאות - בתפריט "הוסף פסקה (יהיה (2))". הנוסח הקיים של הסעיף הקטן עובר לפסקה (1) בלי שינוי (transform.AddFirstParagraph), ובהצעה: `בסעיף 6(א) לחוק העיקרי, האמור בו יסומן "(1)" ואחריו יבוא: "(2) ...".`; בתוך הוראה של כמה פריטים - `(1) בסעיף קטן (א), האמור בו יסומן "(1)" ואחריו יבוא:`. **המקור:** בחוברת הסגולה הדפוס קיים רק ברמת הסעיף (§7.10.6(ד), עמ' 29-30); ברמת הסעיף הקטן - תקדים מרשומות, הצעת חוק הממשלה 1924, עמ' 676 (reference/reshumot-1924-government-bills.pdf, עמ' PDF 71), פריט (8): `בסעיף 34(א), האמור בו יסומן "(1)" ואחריו יבוא: "(2) על אף האמור בפסקה (1), ..."`. ב-40 ההצעות אין מופע ברמה הזו. נוסח שעבר ל-(1) **וגם** השתנה - לא מנוסח כ"יסומן" (החוברת מפצלת לשתי הוראות) אלא נדחה בגלוי. גם הנוסח המשולב קורא את ההוראה ומחיל אותה. **ובדרך - באג קיים בנוסח המשולב:** ביחידה חדשה שנוספה ב"האמור בו יסומן" נשאר `".` בסוף הנוסח (גם ברמת הסעיף) - תוקן. tests/golden/test_first_paragraph_in_subsection.py (11/11; על הקוד הקודם נכשל), tests/unit/test_first_paragraph_multi_item.py, tests/unit/test_parse_instructions.py (נכשל על הקוד הקודם), tests/browser/test_first_paragraph.py (6/6); test_insert_preview עודכן (נעל את המגבלה הישנה); בבדיקה החיה. tests/browser/test_unit_actions.py (13/13; על הקוד הקודם נכשל); שלוש בדיקות קיימות עודכנו לאתר את הכפתור לפי היחידה ולא לפי השורה הריקה שהוסרה; בבדיקה החיה |
| צ6 | **כפתור ההעתקה בצ'אט מעתיק את כל ההודעה, לא רק את הנוסח** | 26.9.2026 | בהודעה עם נוסח (שאילתה, הצעה לסדר) - רק מה שבתוך חלון הנוסח: בלי "ניסחתי טיוטה לפי הפורמט המקובל" / "הנה נוסח מוצע" ובלי מונה המילים. בהודעה בלי נוסח - ההודעה. **ובדרך:** כל ההעתקות עד כאן איבדו את שבירות השורה ("הצעה לסדר היוםהנושא: ...") - הטקסט נקרא מעותק מנותק של ההודעה, ששם innerText מתנהג כמו textContent. עכשיו נקרא מההודעה שבדף. tests/browser/test_copy_draft.py (8/8, קורא את הלוח; על הקוד הקודם 5 נכשלות); בבדיקה החיה |
| צ4 | **עברית מביכה בתשובות החולין ("מה כולך חושב?")** | 26.9.2026 | המקור: תשובת החולין של השכבה המשותפת (packages/llm/chat_layer.py) נכתבה במודל הקטן (claude-haiku-4-5). שוחזר ב-30 דגימות (10 הודעות × 3 צ'אטבוטים, tools/chitchat_sample.py): **13 מתוך 30** עם שגיאה או צירוף מביך ("בעצם בחרתי!", "אני כאן וחדשפני", "אתה גם בהנאה", "טוב לשמוע מך", "בפרלמנט"). עכשיו התשובה במודל הראשי (claude-sonnet-5) והנחיה לעברית תקנית ו"הכנסת": **2 מתוך 30** ("זרוק אותי", "מנוסח פתיחה"). הסיווג נשאר במודל הקטן (30/30 נכון). מחיר: 2.1-3.0 שניות לתשובה במקום 1.1-1.8. כל התשובות, לפני ואחרי: docs/chitchat-samples-26.9.md. tests/unit/test_chat_layer.py (נכשל על הקוד הקודם); בבדיקה החיה |
| ש8-ש11+ש14 | **תצוגת השאילתה בצ'אט ומגבלת המילים** | 26.9.2026 | ש8 - השאלות ממוספרות גם בצ'אט (1., 2.), כמו המספור האוטומטי בקובץ; מספר שהמודל כתב בעצמו מוסר. ש9 - בלי התווית "גוף", ושורה ריקה בין הנושא לגוף. ש10 - שינוי סוג כשיש כבר טיוטה שולח "נסח לי אותה כשאילתה <סוג>" כאילו המשתמש כתב, עם הסוג החדש; לפני טיוטה ראשונה - לא נשלח כלום. ש11 - **נבדק באתר החי: הספירה כבר הייתה של הגוף בלבד** (40 מילים בגוף; 5 מילות הנושא לא נספרו). חסר היה שההנחיה למודל תאמר במפורש שהכותרת והנושא לא נספרים, ושמספור שהמודל מוסיף לא ייספר - תוקן; והמונה בממשק אומר "(בלי הכותרת והנושא)". "רצוני לשאול:" נספר, כמו קודם (ברק לא ציין אותו). ש14 - "חורג מהמגבלה" בלי "!", ולידו "התכנס למגבלת המילים": נשלח כהודעה בצ'אט, השרת סופר בקוד אחרי הניסוח, ואם עדיין חורג - ניסיון נוסף אחד עם הספירה בפועל; אם גם הוא חורג - הודעה גלויה עם המספרים. **נמדד באתר החי:** עם "נסח עד 40 מילים" המודל החזיר 44 בשני הניסיונות - הוא לא סופר מילים בעברית באמינות. הניסיון הנוסף מבקש עכשיו מספר מילים להוריד ויעד נמוך מהמגבלה, ומתיר לוותר על השאלה האחרונה: 2 מתוך 3 הרצות נכנסו (34, 35), השלישית 43/40 - והממשק אומר זאת. tests/browser/test_query_draft_ui.py (11/11; על הקוד הקודם נכשל), tests/unit/test_query_word_limit.py (6/6; על הקוד הקודם נכשל); test_query_word_icon עודכן ל-ש9; בבדיקה החיה **26.9, ש14-ב (ברק: בלי ניסיון שלישי, לכוון מתחת למגבלה):** ההנחיה אומרת "כוון ל-35 מילים לכל היותר" (44 בשאילתה רגילה) ונותנת תקציב לכל חלק - רקע במשפט אחד עד 14 מילים (15 ברגילה), 2 שאלות (3 ברגילה) של עד 8 מילים. יעד כולל לבד לא עזר: 1 מתוך 10 (41-52 מילים) - המודל כתב רקע של ~18 מילים ושלוש שאלות. **נמדד על עשר טיוטות, ניסוח ראשון בלבד (tools/query_limit_sample.py):** לפני - 2 מתוך 10 נכנסו (38-48/40). **אחרי, באתר החי: דחופה 8 מתוך 10** (32-39; שתי החריגות - 41 ו-42), **רגילה 10 מתוך 10** (33-42/50); מקומית, אותו קוד: 10/10 ו-10/10. הממשק אומר את הספירה האמיתית, ו"התכנס למגבלת המילים" (ניסיון נוסף אחד) נשאר לחריגות. הספירה בממשק היא תמיד של הקוד (body_word_count), לא של המודל. tests/unit/test_query_word_limit.py (9/9; על הקוד הקודם נכשל) |
| ש12+ש13 | **החזרת כנסת שהוסרה מריצה חיפוש מחדש; "?" ליד הכנסת הוותיקה** | 26.9.2026 | ש12 - הסרת כנסת מחקה את השורות שלה, ולכן ההחזרה הריצה את החיפוש שוב. עכשיו ההסרה מסתירה, ההחזרה מציגה בלי שום בקשה, וחיפוש רץ רק על כנסת שעוד לא חופשה; שורת הספירה סופרת את הגלויות. ש13 - **ממצא: במאגר הכנסת אין שאילתות גם בכנסת ה-18**, לא רק לפניה. נמדד באתר החי על שבע מילים נפוצות: הכנסת ה-17 וה-18 - 0; ה-19 - 92; ה-20 - 221. לכן, בשונה מהנוסח בתוכנית ("משמאל לכנסת ה-18"): כפתור 18 ירד (תמיד החזיר כלום), וה-"?" משמאל לכנסת ה-19, עם הטולטיפ "אין חיפוש בכנסות שלפני הכנסת ה-19: השאילתות שלהן אינן קיימות במאגר המידע של הכנסת". tests/browser/test_pq_knesset_toggle.py הורחב (15/15; על הקוד הקודם 3 נכשלות); בבדיקה החיה |
| ת8+ת9 | **קו מקווקו מתחת לאזכורים; שם המקור חוזר בכל אזכור רצוף** | 26.9.2026 | ת8 - בלי קו מתחת לאזכור (קו מלא רק במעבר עכבר/פוקוס, כדי שיהיה ברור שזה לחיץ). ת9 - השרת מקבץ תגים רצופים מאותו מקור (rules_expert.group_citation_labels): "(תקנון הכנסת סעיף 56, סעיף 57, סעיף 59 וסעיף 60; חוק הכנסת, סעיף 12)" במקום "תקנון הכנסת, סעיף 56; תקנון הכנסת, סעיף 57; ...". אזכור יחיד - כמו שהיה ("תקנון הכנסת, סעיף 52"). בממשק כל "סעיף N" קישור נפרד לסעיף שלו במסך השמאלי. שומר הציטוט לא השתנה - הוא בודק את התגים, לא את התצוגה. tests/unit/test_rules_expert.py (נכשל על הקוד הקודם), tests/browser/test_rules_cite_group.py (7/7; על הקוד הקודם נכשל); בבדיקה החיה |
| ת6 | **מקור פרשני שברק מזין** | 26.9.2026 | reference/takanon/interpretations.md - רק ברק מוסיף רשומות. כל רשומה: מזהה הסעיף, יחידה (לא חובה), תאריך הוספה, נוסח. הרשומה נטענת **בתוך** המקור של הסעיף (rules_expert._load_sources), ולכן המודל מאזכר את הסעיף עצמו ("תקנון הכנסת, סעיף 49") ושומר הציטוט בודק מול סעיף אמיתי; ההנחיה למודל אומרת שזו פרשנות שלפיה נוהגים. במסך הקריאה - מתחת לסעיף, בתיבה "פרשנות שלפיה נוהגים בכנסת". **שתי הרשומות הראשונות (הכרעת ברק, 26.9 - כל אחת בסעיף שלה):** 48(א) - "בשאילתה רגילה, הכותרת והנושא אינם נספרים במניין המילים."; 49(א) - אותו דבר לשאילתה דחופה. tests/unit/test_interpretations.py (3/3; נכשלת אם רשומה מפנה לסעיף שאינו קיים); בבדיקה החיה (--with-llm) - "האם הכותרת נספרת במגבלת המילים בשאילתה דחופה?" נענית "לא" עם הפניה ל-49 |
| ת7+ס2 | **היסטוריית שיחות במומחה התקנון ובהצעה לסדר - רכיב אחד משותף** | 26.9.2026 | ההיסטוריה של השאילתות (ב4, ש5) הפכה לרכיב אחד, mountConvHistory, ושלושת הצ'אטים מתחברים אליו: שאילתות, מומחה התקנון והצעה לסדר. כל צ'אט אומר רק מה נשמר בתור ואיך משחזרים; הכפתורים, הרשימה הצפה, החיפוש, הקיבוץ לפי זמן, "כל N השיחות ←" והטיפול בכישלון קריאה - קוד אחד. בתקנון - "שיחה חדשה" מנקה גם את תגיות הסעיפים ומחזירה את השאלה לדוגמה; פתיחה משחזרת את הקישורים לסעיפים. בהצעה לסדר - גם שם חבר/ת הכנסת והטיוטה, ומה שנענה כחולין לא חוזר לנושא. לכל צ'אט מאגר משלו (localStorage). בשינוי: בכותרות התקנון וההצעה לסדר ירדה שורת ההסבר ("שאלו על סדרי עבודת הכנסת", "נושא, נימוק, ובקשה לדיון") - במקומה שם השיחה הפתוחה, כמו בשאילתות. tests/browser/test_conv_history_shared.py (11/11; על הקוד הקודם נכשל); בדיקות ההיסטוריה של השאילתות עוברות בלי שינוי; בבדיקה החיה |
| ס1 | **הכפתור הכחול "העתקה" בפינה העליונה של ההצעה לסדר** | 26.9.2026 | ירד, עם הקוד שלו. אייקון ההעתקה בהודעה (צ6) מעתיק את הנוסח בלבד. tests/browser/test_copy_draft.py (בדיקה נוספת; על הקוד הקודם נכשלת); בבדיקה החיה |
| ס3 | **מבנה ההצעה לסדר וייצוא ל-Word על שלד הדוגמאות** | 26.9.2026 | הדוגמאות הן טופס של הכנסת עם סימנייה לכל שדה (Heb_Date, Eng_Date, AGN_Num, AGN_Yor_Name, AGN_Yor_Gender, AGN_Type, AGN_Subject, AGN_Description, PM_Gender, PM_Name). packages/render/agenda_doc.py בונה את הקובץ מהדוגמה עצמה וממלא רק את הסימניות - סגנונות, גופנים, גדלים, קו תחתון, יישור, ריווח והסמל אחד לאחד. תאריך ומספר - ריקים (הכנסת מוסיפה). מטא-דאטה: שמות היוצר/עורך הדוגמה נמחקו. **השוואת XML:** ממולא בתוכן של הדוגמה השנייה, document.xml זהה ל-(10) פרט לתאריך, למספר ולשבירת שורה ריקה בסוף דברי ההסבר - פירוט ב-docs/agenda-xml-compare-26.9.md. בצ'אט - אותו מבנה (לכבוד / יו"ר / פנייה / משפט הבקשה / נושא / דברי הסבר / בכבוד רב / חתימה), ואייקון ורד על ההודעה. המודל מנסח רק נושא ודברי הסבר; משפט הבקשה הוא של הטופס (עד כאן "בקשה: מוצע כי הכנסת תדון..."). נוסף שדה "סוג" (דחופה/רגילה). **26.9 (ברק): הצעה רגילה היא אותו טופס בדיוק, בלי המילה "דחופה"** ("...הצעה בנושא:") - כך היא נכתבת. tests/unit/test_agenda_doc.py (7/7, כולל ההשוואה ל-(10)), tests/unit/test_agenda_tool.py עודכן, tests/browser/test_agenda_form.py (8/8; על הקוד הקודם נכשל); בבדיקה החיה |
| ס4 | **יו"ר הכנסת - שליפה ממאגר הכנסת, גיבוי ידני, מטמון** | 26.9.2026 | apps/api/knesset_speaker.py: KNS_PersonToPosition במשרה 122/123 ("יושב–ראש"/"יושבת–ראש הכנסת") עם IsCurrent, ואז KNS_Person. נבדק מול המאגר: אמיר אוחנה, PersonID 30300, מ-29.12.2022. הצלחה - במטמון 12 שעות; כישלון - 10 דקות, הגיבוי מ-apps/api/speaker_fallback.json (לעדכן כשמתחלף יו"ר), ורישום ביומן. היו"ר מגיע עם הטיוטה, כך שההורדה לא ממתינה למאגר. הפנייה - "אדוני היושב ראש"/"גבירתי היושבת ראש" לפי המגדר. GET /api/agenda/speaker מחזיר גם את המקור (feed/fallback). tests/unit/test_agenda_doc.py (מטמון וגיבוי); בבדיקה החיה - "מהמאגר, לא מהגיבוי" |
| - | **מומחה התקנון: תשובה תקינה הוחלפה בסירוב כשתג מקור אחד נשא שני מזהים** (נמצא בבדיקה החיה, 26.9) | 26.9.2026 | המודל כתב "[מקור:law-tkanon-haknesset/54, law-tkanon-haknesset/53]"; השומר קרא את כל התג כמזהה אחד לא מוכר והציג "התשובה ציטטה מזהי מקור לא מוכרים". עכשיו תג מפוצל בפסיקים - בשומר (service._cited_ids) ובהמרה לשמות בעברית (rules_expert.humanize_citations). מזהה לא מוכר בתוך תג כזה - עדיין נדחה. tests/unit/test_llm_service.py (נכשל על הקוד הקודם); בבדיקה החיה **המשך (26.9, נמצא בבדיקה החיה אחרי ח11-ג):** אותו כשל כשכל מזהה בתג נושא "מקור:" משלו - `[מקור:law-tkanon-haknesset/52, מקור:law-tkanon-haknesset/53]`: המזהה השני נקרא "מקור:law-..." ונדחה, והאזכור השני נעלם גם מהשמות בעברית. תוקן במקום אחד - service.citation_ids, שגם rules_expert.humanize_citations משתמש בו. tests/unit/test_llm_service.py ו-tests/unit/test_rules_expert.py (שתיהן נכשלו על הקוד הקודם) |
| לוגו | **הלוגו של המערכת בתפריט הצד** | 26.9.2026 | apps/api/static/logo.webp (הקובץ של ברק, 2000×727). מחליף את כל הבלוק שבראש התפריט - "Legislator AI" ו"ארגז הכלים הפרלמנטרי" כבר בתוך הלוגו. הרקע של הלוגו #0E2B5A, כמעט זהה לצבע התפריט (#0B2A5B), ולכן אין מסגרת נראית; ממלא את רוחב התפריט ביחס המקורי, עם טקסט חלופי. tests/browser/test_logo.py (5/5); בבדיקה החיה (הדף מפנה ללוגו, והקובץ נטען) |
| ח11-ג | **התוכן הקיים מקבל תווית וגם משתנה - הכלי לא ניסח; ברמת הסעיף העריכה נבלעה בשקט** | 26.9.2026 | **נסגר (ברק, 26.9).** ההבדל היחיד מהדוגמה של החוברת - הציטוט: הכלי מצטט את המילה שהשתנתה (`במקום "בתחום" יבוא "בתחומי"`), החוברת את הצירוף (`"בתחום פעולה"`); ברק: נשארים כך, הרחבה - ידנית (DECISION_HISTORY). שני מצבים שוחזרו לפני התיקון, דרך apply_pending_changes (המסלול של הממשק): ברמת הסעיף הקטן (6(א) בחוק הקייטנות הופך ל-(א)(1) וגם משתנה) - נדחה ("לא נתמך בכלי העריכה"); **ברמת הסעיף - באג שקט:** סעיף 3 בחוק הקייטנות, "רשיון" שונה והסעיף הקטן (ב) נוסף - ההוראה יצאה `בסעיף 3, האמור בו יסומן "(א)" ואחריו יבוא:` בלי העריכה, בלי הודעה (`_diff_section` לא השווה את הטקסט של התוכן שקיבל תווית). עכשיו שתי הוראות, לפי החוברת (§7.10.6(ד), ההערה - הדוגמה על חוק רשות הספנות והנמלים, סעיף 6) ורשומות 1924 (עמ' 694 ברמת הסעיף; עמ' 897 מתחת לסעיף, "אחרי פסקה (1) יבוא"; עמ' 672 כתת-פריטים): `בסעיף 6(א) לחוק העיקרי –` / `(1) האמור בו יסומן "(1)", ובו, במקום "X" יבוא "Y";` / `(2) אחרי פסקה (1) יבוא:` / `"(2) ..."`; בתוך הוראה של כמה פריטים - `(n) בסעיף קטן (א) –` ותת-פריטים (א)/(ב) בעומק 1; ברמת הסעיף עם עוד פריטים (כותרת שוליים) - פריטים שטוחים (1)-(3). גם הנוסח המשולב קורא את ההוראה הבודדת (parse_instructions._parse_split); הוראה של כמה פריטים עדיין אינה נקראת בנוסח המשולב - כמו כל הוראה כזו היום, בהודעה גלויה. drafting-rules §8.4ב. tests/golden/test_split_relabel.py (9/9; הדוגמה של החוברת על החוק עצמו, fixture sapanut-2004 באותה גרסה כמו ב-DB; נכשל על הקוד הקודם); tests/unit/test_first_paragraph_multi_item.py עודכן (נעל את הדחייה); בבדיקה החיה [1ו] |
| הסתייגויות 1 | **הכלי קרא Word בלבד, ו-2,676 מתוך 2,680 נוסחים לקריאה שנייה-שלישית הם PDF** | 26.9.2026 | packages/reservations/pdf_bill.py (pdfminer.six, MIT). סוגריים הפוכים, מספר הערת שוליים שנדבק לשנה, כותרת שוליים בתוך הגוף, שבירות שורה - מטופלים; רק חלק ההצעה נקרא; ביטוי לא ודאי אינו עוגן. **טבריה: 116/124 (93.5%)** - שמונת החסרים ציטוטים לא מדויקים בהסתייגויות עצמן, והאבחון האוטומטי מסווג את כולם כך (0 כשלי קריאה). **מדגם 60 נוסחים (GitHub Actions): 454/568 (79.9%)** - סוגי הכשלים: docs/night-report.md. ברק הוריד את סף ה-95% (26.9). tests/golden/test_tiberias_pdf.py; tools/reservations_quote_check.py (+diagnose); .github/workflows/reservations-scale.yml (+ids לאבחון) |
| הסתייגויות 2 | **ההסתייגויות לא נראו כמו הסתייגויות אמיתיות, ומצב "איכות" נתן למודל לכתוב** | 26.9.2026 | שבע משפחות (שינוי ערך, החלפת גורם, אישור/התייעצות, חובה/רשות, מחיקה, סעיפים אחרי האחרון, תנאים); **רק צורות שמופיעות בטבריה** (forms.py, עם מספרי ההסתייגויות); פיזור לפני עומק; "לפחות X ייספרו בנפרד". משחזר מילה במילה את טבריה 18, 19, 20, 46, 48, 48/ג, 50, 65, 65/א. שומרים: 86(ד)(1) (מחיקת יחידה רק כשיש שתיים; אף פעם לא סעיף), 86(ד)(3) (אף עוגן בציטוט שם חוק), §3ג (מסומן). **המודל לא כותב אף מילה.** tests/golden/test_tiberias_forms.py, tests/golden/test_reservations_573919.py (Word, מול 484 אמיתיות), tests/unit/test_reservation_guards.py |
| הסתייגויות 3 | **אין דרך לשלוט ב"כמה הזוי"; תוכן לא מבוקר** | 26.9.2026 | רציני/מתחכם/הזוי; בנק - data/reservations_bank.json (115 רשומות, **כולן ממתינות**); קובץ סקירה docs/reservations-bank-review.md; קווים אדומים בטעינה (data/reservations_redlines.json); שומר 86(ד)(2) על הבנק (data/reservations_bank_screen.json; רשומה ששונתה - חסומה עד סינון חוזר). approval-047 נחסם בסינון. **באג שנמצא בדרך:** "לועדת" במקום "לוועדת" (אות שימוש לפני ו') - תוקן. tests/unit/test_reservations_bank.py (נכשל על הקוד הקודם בבדיקת "לוועדת") |
| הסתייגויות 4 | **תשלום** | 26.9.2026 | 50 כלולות, $10 לכל 100 נוספות או חלק - config/reservations.json; **אין אף שדה של פרטי תשלום**; מצב הדגמה גלוי; "live" - 402 מעבר לכלול עד שתהיה סליקה; ביקשו יותר ממה שיש - נאמר, בלי השלמה בחזרות, והמחיר על מה שנוצר. tests/unit/test_reservations_pricing.py |
| הסתייגויות 5 | **פלט Word** | 26.9.2026 | David 12, מיושר; שם ההצעה; כותרות מיקום; מספור רציף בלי חלופות; בלי "קבוצת X מציעה"; עובר docx_check. tests/unit/test_reservations_render.py |
| הסתייגויות 6 | **הממשק** | 26.9.2026 | גרירת PDF/Word, מד שלוש רמות, משפחות (כולן מסומנות), כמות/מחיר/"לפחות X", "לתשלום", תצוגה מקדימה, הורדת Word. tests/browser/test_reservations_ui.py (10/10); בבדיקה החיה (journey_reservations) |
| בנק §0.1 | **רשימת השרים קבועה בבנק** (שר האוצר, שר המשפטים - רשומות מפורשות) | 26.9.2026 | אישורי הבנק §0.1: packages/reservations/ministers.py - משרדים פעילים (KNS_GovMinistry) עם שר/שרה מכהנים (KNS_PersonToPosition, משרות 39/57, IsCurrent), מטמון 12 שעות, גיבוי data/ministers_fallback.json (מהמאגר, 26.9: 30 משרדים, 29 תארים), יומן בנפילה לגיבוי. תואר <-> משרד דטרמיניסטי ("משרד X"/"שר X", "המשרד ל-X"/"השר ל-X", "משרד ראש הממשלה"/"ראש הממשלה"). **מול הרשימה שבמסמך:** אין במאגר שר פנים מכהן; השמות במאגר: "שר ירושלים ומסורת ישראל", "שר הנגב, הגליל והחוסן הלאומי", "שר התפוצות והמאבק באנטישמיות", "השר לשוויון חברתי וקידום מעמד האישה" (משרד אחד), "שר ההתיישבות והמשימות הלאומיות"; רק במאגר - "שר הקשר בין הממשלה לכנסת"; רק במסמך - "השר לעניינים אסטרטגיים". tests/unit/test_ministers.py (כולל ממשלה אחרת במוק -> רשימה אחרת, בלי שינוי קוד) |
| בנק §0.2 | **שנה בנוסח ההסתייגות - "של השנה הבאה"** (after_last-001) | 26.9.2026 | אישורי הבנק §0.2: "{השנה הבאה}" / "{השנה שאחרי הבאה}" - ממועד הייצור לפי Asia/Jerusalem, כמספר ("ביום 1 בינואר 2027"). bank._years בזמן הרינדור - הגיבוב ופסק השומר לא משתנים משנה לשנה; tzdata נוסף ל-requirements (ב-Vercel אין tzdata מובטח). tests/unit/test_reservation_year.py (נכשל על הקוד הקודם; כולל מעבר שנה בירושלים ב-22:30 UTC) |
| בנק §1-§5 | **הבנק כולו ממתין לאישור** (115 רשומות) - רק שינוי ערך ומחיקה הגיעו לפלט | 26.9.2026 | **נסגר - אישורי הבנק (ברק 26.9, docs/bank-approvals-2026-09-26.md).** הבנק נבנה מהמסמך (tools/build_reservations_bank.py): 756 רשומות, 1,386 אחרי הרחבה; כל רשומה approved, מקור "ברק 26.9 (סשן הבנק)"; המחיקות נמחקו; 106 הגורמים ההזויים - רשימה אחת עם מין (ז/נ), משותפת להחלפת גורם ולאישור; תנאים - בלי רציני ובלי "החל מיום"; after_last - לא מוסיפים סעיף שכותרת השוליים שלו כבר קיימת בהצעה. הספירה לפי משפחה ורמה - docs/reservations-bank-review.md. tests/unit/test_bank_approvals.py (הספירות לפי המסמך, השוואה מילה במילה, מחיקות, רשימה אחת, מין; נכשל על הקוד הקודם); בבדיקה החיה |
| בנק §5 | **חובה ורשות - 12 פעלים, זכר יחיד בלבד, בלי שומר שלילה** | 26.9.2026 | 26 פעלים (14 חדשים) ו"חייב" בטבלה סגורה אחת - מין ומספר (תתקן -> רשאית לתקן, יקצו -> רשאים להקצות); עתיד ברבים לעולם לא "רשאיות"; שלילה ("אינו", "לא") חוסמת; "יביא בחשבון" לא מופעל. tests/unit/test_reservation_duty_modifier.py (טסט שלילי לכל כלל; נכשל על הקוד הקודם) |
| בנק §6 | **משפחה חדשה: "תוספת לפועל"** | 26.9.2026 | `במקום "{פועל}" יבוא "{פועל} {תוספת}"` - מיד אחרי הפועל, באותם שומרים כמו חובה ורשות; 20/50/100 רשומות; בממשק מסומנת כברירת מחדל; נכללת במקסימום וב"לפחות X". tests/unit/test_reservation_duty_modifier.py; tests/browser/test_reservations_ui.py (11/11, גם מול האתר החי); בבדיקה החיה |
| בנק §0.3/§0.4 | **סינון כל רשומה חדשה/ששונתה; ועד הדולפינים** | 26.9.2026 | קווים אדומים ושומר 86(ד)(2) על 1,386 הרשומות (data/reservations_bank_screen.json). **28 חסומות, נשארות חסומות** - הרשימה עם הסיבה בראש docs/reservations-bank-review.md (14 בשומר, 14 בקו אדום, מהן 12 תארים מהמאגר: "שר העבודה", "שר ירושלים ומסורת ישראל"). חריג יחיד (§0.4, חריג לחוק ברזל 7 בהחלטת ברק - DECISION_HISTORY): human_approval {by, at, why} גובר על פסק השומר רק ברשומות המסומנות; approval-164/actor-001 (תוכן approval-047) - חסום בשומר, יוצא לפלט. tests/unit/test_bank_approvals.py (אישור אנושי, וחסומה בלי אישור נשארת חסומה) |
| בנק §7 | **מקסימום טבריה ודוגמאות** | 26.9.2026 | tools/reservations_tiberias_samples.py - עם פסקי הסינון האמיתיים. **טבריה: רציני 241, מתחכם 307, הזוי 1,184** (לפי משפחה - docs/night-report.md); שלוש דוגמאות לכל משפחה ורמה - docs/reservations-samples/{serious,clever,absurd}.docx. "המודל לא כותב אף מילה" - עדיין עובר (test_reservations_bank). בבדיקה החיה: אותם מספרים בפרודקשן |
| הכרעה ז | **זיהוי מספרי הסעיפים ב-PDF - 79.9% במדגם** | 26.9.2026 | **האבחון המקורי (תוכן עניינים/היסט) היה שגוי ברובו:** הכשלים היו בכלי המדידה (כותרת עם יחידה "לסעיף 2(1)", "עד (12)", זנב הערה שנקרא ככותרת) ובשני מקרים של הקורא (מספר בלי נקודה - 628446; ספרות מוזזות - 240417). תוקן: כלל מונוטוני + "המספור קריא" (packages/reservations/pdf_bill.py), כותרות עם יחידה והיקף תוספת בכלי המדידה. **המדגם (60 נוסחים, Actions): לפני 454/568 (79.9%); אחרי 513/568 (90.3%), מילה במילה 501 (88.2%); בלי 7 הקבצים שסומנו כלא קריאים - 508/559 (90.9%).** 20 fixtures ציבוריים בריפו (tests/fixtures/reservations/pdf). tests/golden/test_knesset_pdf_fixtures.py (נכשל על הקוד הקודם) |
| הכרעה ט | **תאריך שגוי - "27.9"** בכותרת דוח הבוקר, ב-open-gaps, בתיעוד ובהערות הקוד; העבודה הייתה בלילה שבין 25.9 ל-26.9 | 26.9.2026 | הכרעת ברק: תאריכים רק מ-`TZ=Asia/Jerusalem date`, לא מהזיכרון. כל ה-"27.9" תוקנו ל-26.9 (24 קבצים, הערות ותיעוד בלבד). **לא שונו:** שם המיגרציה 20260927010000_contact_messages.sql ותוכנה (כבר הורצה - לא משנים שם), והציטוט ב-DECISION_HISTORY. בלי בדיקה אוטומטית: "27.9" יהיה תאריך נכון מחר |
| הכרעה ו | **שני גווני נייבי במערכת** - פס ההרשמה #0E2A5A (טוקן --band), ה-favicon #1f3a5f, התפריט #0B2A5B | 26.9.2026 | הכרעת ברק: גוון אחד - #0B2A5B. --band הוסר, פס ההרשמה ב-var(--navy), ה-favicon עבר ל-#0B2A5B. הגדרת "נייבי": גוון 200-250, בהירות 12%-30%, רוויה 35%+ (כחול הדגל #0038B8 וכמעט-שחור #14171C אינם). tests/unit/test_single_navy.py - סריקה סטטית של CSS/תבניות/JS/SVG (נכשל על הקוד הקודם); tests/browser/test_navy.py - הצבעים המחושבים בכל המצבים, הלשוניות והחלונות (נכשל על הקוד הקודם: rgb(14, 42, 90) בפס ההרשמה); בבדיקה החיה. הרקע בתוך קובץ הלוגו (תמונה, #0E2B5A) לא שונה - קובץ של ברק |
| הכרעה ח | **קובץ ה-Word של ההסתייגויות בגודל Letter** (12240×15840) - תבנית ברירת המחדל של python-docx | 26.9.2026 | הכרעת ברק: A4 - 11906×16838 twips (render_docx.A4_TWIPS). tests/unit/test_reservations_render.py::test_a4_page (נכשל על הקוד הקודם: 12240×15840); בבדיקה החיה ("ייצור: עמוד A4") |
| הכרעה ד | **"השר" בתבניות הבנק** - "ובלבד שהשר ידווח..." על הצעה ששר מסוים בה (שאלה 6) | 26.9.2026 | הכרעת ברק: שם השר המפורש מההצעה עצמה - שר בשמו בסעיף; אחרת שר יחיד בהצעה כולה; אחרת "השר" אם ההצעה משתמשת בו; אחרת התבנית לא מופעלת. סעיף עם שני שרים שונים - לא מנחשים, התבנית לא מופעלת. ההחלפה בזמן הרינדור (families._with_minister) - הרשומות, הסטטוס שלהן ופסקי הסינון לא משתנים; אין סינון חוזר. טבריה: `ובלבד ששר הפנים ידווח לוועדת הפנים...`, `סגן שר הפנים`. tests/unit/test_reservation_minister.py (נכשל על הקוד הקודם); בבדיקה החיה |
| הכרעה ג | **החלפת גורם בלי התאמת מין** - "הממשלה יתקן" (שאלה 5) | 26.9.2026 | הכרעת ברק: מחליפים את הגורם **ומתאימים את המילה שצמודה אחריו**, רק מטבלה סגורה (packages/reservations/agreement.py - מין הגורם לפי שם העצם הראשון; צמדי צורות יתקן/תתקן, יקבע/תקבע, רשאי/רשאית, יפרסם/תפרסם ועוד). טבריה סעיף 3: `במקום "שר הפנים יתקן" יבוא "הממשלה תתקן".`; גורם זכר - `במקום "שר הפנים" יבוא "שר האוצר".`. מילה שאינה בטבלה - רק גורם מאותו מין; מין לא ידוע - הרשומה לא מופעלת. 14 רשומות החלפת הגורם בבנק - לכולן מין ידוע. קובץ הסקירה מציג את הצורה המותאמת. tests/unit/test_reservation_agreement.py (נכשל על הקוד הקודם); בבדיקה החיה |
| הכרעה ב | **מספרים גדולים בלי מפריד אלפים** - "10000", "3000 ימים" (שאלה 4) | 26.9.2026 | הכרעת ברק: "שנה 3000" ו"10,000 ימים" בהזוי מאושרים; מ-1,000 ומעלה - עם מפריד ("10,000", "3,000 ימים"). שנה - בלי מפריד ("3000"). tests/unit/test_reservation_numbers_format.py (נכשל על הקוד הקודם: "1000", "10000"); בבדיקה החיה |
| הכרעה א | **שינוי ערך שינה מספרי הפניה** - "בסעיף 7" -> "8", "143א" -> "143ב" (שאלה 3 בדוח הבוקר) | 26.9.2026 | הכרעת ברק: הפניה לא משתנה אף פעם, בכל הרמות. `anchors.reference_spans` - הגדרה מבנית (שם עצם של הפניה + תווית/רשימת תוויות), ו-`families` מדלג על כל עוגן שבתוכה. **טבריה, המקסימום לכל רמה: לפני אישור הבנק 9/9/8 (היה 34/35/26); אחרי אישור 53/48/47 (היה 78/74/65).** "שלושים ימים" בסעיף 3 עדיין משתנה. tests/unit/test_reservation_references.py (נכשל על הקוד הקודם: 7, 2, 143, 143א, 206, 38, 38א); בבדיקה החיה |
| דף הבית | **מי שנכנס נוחת ישר בהצעות חוק, ולא רואה שיש עוד שבעה כלים** (ברק, 26.9) | 26.9.2026 | דף בית בשלושה מצבים, בשפת העיצוב הקיימת (כרטיסים לבנים על --shell, Assistant; שני טוקנים חדשים מהמוקאפ - מנטה #5EE0A8 לכפתורי ההרשמה בלבד; פס ההרשמה - בנייבי של התפריט, #0B2A5B, לפי הכרעה ו). **אורח:** לוגו + "הרשמה" + "כבר רשומים? כניסה", בלי פריטי ניווט; "ברוכים הבאים", פס ההרשמה, שמונת הכלים; לחיצה על כלי - חלון "כדי להשתמש ב[כלי] — נרשמים", לא הכלי (גם דרך switchTab, לא רק בכרטיס). **נרשם:** התפריט המלא ו"דף הבית" בראשו; "שלום, [שם פרטי]"; "+ הצעת חוק חדשה"; מצב ריק. **חוזר:** "בוקר טוב / צהריים טובים / ערב טוב"; ארבעת הפריטים האחרונים מכל הכלים - תגית, כותרת, פרט ("3 שינויים", "שאילתה רגילה", "שאלה אחת"), זמן יחסי ("לפני שעתיים", "אתמול"); לחיצה פותחת את ההצעה או השיחה בכלי שלה. המקור - ההצעות השמורות והרכיב המשותף של ההיסטוריה (חשפתי ממנו list() לקריאה בלבד), לא מקור חדש; אין עמוד "כל העבודה שלי" ולכן אין קישור. **מתג פיתוח:** `?view=guest|new|returning`. בלי המתג - נרשם, ונגזר מההיסטוריה (יש פריטים -> חוזר); **לא אורח**, אחרת האתר החי היה ננעל לכולם לפני שיש הרשמה. **כולם נוחתים בדף הבית.** הרשמה/כניסה -> /auth/signup, /auth/login - מקום שמור ("עוד לא פתוחה"); כשתהיה הרשמה רק הם משתנים. tests/browser/test_home.py (34/34 עם --send, גם מול האתר החי), tests/unit/test_home_page.py (6/6; נוסח שמונת הכרטיסים מילה במילה); 11 בדיקות דפדפן קיימות עודכנו ללחוץ "הצעות חוק" אחרי הטעינה; בבדיקה החיה (journey_home) |
| יצירת קשר | **אין דרך לפנות אלינו** (ברק, 26.9) | 26.9.2026 | כפתור בתחתית התפריט בכל המצבים (מעטפה, מסגרת בלבד). חלון קופץ באמצע המסך מעל דף מוחשך; Esc, לחיצה על הרקע וה-× סוגרים. **אורח:** שם מלא, אימייל, תוכן - חובה, ובדיקת פורמט לאימייל (בדפדפן ובשרת, שגיאה ליד כל שדה). **רשום:** תוכן בלבד, ו"הפנייה תישלח בשם [שם] · [אימייל]". אחרי שליחה - "הפנייה נשלחה" בתוך החלון. **הטבלה:** public.contact_messages - RLS בלי policy, anon/authenticated בלי הרשאות (כמו chat_log). נבדק בשתי דרכים: SQL (אין select/insert לאף אחד מהם) ו**במפתח הציבורי עצמו מול ה-REST** - קריאה וכתיבה: 401 permission denied. **ספאם:** (1) לפי כתובת - 3 פניות ב-10 דקות, 10 ביום, נספר בטבלה עצמה (ב-Vercel מונה בזיכרון מתאפס בין מופעים); חריגה - 429 עם הודעה; (2) שדה נסתר (מחוץ למסך, tabindex=-1) - מי שממלא אותו מקבל "נשלח" ולא נשמר. הכתובת לא נשמרת - רק HMAC שלה. תקלה בשמירה - 503 גלוי, לא "נשלח". הבדיקה החיה מסמנת את הפנייה שלה (is_test). apps/api/contact.py, supabase/migrations/20260927010000_contact_messages.sql. tests/unit/test_contact.py (12/12); בבדיקה החיה (journey_contact: 422 על פנייה ריקה, שליחה נשמרת) |
| 186 | **שיחה שמורה אחת בהצעה לסדר הפילה את כל הסקריפט בטעינה** (נמצא תוך כדי דף הבית; לא היה רשום) | 26.9.2026 | CONV_LIMIT/CONV_SHOWN הוגדרו ב-app.js **אחרי** שהרכיב של ההיסטוריה בהצעה לסדר כבר נטען; const שנקרא לפני ההגדרה זורק. בלי שיחות שמורות הקוד לא הגיע לשורה הזו - ולכן לא נראה. **שוחזר באתר החי:** "Cannot access 'CONV_SHOWN' before initialization", וההיסטוריה של השאילתות ושל מומחה התקנון לא נטענה בכלל. ההגדרה עברה לפני הרכיב הראשון. tests/unit/test_home_page.py::test_conv_constants_before_first_use (נכשל על הקוד הקודם), tests/browser/test_home.py; בבדיקה החיה |
| ס5 | **המודל בהצעה לסדר מוסיף עובדות שלא נמסרו לו** (נמצא באתר החי, 26.9) | 26.9.2026 | על "מחסור חמור בשוטרים בנגב, תחנות נסגרות בלילה" נכתב "בתקופה האחרונה **מתרבים הדיווחים**..." ו"...**לצד עלייה במקרי פשיעה** ואירועים פליליים באזור". בשאילתות היה שומר, בהצעה לסדר לא. **עכשיו אותו שומר (ברק: פרט חדש נתפס, ניסוח מחדש מותר):** מקור, גוף או מספר שלא נמסרו - השומר של השאילתות עצמו (query_tool.unsupported_source_claims), בלי "דיון" (בהצעה לסדר הוא מהות הבקשה; דיון בוועדה עדיין נתפס על "ועד"); ובנוסף טענות מגמה, זמן ודיווח - עלייה, ירידה, גידול, זינוק, שיא, "מתרבים", "מסתמנת מגמה", "בתקופה/בחודשים האחרונים", נתונים, אחוזים, דיווחים, פרסומים, תלונות - נחשבות נתמכות רק אם אותו שורש מופיע בדברי המשתמש. נמצא: ניסוח מחדש אחד בלי הפרט; אם חוזר - עצירה גלויה עם הפרט. **נמצא גם בהרצה מקומית:** "סגירת סניפי דואר ביישובים קטנים בגליל" -> "בחודשים האחרונים מסתמנת מגמה" - נתפס עכשיו. חמש הרצות אמיתיות אחרי התיקון: 5/5 נוסחו, 0 חסימות שווא, 0 מגמות מומצאות. tests/unit/test_agenda_invented_facts.py (6/6, משחזר את התשובה מהאתר החי מילה במילה; על הקוד הקודם נכשל); test_agenda_tool עודכן (הדוגמה שלו עצמה המציאה "עלו משמעותית בשנים האחרונות"); בבדיקה החיה (--with-llm) |
| צ7 | **השיחות בצ'אטים לא נשמרות - אין דרך לדעת מה משתמשים שואלים ומה קיבלו** | 26.9.2026 | כל תור בשלושת הצ'אטים (שאילתות, הצעה לסדר, מומחה התקנון) נרשם בטבלה public.chat_log: כלי, מזהה שיחה, מזהה משתמש אנונימי, ההודעה, התשובה, סוג (chitchat/insult/substantive/refused/not_found/guard_replaced/error) ומטא (מודל, טוקנים). **גישה מהשרת בלבד:** RLS פעיל בלי אף מדיניות, והרשאות anon/authenticated נשללו - נבדק ב-SQL: לאף אחד מהם אין select/insert; השרת כותב במפתח השירות. המזהה האנונימי - UUID שנוצר בדפדפן ונשמר ב-localStorage (legislator.anonUserId), אותו מזהה בשלושת הצ'אטים; מזהה השיחה - מההודעה הראשונה, חדש בכל "שיחה חדשה". הרישום ברקע ולא משהה את התשובה; כישלון רישום לא מפיל את הצ'אט; שגיאה נרשמת לפני שמוחזרת. טקסט ארוך נחתך ב-20,000 תווים. apps/api/chat_log.py, supabase/migrations/20260926120000_chat_log.sql. tests/unit/test_chat_log.py (7/7; על הקוד הקודם נכשל), tests/browser/test_chat_log_headers.py (5/5); בבדיקה החיה - שורה נרשמה בפרודקשן ונמחקה. **🅓 פתוח לפני השקה: תנאי השימוש חייבים לומר שהשיחות נשמרות** (אין עדיין דף תנאי שימוש). |
| ח14+ח20+ח21 | **מספרי סעיפים אפורים; "הצעה חדשה" בתוך "ההצעות שלי"; "לחצו לעריכה"** | 26.9.2026 | ח14 - מספר הסעיף בכותרת יורש את הגוון, הגודל, הגופן והמשקל של כותרת השוליים (עד כאן 12.5px באפור); התוויות "(א)", "(1)" בשורת הטקסט - כמו שהיו. ח20 - "+ צור הצעה חדשה" בסרגל העליון, כחול, גלוי תמיד (גם כשהחלונית סגורה ולפני שנבחר חוק); ירד מתוך החלונית. ח21 - "לחצו לעריכה" ירד. tests/browser/test_bills_header.py (6/6; על הקוד הקודם נכשל); בבדיקה החיה |
| ח4 | **אין דרך למחוק סעיף בלחיצה; הפלוס כמעט בלתי נראה** | 25.9.2026 | פח ליד הפלוס, על סעיף/סעיף קטן/פסקה/הגדרה. מאחוריו - ביטול יחידה במנוע (לא היה): "סעיף קטן (ב) – בטל", "בסעיף 2(א), פסקה (1) – תימחק" (§7.10.7), כותרת "תיקון סעיף N"; סעיף שלם - "סעיף 5 – בטל" (ח3). אותו מסלול כמו מחיקה ידנית של כל הטקסט. שני האייקונים גלויים תמיד, 28px; התברר ש-button.subtle מחק בשקט את העיגול של הפלוס. test_repeal_section 19/19, tests/browser/test_trash_icon.py 7/7 |
| ח1 | **"עקוב אחר שינויים" נעלם בזמן עריכה** - לחיצה על סעיף שנערך הציגה רק את הטקסט החדש | 25.9.2026 | השדה הוא עכשיו תצוגה של הטקסט הנוכחי מול המקור (השוואת מילים בדפדפן): מחוק - אדום ונעול לעריכה (contenteditable=false), נוסף - ירוק, גם בזמן ההקלדה. כל הקלדה ומחיקה נתפסת ב-beforeinput ומוחלת על הטקסט, והסמן חוזר למקומו. **באג שנמצא בדרך:** בשני שינויים באותו סעיף סומן אחרי היציאה רק האחרון, והראשון לא הופיע כלל - תוקן (הסימון כבר לא תלוי בסטטוס אחד מהשרת). מגבלות: הסימון ברמת מילה (אות אחת ששונתה - כל המילה מסומנת); הקלדה בהרכבה (IME, מקלדות טלפון) לא נבדקה בדפדפן אמיתי. ביטול פעולה (Ctrl+Z) - ח7. tests/browser/test_track_while_editing.py: 13/13, על הקוד הקודם 8 נכשלות **26.9 (ברק): הסימון נשאר ברמת מילה - משנים רק מילים ומעלה.** |
| ח7 | **אין ביטול פעולה** - מחיקת סעיף או ביטוי לא ניתנת להחזרה | 25.9.2026 | מחסנית ביטול אחת לכל העריכה בחוק: Ctrl+Z, Ctrl+Y / Ctrl+Shift+Z (לפי המקש - עובד גם בפריסה עברית), וכפתורי "ביטול" ו"חזרה" בכותרת "נוסח החוק". הקלדה ברצף באותו שדה היא צעד אחד (הפסקה של שנייה, מעבר שדה או מעבר ממחיקה להקלדה - צעד חדש); מחיקה בפח, הוספת סעיף והסרת הוספה - צעד אחד כל אחת. ההצעה מתעדכנת אחרי כל ביטול. בתיבות טקסט רגילות (שם ההצעה, תפריט ההוספה) - הביטול של הדפדפן. המחסנית מתאפסת בפתיחת חוק או הצעה. מגבלה: המחסנית בזיכרון הדף - רענון הדף מוחק אותה (העריכות עצמן נשמרות). tests/browser/test_undo.py: 19/19, נכשל על הקוד הקודם **26.9 (ברק): מקובל שהמחסנית לא שורדת רענון.** |

**שני ממצאים של הביקורת הזו עצמה, שתוקנו עכשיו:**

- **לשונית "נוסח משולב" עדיין הציגה "בקרוב"** מעל כלי עובד. זו
  הסיבה שהפריט נראה פתוח — התווית סתרה את המציאות.
- **`raw_block` חזר מה-API עם `editable: true`.** `amend()` זורקת
  עליו `NotImplementedError` מפורש, אבל בעץ הוא נראה ככל צומת
  אחר: המשתמש לוחץ, עורך, ומקבל שגיאה רק בסוף. **אותו כלל בדיוק
  שהוחל בזמנו על סעיף בלי מספר** — מה שאי אפשר לערוך חייב
  להיראות אחרת מראש.

## מה שנמדד מחדש ונשאר פתוח

| # | הפער | תועד | נמדד עכשיו | |
|---|---|---|---|---|
| 2 | סעיפים בלי מספר (מרחב `law`) | 429 | **179 ב-40 חוקים** | 🔻 |
| 5 | בלוקי `raw_block` שאי אפשר לערוך | 397 | **397 ב-119 חוקים** | ➡️ |
| 9 | חוקים מאונדקסים לחיפוש סמנטי | 92/1,109 | **הפיצ'ר הוסר (25.9.2026)** | 🅓 **החלטה, לא פער** |
| 3 | שארית wikitext בשדה המספר | 80 | **2 צמתים ב-2 חוקים** | 🔻 |
| 18 | שורת "הצעה זהה או דומה" | אין תשתית | **התשתית קיימת וחיה** (`/api/bills/similar`); נותר רק לחבר לבדיקה 13 | 🔻 |
| — | **הנוסח המשולב תומך בחוק אחד** | — | — | 🅓 **החלטה, לא פער** |
| הסתייגויות | **קריאת PDF במדגם: 90.3% (513/568).** 55 החסרים (ריצה 36240704405, 26.9): 21 נוסח אחר, 16 כמעט-זהים, 6 פיסוק - **הבדלי נוסח בין ההסתייגות להצעה** (כנראה גרסה קודמת); 8 ביחידה אחרת (כולל "ש-04%" - ספרות הפוכות במקור); 4 בלי סעיף (263401 - ספרות מוזזות, מסומן לא ודאי). 7 קבצים מסומנים "המספור לא קריא" ומקבלים אזהרה ולא עוגנים | 26.9 | 513/568 | ⏳ |
| הסתייגויות | צורות טבריה שלא נבנו (במכוון): "לפני הסעיף יבוא" (מטרה, טקסט חופשי), "אחרי פסקה (1) יבוא:" (נוסח משפטי חדש), "יסומן כפסקה (1)", ותיקון החוק העיקרי מתוך סעיף חדש | 26.9 | — | 🅓 |
| דף הבית | **הרשמה והתחברות עם Google** - לא נבנו (ברק: "נבנית אחרונה"). היום: מתג פיתוח וחשבון פיתוח (DEV_ACCOUNT ב-app.js - "ישראל ישראלי"). כשתיבנה: /auth/signup, /auth/login, currentAccount(), ובשרת - השם והאימייל של פנייה מהסשן ולא מהבקשה. הכפתור של Google - הכפתור הרשמי שלהם, לפי ההנחיות | 26.9 | — | ⏳ |
| דף הבית | **האורח חסום בממשק בלבד** - ה-API פתוח לכולם, כמו היום. ייסגר יחד עם ההרשמה | 26.9 | — | ⏳ |
| דף הבית | **"המשך מאיפה שעצרת" - מהדפדפן הזה בלבד**, כמו ההצעות והשיחות עצמן (localStorage) | 26.9 | — | ⏳ |
| יצירת קשר | **מייל לברק על כל פנייה** - אין עדיין שירות שליחת מייל | 26.9 | — | ⏳ |
| יצירת קשר | **הצגת הפניות בממשק הניהול** (עוד לא קיים). עד אז - SQL על contact_messages | 26.9 | — | ⏳ |
| יצירת קשר | הבדיקה "הדפדפן לא יכול לקרוא את הטבלה" בבדיקה החיה רצה רק כשמוגדר SUPABASE_PUBLISHABLE_KEY - ב-CI לא מוגדר, ושם היא "לא נבדק". נבדקה ידנית 26.9 (401) | 26.9 | — | ⏳ |

**שלוש הערות על המדידה:**

- **§4 מדויק יותר עכשיו, ונמוך ממה שדיווחתי בבוקר.** 184 היה מספר
  **כל** הצמתים שמשתתפים בכפילות. מה שבאמת נעלם הוא אחד מכל קבוצה:
  **96**. עדיין הפער החמור ביותר — `find_sections` בונה
  `dict[number, node]`, ואין שום חיווי למשתמש.
- **§2 הצטמצם בגלל שינוי בהגדרה, לא רק בנתונים.** בסך הכול 1,837
  סעיפים בלי מספר, אבל 1,658 מהם במרחבי תוספת/לוח-השוואה
  ש-`find_sections` מסננת **במכוון**. הפער האמיתי הוא 179 — וכולם
  כבר חוזרים עם `editable: false`, כלומר גלוי ולא שקט.
- **§8 ו-§28 אינם ניתנים לאימות מול ה-DB:** חוק הפטנטים ופקודת
  רופאי השיניים **אינם בקורפוס כלל**. §28 נסגר לפי הקוד והבדיקה;
  §8 נשאר פתוח-לא-מאומת.

## השומר בשאילתות — מה הוא תופס ומה הוא במכוון לא (23.9.2026)

ברק: השומר חסם שאילתה תקינה. הוא כתב שהייתה כתבה בתכנית, והשומר
חסם על המילה "פורסם". **הכלל הנכון: המודל מנסח את מה שהמשתמש אמר
ולא מוסיף עליו** — ולכן השומר תופס **פרט חדש** (מקור, גוף, מספר,
תאריך שלא הוזכרו) ולא מילים שמנסחות מחדש את מה שכן נאמר.

| ברקע | מול "הייתה כתבה בתכנית עובדה" | |
|---|---|---|
| `שודרה כתבה` · `כתבה שפורסמה` · `דווח` | ניסוח מחדש | ✅ עובר |
| `על-פי פרסום בתכנית "עובדה"` | ניסוח מחדש | ✅ עובר (נחסם עד היום) |
| `דוח מבקר המדינה` · `3,000 ילדים` · `בוועדת החינוך` | פרט חדש | ❌ נחסם |

שלושה תיקונים נוספים באותה נגיעה:

- **הרשימה לא הייתה עקבית עם עצמה:** `פורסם` נתפס אבל `פורסמה` לא,
  כי המחרוזת שנבדקה הייתה `פרסמ`. אותה טענה בדיוק נחסמה או עברה
  לפי הנטייה.
- **`3,000` מול `3000`** נחשבו מספרים שונים. מנורמל.
- **חסימה היא המוצא האחרון:** כשנתפס פרט חדש, הכלי מבקש מהמודל
  לנסח מחדש בלעדיו, ורק אם גם זה חוזר — מוצגת הודעה. ההודעה נוקבת
  בפרט שלא הופיע, ואינה מבקשת "להוסיף את המקור" ממי שכבר הוסיף.

**מה שלא נבדק:** לא הצלחתי לשחזר את החסימה מהקלט המשוחזר — שמונה
הרצאות חיות על נוסחים קרובים עברו כולן. המנגנון הודגם דטרמיניסטית
על השומר עצמו (פונקציה טהורה), וזה מה שתוקן.

## מקרא

| | |
|---|---|
| 🔴 | המשתמש מקבל תוצאה שגויה או חסרה, בלי לדעת |
| 🟠 | המשתמש נחסם — לא עובד, אבל זה גלוי |
| 🟡 | פיצ'ר חסר; המוצר עובד בלעדיו |
| ⚪ | חוב פנימי; המשתמש לא מרגיש |
| 🅓 | החלטה שהתקבלה, לא פער |

---

# 🔴 אובדן שקט

### 2. 🔻 סעיפים עם מספר ריק — **179 ב-40 חוקים** (נמדד 23.9.2026; תועד 429)

`find_sections` מדלגת עליהם, כלומר **אי אפשר לערוך אותם**.

| חוק | סעיפים ריקים |
|---|---|
| פקודת מס הכנסה | **111** |
| פקודת השטרות | **92** |
| פקודת המסים (גביה) | 44 |
| חוק הביטוח הלאומי | 38 |
| פקודת העיריות | 37 |

**הממצא החשוב:** 16 החוקים שנטענו היום הוסיפו **217** סעיפים כאלה —
יותר ממה שהיה בכל 1,093 החוקים הקודמים יחד. הפקודות הישנות נגועות
בסדר גודל אחר, ו-**111 סעיפים לא-ערוכים בפקודת מס הכנסה** הוא פער
מוצרי ולא סטטיסטיקה.

### 3. 🔻 שארית wikitext בשדה המספר — **2 צמתים** (נמדד 23.9.2026; תועד 80)

`אחר=[<wbr>(א)]`, `עוגן=סעיף פרק כא תוספת`, `{{ח:הערה|7}}ג1`, `'4ח`.
ארגומנט תבנית שדלף ל-`number`. אותה תוצאה: הסעיף אינו נראה לעריכה.

### 4. ✅ **סגור 23.9.2026** — סעיפים שנעלמו בגלל מספר כפול

> **מה המשתמש רואה עכשיו:** סעיף שמספרו אינו ייחודי בחוק מסומן
> בעץ כ**"מספר כפול"** ואינו ניתן לעריכה, עם הסבר למה. קודם הוא
> נראה תקין לחלוטין, המשתמש ערך אותו, ו**לא יצאה שום הוראת תיקון
> — בלי שום הודעה**.

**הפער.** `find_sections` בנתה `dict[number, node]`, ולכן מתוך כל
קבוצת סעיפים בעלי אותו מספר רק אחד שרד — **והוא היה האחרון בסדר
המסמך**, כי הלולאה פשוט דרסה. **96 צמתים ב-38 חוקים.**

**ומה שהיה חמור מהאובדן:** הצומת ששרד הוא כמעט תמיד **לא** הנוסח
שבתוקף. מתוך 78 הכפילויות בגוף החוק:

| מה הצומת השני באמת | כמה |
|---|---|
| נוסח עתידי או זמני — `(החל מהמועד הקובע)`, `(הוראת שעה עד יום 31.12.2026)` | 45 |
| סעיף שפקע, או טקסט מחוק `<s>…</s>` | ~9 |
| טעות מספור שוויקיטקסט **עצמו** מסמן — `(ספרור שגוי במקור)`, `(מספור כפול במקור)` | 2 |
| כפילות אמיתית בין יחידות מבנה שונות (סימן ד מול סימן ד1) | השאר |

כלומר מי שערך את סעיף 25 **שבתוקף** בחוק אוויר נקי השווה מול הנוסח
שייכנס לתוקף בעתיד — ומכיוון שהנוסח ההוא לא השתנה, התוצאה הייתה
אפס הוראות. אומת בשחזור לפני התיקון.

**התיקון (בלי נגיעה בפרסר):**

1. `node.find_sections_all` — לכל מספר **כל** הצמתים, בסדר המסמך.
   שום צומת אינו נעלם.
2. `node.find_sections` מחזירה מעכשיו את **הראשון** בסדר המסמך —
   הנוסח שבתוקף — ולא את האחרון.
3. `node.duplicate_section_numbers` — המספרים הכפולים בלבד.
4. `engine.amend` **חוסמת במפורש** עריכה של סעיף שמספרו כפול:
   `"בסעיף 25 לחוק העיקרי"` הייתה מפנה לשניהם, ואין בחוברת הסגולה
   צורה שמבחינה ביניהם. החסימה חלה רק כשהסעיף הכפול הוא זה
   שהשתנה — כפילות במקום אחר בחוק אינה חוסמת עריכה.
5. `tree_view.node_view` מסמן **את שני העותקים** (`ambiguous_number`),
   והממשק מציג תג "מספר כפול" עם ההסבר. הבעיה היא הכתובת, לא הצומת.

**המחיר, במפורש:** 34 מספרי סעיף (68 צמתים) עוברים מ"נראים ניתנים
לעריכה ולא עובדים" ל"מסומנים ואינם ניתנים לעריכה". זו אינה נסיגה —
זו אותה יכולת בדיוק, גלויה במקום שקטה. **44 הזוגות הנותרים אינם
נחסמים כלל** — ראו §4ב.

### 4א. ✅ **סגור 23.9.2026** — לוחות שתויגו כמרחב `law`

באג תיוג בן שורה אחת: `wikitext_parser` בדק
`anchor.startswith("תוספת")` בלבד, ולכן יחידה שכותרתה **"לוח"**
ירשה את מרחב המספור של גוף החוק. הסעיפים שבתוכה, שמתחילים מ-1,
התנגשו עם סעיפי החוק.

| מה תויג שגוי | סעיפים | חוקים |
|---|---|---|
| `לוח יד` / `לוח טו` / `לוח יח` בחוק הביטוח הלאומי | 57 | 1 |
| `לוח השוואה` | 32 | 12 |

**השני הוא בדיוק הפער שתועד ב-`node.find_sections`** כ"פער תיוג
אמיתי ב-ingest, מחוץ להיקף התיקון" — ונסגר עכשיו יחד איתו.

אחרי התיקון, בפרסור חי של חוק הביטוח הלאומי: כל 64 הסעיפים שתחת
לוח מתויגים `schedule`, וסעיפים 1, 2 ו-10 של גוף החוק חזרו להיות
ייחודיים. **הכפילויות החסומות בחוק ירדו מ-12 ל-1.**

**ההחלפה הרגילה הורצה על כל 12 החוקים** (23.9.2026): חוק הביטוח
הלאומי (log 232, v1385→v1478) ועוד 11 - חוק העונשין, פקודת בתי
הסוהר, פקודת המשטרה, חוק שירות בטחון ועוד (v1479–v1489). **מספר
הצמתים זהה בכולם** - רק התיוג השתנה. אומת: כל 33 סעיפי "לוח
השוואה" ב-13 הגרסאות מתויגים `schedule`, ובקורפוס כולו הכפילויות
ירדו מ-88 ל-78.

**למה הורץ גם על מה שהמשתמש לא ראה** (ברק): דאטהבייס שאינו תואם
לפרסר הוא מצב שלא משאירים. סעיפי "לוח השוואה" ממילא דולגו על ידי
`parse_section_number` (המספר שם הוא אות בודדת), ולכן הם לא ייצרו
חסימה - אבל הפער בין הקוד לנתונים היה נשאר.

### 4ב. 🅓 הוראה שתחולתה ממועד קובע — החלטה, לא פער

**דיוק במונחים (ברק, 23.9.2026):** הוראה שחלה ממועד קובע **אינה**
"נוסח שאינו בתוקף". היא נחקקה והיא בתוקף; רק תחולתה מתחילה במועד.
אסור לסמן אותה כ"לא בתוקף" בשום מקום — לא בקוד, לא בתיעוד ולא
בממשק. המונח הוא **"יחול החל מ-"**.

ההחלטה: **תיקון בהצעת חוק נעשה על גבי הנוסח שבתוקף עכשיו.** לכן

- הנוסח שבתוקף — ניתן לעריכה כרגיל;
- ההוראה שתחולתה ממועד קובע — מוצגת לצידו לקריאה בלבד, עם המועד
  **כלשונו מהמקור** (`החל מיום 26.1.2027`, `הוראת שעה עד יום
  31.12.2026`);
- החסימה נשארת רק לכפילויות אמיתיות.

נמדד על הקורפוס — 78 הכפילויות בגוף החוק:

| משפחה | זוגות | סעיפים | חסום? |
|---|---|---|---|
| תחולה ממועד קובע / הוראת שעה | **44** | 88 | **לא** |
| שני נוסחים בלי שום סימון | 19 | 38 | כן |
| `(פקע)` | 11 | 22 | כן |
| `(ספרור שגוי במקור)` / `(מספור כפול במקור)` | 4 | 8 | כן |

### 5. ➡️ 397 בלוקי `raw_block` ב-119 חוקים (אומת 23.9.2026)

נשמרים ואינם אבודים, אבל **אינם ניתנים לעריכה**. גדל מ-366 בעקבות
הכרעת מילון המונחים — זו תוצאה מכוונת.

**מ-23.9.2026 זו חסימה גלויה ולא קיר שקט:** `node_view` מחזיר
`editable: false` על כל `raw_block`, כמו על סעיף בלי מספר. עד אז
הצומת נראה ככל צומת אחר והשגיאה
(`NotImplementedError` מ-`amend()`) הגיעה רק בסוף העריכה.

---

# 🟠 חסימה גלויה

### 6. `.doc` בינארי אינו נתמך בהעלאה

`python-docx` קורא `.docx` בלבד. כל הכלים (תקציר, ביקורת,
הסתייגויות) דוחים `.doc`. **המשתמש מעלה את מה שקיבל מהוועדה**, וזה
לעתים הפורמט הישן — 2 מתוך 4 נוסחי הקריאה 2-3 היחידים שקיימים.
יש לי חולץ עובד (טבלת pieces של Word 97) שנכתב היום לצורך חקירה
ואינו מחובר לשום מסלול.

## המשתמש מקבל `.doc` מהוועדה — והתשובה אינה "שימיר בעצמו"

**המשימה:** 2 מתוך 4 נוסחי הקריאה 2-3 היחידים שקיימים ב-OData הם
`.doc` בינארי. המשתמש מעלה את מה שקיבל, לא את מה שנוח לנו.

### נבדק בניסוי, לא בהערכה (2026-09-19)

התקנתי `libreoffice-writer` בסביבה (`apt` זמין כאן), המרתי את שני
הקבצים האמיתיים, והרצתי עליהם את `extract_bill` הרגיל:

| | `extract_doc.py` שלי | דרך LibreOffice |
|---|---|---|
| טקסט עברי | ✅ | ✅ |
| מספרי סעיפים | ❌ | ✅ `1.` `2.` `3.` … |
| **עומק הזחה** | ❌ **לא ניתן לשחזור** | ✅ `[0,1,2,4,5]` |
| כותרות שוליים | חלקי (תאים) | ✅ |
| שם ההצעה והמגיש | ❌ | ❌ **אבל מזהיר** |

**ההמרה משחזרת בדיוק את מה שחסם את המחלץ שלי.** `gridSpan` נבנה
מחדש ב-docx, ולכן `extract_bill` הקיים עובד עליו בלי שינוי.

מה שאובד: הסגנונות בעלי השם (`Head HatzaotHok`, `Hesber`), ולכן
שם ההצעה והמגיש אינם מזוהים. **זה נכשל ברעש** —
`warnings=["לא זוהה שם הצעת חוק…", "לא זוהה מגיש."]` — ולא בשקט.

### למה זה עדיין לא פתרון מוצר

`libreoffice` הוא בינארי. **ה-Python runtime של Vercel אינו
מריץ בינאריים**, וההתקנה שעשיתי כאן היא בקונטיינר ארעי שנעלם.
שלוש דרכים אמיתיות, וההכרעה של ברק:

1. **המשתמש ממיר ב-Word.** עובד היום, ובעצם **איכותי יותר** —
   Word שומר את הסגנונות בעלי השם, ולכן גם שם ההצעה והמגיש
   יזוהו. החיסרון: שלב ידני שהמשתמש צריך לדעת עליו.
2. **שירות המרה נפרד** (קונטיינר עם LibreOffice) שה-API קורא לו.
   אוטומטי, אבל תשתית נוספת לתחזק, ושם ההצעה עדיין יאבד.
3. **לפרסר את מאפייני הטבלה ב-MS-DOC** ולשחזר עומק מה-SPRM-ים.
   אפשרי אך עבודה משמעותית, בשביל 2 קבצים ידועים.

**ההמלצה: 1.** ההפרש בין "המשתמש לוחץ שמירה בשם" לבין תשתית
המרה שלמה אינו מוצדק בשביל 2 מתוך 2,680, **והמסלול הידני מפיק
תוצאה טובה יותר**. מה שכן חסר היום: הודעת ה-415 מסבירה את
הסיבה אבל אינה אומרת "פתח ב-Word ושמור כ-docx" כהוראה מעשית.

---

### 7. מספור אוטומטי של Word → אפס סעיפים

`13948386` מבין 40 הפיקסצ'רים מפיק 299 שורות ו-**0 ממוספרות**.
**נחקר 2026-09-19 — והאבחנה הקודמת כאן הייתה שגויה בשני דברים:**
הטבלה אינה שטוחה (עומקים 0-2 נקראים נכון, כותרות השוליים נקראות
נכון), והחילוץ **אינו** מזהיר — `warnings` יוצא ריק.

מה שבאמת קורה: 55 מספרי הסעיפים אינם טקסט כלל. הם `w:numPr`
(`numId=26`, `ilvl=0`, `numFmt=decimal`, `lvlText=%1.`) על פסקה
**ריקה** בעמודה 1. Word מצייר "1." "2." בזמן התצוגה; `python-docx`
מחזיר `""`. שני סימנים בלתי תלויים מסכימים על הזיהוי: 55 פסקאות עם
מספור אוטומטי מול 55 שורות עם כותרת שוליים — התאמה מלאה.

**וזה לא רק החוק הזה, ולא רק 422.** ב-`13948363` וב-`13948394`
המספור מעורב: רוב המספרים מוקלדים ואחד אוטומטי. שם החילוץ מצליח
ו**מפיל סעיף אחד בשקט** — גרוע מ-422.

### 7ב. המספור המעורב — **הסעיף שנופל הוא תמיד סעיף 1**

נחקר לעומק 2026-09-19, לבקשת ברק. בשני הקבצים המעורבים
(`13948363`, `13948394`) הדפוס **זהה, ולא אקראי**:

| | סעיף 1 | סעיפים 2 ואילך |
|---|---|---|
| מקור המספר | `w:numPr` (אוטומטי) | טקסט מוקלד |
| מה `python-docx` מחזיר | `""` | `"2."`, `"3."` … |

**והסעיף שנופל הוא זה שמגדיר את "החוק העיקרי".**

```
13948363  שורה 0:  תיקון סעיף 6ג  | (ריק) | בחוק שיווי זכויות האישה,
                                              התשי"א–1951 (להלן – החוק העיקרי)…
13948394  שורה 0:  תיקון סעיף 2   | (ריק) | בחוק הפיקוח על מעונות יום
                                              לפעוטות, התשע"ט–2018 (להלן – החוק העיקרי)…
```

כל הסעיפים שאחריו אומרים "בסעיף 6 לחוק העיקרי" ומפנים להגדרה
שנמחקה.

**הנזק במורד הזרם, נמדד ולא משוער.** `_reservation_sections`
פותחת סעיף חדש רק כשיש `line.number`, וטקסט שלפני הסעיף
הממוספר הראשון **נזרק לגמרי**:

- `13948394` — 5 סעיפים בהצעה, 4 נתפסים.
- **`13948363` — 2 סעיפים בהצעה, 1 נתפס. חצי מההצעה נעלם.**

ובשניהם `warnings == []`. ההצעה נראית תקינה לחלוטין.

**לא תוקן** — ברק ביקש חקירה. הכיוון המתבקש: לקרוא את `w:numPr`
ולגזור ממנו את התווית לפי `numFmt`/`start`, או לפחות **לסמן
אזהרה** כשיש פסקה ממוספרת-אוטומטית בעמודת המספר. אזהרה לבדה
הייתה הופכת את שלושת המקרים האלה מאובדן שקט לאובדן גלוי.

### 7א. סעיף נופל בין הכיסאות ב-`Table InnerSideHeading`

נמצא תוך כדי החקירה של §7. ב-`13695200` סעיף **12.** ("החלטת
הממונה ודרישת תשלום") קיים בטבלה, ו-`extract_bill` מפיל אותו
לגמרי — לא `number` ולא `inner_number`. השורה: עמודה 0 בסגנון
`Table InnerSideHeading`, עמודה 1 `12.`, עמודה 2 `(א)`, הגוף
בעמודה 3. 23 סעיפים מתוך 24 מחולצים.

### 8. חוק הפטנטים — סוגריים כפולים `((...))` רב-שורתיים

אותה משפחת באג שתוקנה, בתחביר אחר. `((רשימת המדינות` נפתח בשורה
אחת ונסגר באחרת.

**לא ניתן לאמת מול ה-DB (23.9.2026):** חוק הפטנטים אינו בקורפוס
כלל — רק `פקודת הפטנטים וסימני האמצאה` ופקודת שעת החירום מ-1939,
ושתיהן נטענות נקי. `a84e5ca` פתר את המשפחה של **תבנית** רב-שורתית
לא-מוכרת (נשמרת כ-`raw_block`), אבל `((...))` הוא תחביר אחר ולא
נבדק עליו. פתוח-לא-מאומת.

### 9. 🅓 הוסר: החיפוש הסמנטי ולשונית "מחקר" (25.9.2026)

**כבר אינו פער** - ברק החליט להסיר את הפיצ'ר (ביצועים, ערך למשתמש).
ראו DECISION_HISTORY "חיפוש סמנטי ולשונית מחקר". מה שכתוב מתחת
הוא המצב ביום ההסרה. **26.9: גם הטבלאות נמחקו מה-DB (task 130) -
529MB -> 208MB**, ומגבלת הכתיבה של הלילה (עד שה-DB מתחת ל-450MB) הוסרה.


17,748 chunks. הממשק מבדיל בין "לא נמצא" ל"לא מאונדקס", אבל
**16 החוקים שנטענו היום אינם באינדקס** — כולל מס הכנסה והתעבורה.
רשימת 250 המדורגת מוכנה.

### 10. המנוע אינו מנסח סעיפי סיום בכלל

תחילה, תחולה, הוראת מעבר, תקנות ראשונות — הוולידטור **בודק**,
`amend()` לא **מייצר**. כמעט כל הצעה אמיתית כוללת סעיף תחילה.

### 11. הוספת פרק שלם

`skeleton-pshia.docx` מדגים (`הוספת פרק ב'1`). דפוס נפרד מהוספת
סעיפים רצופים שמומשה.

### 12. תיקון צו, לא חוק

**דורש הכרעה משפטית לפני קוד.** שתי צורות נצפו באותה הצעה.

---

# 🟡 פיצ'רים חסרים

### 15. 20 מתוך 41 סעיפים מניבים אפס הסתייגויות

סעיפים הצהרתיים בלי ערך מספרי. המנוע לא מכסה **חצי** מהצעה
טיפוסית, ומצב האיכות הוא התשובה לזה.

**עדכון 26.9:** מצב האיכות **ירד** (הבנייה מחדש - המודל לא כותב אף מילה בפלט).
התשובה עכשיו - משפחות הבנק (החלפת גורם, אישור/התייעצות, חובה/רשות, תנאים,
סעיפים אחרי האחרון), שעובדות גם על סעיף בלי ערך מספרי. **כל הבנק ממתין לאישור
של ברק**, ועד אז רק שינוי ערך ומחיקה מגיעים לפלט (טבריה, אחרי הכרעה א: 9/9/8 לרמה; עם הבנק
מאושר - 53/48/47).

### 16. 🔻 בדיקות שמעולם לא רצו — 1, 4, 7 ו-12 רצות מ-23.9.2026

**מה המשתמש מקבל בבודק הניסוח:** תשע בדיקות במקום שבע, ובהן
הבדיקה שמאמתת שמספרי הסעיפים שההצעה מתיימרת לתקן קיימים בפועל
בחוק — מול נוסח החוק שבמאגר, לא מול ההצעה בלבד.

- **בדיקה 1 (מספרי הסעיפים קיימים בחוק המתוקן)** — מומשה מחדש
  למסמך חיצוני (`packages/validate/cited_sections.py`). המימוש
  הישן השווה `source_node_id`, שדה שהמנוע שלנו מייצר ושאינו קיים
  במסמך שהועלה — כלומר הוא היה מדווח "עבר" בלי לבדוק דבר. עכשיו:
  שם החוק נקרא מהוראת התיקון עצמה, החוק נשלף מהקורפוס (אותו מנגנון
  כמו א6), ומספרי הסעיפים מושווים. **נמדד: 0 כשלים ב-40 ההצעות
  האמיתיות; 22 מתוך 23 ההצעות המתקנות אותרו במאגר.** בקרה שלילית
  (שיבוד סעיף 110 ל-999) מפילה את הבדיקה כמצופה.
- **בדיקה 12 (סעיפי תחילה/תחולה/מעבר בסוף)** — הופעלה כמות שהיא.
  הסיבה שנרשמה להשבתתה ("נכשלה על 4 מתוך 40") הייתה מיושנת: מדידה
  מחדש נתנה **0 כשלים על 23 מתקנות ועל 15 חדשות**.

עדיין לא רצות: 2 (מראי מקום — דורש `refs` שהמערכת מייצרת), 8
(תאריך ההגשה — נכתב במזכירות הכנסת), 10 (תיקונים עקיפים), 13
(הצעות דומות — דורש OData, משימה 8), 14 (סגנונות docx — רץ רק על
מה שאנחנו מפיקים), 15 (provenance — קיים רק לנוסח שהרכבנו).

**נמצא תוך כדי ותוקן — ה"א הידיעה בשנה העברית.** ויקיטקסט כותב
`חוק העונשין, תשל"ז–1977` והכנסת (וכל הצעת חוק) כותבת `התשל"ז`.
עשרה מתוך 23 החוקים לא נמצאו בקורפוס **רק** בגלל ההפרש הזה, ובהם
חוק העונשין, חוק התכנון והבניה וחוק הבחירות לכנסת. הנורמליזציה
ב-`law_search` מורידה את ה"א בשני הצדדים — **וזה מתקן גם את חיפוש
החוקים שהמשתמש מקליד בלשונית "הצעות חוק"**, שנכשל עד עכשיו על
הכתיב הרשמי.

**פער שנותר בבדיקה 1:** הצעה שמפנה ל"החוק העיקרי" בלי לחזור על
שמו (13948363) וחוק שאינו בקורפוס אינם מאומתים — מדווח "לא נבדק"
עם הסיבה המדויקת, שמוצגת עכשיו למשתמש בממשק.

### 18. 🔻 בדיקה 13 — לחבר את "הצעה זהה או דומה" לוולידטור

חובה לפי החוברת הסגולה. **התשתית כבר קיימת וחיה:**
`knesset_bills.similar_bills`, נקודת הקצה `/api/bills/similar`,
והתצוגה בלשונית הצעות החוק (משימות 69–72). מה שנשאר הוא רק
לחבר אותה לבדיקה 13 — **אותו דפוס בדיוק של בדיקה 1**: הוולידטור
נשאר טהור ומקבל את התוצאה כארגומנט, ושכבת ה-API היא זו שפונה
לרשת.

נוסח הדילוג הנוכחי בבדיקה 13 ("טרם מומש, ממתין למשימה 8") אינו
נכון יותר.

### 19. טבלה רב-טורית — אין תקדים לניסוח תיקון

**חור מוצרי, לא פינה:** 46 מתוך 97 הטבלאות יושבות בגוף החוק ולא
בתוספות. אי אפשר לתקן את מדרגות מס הרכישה. `amend()` זורק
`NotImplementedError` מפורש במקום לנחש.

### 20. הצבעות מליאה בכלי המחקר · 21. התרעת ק״ת · 22. PDF פחות מדויק · 23. סוגי מסמכים נוספים

נחקרו ותועדו, לא מומשו. PDF מסומן מפורשות בממשק.

---

# ⚪ חוב פנימי

### 29. הקישור לדף השאילתה באתר הכנסת — לא אומת מעולם

ב6 החליף את הורדת קובץ ה-Word בקישור ל-
`m.knesset.gov.il/apps/query/details/<Id>`. **הדפוס והדוגמה הגיעו
מברק; הדף עצמו מעולם לא נפתח מסביבת הסוכן** — `m.knesset.gov.il`
חסום כאן (403 מהפרוקסי), כמו `fs.knesset.gov.il`.

נשאלה שאלה מפורשת (23.9.2026) אם הקישור אכן נפתח, ולא התקבלה
תשובה. **עד שנלחץ עליו פעם אחת, זו הנחה ולא עובדה** — וקישור
שבור נראה למשתמש כמו תקלה במוצר.

### 30. מטמון השעה במומחה התקנון — לא נמדד בפועל

מטמון ה-prompt הועבר ל-TTL של שעה (`ephemeral`, 1h + הכותרת
`anthropic-beta: extended-cache-ttl-2025-04-11`), ואומת חי פעם
אחת (`ephemeral_1h_input_tokens: 154,230`).

**ברק ביקש למדוד אחרי שבוע כמה כתיבות מטמון היו בפועל** (22.9.2026).
כתיבה עולה פי 1.25/2 מקריאה רגילה, ולכן מטמון שנכתב יותר משהוא
נקרא **מייקר** במקום להוזיל. טרם נמדד.

### 31. שתי ריצות ושני מיילים לכל דחיפה

`live-smoke.yml` מופעל גם ב-`push` וגם ב-`deployment_status`
(ה-webhook של Vercel). שתיהן מריצות את אותה חבילה מול אותו אתר,
ולכן **כל כשל שולח שני מיילים**. זה מוכפל מעל הרעש שכבר קיים.

ה-`push` הוא רשת ביטחון מכוונת למקרה שה-webhook לא מגיע (ראו
ההערה בראש הקובץ), ולכן זו אינה סתם כפילות מיותרת — אבל צריך
להכריע אם רשת הביטחון שווה את הכפלת ההתראות.

### 26. `next_appended_label`: `4א` → `4ב` או `5`?

הכרעה פתוחה. קודם זה קרס, אז אין התנהגות לשמר. שלוש שורות.

### 27. אזהרות אבטחה ב-Supabase — **רשימה אחרת מזו שתועדה**

`resolved_amendments` עם `SECURITY DEFINER` ושלוש הפונקציות בלי
`search_path` **אינן מופיעות יותר** ב-advisors (נבדק 23.9.2026).
מה שכן מופיע:

- **INFO** — עשר טבלאות עם RLS מופעל ובלי שום policy
  (`laws`, `nodes`, `law_versions`, `search_chunks` ועוד).
  לא דליפה: אין policy = אין גישה אנונימית, והשרת ניגש
  ב-service_role. עדיין ראוי להכרעה מפורשת.
- **WARN** — ההרחבה `vector` מותקנת ב-schema `public`.

### 32. קובץ המפתחות וההוק שמזהיר עליו — שניהם בקונטיינר, ושניהם נעלמים איתו (24.9.2026)

ש-`/root/.claude/legislator.env` אינו שורד החלפת קונטיינר — כתוב
ב-CLAUDE.md. מה שלא היה כתוב: **גם ההוק שאמור להזהיר על כך**
(`tools/session_env_notice.py`, רשום ב-`/root/.claude/settings.json`)
יושב באותה תיקייה ונעלם באותו רגע. בסשן חדש אין קובץ ואין הודעה —
רשת הביטחון נופלת יחד עם מה שהיא שומרת עליו.

**נמדד 24.9.2026:** חמשת המשתנים חסרים, `loaded_names() == ()`,
ו-`tools/sql.py` נכשל ב-`MissingSecret`. הכשל בקוד רועש, וזה טוב;
השקט הוא בתחילת הסשן. התגלה רק כי ברק ביקש לוודא.

**הפתרון הקבוע, בלי שינוי קוד:** חמשת השמות כמשתני סביבה בהגדרות
סביבת הענן. `env_file` כבר מעדיף סביבה על קובץ (החלטה 19.9), והסביבה
מועתקת לכל סשן חדש. **ממתין לברק.**

---

## סיכום — 23.9.2026, אחרי הביקורת מול git log

| רמה | לפני הביקורת | אחרי |
|---|---|---|
| 🔴 אובדן שקט | 5 | **0** |
| 🟠 חסימה גלויה | 7 | **4** (§2, §6, §7, §19) |
| 🟡 פיצ'ר חסר | 11 | **7** |
| ⚪ חוב פנימי | 5 | **8** (נוספו §29, §30, §31; §32 ב-24.9) |
| **סה״כ** | 28 | **19** |

**שנים-עשר פריטים יצאו מהרשימה** — כולם עם קומיט סוגר, בטבלה
למעלה (§4 ו-§4א נסגרו בהמשך היום). **ושלושה נוספו בסוף היום** —
§29, §30, §31 — כולם דברים שנאמרו בשיחה ולא היו כתובים בשום מקום.

**מדורג לפי השפעה על המשתמש:**

1. ~~**§4 + §4א — 96 סעיפים נעלמים בשקט, ותיוג הלוחות.**~~
   ✅ **שניהם נסגרו 23.9.2026**, וההחלפה הורצה על כל 12 החוקים.
   **לא נותר אף פער ברמה 🔴.**
2. **§7/7א/7ב — מספור אוטומטי של Word וטבלה שטוחה.** הצעה אמיתית
   מניבה אפס סעיפים, או שסעיף 1 נופל. נכשל בכל הכלים שמקבלים מסמך.
3. **§2 — 179 סעיפים בלי מספר.** לא ניתנים לעריכה, אבל **גלוי**
   (`editable: false`), ולכן לא 🔴.
4. **§6 — `.doc` בינארי.** המחלץ קיים, לא מחובר; ההודעה למשתמש
   מסבירה בדיוק מה לעשות.
5. **§9 — 92 מתוך 1,109 מאונדקסים.** רשימת 250 המדורגת מוכנה.
6. **§15 — 20 מתוך 41 סעיפים מניבים אפס הסתייגויות** (לא נמדד
   מחדש).
7. **§5 — 397 raw_block.** מעכשיו מסומנים `editable: false`,
   כלומר חסימה גלויה ולא קיר שקט.
8. **§18 — לחבר את `similar_bills` לבדיקה 13.** התשתית קיימת
   וחיה בממשק; נשאר לחבר, באותו דפוס של בדיקה 1.
9. **§10/§11/§12 — המנוע אינו מנסח סעיפי סיום, הוספת פרק, תיקון
   צו.** §12 דורש הכרעה משפטית לפני קוד.
10. **§19 — טבלה רב-טורית.** `amend()` זורק `NotImplementedError`
    מפורש; 46 מתוך 97 הטבלאות בגוף החוק.
11. **§3, §8, §20–23, §26, §27 — ⚪ חוב פנימי.** §26 הוא הכרעה
    שממתינה לברק (`4א` → `4ב` או `5`).

---


## מה **אי אפשר** לבדוק מהסביבה של הסוכן (2026-09-19)

מיפוי בפועל של מה שנגיש ומה שלא, אחרי שהמפתחות סודרו. זו רשימת
**השינויים שנבדקים רק בפרודקשן** — כל דבר כאן עולה לאוויר בלי
שהורץ אצלי ולו פעם אחת.

| יעד | מצב | |
|-----|-----|--|
| `api.anthropic.com` | נגיש | נבדק, החזיר תשובה |
| `*.supabase.co` | נגיש | נבדק, 200 |
| `he.wikisource.org` | נגיש | 256 חוקים נמשכו |
| `legislator-tau.vercel.app` | נגיש | 200 |
| `knesset.gov.il` (OData) | נגיש | |
| **`api.openai.com`** | **חסום** | `connect_rejected` — מדיניות ארגונית של ה-proxy, לא המפתח |
| **`fs.knesset.gov.il`** | **חסום** | מסמכי הוועדות. נגיש מ-Vercel |

### מה נופל בגלל חסימת OpenAI

`api.openai.com` הוא ספק ה-embeddings היחיד. **כל הנתיבים האלה
אינם ניתנים להרצה מקומית ונבדקים אך ורק בפרודקשן:**

1. **`tools/build_search_chunks.py`** — בניית chunks ואינדוקס.
   כל שינוי ב-chunking, בגודל המנה או בטיפול בחריגות אורך.
2. **`/api/admin/ingest-embeddings`** — כולל
   `_embed_batch_skipping_too_long`, לוגיקת ה-fallback
   לחתיכה-אחת-בכל-פעם, וספירת העלות.
3. **`/api/semantic-search`** — `embed_one` לשאילתה, ואז ה-RPC.
   הדירוג ההיברידי עצמו נבדק על וקטורים מדומים; **ההתאמה בין
   וקטור אמיתי לתוצאה אמיתית — לא**.
4. **`/api/diag/embeddings`** — נקודת האבחון עצמה.
5. **עלות ה-embeddings בפועל** — `usage.total_tokens` האמיתי.

**מה כן נבדק, ולמה זה לא מספיק:** `tests/unit/test_embeddings_errors.py`
מכסה את טיפול השגיאות (400 באורך מול 400 אחר) מול כפיל, ולכן
**ההיגיון** של הטיפול מכוסה. מה שאינו מכוסה הוא שהחוזה מול OpenAI
עצמו לא השתנה — שם שדה, צורת הודעת שגיאה, ממד הווקטור. בדיוק
המשפחה שכבר הפילה אותנו: `temperature` ש-claude-sonnet-5 החל
לדחות לגמרי, ולא היה נתפס בשום טסט עם כפיל.

**הסימן שזה נשבר בפרודקשן:** `/api/diag/embeddings` מחזיר שגיאה,
או ש-`ingest_progress` מצטבר ב-`status='failed'` בלי שהקוד השתנה.

### ומה נופל בגלל `fs.knesset.gov.il`

משיכת נוסחי הוועדות (`.docx`/`.doc`/PDF). עוקף בפועל:
`/api/admin/knesset-bill-docs` רץ מ-Vercel ומחזיר את הקובץ, וזה
מה שמילא את `tests/fixtures/real-bills/` (40 הצעות). כל בדיקה על
מסמך חדש מחייבת או את הנתיב הזה או קובץ שברק מעלה ידנית.

---

## 2,157 תבניות עיצוב של ויקיפדיה בתוך נוסח החוק (2026-09-19)

`{{מוקטן}}` (1,268 מופעים ב-63 חוקים), `{{דוכיווני}}` (889 ב-11),
`{{ממורכז}}` (241), `{{טורים שווים}}` (228), `{{ש}}` (117),
`{{מוגדל}}` (71). אלה **אינן** תבניות `ח:` של מרחב החקיקה, ולכן
`_flatten` אינה יודעת לפרק אותן והן נשארות בטקסט כפי שהן.

### כן, המשתמש רואה אותן

נבדק בפועל, לא הונח: `node_view` מחזירה את `text` כמות שהוא,
וה-JS עושה `escapeHtml`. כלומר בלשונית החוק מוצג למשתמש הטקסט

```
{{טורים שווים ממורכז | {{מוקטן|טור א׳}} {{ש}} שם החוק או הפקודה |
 {{מוקטן|טור ב׳}} {{ש}} התקנות שבהן נקבעה עבירה ... }}
```

במקום טבלה בת שתי עמודות. זהו **פגם תצוגה אמיתי**, לא לכלוך
בנתונים בלבד.

### אבל כמעט ולא דולפות להוראת תיקון

| מרחב | צמתים | חוקים | |
|------|-------|-------|--|
| `schedule` | 3,606 | 133 | `amend()` אינה רואה אותם כלל |
| **`law`** | **124** | **39** | **אלה כן יכולים להגיע לנוסח תיקון** |

`amend()` עובדת על `find_sections(..., numbering_space="law")`
בלבד, ולכן 96.7% מהמקרים אינם יכולים להגיע למסמך Word. 124
הצמתים במרחב `law` הם החשיפה האמיתית, והיא לא נבדקה אחד-אחד.

**לא תוקן.** דורש החלטה: לפרק את תבניות העיצוב לטקסט (ולאבד את
העיצוב), לרנדר אותן, או לסמן אותן כ-`raw_block` שאינו ניתן
לעריכה.

---

## `אחר=` — ארגומנט בעל שם שלא היה מוכר (2026-09-19) — ✅ סגור

נמצא תוך כדי סריקת 1,498 הצמתים הלא-מזוהים. **2,274 מופעים
ב-16 חוקים**, כמעט כולם (2,250) על `{{ח:סעיף}}` הרגיל ולא על
המכוכב. הערך הוא תווית חלופית בסוגריים מרובעים: `אחר=[יד]`,
`אחר=[166(4)]`, `אחר=[(א)]` — מספור הסעיף במהדורה אחרת.

הוא נפל לאותה מלכודת שכבר תועדה פעמיים: הפרסר לקח
`number = call.args[0]` בלי להבחין בין ארגומנט מיקומי לארגומנט
בעל שם, ולכן ב-`{{ח:סעיף*|אחר=[יד]}}` המספר שנשמר היה המחרוזת
`"אחר=[יד]"`.

**✅ סגור ב-`18383e8` (19.9.2026).** הפתרון הוא **כלל ולא רשימת
שמות**: כל ארגומנט שיש בו `=` לפני התו הראשון אינו מיקומי. זו
הייתה הפעם השלישית שאותה מלכודת הכתה (`עוגן=`, ואז `אחר=`),
ורשימת שמות הייתה נשברת בפעם הרביעית.

**אומת 23.9.2026 מול ה-DB החי: 0 סעיפים שהמספר שלהם מכיל `=`**
(היו 368).

---

## דריפה: ה-DB מפגר אחרי ויקיטקסט (2026-09-19)

**איך נמצא:** בדקתי ש-28 החוקים שהוחלפו נמצאים במצב הסופי ולא
בביניים (ברק ביקש, אחרי שפקודת השטרות אכן נתקעה בביניים פעם
אחת). 27 מתוך 28 תואמים לפרסר הנוכחי. **חוק ניירות ערך אינו** —
ולא בגלל הפרסר:

| | |
|---|---|
| `wikitext_revision_id` ב-DB | 3,078,252 (נטען 16.9) |
| revision בוויקיטקסט היום | **3,083,549** |
| ההפרש בעץ | סעיף 35ג נוסף, ועוד 5 פסקאות |

**החוק תוקן בוויקיטקסט אחרי שטענו אותו.** זה לא מצב ביניים של
פרסר אלא **תוכן ישן**, וזו בעיה אחרת לגמרי: המשתמש מקבל נוסח
שאינו הנוסח העדכני, בלי שום סימן.

**לא כומת על הקורפוס המלא.** ניסיתי ונחסמתי פעמיים: דפדוף
PostgREST נחתך ב-1,000 (שוב), ואגרגציות אינן מופעלות בשרת. הדרך
הנכונה כשנגיע לזה: שאילתת SQL אחת שמחזירה `law_id` ו-
`wikitext_revision_id`, מול משיכה של ה-revision העדכני לכל חוק.
`latest_known_revision_id` ו-`latest_checked_at` כבר קיימים
בטבלת `laws` בדיוק בשביל זה — ולא נבדק מתי עודכנו לאחרונה.

**מה זה אומר על "לטעון הכול מחדש":** הנימוק נגד היה שבירת
provenance ל-1,109 חוקים במקום ל-28. אם יתברר שעשרות חוקים
מפגרים בתוכן, הטעינה המחודשת נדרשת ממילא — ואז היא לא שוברת
provenance בחינם אלא מעדכנת נוסח. **זה משנה את ההכרעה**, ולכן
המדידה הזו קודמת להחלטה הבאה על טעינה מלאה.

## הערות "צריך להיות" (צ״ל) - נמדד 21.9.2026, סגור

`[צ״ל: X]` היא הערה שמנסחי הוויקיטקסט הוסיפו לנוסח הרשמי כדי לסמן
טעות דפוס במקור. לדוגמה, בחוק השימוש בהיפנוזה כותרות השוליים של
סעיפים 27 ו-28 הוחלפו ברשומות, וההערה מתקנת: *"תקנות [צ״ל: עונשין]"*.
זה תוכן משפטי ממשי, לא רעש.

**הספירה המלאה בקורפוס (1,109 חוקים):**

| מיקום בוויקיטקסט | מופעים | מגיע למסך |
|---|---|---|
| `{{ח:הערה}}` - הערת שוליים בגוף החוק | 207 | כן |
| `{{ח:תיבה}}` - תיבת היסטוריית הפרסום בכותרת | 36 | **לא** |
| `{{מוקטן}}` בתוך `{{ח:קטע4\|\|...}}` (עוגן ריק) | 2 | **לא** |
| `{{מוקטן}}` במקום אחר | 2 | כן |
| כלל לא הערה (אצ״ל, זצ״ל, דצ״ל, נוסח פקודת 1921) | 10 | כן, כטקסט רגיל |
| **סה״כ** | **257** | **219** |

**ה-DB מחזיק 219 ב-102 חוקים - זהות מדויקת מול הפרסר**, לא אי-שוויון.

### מה לא מגיע למסך, ולמה

**36 בתיבת הפרסום** - `{{ח:תיבה}}` היא רשימת התיקונים וקישורי
הרשומות בראש הדף, לא נוסח החוק. ההערות שם מתקנות את *מספר התיקון
בציטוט ההיסטורי* (`תיקון מס׳ 59 [צ״ל 60]`), לא את נוסח החוק. **לא
פער** - התיבה כולה אינה נטענת, במכוון.

**2 בתוך `{{ח:קטע4||...}}`** - קטע4 עם ארגומנט ראשון ריק עוטף רק
הערה ומדולג לגמרי (החלטה מ-16.9.2026, אחרי סריקת קורפוס-שלם: 90.3%
מהמופעים). שתי ההערות האלה (`law-2000329`, `law-2000331`, שתיהן
"[צ״ל: סעיף 2]" בכותרת תוספת) הן תופעת לוואי של ההחלטה ההיא שלא
תומחרה בזמנו. **פער אמיתי, קטן.** לא תוקן - שינוי בקטע4 נוגע
ב-927 מופעים בשביל 2.

### החיווי בממשק

כל צומת שיש בו הערת צ״ל מקבל תג כתום בכותרת, עם ההערה ב-tooltip.
208 הערות ב-97 חוקים. **התג יושב בכותרת ולא בתוך הנוסח**: הנוסח
הוא `contentEditable` ונקרא על ידי צינור ה-diff, ועטיפת חלק ממנו
ב-`<span>` הייתה נכנסת להוראת התיקון.

`SCRIVENER_NOTE_RE` ב-`app.js`, בדיקה ב-`tests/unit/test_scrivener_notes.py`
(קוראת את הביטוי מהקובץ עצמו ומריצה ב-node - לא העתק שיכול להתיישן).

**ידוע ולא נתפס:** מופע אחד, `law-2001104`, כתוב
`{{ח:הערה|[ככל הנראה צ״ל 8/10,000]}}` - "ככל הנראה" מפריד בין הסוגר
ל-צ״ל. לא הורחב בשבילו: הסוגר הצמוד הוא מה שמונע תפיסה של אצ״ל,
זצ״ל ודצ״ל, ומופע יחיד אינו מצדיק לוותר עליו.

## היסטוריית הצעות - אחסון בדפדפן, ומה שחסר בו (21.9.2026)

הטיוטות נשמרות ב-`localStorage` של הדפדפן ולא בדאטהבייס, מפני
שאין עדיין הרשמה וטבלה משותפת בלי מזהה משתמש הייתה מערבבת בין
אנשים. כל טיוטה מחזיקה את `law_versions.id` שעליו היא נבנתה,
וכשפותחים אותה והמזהה השתנה - המשתמש מקבל אזהרה והעריכות שלו
נטענות כפי שהן, בלי דריסה.

**שלושה פערים ידועים, שכולם נסגרים עם המעבר לדאטהבייס:**

1. **הגנת הטיוטות של הריצה היומית אינה רואה אותן.**
   `tools/check_for_update.py` מדלג על חוק שיש עליו טיוטה פתוחה -
   והוא מחפש טבלת `drafts` בדאטהבייס. טיוטה בדפדפן אינה שם, ולכן
   החוק *כן* יתעדכן מתחתיה. **זה מטופל בצד השני:** הטיוטה זוכרת
   את מזהה הגרסה ומזהירה בפתיחה. זו הגנה בדיעבד ולא מניעה.

2. **הטיוטות קשורות לדפדפן, לא לאדם.** מעבר למחשב אחר, או ניקוי
   נתוני האתר, מאבד אותן. נאמר למשתמש במפורש בפאנל: "נשמרות
   בדפדפן הזה בלבד".

3. **אין שיתוף.** שני אנשים שעובדים על אותה הצעה אינם רואים זה
   את זה.

המבנה שנשמר (`{id, law_id, version_id, title, edits, insertions}`)
עובר לטבלה כמו שהוא כשתגיע ההרשמה - אין כאן מודל נתונים זמני
שיצטרך להיכתב מחדש.

## תבניות ויקי שעדיין מגיעות למסך - 647 צמתים (נמדד 21.9.2026)

"68 טבלאות ה-HTML" התברר כקצה של משהו גדול יותר. מדידה על כל
1,109 קובצי הוויקיטקסט, אחרי שני סבבי הרנדר:

| מקור | צמתים | חוקים | מה המשתמש רואה |
|---|---|---|---|
| `raw_block` - בלוק `<table>` גולמי | 320 | 102 | טבלה שלמה עם `{{ח:פנימי}}`, `{{ש}}`, `{{מוקטן}}` בתוכה |
| תבנית מבנה `ח:` בתוך טקסט | 317 | 8 | `{{ח:תת\|(א)}}` באמצע נוסח סעיף |
| תבנית עיצוב שנותרה | 10 | 3 | בעיקר `{{שבר}}`/`{{sfrac}}` |

המספרים מאומתים מול ה-DB הפעיל (320+317+10=647), כלומר זה בדיוק
מה שמוצג היום.

### א. raw_block - 320 צמתים, 102 חוקים

`<table>` נשמר verbatim כ-`raw_block` ואינו עובר ב-`_flatten`
בכלל, ולכן כל תבנית בתוכו שורדת. **זו הקבוצה הגדולה והנפוצה
ביותר**, ומופיעה בתוספות ובלוחות תעריפים - בדיוק המקומות שבהם
יושבים סכומים ומועדים.

**ההצעה:** להריץ `_flatten` על תוכן התאים בלבד, בלי לגעת בשלד
ה-HTML. התגיות (`<table>`, `<tr>`, `<td>`) נשארות כפי שהן -
הן מה שמחזיק את המבנה - ורק הטקסט שבין התגיות עובר רנדר.
**דורש הכרעה: זו נגיעה בפרסר.**

### ב. תבנית מבנה בתוך טקסט - 317 צמתים, 8 חוקים בלבד

`{{ח:תת}}` בתוך `.text` של צומת פירושו שהפרסר בלע שורה מבנית
כטקסט במקום לפתוח ילד. **מרוכז מאוד** - שמונה חוקים, ורובו
המכריע בפקודת הרוקחים ובחוק מס חברות מזערי, שניהם בעלי מבנה
טבלאי דו-לשוני חריג.

**זה שינוי מבני, לא רנדר**: תיקון כאן משנה את **צורת העץ**, ולכן
גם את מספרי הצמתים ואת העוגנים של שכבת העריכה. **דורש הכרעה
נפרדת, ואני ממליץ לא לגעת בו לפני שיש גולדן על אחד משמונת
החוקים.**

### ג. שבר מתמטי - 10 צמתים, 3 חוקים

`{{שבר|מונה|מכנה}}` ו-`{{sfrac}}`, בעיקר בחוק מס חברות מזערי
בקבוצה רב-לאומית. **לא נוספו לרנדר בכוונה:** הסרת המעטפת תהפוך
"a/b" ל-"a b" - שינוי משמעות, לא עיצוב. צריך החלטה איך מציגים
שבר בטקסט רץ ("a חלקי b"? "a/b"?).

## כלי המחקר: מה נדרש לסינון לפי כנסת (נמדד 22.9.2026)

שתיים משלוש התבניות אינן מחילות אף מגבלה. המגבלה הנפוצה ביותר
בשאלות אמיתיות היא **כנסת מסוימת**. מה שלמטה נמדד מול הפיד החי,
בקשה אחת לכל שאלה — לא הונח.

### הממצא שמשנה את התמונה: הפיד לא חוסם סינון

בקוד כתוב שהפיד חוסם `filter` ולכן אין חיתוך. **זה נכון רק
בחלקו, וההכללה הזו היא שהקפיאה את שלוש התבניות.** ה-WAF חוסם
`filter(...)` **בתוך `$apply`** (סטטוס 473). `$filter` רגיל
ברמה העליונה עובד — כולל על תכונה מורחבת:

| שאילתה שנבדקה | תוצאה |
|---|---|
| `KNS_BillInitiator?$filter=KNS_Bill/KnessetNum eq 25` | **200**, 16,832 שורות |
| `KNS_Bill?$filter=contains(Name,'חינוך') and KnessetNum eq 25` | **200**, 170 שורות |
| `KNS_Bill?$filter=contains(Name,'חינוך') and PublicationDate gt 2024-01-01Z` | **200** |
| `KNS_Bill?$filter=contains(Name,'חינוך') and SubTypeDesc eq 'פרטית'` | **200**, 1,200 שורות |
| `groupby((KnessetNum,StatusID,SubTypeDesc),aggregate(...))` | **200**, 508 שורות ב-4 שניות |

### מה זה אומר לכל תבנית

**`bills_on_topic` — כמעט חינם.** כנסת, טווח תאריכים וסוג הצעה
הם כולם שדות של `KNS_Bill`, והוספתם היא סעיף נוסף באותו
`$filter` הקיים. **אותה עלות רשת בדיוק** (בקשה אחת). זו ההרחבה
שכדאי לעשות ראשונה.

**`pass_rates` — זול, ומחזיר תשובה אחרת לגמרי.** הצבירה הקיימת
היא `groupby((KnessetNum,StatusID))`. הוספת `SubTypeDesc` כמימד
שלישי רצה ב-4 שניות והחזירה 508 שורות (5 עמודים מול 4 היום).
**המספר שזה מגלה:** בכנסת ה-25 הוגשו 6,839 הצעות פרטיות ומתוכן
עברו 199 — **2.9%**, לא 7.6% ששיעור-כל-ההצעות מראה. כלומר
השאלה "כמה הצעות חוק פרטיות באמת עוברות" קיבלה מספר גבוה פי
2.6 מהאמת.

**`top_initiators` — אפשרי, אבל יקר.** אין ב-`KNS_BillInitiator`
שדה כנסת כלל (השדות: `BillID, Id, IsInitiator, LastUpdatedDate,
Ordinal, PersonID`), ולכן נדרש `$filter=KNS_Bill/KnessetNum eq N`.
לכנסת 25 זה 16,832 שורות = **169 בקשות** (הפיד מחזיר 100 לעמוד).
מתחת לתקרת `_MAX_PAGES=400`, אבל יקר: התבנית הזו כבר היום היקרה
ביותר (11 בקשות).

**מה שלא עובד:** `groupby((PersonID,BillID))` על 169,310 רשומות
היוזמים = 1,694 עמודים, מעל התקרה. נוסה ולא הסתיים ב-300 שניות.

### ההמלצה, לכשיוחלט לבנות

1. **`bills_on_topic` קודם** — שלוש מגבלות, סעיף filter נוסף,
   אפס עלות רשת.
2. **`pass_rates` אחריה** — מימד `SubTypeDesc`, עמוד אחד נוסף.
3. **`top_initiators` אחרונה**, ורק עם מטמון **לכל כנסת בנפרד**
   (המטמון הקיים הוא שבוע — מתאים). בלי מטמון, שאלה אחת = 169
   בקשות מ-IP אחד של Vercel, וזה בדיוק מה שמזמין את ה-WAF.

**לא נבנה — תיעוד בלבד** (ברק, 22.9.2026).

## עדכון אחרי תיקון raw_block (22.9.2026, נמדד מול ה-DB הפעיל)

101 חוקים נטענו מחדש. התמונה אחרי:

| מקור | לפני | אחרי |
|---|---|---|
| `raw_block` עם תבנית **עיצוב** | 320 צמתים, 102 חוקים | **0** |
| `raw_block` עם תבנית **מבנה** `ח:` | (נכלל ב-320) | 153 צמתים, 40 חוקים |
| טקסט רגיל עם תבנית מבנה `ח:` | 317 צמתים, 8 חוקים | 316 צמתים, 7 חוקים |
| תבנית עיצוב שנותרה (שבר וכו') | 10 צמתים, 3 חוקים | **0** |

**מה שנשאר הוא קבוצה אחת, לא שתיים.** כל 469 הצמתים שנותרו
מכילים תבנית מבנה של הפרסר עצמו (`{{ח:ת}}`, `{{ח:תת}}`,
`{{ח:תתת}}`), בין אם בתוך `<table>` ובין אם בטקסט רגיל. זו בדיוק
הקבוצה שברק דחה במפורש ("לא עכשיו, מסכים איתך. רשום").

**הריכוז קיצוני:** הרוב המוחלט יושב ב-`bill-1046679`, חוק התקציב,
שבו טבלת סעיפי התקציב משתמשת ב-`{{ח:תת|01|20}}` **כתווית בתוך תא**
ולא כפותחת מבנה. זה מסביר למה `_flatten` אינו נוגע בהן: הן אינן
אמורות להיות שם, והפרסר צורך אותן בלולאה הראשית ולא ברנדר.

**מה שזה אומר לכשיוחלט:** התיקון אינו "להוסיף `ח:תת` ל-render" -
זה היה שובר את פרסור המבנה בכל הקורפוס. הוא "לזהות תבנית מבנה
שמופיעה במקום שבו היא אינה פותחת מבנה, ולהציג את התווית שלה" -
ולכן הוא דורש גולדן על חוק התקציב לפני שנוגעים.


## הפיד של הכנסת מגביל קצב - HTTP 473 (22.9.2026)

**נמדד:** `knesset.gov.il/OdataV4` מחזיר **HTTP 473** מעל ~4-6
בקשות בו-זמנית מאותו מקור. הסטטוס אינו תקני ואינו מתועד בפיד.
בבדיקת מקביליות מבוקרת: 1-4 עובדים עברו נקי; 6-8 החזירו 473
בחלק מהבקשות, ולעתים גם אחרי שלושה ניסיונות חוזרים.

**איפה זה נוגע היום:** חיפוש השאילתות הקודמות, שמפצל נושא
לכמה צירופים ושולח קריאה לכל אחד. המקביליות נעולה על 3 + בדיקת
הפתיחה, ו-`knesset_queries.run_unit` מנסה שוב פעמיים בהשהיה
גדלה. מה שנשאר **נספר ונאמר למשתמש** ("7 מתוך 9 מקורות נבדקו"),
ולא נבלע כתוצאה ריקה.

**למה זה יחזור:** כל פיצ'ר עתידי שמפזר קריאות מקבילות לפיד -
מראי מקום להרבה חוקים, הצעות דומות על רשימה, רענון רקע - ייתקל
באותו קיר. שלושה כלים כבר קוראים לפיד, ואין ביניהם תיאום קצב.

**מה שיידרש כשזה יכאב:** מגביל קצב **משותף** לכל הקוראים
(`packages/knesset/odata.py` הוא המקום), ולא עוד לולאת ניסיונות
לכל קורא בנפרד. לא נבנה עכשיו - ההגבלה הנוכחית מספיקה למדידות
שנעשו, והבנייה מראש הייתה ניחוש לגבי הסף.

## מומחה התקנון תומך בשאלה בודדת, לא בשיחה (22.9.2026)

**נמדד:** שלושת המקורות (תקנון הכנסת, חוק הכנסת, חוק-יסוד: הכנסת)
הם **154,238 טוקנים** - נמדד מול `count_tokens`, לא הוערך. מאז
22.9.2026 כולם נכנסים ל-system בכל שאלה, בלי שלב אחזור.

**המגבלה:** מחלון של 200K נשארים ~45K לתשובה. זה מספיק לשאלה
בודדת ולתשובה מנומקת (נמדד: 865-900 טוקני פלט בפועל, התקרה
2,500), ו**אינו מספיק להיסטוריית שיחה**. כרגע הכלי אינו שולח
היסטוריה - כל שאלה עומדת בפני עצמה - ולכן זו אינה תקלה אלא גבול.

**מה יישבר אם ננסה רב-תורי:** כל תור מוסיף שאלה + תשובה (~1,000
טוקנים), ואחרי כעשרים תורים החלון מתמלא. הכשל לא יהיה שגיאה
ברורה אלא קטיעה של התשובה.

**שתי הדרכים כשזה יידרש**, אף אחת מהן לא נבחרה:
1. לחזור לאחזור - אבל רק להיסטוריה, לא למקורות (המקורות נשארים
   מלאים, ומה שמצטמצם הוא השיחה).
2. מאגר מצומצם - למשל רק תקנון הכנסת בלי שני החוקים (152K -> 118K).
   מוזיל גם את המטמון, ומאבד כיסוי.

**ולא לשכוח:** גם המטמון קשור לזה. הוא נכתב על גוש ה-system;
היסטוריה משתנה בתוך ה-messages ולכן אינה פוגעת בו, אבל כל שינוי
שיכניס היסטוריה ל-system יבטל את המטמון בכל תור.


## 🅓 הנוסח המשולב תומך בחוק אחד - **החלטה, לא פער** (23.9.2026)

הצעה שמתקנת יותר מחוק אחד ("תיקוני חקיקה") נעצרת עם הודעה ברורה
בעברית. **זו הכרעה של ברק לשלב הראשון**, לא מגבלה שנתקלנו בה
ולא חוב טכני - ולכן היא אינה נספרת כפער ואינה מדורגת מולם.

נמדד (23.9.2026): 4 מתוך 40 ההצעות בקורפוס הן מסוג "תיקוני
חקיקה", כלומר ההחלטה מכסה 36 מתוך 40.

**רשום כאן כדי שלא ידווח שוב כחור.** אם וכאשר תשתנה ההחלטה - זו
הרחבה מתוכננת, לא תיקון.
