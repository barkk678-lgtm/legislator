# מדידה: צורת הכתובת בהוראת תיקון מקוננת (22.9.2026)

השאלה: כשהשינוי יושב בתוך סעיף קטן — `בסעיף 4(א), אחרי פסקה (2)` או
`בסעיף 4, בסעיף קטן (א), אחרי פסקה (2)`?

`measure.py` מייצר כל מספר שמופיע ב-`docs/drafting-rules.md` §8.7.
רץ אופליין מול `tests/fixtures/real-bills/` (40 הצעות אמיתיות), בלי
רשת ובלי DB:

```
python3 docs/measurements/nested-locator-2026-09-22/measure.py
```

תוצאה: 40 הצעות, 2,642 פסקאות. הצורה הקצרה — 8 מופעים ב-4 הצעות;
הצורה הארוכה — **0**. `אחרי המילים "..."` — **0**, מול 13 של
`אחרי "..."`. ספירת תת-ההוראות מתחת לשורות הפתיח נעשתה בעין
(ראו הערה בסקריפט) — הסקריפט מדפיס את השורות ואינו סופר.
