# נתוני הגלם של סקירת כלי הצ'אט, 21.9.2026

`../../chat-tools-review-2026-09-21.md` נבנה מכאן. **אף מספר במסמך
אינו מוקלד ביד** — הוא נגזר מקובצי ה-JSON האלה בסקריפט, וכל מי
שרוצה לאמת אותו יכול להריץ:

```
python3 build_review.py
```

וההרצה מחדש תיתן את אותו קובץ בדיוק.

## הקבצים

| קובץ | מה זה |
|------|-------|
| `chat50.py` | ההרצה: ארבעה כלי צ'אט, עשר שאלות כל אחד, מול הפרודקשן |
| `chat50.json` | התשובות הגולמיות מ-21.9, כולל זמן תגובה לכל בקשה |
| `chat30.json` | אותה הרצה מ-18.9 — בסיס ההשוואה "מה השתנה" |
| `qual10.py` | הכלי החמישי: מצב האיכות בהסתייגויות, על הצעת חוק אמיתית |
| `qual10.json` | 19 ההסתייגויות שהופקו, עם `usage` (טוקנים בפועל) |
| `bill-13821884.docx` | הצעת חוק אספקת החשמל — הקלט לכלי החמישי |
| `build_review.py` | בונה את המסמך מכל אלה |

## למה זה כאן ולא בסקרטצ'פאד

הגרסה הראשונה של המסמך נכתבה עם מספרים שהוקלדו מתוך התבוננות בפלט:
היא הצהירה "חמישה כלים, 40 שאלות" בזמן שרצו ארבעה כלים, והציגה
כחציון ערך שבמקרה הופיע בשורה בודדת. **המספרים היו לא נכונים ולא
היה שום דבר שיתפוס את זה.** מאז הם מחושבים בקוד, מקובץ שמונח לצד
המסמך — כך שהטעות הזו אינה יכולה לחזור בשקט.

`bill-13821884.docx` הוא מסמך ציבורי מה-OData של הכנסת, נמשך דרך
`/api/admin/knesset-bill-docs`. הוא כאן כדי שההרצה תהיה ניתנת
לשחזור בלי גישה לשרת הקבצים של הכנסת.
